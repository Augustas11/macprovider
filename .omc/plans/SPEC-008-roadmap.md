# SPEC-008 Tier-2 Implementation Roadmap

**Created:** 2026-05-31

**Strategy:** Activate Phase 1 in observe mode immediately (no risk, real signal).
Each subsequent phase activates as soon as its build is deployed — no big-bang config flush at the end.

## Progress update — 2026-05-31

| Item | Status | Evidence |
|---|---|---|
| B1 catalog tooling | Built locally | `scripts/hash-model-weights.go`, `scripts/sign-catalog.go`, `scripts/check-tier2-release-artifacts.sh`, guarded `scripts/activate-tier2-observe.sh`, guarded `scripts/enforce-tier2-hash.sh`, and read-only `scripts/verify-tier2-live.sh` with C1/C2/B6/C4 parser modes; keypair + signed catalog generated under ignored `.omc/tier2/` |
| B2 provider `model_hash` | Built, packaged, and published | phase3-binary computes `artifact_manifest` hashes after model load and includes `model_hash` in hello when available; GitHub Release `v1.2.5` published `macprovider-cli-v1.2.5-darwin-arm64.tar.gz`, `checksums.txt`, and `checksums.txt.sig`; downloaded release assets verify with installer public key, tarball SHA-256 `9a8a7e82b0dd547f312528c63cd61dd916dfd548de1804c5b56ffe72bdaa8de9`, and extracted `macprovider-cli --version` reports `1.2.5` |
| C1 catalog/config activation | Full Phase 1 acceptance complete in observe mode | `scripts/activate-tier2-observe.sh --apply` exited 0 on the second live attempt after the required gateway `/v1/models` Tier-2 passthrough fix; live coordinator has the signed catalog, observe-mode `tier2:` config, coordinator binary SHA-256 `8b8bbb58f1062e504d414aaec3712660bf4c98b53a8d49a7554a2288687b1a91`, gateway binary SHA-256 `4e529f0f9c6ef242eb27c70d5ec6827bd7b5b6ec2f00ffbd16efa9b72dc3090d`, and both services active; catalog-only verifier passed; full verifier passed after both live providers upgraded to v1.2.5 with `hash_status: hash_verified` |
| C2 hash enforcement | Applied and verified live | `scripts/enforce-tier2-hash.sh --apply` backed up live config to `/opt/macprovider/coordinator.yaml.bak-c2-20260531074241`, changed only `tier2.require_hash_verified` to `true`, SIGHUPed `macprovider-coordinator`, observed `tier2 config reloaded` at `2026-05-31T07:42:45Z`, and `scripts/verify-tier2-live.sh --enforced` exited 0 with `require_verified: true`, `model_hash_state: all`, and both live providers hash-verified |
| B3 behavioral safety build | Built and deployed live | Added `phase4-coordinator/internal/tier2/pillar_d.go` with disabled-by-default Pillar D guard for output caps, encoding validation, TTFT anomaly logging, and `T2.D` audit events; wired WSTunneled relay paths, coordinator Tier-2 metadata, and gateway `untrusted_provider_safety` disclosure using the spec `none`/`partial`/`enforced` matrix; deployed B3-capable coordinator SHA-256 `0434d09322ac5ae6351998234382273816d317bda74f978dc5a186e562bfc287` and gateway SHA-256 `cfa9ec22125dc3eec66cbec26596d8b271d12d57f7e6442035acc245271208c3` with live binary backups `/opt/macprovider/coordinator.bak-c3-20260531082235` and `/opt/macprovider/gateway.bak-c3-20260531082235` |
| C3 behavioral safety activation | Applied and verified live | `scripts/activate-tier2-behavioral-safety.sh --apply` first passed `scripts/verify-tier2-live.sh --enforced`, backed up config to `/opt/macprovider/coordinator.yaml.bak-c3-20260531082623`, changed only Pillar D `tier2:` keys, SIGHUPed `macprovider-coordinator`, observed `tier2 config reloaded` at `2026-05-31T08:26:26Z`, and verified `/v1/models` reports `behavioral_safety.state: enforced`, `size_cap: true`, `encoding_validation: true`, `ttft_anomaly_logging: true`, and `tier1_disclosure.untrusted_provider_safety: enforced`. A later authorized production re-run at `2026-05-31T10:55:11Z` reported `already_behavioral_safety=1`, SIGHUPed the coordinator, observed fresh `tier2 config reloaded` journal evidence, and re-verified enforced behavioral disclosure. |
| B5 coordinator Pillar B/C foundation | Local build complete; activation external | Added disabled-by-default X25519/HKDF/A256GCM envelope helpers, v2 WebSocket first-message dispatch, coordinator auth challenge/response, transcript-bound key agreement, coordinator-side AEAD relay request encryption and response-chunk decryption, structural/mock attestation validation, token-size and token-encoding rejection, rootless-token refusal, explicit `mock-root`-only positive attestation, non-mock root fail-closed behavior, Apple-Silicon hardware-family policy checks, forward-compatible MDA certificate-chain parsing from `certificate_chain`/`x5c` and compact-JWS headers, PEM/base64 DER attestation root parsing, production x509 chain verification, Apple MDA freshness-code comparison against the SHA-256 hash of the `device-attest-01` token, EC P-256/P-384 leaf public-key shape validation, recognized nonblank Apple MDA device-property OID presence checks, and CSR signature/public-key equality checks against the MDA leaf key before returning `attested`. Missing CSR evidence remains unsupported/fail-closed. Also added pool session metadata, request-count/age-based AEAD rekey session closure with active-request drain protection and `aead_rekey` audit logs, buyer routing predicates for `require_encrypted_leg` and `require_attestation`, `/internal/routing` encrypted-leg/attestation metadata, and gateway `provider_leg_encryption`/`hardware_attestation` disclosure. Verified by focused v2 auth/relay/attestation tests plus `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...` in `phase4-coordinator` and prior `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...` in `phase5-gateway`. Still remaining for full Phase 2 production completion: live production ACME/MDM issuance/enrollment, publish/roll out the v1.2.6+ provider artifact, and later production C4 activation. |
| B6 provider encrypted-leg support | Local build complete; external release/rollout pending | Added Swift provider-side v2 auth initial/proof messages, X25519/HKDF-derived Tier-2 provider sessions, v2-to-legacy hello fallback before `auth_challenge` for mixed coordinator rollout safety, default Managed Device Attestation unsupported fallback using `attestation_token: null`, configured `tier2_mda_artifact_path` / `MACPROVIDER_TIER2_MDA_ARTIFACT_PATH` MDA artifact ingestion for operator-provided `certificate_chain`/`x5c` plus signed `certificate_signing_request` evidence, provider-side DER/PEM ASN.1 sequence shape validation before emitting configured MDA evidence, A256GCM request decryption and response-chunk encryption for tunneled WebSocket inference, replay/sequence rejection, and Go-compatible deterministic AEAD AAD byte ordering. `swift test` in `phase3-binary` passes 30 tests, including v2 auth payload coverage, v2 fallback coverage, injected attestation-envelope coverage, configured MDA artifact-source coverage, invalid MDA evidence fallback coverage, encrypted relay coverage, and AAD-order/escaping regression coverage; `swift build -c release` also passes. Local provider artifact `phase3-binary/dist/phase3-binary-m4-v1.2.6.tar.gz` was repackaged with SHA-256 `d096ecb82863275478e919a4c0741750c272beb4a0ba5e5c3e778cba159184e2`, extracted `macprovider-cli --version` reports `1.2.6`, and `scripts/check-tier2-provider-artifact.sh` passes against it, verifying the checksum, version, Tier-2 v2/encrypted-leg/MDA-artifact-source binary surface strings, and absence of `DeviceCheck`/`devicecheck` strings. `scripts/test-tier2-provider-artifact.sh` covers pinned SHA, dynamic SHA, version mismatch, SHA mismatch, missing Tier-2 surface, forbidden attestation strings, and optional string guard bypasses. `.github/workflows/release.yml` now runs that provider preflight before publishing v1.2.6+ release assets while skipping pre-B6 tags. `scripts/verify-tier2-provider-release.sh` is built as the post-publication GitHub Release verifier for v1.2.6 assets: it downloads the release tarball plus `checksums.txt` and `checksums.txt.sig`, verifies the signed checksum manifest with the installer public key, verifies the tarball SHA-256, then re-runs `scripts/check-tier2-provider-artifact.sh` against the downloaded artifact. `scripts/verify-tier2-live.sh --b6-ready` is built as a read-only live rollout gate requiring C2 enforced state, `provider_leg_encryption: all`, no currently routable unencrypted provider, and at least one currently routable v1.2.6+ encrypted provider. Provider artifact publication/live rollout and live production ACME/MDM MDA issuance remain open, coordinator-side rekey closure is local-build complete, and no C4 production enforcement was activated. |
| B6 MDA artifact pack/check tooling | Built locally | Added `phase4-coordinator/cmd/tier2-mda-artifact` plus `scripts/make-tier2-mda-artifact.sh`, `scripts/check-tier2-mda-artifact.sh`, and `scripts/test-tier2-mda-artifact.sh`. The `make` path packages operator-provided Apple MDA certificate-chain and CSR evidence into the JSON contract consumed by `MACPROVIDER_TIER2_MDA_ARTIFACT_PATH`; the `check` path always validates the provider artifact contract and, when root/challenge/provider-key inputs are supplied, runs the coordinator Pillar-C verifier and requires `attested`. `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./cmd/tier2-mda-artifact` and `scripts/test-tier2-mda-artifact.sh` passed with synthetic MDA-shaped certificate/CSR fixtures covering contract-only validation, coordinator-verified attestation, and mismatched-freshness failure. This does not perform live ACME/MDM issuance or production rollout. |
| B6 provider release verifier | Built locally | Added read-only `scripts/verify-tier2-provider-release.sh` plus hermetic `scripts/test-tier2-provider-release.sh`. The verifier downloads a GitHub Release asset set, verifies `checksums.txt.sig` against the installer public key, verifies the provider tarball checksum, and then runs `scripts/check-tier2-provider-artifact.sh` on the downloaded tarball. The release workflow now runs this post-publication check for v1.2.6+ tags with bounded download retries, while still skipping pre-B6 tags. The hermetic test covers success, bad signature refusal, tarball checksum mismatch, missing Tier-2 release-binary surface refusal, and invalid retry configuration refusal. A live read-only run for `v1.2.6` currently returns GitHub 404, so provider artifact publication remains open. |
| B6 live rollout verifier mode | Built locally | Extended `scripts/verify-tier2-live.sh` with `--b6-ready` as the read-only pre-C4a gate for live provider rollout. It still requires C2 enforced hash state, then verifies `/v1/models` reports `provider_leg_encryption: all`, `/poolz` has no currently routable unencrypted provider, and every currently routable encrypted provider is v1.2.6+ with at least one ready v1.2.6+ encrypted provider. `scripts/test-tier2-live-verifier.sh` covers B6 success, disclosure failure, and old-provider failure. |
| C4a encrypted-leg activation tooling | Built locally; not activated | Added guarded `scripts/activate-tier2-encrypted-leg.sh` for the later C4a config flip. Default `--plan` is non-mutating; `--apply` requires `scripts/verify-tier2-live.sh --b6-ready`, proves `/v1/models` reports `provider_leg_encryption: all`, proves `/poolz` has at least one ready v1.2.6+ encrypted provider and no currently routable unencrypted or pre-v1.2.6 encrypted provider, patches only `tier2.require_encrypted_leg: true`, SIGHUPs the coordinator, requires recent `tier2 config reloaded` journal evidence, re-verifies encrypted-leg disclosure, and restores the config backup on reload or disclosure failure. `scripts/test-tier2-encrypted-leg-safety.sh` passed with fake verifier/SSH/curl coverage for success, preflight refusal, readiness refusal before remote mutation, old-provider refusal, journal rollback, disclosure rollback, and plan output. No production C4a apply was run. |
| C4b attestation activation tooling | Built locally; not activated | Added guarded `scripts/activate-tier2-attestation.sh` for the later C4b config flip. Default `--plan` is non-mutating; `--apply` requires `scripts/verify-tier2-live.sh --attested`, proves `/v1/models` reports both `provider_leg_encryption: all` and `hardware_attestation: all`, proves `/poolz` has at least one ready v1.2.6+ encrypted+attested provider and no currently routable provider missing encryption, `attestation_status: attested`, or the v1.2.6+ binary floor, patches only `tier2.require_attestation: true`, requires the live config already has C4a (`require_encrypted_leg: true`) plus non-empty `attestation_roots`, SIGHUPs the coordinator, requires recent `tier2 config reloaded` journal evidence, re-verifies hardware-attestation disclosure, and restores the config backup on reload or disclosure failure. `scripts/test-tier2-attestation-safety.sh` passed with fake verifier/SSH/curl coverage for success, preflight refusal, readiness refusal before remote mutation, old-provider refusal, journal rollback, disclosure rollback, and plan output. No production C4b apply was run. |
| C4 read-only verifier modes | Built locally | Extended `scripts/verify-tier2-live.sh` with `--encrypted-leg` for post-C4a verification and `--attested` for C4b readiness/post-apply verification. Both modes still require C2 enforced hash state, then verify `/v1/models` disclosure and `/poolz` provider metadata: `--encrypted-leg` requires `provider_leg_encryption: all`, nonzero encrypted provider count, zero unencrypted count, and no currently routable plain provider; `--attested` additionally requires `hardware_attestation: all`, nonzero attested provider count, zero unsupported count, no currently routable provider lacking `attestation_status: attested`, and at least one ready v1.2.6+ encrypted provider. `scripts/test-tier2-live-verifier.sh` passed fixture-backed success and failure coverage for C2 enforced, C4a disclosure failure, C4a pool failure, C4b disclosure failure, and C4b pool failure. |

## Wrap boundary — 2026-05-31

The local SPEC-008 build, verifier, and guarded-activation tooling scope is
complete through B6/C4 readiness. Remaining incomplete roadmap items require
external production state changes rather than additional local hardening:

- publish the v1.2.6 provider GitHub Release asset set, then let
  `scripts/verify-tier2-provider-release.sh --tag v1.2.6` prove the signed
  release artifacts;
- roll live providers to v1.2.6+ and require
  `scripts/verify-tier2-live.sh --b6-ready` to pass against production;
- provision real production Apple ACME/MDM Managed Device Attestation
  certificate-chain/CSR artifacts and configured roots;
- after explicit production authorization, run C4a/C4b guarded activation and
  post-apply verifier modes.

Read-only audit on 2026-05-31: `gh release view v1.2.6 --repo
Augustas11/macprovider --json tagName,url,publishedAt,assets` still returns
`release not found`. The current shell has no `DEMO_TOKEN` or `OPERATOR_KEY`,
so live B6/C4 verifier modes cannot be proven from this session. Stop local
hardening here unless a concrete verifier, test, release check, or production
preflight fails.

Verification so far:
- `swift test` in `phase3-binary`: 30 tests passed, including provider hello/model-hash tests, v2 auth initial/proof payloads, v2-to-legacy hello fallback before `auth_challenge`, attestation-token envelope generation, unsupported fallback, configured MDA artifact-source ingestion, DER/PEM evidence-shape validation, invalid MDA evidence fallback, config/env artifact path precedence, provider AEAD round trip/replay rejection, Go-compatible AAD ordering/escaping, encrypted tunneled relay request/response handling, and `SelfUpdate` default release repository check.
- `swift build -c release` in `phase3-binary`: passed; existing Swift 6 concurrency warnings remain in unrelated send/relay paths.
- `phase3-binary/dist/package.sh v1.2.6`: passed after adding provider-side DER/PEM shape validation for configured MDA artifact evidence while preserving the default unsupported/null attestation behavior. It produced `phase3-binary/dist/phase3-binary-m4-v1.2.6.tar.gz` with SHA-256 `d096ecb82863275478e919a4c0741750c272beb4a0ba5e5c3e778cba159184e2`; extracted `macprovider-cli --version` reports `1.2.6`, and release-binary string checks show `managed_device_attestation_acme_unavailable`, `certificate_signing_request`, `MACPROVIDER_TIER2_MDA_ARTIFACT_PATH`, plus no `DeviceCheck`/`devicecheck` string. This was local packaging only; no GitHub Release publication or live provider rollout was performed.
- Official Apple documentation check for Pillar C source semantics: Managed Device Attestation is described as an ACME/device-management attestation that provisions a hardware-bound identity and validates certificate chains rooted in Apple's expected CA, while `DeviceCheck` is a separate app/device-token service. Apple's validation guidance requires more than chain trust for ACME MDA: the server must validate the device-attest-01 token/freshness binding, public key, device-property OIDs, retained key, and CSR key match before treating the attestation as trustworthy. Based on this, the provider no longer labels `DeviceCheck` output as `apple-managed-device-attestation-acme-v1`; it sends `attestation_token: null` by default unless `tier2_mda_artifact_path` / `MACPROVIDER_TIER2_MDA_ARTIFACT_PATH` points to operator-provided MDA certificate-chain and CSR evidence, and the coordinator's production-root path now parses/verifies certificate chains, validates MDA freshness/public-key/device-property prerequisites, and requires a signed CSR whose public key exactly matches the MDA leaf before returning `attested`.
- `scripts/check-tier2-provider-artifact.sh`: added as a non-mutating B6 provider artifact preflight. It defaults to the local v1.2.6 tarball and expected SHA-256 `d096ecb82863275478e919a4c0741750c272beb4a0ba5e5c3e778cba159184e2`, extracts only `macprovider-cli` into a guarded temp directory, verifies `--version` is `1.2.6`, confirms the release binary contains Tier-2 v2/encrypted-leg/MDA-artifact-source surface strings (`provider_ecdh_public_key`, `tier2_capabilities`, `selected_aead_suite`, `attestation_token`, `certificate_signing_request`, `MACPROVIDER_TIER2_MDA_ARTIFACT_PATH`, `A256GCM`, `inference_response_chunk`), and rejects `DeviceCheck`/`devicecheck` strings by default to prevent non-MDA DeviceCheck tokens from being reintroduced as Pillar C evidence. `.github/workflows/release.yml` now runs this preflight before `gh release create` for v1.2.6+ tags and passes `PROVIDER_SHA256=""` so CI-built tarballs log their observed SHA rather than requiring the local tarball's byte-for-byte hash. `scripts/test-tier2-provider-artifact.sh` covers pinned SHA success, dynamic-SHA success, version mismatch failure, SHA mismatch failure, missing Tier-2 surface failure, `REQUIRE_TIER2_STRINGS=0` bypass, forbidden attestation string failure, and `FORBID_TIER2_STRINGS=""` bypass. `bash -n scripts/check-tier2-provider-artifact.sh`, `bash -n scripts/test-tier2-provider-artifact.sh`, pinned local `scripts/check-tier2-provider-artifact.sh`, dynamic-SHA workflow-equivalent `PROVIDER_ARTIFACT=phase3-binary/dist/phase3-binary-m4-v1.2.6.tar.gz PROVIDER_VERSION=1.2.6 PROVIDER_SHA256="" scripts/check-tier2-provider-artifact.sh`, `scripts/test-tier2-provider-artifact.sh`, Ruby YAML parsing of `.github/workflows/release.yml`, and `git diff --check` all passed.
- `scripts/verify-tier2-provider-release.sh`: added as a non-mutating post-publication B6 GitHub Release verifier. It downloads `macprovider-cli-v1.2.6-darwin-arm64.tar.gz`, `checksums.txt`, and `checksums.txt.sig`, extracts the release signing public key from `phase3-binary/dist/install.sh` unless overridden, verifies the manifest signature, verifies the tarball SHA-256, and then runs `scripts/check-tier2-provider-artifact.sh` against the downloaded artifact. `.github/workflows/release.yml` now runs it after publish for v1.2.6+ tags with `DOWNLOAD_ATTEMPTS=6` and `DOWNLOAD_RETRY_SLEEP=5`, while skipping pre-B6 tags. `bash -n scripts/verify-tier2-provider-release.sh`, `bash -n scripts/test-tier2-provider-release.sh`, and `scripts/test-tier2-provider-release.sh` passed; fixture coverage includes signed success, bad signature refusal, tarball checksum mismatch, missing Tier-2 surface refusal, and invalid retry configuration refusal. A read-only live run of `scripts/verify-tier2-provider-release.sh --tag v1.2.6` reached GitHub and returned HTTP 404 for the tarball URL, confirming v1.2.6 publication remains open.
- `phase4-coordinator/cmd/tier2-mda-artifact`: added as a local B6 MDA artifact pack/check command with shell wrappers `scripts/make-tier2-mda-artifact.sh` and `scripts/check-tier2-mda-artifact.sh`. It packages operator ACME/MDM certificate-chain and CSR outputs into the provider artifact JSON, validates PEM/base64 DER certificate and CSR shape, and can run the coordinator verifier with supplied roots, challenge, provider ID, and provider ECDH key to require an `attested` result before C4b readiness claims. `bash -n scripts/make-tier2-mda-artifact.sh`, `bash -n scripts/check-tier2-mda-artifact.sh`, `bash -n scripts/test-tier2-mda-artifact.sh`, `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./cmd/tier2-mda-artifact`, `scripts/test-tier2-mda-artifact.sh`, and wrapper `--help` smokes passed.
- `scripts/verify-tier2-live.sh --b6-ready`: added as the read-only pre-C4a B6 rollout verifier. Fixture-backed coverage in `scripts/test-tier2-live-verifier.sh` passes for B6-ready success, `provider_leg_encryption` disclosure failure, and currently routable encrypted-provider `< v1.2.6` failure.
- `gh release view v1.2.6 --repo Augustas11/macprovider --json tagName,url,publishedAt,assets`: read-only lookup returned `release not found`, so B6 provider artifact publication remains open.
- `phase3-binary/dist/package.sh v1.2.5`: passed after aligning `macprovider-cli update` with the public `Augustas11/macprovider` release repository; latest clean-worktree package SHA-256 observed as `0999f5e34d78c4af7df9f9c1b83eef9909a3f28e14f3db3cbb1114a0d58dd002` and extracted `macprovider-cli --version` reports `1.2.5`.
- Clean release commit `03defd723368ec98aa31db1df3f37020cdf9e007` was tagged `v1.2.5` and pushed to `Augustas11/macprovider`; GitHub Actions run `26706294833` completed successfully and published latest release `https://github.com/Augustas11/macprovider/releases/tag/v1.2.5`.
- Downloaded public `v1.2.5` release assets, verified `checksums.txt.sig` with the public key embedded in `install.sh`, verified tarball checksum `9a8a7e82b0dd547f312528c63cd61dd916dfd548de1804c5b56ffe72bdaa8de9`, and confirmed extracted `macprovider-cli --version` is `1.2.5`.
- Extracted artifact `macprovider-cli --version`: `1.2.5`.
- `go test ./...` in `phase4-coordinator`: passed.
- Rebuilt `phase4-coordinator/dist/coordinator-linux-amd64` for Linux amd64; artifact SHA-256 `8b8bbb58f1062e504d414aaec3712660bf4c98b53a8d49a7554a2288687b1a91`; local artifact contains the `tier2 catalog loaded` acceptance log string.
- `scripts/hash-model-weights.go` smoke: fixture `artifact_manifest` hash `46f90a96d0db51ec7feb04136b986a74b78ce5e5f5cc8aeef4e2d8edef88563e`.
- `scripts/sign-catalog.go sign` smoke: produced signed catalog with 2 models, Ed25519 signature, key id `catalog-key-2026q2`.
- `scripts/sign-catalog.go verify`: verified `.omc/tier2/tier2-catalog.json` against `.omc/tier2/catalog-signing-key.pub` with catalog id `macprovider-tier2-model-catalog-2026-05-31`, 2 models, key id `catalog-key-2026q2`; verifier now mirrors coordinator rejection rules for timestamps, expiry, duplicate model IDs, model fields, hash scope, and lowercase SHA-256.
- `scripts/sign-catalog.go keygen` smoke: generated public key mode `0644` and private key mode `0600`.
- `scripts/check-tier2-release-artifacts.sh`: verifies provider artifact version and logs provider artifact SHA-256 unless `PROVIDER_SHA256=<expected>` is set, verifies coordinator binary checksum and `tier2 catalog loaded` log string, expected catalog public key, and Ed25519 signature plus coordinator-compatible body validation on the signed catalog; extraction now honors `TMPDIR` or `/tmp` with guarded cleanup, and passed with default temp settings, `TMPDIR=/tmp`, and `TMPDIR=/private/tmp` with no leftover `tier2-artifacts.*` directories.
- `scripts/test-tier2-activation-safety.sh`: passed with fake `ssh`/`scp`/`curl`; proved apply mode refuses a connected-provider restart before remote commands, refuses unparseable health without `FORCE_RESTART=1` before remote commands, rolls back config/binary and removes a newly created catalog after config-merge failure, rolls back after gateway disclosure failure, and reaches health, journal, and gateway checks on a fake success path.
- Gateway `/v1/models` Tier-2 passthrough fix: `go test ./internal/router` and `go test ./...` in `phase5-gateway` passed; rebuilt `phase5-gateway/dist/gateway-linux-amd64` with SHA-256 `4e529f0f9c6ef242eb27c70d5ec6827bd7b5b6ec2f00ffbd16efa9b72dc3090d`; deployed it to Pearl VPS with backup `/opt/macprovider/gateway.bak-spec008-20260531064346`; loopback `/healthz` and public `/v1/status` passed after restart.
- `scripts/activate-tier2-observe.sh --plan`: verified the local signed catalog against the configured public key with `scripts/sign-catalog.go verify`, validated the coordinator binary, then printed the guarded Pearl VPS activation plan without production mutation. Negative plan smokes reject both invalid SHA body content and invalid Ed25519 signatures before plan output. Apply mode now requires local `python3` before mutation, requires `DEMO_TOKEN` by default, fails closed if the connected-provider count cannot be parsed without `FORCE_RESTART=1`, fails if post-restart catalog-loaded logs or `tier2.model_hash.state` are missing, restores available config/binary/catalog backups on catalog upload, binary upload, config merge, restart, or verification failure, and removes a newly created remote catalog when no prior catalog backup existed.
- First live apply attempt failed at gateway `/v1/models` because the then-live gateway stripped top-level `tier2`; rollback restored config and removed the newly created catalog, and a manual repair restored config ownership after the backup restore exposed root-owned `0600` config. The rollback path was hardened so future config backups preserve owner/mode and binary restore stops the coordinator before copying the executable.
- Second live apply attempt succeeded with fresh demo token from `/auth/demo-session`: uploaded signed catalog, backed up binary to `/opt/macprovider/coordinator.bak-tier2-20260531064431`, backed up config to `/opt/macprovider/coordinator.yaml.bak-tier2-20260531064434`, merged observe-mode `tier2:`, restarted coordinator, found `tier2 catalog loaded` journal evidence at `2026-05-31T06:44:38Z`, and verified gateway `/v1/models` with `tier2_phase: 1`, `model_hash_state: none`, `require_verified: false`, and `catalog_available: true`.
- `scripts/verify-tier2-live.sh --catalog-only`: passed against live gateway/coordinator after activation with `gateway_status: up`, `coordinator_status: ok`, `pool_ready: 2`, `tier2_phase: 1`, `model_hash_state: none`, `require_verified: false`, and `catalog_available: true`.
- `gh release view v1.2.5 --repo Augustas11/macprovider`: read-only lookup reports latest published release `v1.2.5` at `https://github.com/Augustas11/macprovider/releases/tag/v1.2.5`, with `macprovider-cli-v1.2.5-darwin-arm64.tar.gz`, `checksums.txt`, and `checksums.txt.sig` assets.
- `scripts/verify-tier2-live.sh --full`: read-only acceptance verifier for live status, `/v1/models` Tier-2 disclosure, `require_verified: false`, and `/poolz` provider `binary_version >= 1.2.5` with `hash_status: hash_verified`; fixture-backed full and catalog-only parser checks passed again after script finalization. Live full verification passed at `2026-05-31T07:30:05Z` with `model_hash_state: all`, `verified_provider_count: 2`, `air8gb` on `mlx-community/Llama-3.2-3B-Instruct-4bit` at `binary_version: 1.2.5` and `hash_status: hash_verified`, and `air5` on `mlx-community/Qwen2.5-7B-Instruct-4bit` at `binary_version: 1.2.5` and `hash_status: hash_verified`.
- `scripts/verify-tier2-live.sh --enforced`: added for C2 post-apply verification; fixture-backed smoke passes when `require_verified: true` and `model_hash_state: all`, and fails against observe-mode fixtures with `require_verified: false`.
- `scripts/verify-tier2-live.sh --encrypted-leg` / `--attested`: added as read-only C4 verifier modes; `--attested` is also the pre-C4b readiness gate. `scripts/test-tier2-live-verifier.sh` passed fixture-backed parser coverage for C2 enforced success, B6-ready success/failure checks, C4a encrypted-leg success, C4a disclosure failure, C4a `/poolz` failure, C4b attested success, C4b disclosure failure, and C4b `/poolz` failure.
- `scripts/enforce-tier2-hash.sh --plan`: prints the C2 production enforcement plan without mutation. The apply path runs the full observe-mode verifier before mutation, backs up the live config, changes only `tier2.require_hash_verified` to `true`, SIGHUPs the coordinator, checks `tier2 config reloaded` journal evidence, runs `scripts/verify-tier2-live.sh --enforced`, and restores the config backup plus SIGHUPs again on reload or verification failure.
- `scripts/test-tier2-enforcement-safety.sh`: passed with fake `ssh` and verifier commands; proved C2 apply reaches reload + enforced verification on success, refuses remote mutation when the full preflight verifier fails, and restores the config backup when enforced verification fails.
- `scripts/enforce-tier2-hash.sh --apply`: live C2 enforcement applied with operator authorization. The guarded script first re-ran `scripts/verify-tier2-live.sh --full` successfully, backed up config to `/opt/macprovider/coordinator.yaml.bak-c2-20260531074241`, set `require_hash_verified: true`, SIGHUPed the existing coordinator process, found `tier2 config reloaded` journal evidence at `2026-05-31T07:42:45Z`, and then `scripts/verify-tier2-live.sh --enforced` passed with `require_verified: true`, `model_hash_state: all`, `verified_provider_count: 2`, and both `air8gb` and `air5` on `binary_version: 1.2.5` with `hash_status: hash_verified`. A final read-only enforced verifier also passed at `2026-05-31T07:45:40Z`.
- B3 local validation: `go test ./internal/tier2 ./internal/config ./internal/buyer` in `phase4-coordinator` passed; `go build ./...`, `go test ./...`, and `go test -race ./... -run TestPillarD` in `phase4-coordinator` passed; `go test ./internal/router` and `go test ./...` in `phase5-gateway` passed; `git diff --check` passed. Coverage now maps directly to AC-D-1 through AC-D-5: exact 32-byte ASCII streaming cap from 64 bytes, UTF-8-boundary truncation with `configured_cap_bytes` and `emitted_bytes` logs, invalid UTF-8 rejection before commit, decoded forbidden-control rejection, and TTFT anomaly WARN without response rejection. Integration coverage includes inactive byte preservation, JSON-valid nonstreaming truncation, WSTunneled pre/post-commit invalid-output handling, streaming cap stop behavior, coordinator behavioral metadata, and gateway `partial`/`enforced` behavioral disclosure.
- C3 activation tooling validation: `bash -n scripts/activate-tier2-behavioral-safety.sh` and `bash -n scripts/test-tier2-behavioral-safety.sh` passed; `scripts/test-tier2-behavioral-safety.sh` passed with fake `ssh`, verifier, and `curl`, proving the success path reaches C2 preflight, config patch, SIGHUP, journal check, and disclosure verification, proving enforced-preflight failure prevents remote mutation, and proving journal/disclosure failures restore the config backup; `scripts/activate-tier2-behavioral-safety.sh --plan` printed the non-mutating C3 production plan with expected `enforced` behavioral disclosure; `git diff --check` passed after the script additions.
- C3 production binary deploy: rebuilt Linux amd64 artifacts from the B3 code, producing coordinator SHA-256 `0434d09322ac5ae6351998234382273816d317bda74f978dc5a186e562bfc287` and gateway SHA-256 `cfa9ec22125dc3eec66cbec26596d8b271d12d57f7e6442035acc245271208c3`; uploaded both to Pearl VPS, verified remote upload hashes before install, backed up the previous live binaries to `/opt/macprovider/coordinator.bak-c3-20260531082235` and `/opt/macprovider/gateway.bak-c3-20260531082235`, installed both artifacts, restarted both services, and verified `/v1/status`, `/healthz`, live binary hashes, and provider reconnect/model-hash evidence.
- C3 production config apply: `scripts/activate-tier2-behavioral-safety.sh --apply` ran with fresh demo token and production operator key, passed the enforced C2 preflight with `require_verified: true`, `model_hash_state: all`, and 2 hash-verified providers, backed up config to `/opt/macprovider/coordinator.yaml.bak-c3-20260531082623`, SIGHUPed the coordinator, observed `tier2 config reloaded` at `2026-05-31T08:26:26Z`, and verified `behavioral_safety.state: enforced`, `size_cap: true`, `encoding_validation: true`, `ttft_anomaly_logging: true`, and `untrusted_provider_safety: enforced`. Independent post-apply checks showed `/v1/status` up, `/healthz` ok with `pool_ready: 2`, `scripts/verify-tier2-live.sh --enforced` still passing with `tier2_phase: mixed`, `model_hash_state: all`, and 2 verified providers, and the live `tier2:` block preserving `catalog_path`, `catalog_public_key`, and `require_hash_verified: true`.
- C3 live disclosure recheck: at `2026-05-31T09:37:15Z`, a fresh demo-token `/v1/models` read reported `tier2_phase: mixed`, `model_hash_state: all`, `require_verified: true`, and `behavioral_safety` enforced with `size_cap`, `encoding_validation`, and `ttft_anomaly_logging` all `true`. This was read-only; no production mutation was performed.
- C3 live disclosure recheck: at `2026-05-31T10:05:33Z`, a fresh demo-token `/v1/models` read reported `tier2_phase: mixed`, `model_hash_state: all`, `require_verified: true`, `behavioral_safety.state: enforced`, `size_cap: true`, `encoding_validation: true`, `ttft_anomaly_logging: true`, and `untrusted_provider_safety: enforced`. This was read-only; no production mutation was performed. `scripts/verify-tier2-live.sh --enforced` was not rerun because `OPERATOR_KEY` is not present in this shell.
- C3 authorized production no-op/recheck: at `2026-05-31T10:30:22Z`, after a transient empty-pool read and provider reconnect window, read-only live checks showed gateway `/v1/status` up with `pool.ready: 1`, coordinator `/healthz` ok with `pool_ready: 1`, and fresh demo-token `/v1/models` reporting `tier2_phase: mixed`, `model_hash_state: all`, `require_verified: true`, `behavioral_safety.state: enforced`, `size_cap: true`, `encoding_validation: true`, `ttft_anomaly_logging: true`, `tier1_disclosure.model_hash_verified: all`, and `untrusted_provider_safety: enforced`. The C3 apply script was not rerun because production was already in the desired C3 state; no config rewrite, SIGHUP, or other production mutation was performed.
- C3 authorized production apply re-run: at `2026-05-31T10:55:11Z`, `scripts/activate-tier2-behavioral-safety.sh --apply` ran with a fresh demo token and production operator key. The enforced C2 preflight passed with `require_verified: true`, `model_hash_state: all`, and 1 hash-verified provider (`air8gb` on `mlx-community/Llama-3.2-3B-Instruct-4bit`, `binary_version: 1.2.5`); the config patch phase reported `already_behavioral_safety=1`, so no config rewrite or new backup was needed; the script SIGHUPed `macprovider-coordinator`, observed fresh `tier2 config reloaded` journal evidence at `2026-05-31T10:55:11Z`, and verified `behavioral_safety.state: enforced`, `size_cap: true`, `encoding_validation: true`, `ttft_anomaly_logging: true`, and `untrusted_provider_safety: enforced`. A post-run `scripts/verify-tier2-live.sh --enforced` also passed, `/v1/status` was up with `pool.ready: 1`, `/healthz` was ok with `pool_ready: 1`, and the live `tier2:` block still preserves `catalog_path`, `catalog_public_key`, `require_hash_verified: true`, and all Pillar D controls.
- B5 local foundation validation: `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/tier2 ./internal/config ./internal/pool ./internal/buyer` in `phase4-coordinator` passed under local `httptest` listener permission after a sandbox-only bind denial; `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/router` in `phase5-gateway` passed; broader `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...` passed in both `phase4-coordinator` and `phase5-gateway`. Coverage includes AEAD envelope round trip and tamper rejection, deterministic Pillar B key derivation, structural attestation success/failure/stale states, encrypted-leg and attestation routing predicates, `/internal/routing` B/C metadata, gateway B/C disclosure, and non-serialization of Tier-2 session keys. No production `require_encrypted_leg` or `require_attestation` activation was performed.
- B5 coordinator v2 auth validation: `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/ws -run 'TestProviderAuthV2|TestProviderAuthFirstMessageDispatch|TestProviderHelloRejectsUnsupportedVersionAndTier'`, `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/ws ./internal/tier2 ./internal/config ./internal/pool ./internal/buyer`, and `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...` passed in `phase4-coordinator` under local `httptest` listener permission. Coverage includes first-message dispatch (`hello` v1 vs `auth_request` v2), v2 auth challenge/response, provider-side derivation of the advertised `key_id`, encrypted session metadata registration, mock-attested acceptance when `require_attestation` is true, missing-attestation rejection, unsupported AEAD rejection, malformed attestation-token encoding rejection, rootless token refusal, and preservation of the old v1 tier rejection path. No C4 production activation was performed.
- B5 Pillar C fail-closed validation: `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/tier2 -run TestVerifyAttestationToken`, `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/tier2 ./internal/ws -run 'TestVerifyAttestationToken|TestProviderAuthV2|TestProviderAuthFirstMessageDispatch|TestProviderHelloRejectsUnsupportedVersionAndTier'`, and `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...` passed in `phase4-coordinator`. Coverage includes explicit `mock-root` acceptance for mock tokens, non-mock token rejection even with `mock-root`, production-root fail-closed behavior when `require_attestation: true`, and optional production-root reporting as `unsupported` until production Apple MDA verification is implemented. No C4 production activation was performed.
- B5 MDA certificate-chain, Apple prerequisite, and CSR-binding validation: `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/tier2 -run TestVerifyAttestationToken`, `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/tier2 ./internal/ws ./internal/config ./internal/pool ./internal/buyer`, and `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...` passed in `phase4-coordinator` after adding forward-compatible `certificate_chain`/`x5c` fields, compact-JWS `x5c` extraction, PEM/base64 DER root parsing, x509 chain verification, Apple-Silicon hardware-family policy checks, Apple MDA freshness comparison against SHA-256 of the `device-attest-01` token, EC P-256/P-384 leaf public-key validation, recognized nonblank Apple MDA device-property OID checks, and signed CSR public-key equality checks. New coverage rejects malformed production certificate chains, missing or mismatched MDA freshness, missing or blank device-property OIDs, unsupported leaf public keys, malformed CSRs, mismatched CSR keys, and hardware-family policy mismatches; a synthetic trusted production chain with matching CSR now returns `attested`, while missing CSR evidence remains `unsupported` when optional and `failed` when required. No C4 production activation was performed.
- B5 coordinator AEAD relay validation: `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/tier2 ./internal/ws ./internal/buyer -run 'TestPillarB|TestEncryptedRelay|TestRelayDispatchRoutesChunkAndEndByRequestID|TestChatCompletionsWSTunneledAEADFailure|TestChatCompletionsStreamingWSTunneledAEADFailure'`, `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/tier2 ./internal/ws ./internal/buyer ./internal/config ./internal/pool`, and `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...` passed in `phase4-coordinator`; `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...` passed in `phase5-gateway`. Coverage includes encrypted `inference_request` frames with no plaintext `body`, base64url deterministic AAD, provider-side request decryption using the session `c2p` key, encrypted `inference_response_chunk` decryption using the session `p2c` key, tamper rejection with `ErrRelayAEADFailed`, `T2.B` AEAD failure/session-close logging, and buyer-facing `tier2_aead_decrypt_failed` behavior before and after SSE commit. No C4 production activation was performed.
- B5 coordinator AEAD rekey validation: `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./internal/tier2 ./internal/ws ./internal/buyer -run 'TestPillarB|TestEncryptedRelay|TestRelayDispatchRoutesChunkAndEndByRequestID|TestChatCompletionsWSTunneledAEADFailure|TestChatCompletionsStreamingWSTunneledAEADFailure'`, `GOCACHE=/private/tmp/macprovider-go-build-cache go test ./...`, and `GOCACHE=/private/tmp/macprovider-go-build-cache go test -race ./internal/ws -run 'TestEncryptedRelay'` passed in `phase4-coordinator`. Coverage includes request-threshold rekey after a completed encrypted request, deferring threshold close while an active encrypted request drains, age-threshold rekey before dispatch, provider state transition to `unavailable`, session removal, no encryption counter increment before expired-session closure, and `aead_rekey` logs with threshold-specific reasons.
- B6 provider encrypted-leg and attestation-token validation: `swift test` passed in `phase3-binary` with 30 tests, `swift build -c release` passed, `phase3-binary/dist/package.sh v1.2.6` produced local artifact SHA-256 `d096ecb82863275478e919a4c0741750c272beb4a0ba5e5c3e778cba159184e2` with extracted version `1.2.6`, `scripts/check-tier2-provider-artifact.sh` and `scripts/test-tier2-provider-artifact.sh` passed, and the release workflow now gates v1.2.6+ publication with the same checker. New coverage includes v2 `auth_request` initial/proof payloads, v2-to-legacy hello fallback before `auth_challenge` for C3-only coordinator compatibility, deterministic unsupported MDA fallback using `attestation_token: null`, configured MDA artifact-source ingestion through `tier2_mda_artifact_path` / `MACPROVIDER_TIER2_MDA_ARTIFACT_PATH`, DER/PEM evidence-shape validation, invalid evidence fallback before sending, signed CSR evidence emission, injected attestation-envelope binding to the coordinator challenge, provider ID, binary version, model metadata, RAM claim, and provider ECDH key, X25519/HKDF provider session setup surfaces, A256GCM c2p request decrypt and p2c response encrypt, replay/sequence rejection, encrypted tunneled inference response chunks without plaintext `data`, and deterministic AEAD AAD JSON matching Go coordinator field order and string escaping. No provider GitHub Release publication, live provider rollout, or C4 production activation was performed.
- C4a encrypted-leg activation tooling validation: `bash -n scripts/activate-tier2-encrypted-leg.sh`, `bash -n scripts/test-tier2-encrypted-leg-safety.sh`, `scripts/activate-tier2-encrypted-leg.sh --plan`, `scripts/test-tier2-encrypted-leg-safety.sh`, and `git diff --check` passed. The guarded apply path requires the B6-ready live verifier plus independent live encrypted-leg readiness with the v1.2.6+ floor before mutation, changes only `tier2.require_encrypted_leg: true`, checks SIGHUP reload journal evidence, re-verifies `provider_leg_encryption: all`, and rolls back the config backup on journal or disclosure failure. No C4a production apply was run.
- C4b attestation activation tooling validation: `bash -n scripts/activate-tier2-attestation.sh`, `bash -n scripts/test-tier2-attestation-safety.sh`, `scripts/activate-tier2-attestation.sh --plan`, `scripts/test-tier2-attestation-safety.sh`, and `git diff --check` passed. The guarded apply path requires the attested live verifier plus independent live encrypted-leg and hardware-attestation readiness with the v1.2.6+ floor before mutation, requires C4a already active in the config, requires non-empty `attestation_roots`, changes only `tier2.require_attestation: true`, checks SIGHUP reload journal evidence, re-verifies `hardware_attestation: all`, and rolls back the config backup on journal or disclosure failure. No C4b production apply was run.
- Local Llama model hash: `0baf13715db1eeb56e6d0806b0d764aa1c44497aaaaf8d2ba90c21128d9fe2fe`.
- Qwen hash was derived from Hugging Face LFS metadata because the provider host was offline during collection: `c3b0ec86f9a59fb8416f2e037def2ed17ca012173461cd98d7b8b8da7d325e54`.

Production caveat:
- Current coordinator code treats `tier2.catalog_path` and `tier2.catalog_public_key` as startup-only fields. A first-time catalog activation requires a coordinator restart, not just SIGHUP, unless the running process was already started with the same catalog path and key.
- Live read-only checks at `2026-05-31T10:55:44Z` report `https://api.streamvc.live/v1/status` as up with `pool.ready: 1`, `https://coordinator.streamvc.live/healthz` as ok with `pool_ready: 1`, and `scripts/verify-tier2-live.sh --enforced` as passing with `tier2_phase: mixed`, `model_hash_state: all`, `require_verified: true`, and 1 verified v1.2.5 provider. Remote evidence shows `/opt/macprovider/tier2-catalog.json` present, live config with `require_hash_verified: true` and Pillar D controls enabled, coordinator binary SHA-256 `0434d09322ac5ae6351998234382273816d317bda74f978dc5a186e562bfc287`, gateway binary SHA-256 `cfa9ec22125dc3eec66cbec26596d8b271d12d57f7e6442035acc245271208c3`, and both `macprovider-coordinator` and `macprovider-gateway` active. Phase 1 C1/C2 and Phase 3 C3 are complete.

---

## Remaining Phase 1 gates

**Prompt:** `specs/BUILD_SPEC_008_PHASE1_ACTIVATE_PROMPT.md`

**Runbook:** `docs/runbooks/SPEC-008-PHASE1-ACCEPTANCE-RUNBOOK.md`

| Gate | What | Evidence required |
|---|---|---|
| C1a | Apply observe-mode catalog activation on Pearl VPS | Done: live apply exited 0 after restart, catalog-loaded journal check, and `/v1/models` Tier-2 disclosure check |
| C1b | Prove catalog-only live disclosure | Done: `scripts/verify-tier2-live.sh --catalog-only` exited 0 with `tier2_phase: 1`, `require_verified: false`, and `catalog_available: true` |
| C1c | Roll out the v1.2.5 provider artifact to at least one provider | Done: full verifier shows both `air8gb` and `air5` reconnected with `binary_version` `1.2.5`, nonempty `model_hash`, and `hash_status: hash_verified` |
| C1d | Prove full Phase 1 provider acceptance | Done: `DEMO_TOKEN=<redacted> OPERATOR_KEY=<redacted> scripts/verify-tier2-live.sh --full` exited 0 at `2026-05-31T07:30:05Z` with 2 `hash_verified` v1.2.5 providers and no failing hash statuses |

Observe-mode C1 is complete and C2 enforcement is now active. Providers without
the v1.2.5 binary would be excluded by `require_hash_verified: true`.

---

## After Phase 1 session

### Phase 1 enforcement flip
**Depends on:** B2 binary released to operators + pool shows predominantly `hash_verified`

C2 is complete as of `2026-05-31T07:42:45Z`: both live providers are on
`binary_version` `1.2.5`, `/v1/models` reports `model_hash_state: all` and
`require_verified: true`, and the enforced verifier reports 2 `hash_verified`
providers. A read-only SSH config check confirms the live coordinator process is
running with `tier2.catalog_path`, `tier2.catalog_public_key`, and
`require_hash_verified: true`.

| Step | What | Type | Status |
|---|---|---|---|
| C2 | Set `require_hash_verified: true`, SIGHUP coordinator | Config | Done: guarded apply exited 0 and enforced verifier passed |

---

## Phase 3 — Behavioral safety (Pillar D)

**Prompt:** `specs/BUILD_SPEC_008_PHASE3_PROMPT.md`

**Depends on:** Nothing — coordinator-internal, no wire change

**Estimate:** 3-6 days

| Step | What | Type | Status |
|---|---|---|---|
| B3 | Coordinator relay hooks: output size cap, encoding validation, response-time anomaly, audit `T2.D`, disclosure, tests | Build | Done and deployed live with B3-capable coordinator/gateway binaries |
| C3 | Set `behavioral_safety_enabled: true` + tune caps in coordinator.yaml, SIGHUP | Config | Done: guarded apply exited 0 and behavioral disclosure verified as `enforced` |

---

## Phase 2 — Encryption + attestation (Pillars B + C)

**Prompt:** `specs/BUILD_SPEC_008_PHASE2_PROMPT.md`

**Note:** SPEC-001 v2.0 wire spec is already written as SPEC-008 §10 — no separate spec session needed.

**Depends on:** Nothing blocking (B3 and Phase 2 are independent; run in parallel or Phase 3 first)

**Estimate:** 7-14 days

| Step | What | Type | Status |
|---|---|---|---|
| B5 | Coordinator: v2 auth dispatch, X25519/HKDF/AEAD relay, attestation verification, audit `T2.B`/`T2.C`, disclosure | Build | Local build complete: v2 auth dispatch, coordinator challenge/response, X25519/HKDF session derivation, coordinator-side AEAD relay framing/decryption, explicit mock-only structural attestation gating, attestation token encoding/size/root checks, non-mock root fail-closed behavior, MDA certificate-chain shape parsing, x509 chain verification, device-attest-01 freshness comparison, Apple-Silicon hardware-family policy checks, EC P-256/P-384 leaf public-key validation, nonblank recognized MDA device-property OID checks, signed CSR public-key binding to the attestation leaf, metadata, disclosure, routing predicates, AEAD failure handling, request/age threshold rekey closure with active-request drain protection, and local MDA artifact pack/check tooling are built and tested; live production ACME/MDM issuance and activation remain external gates |
| B6 | phase3-binary: v2 auth handshake, AEAD encrypt/decrypt, Apple attestation token | Build | Local build complete: v2 auth initial/proof, v2-to-legacy hello fallback before `auth_challenge`, deterministic MDA unsupported/null fallback, configured MDA certificate-chain/CSR artifact ingestion with DER/PEM shape validation, injectable attestation-envelope builder, X25519/HKDF session derivation, provider-side AEAD request decrypt/response encrypt, replay rejection, Go-compatible AAD ordering, local MDA artifact pack/check tooling, post-publication provider release verifier, and read-only live `--b6-ready` verifier mode are built and tested; local `v1.2.6` provider artifact is packaged with SHA-256 `d096ecb82863275478e919a4c0741750c272beb4a0ba5e5c3e778cba159184e2`; provider artifact publication, live provider rollout, and live production ACME/MDM MDA issuance remain external gates |
| C4a | Set `require_encrypted_leg: true`, verify pool, SIGHUP | Config | Not activated; guarded activation script, rollback safety tests, read-only preflight `verify-tier2-live.sh --b6-ready`, and read-only `verify-tier2-live.sh --encrypted-leg` post-check are built locally, but apply remains blocked until the v1.2.6+ provider artifact is published/rolled out and the live pool reports `provider_leg_encryption: all` |
| C4b | Set `require_attestation: true`, verify pool, SIGHUP | Config | Not activated; guarded activation script, rollback safety tests, and read-only `verify-tier2-live.sh --attested` preflight/post-check are built locally, but apply remains blocked until C4a is active, production MDA roots/artifacts are configured, and the live pool reports `hardware_attestation: all` |

---

## Full dependency graph

```
B1 ─┐
    ├─→ C1 (observe) ─→ C2 (enforce, after B2 deployed)
B2 ─┘

B3 ──────────────────→ C3

B5 ─┐
    ├─→ C4a ─→ C4b
B6 ─┘
```

---

## All build prompts

| Phase | Prompt file |
|---|---|
| Phase 1 (catalog tooling + provider hash + activation) | `specs/BUILD_SPEC_008_PHASE1_ACTIVATE_PROMPT.md` |
| Phase 3 (Pillar D behavioral safety) | `specs/BUILD_SPEC_008_PHASE3_PROMPT.md` |
| Phase 2 (Pillars B + C encryption + attestation) | `specs/BUILD_SPEC_008_PHASE2_PROMPT.md` |
