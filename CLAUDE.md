@AGENTS.md

## Claude Code

Claude reads this file at session start. The imported `AGENTS.md` is the
canonical repo manual and applies to Claude, Codex, Cursor, and other coding
agents. Keep Claude-specific notes here only when they cannot apply to other
agents.

- Use `/context` if you need to confirm this file and `AGENTS.md` loaded.
- Prefer path-scoped `.claude/rules/` files for large or subsystem-specific
  Claude-only guidance instead of expanding this always-loaded file.
