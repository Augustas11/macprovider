| 2026-07-23 | **Entry 190 — SPEC-037 KV survival ships as an encrypted provider-local disk tier that changes KV residency only, default-off and synthetic-key-only, with revocation grounded in per-entry key destruction rather than file deletion.** RESEARCH_233's Approach A landed as normative SPEC-037 v0.1.0 (#702, seven-round five-lane audited) and its provider IMPL (encrypted per-namespace disk tier behind `ConversationCache`: JCS-canonical manifest as non-circular AEAD associated data, framed AES-256-GCM with per-chunk CSPRNG nonces and a `KVCacheSimple`-only allowlist; per-entry Data-Protection-Keychain DEK whose destruction is the rollback-proof revocation authority; durable fail-closed admission derived from on-disk incomplete-tombstones and rotation journals; serialized owned purge fences with tombstone+high-watermark made durable before the suspending hot-tier callback; snapshot-at-commit under the per-key lease promoting into the existing LCP/trim predicate; four-round five-lane BUILD audit converged to 0 C/H/M). Persistence is residency-only: no new wire, receipt, billing, or `cached_prompt_tokens` computation change; `prompt_tokens` stays the full incoming length; eligibility stays the shipped hot-tier TTL. | **Extending reuse eligibility beyond the hot-tier TTL was rejected as a SPEC-024 cache-eligibility change out of scope for a residency optimization. Persisting buyer conversation keys in v0.1 was rejected because coordinator-propagated purge does not yet exist, so `allow_buyer_keys=true` is refused and only reserved `conv:kvs-synth:` direct-HTTP operator keys persist. An external sidecar, global cross-tenant dedup, plaintext token replay as the production cache, and oMLX as the inference engine were rejected per the memo no-go list. Grounding purge revocation in file/tombstone deletion alone was rejected because a whole-directory snapshot rollback resurrects deleted files — per-entry DEK destruction makes restored ciphertext undecryptable. Transient in-memory purge fences were rejected after three audit rounds recurred CRITICALs there: admission must derive from durable on-disk state and survive both mid-purge failure and restart. Treating the KVS-01a benchmark as satisfiable now was rejected because its restart-survival evidence requires a controllable >32 GB lab Mac that does not yet exist (#584).** | **The tier ships disabled-by-default and cannot serve buyer traffic in v0.1; enabling it fails closed without a functioning purge primitive.** Per SPEC-037 FR-KVP13 the tier MUST NOT graduate past synthetic-key experiments until KVS-01b passes on a non-production >32 GB host; that evidence is a blocking-before-enable residual pending #584. First-post-restart-turn promotion (load-time geometry capture) is a documented follow-up — a stale template can only miss, never promote incorrectly. Re-trigger if the residency-only boundary, the DEK-destruction revocation authority, the durable admission model, the synthetic-key gate, or the eligibility-TTL preservation changes. **Owner: provider runtime / prefix-cache persistence.** |
