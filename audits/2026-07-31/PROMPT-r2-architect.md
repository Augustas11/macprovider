Re-audit (round 2) of the SPEC-023 oMLX provisional-gates amendment + its SPEC-032 cross-spec fix (issue #687 Stage 1), as the architect lane. Docs-only but money-path-adjacent (catalog admission / paid-default promotion) — high rigor.

Worktree: /Users/augstar/macprovider-828-revision. Review the FULL amendment as it lands: `git diff origin/main...HEAD -- specs` (captured at audits/2026-07-31/pr828-revised.diff). Open specs/SPEC-023-installer-autotune-recommend.md, specs/SPEC-032-proof-of-weights-hello-gate.md, specs/SPEC-010-model-catalog.md, and docs/research/RESEARCH_231_OMLX_BENCHMARK_CATALOG_MEMO.md for context.

PRIOR AUDIT (r1) findings this revision claims to close — verify each is genuinely closed AND that the fix introduced no new defect: CRITICAL(oMLX-as-promotion-predicate: "each clear the gate" removed, promotion+recompute now solely from N verified measurements); HIGH(SPEC-032 hello-gate no longer hard-rejects on advisory bench_gate TPS/TTFT — check FR-HG3/HG4 + new AC actually remove the veto without breaking the proof-of-weights/hardware gate); HIGH(provenance laundering: promotion sets provenance to verified_provider_matrix, and a non-omlx_seeded row carrying gate_seed is fail-closed); HIGH(K redefined as observations_used_n = observations within one normalized cell, §12.1 mapped to RESEARCH_231 n>=10-per-cell); plus MEDIUMs (positive valid-seed fixture computing ==69; observations_used_n<K rejected; AC-OMLX-5 not satisfiable by constant-8; semantic seed failures fail-closed; donor/status provisional labeling) and LOWs (version/changelog reconcile, seed-expiry rule, schema example on omlx_seeded row).

THE TRUST INVARIANT (issue #687, must hold): unattested oMLX data MAY only seed the STARTING advisory gate of a non-default provisional row; it MUST NEVER set/hold a recommendable gate, raise a verified gate, hard-block a provider, or be sole/partial promotion evidence. Verified provider autotune is the ONLY admission/promotion authority.

For the architect lane specifically:
- code: schema correctness (gate_seed / observations_used_n rules, required-iff-omlx_seeded, fail-closed absence), seed-formula binding, AC completeness/non-vacuousness (every new MUST has a real AC; no AC passable by a trivial constant), promotion determinism, internal consistency, renumbering/version/changelog coherence.
- security: attack every seam for an invariant violation — can oMLX reach recommendable/paid-default, hold/raise a verified gate, hard-block a provider (incl. via SPEC-032 now that it was amended — verify the veto is truly gone and nothing else in SPEC-032 re-introduces a bench_gate hard-gate), act as partial promotion evidence, or persist a seed silently on a relabeled row.
- architect: SPEC-023/032/010 boundary coherence, Stage-1 scope discipline (no Stage-2 impl leakage), and whether the K/N justification (§12.1) is now sound (K=10 correctly mapped to observations-per-cell; N=3 honest policy).

Output: findings first, severity-ordered (CRITICAL/HIGH/MEDIUM/LOW/INFO), each with exact spec location, the concrete defect/violation scenario, and a fix. Explicitly state whether there are ANY CRITICAL/HIGH/MEDIUM findings, and whether each prior r1 finding is CLOSED.
