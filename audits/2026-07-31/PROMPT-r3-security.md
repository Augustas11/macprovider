Re-audit ROUND 3 of the SPEC-023 oMLX provisional-gates amendment + its SPEC-032 cross-spec fix (issue #687 Stage 1), security lane. Docs-only, money-path-adjacent (catalog admission / paid-default promotion) — high rigor.

Worktree: /Users/augstar/macprovider-828-revision. Full amendment: `git diff origin/main...HEAD -- specs` (captured audits/2026-07-31/pr828-r3.diff). Open specs/SPEC-023-installer-autotune-recommend.md, specs/SPEC-032-proof-of-weights-hello-gate.md, specs/SPEC-010-model-catalog.md, docs/research/RESEARCH_231_OMLX_BENCHMARK_CATALOG_MEMO.md for context.

PRIOR ROUND (r2) findings this r3 revision claims to close — verify each is CLOSED and check the r3 fixes introduced no new defect: HIGH(SPEC-032 evidence_invalid taxonomy definition + FR-HG4 outcome table still listed "TPS below gate / TTFT above gate" as rejection causes despite the prose — verify BOTH are now gone and no other bench_gate hard-veto remains); HIGH(r2 regression: semantic oMLX seed failures were fail-closed at "coordinator admission", letting invalid oMLX hard-block a provider — verify fail-close is now scoped to catalog/lint/sign/CLI-decode/download/benchmark/donor/recommendation ONLY and an explicit clause forbids oMLX integrity failure from touching SPEC-032 verified admission); HIGH(promotion path unverifiable — verify it is now DEFINED-but-PROHIBITED/inert in Stage 1 until the Stage-2 evidence-record mechanism exists, fail-closed); MEDIUM(gate_seed required-iff now symmetric — missing/malformed seed on omlx_seeded row fails closed at same boundaries); MEDIUM(donor contradiction — §5 exclusion now scoped to PAID-DEFAULT, seeded rows allowed donor-local with provisional label, §5/§7.2/§8/§9/AC-OMLX-6 consistent); MEDIUM(K schema-enforceable — gate_seed.target_cell + omlx_snapshot_sha256 added, observations_used_n bound to one cell, AC-OMLX-5c cross-cell rejection); plus Q13 reworded and the schema example min_sustained_tps=69.

TRUST INVARIANT (#687): unattested oMLX data MAY only seed the STARTING advisory gate of a non-default provisional row; NEVER set/hold a recommendable gate, raise a verified gate, hard-block a provider, or be sole/partial promotion evidence. Verified provider autotune is the ONLY admission/promotion authority.

security focus: code=schema/AC correctness, non-vacuous ACs, internal consistency of the r3 additions (target_cell schema, prohibited-promotion, donor-local scoping), renumbering/version/date coherence. security=attack every seam for an invariant violation incl. any residual bench_gate hard-veto (SPEC-023 or SPEC-032) and any oMLX-hard-block-of-provider path; confirm prohibited-promotion is truly fail-closed. architect=SPEC-023/032/010 boundary, Stage-1 scope discipline (the prohibited-until-Stage-2 framing must not leak Stage-2 impl), K/N justification soundness.

Output: findings first (CRITICAL/HIGH/MEDIUM/LOW/INFO) with exact location + concrete scenario + fix. Explicitly state whether ANY CRITICAL/HIGH/MEDIUM remain, and whether each prior r2 finding is CLOSED.
