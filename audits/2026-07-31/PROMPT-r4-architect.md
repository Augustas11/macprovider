Re-audit ROUND 4 (final convergence) of the SPEC-023 oMLX provisional-gates amendment + SPEC-032 fix (issue #687 Stage 1), architect lane. Docs-only, money-path-adjacent.

Worktree: /Users/augstar/macprovider-828-revision. Diff: `git diff origin/main...HEAD -- specs` (captured audits/2026-07-31/pr828-r4.diff). Open specs/SPEC-023-installer-autotune-recommend.md (esp. new §12.2/§12.3/§12.4, AC-OMLX-11..15), specs/SPEC-032-proof-of-weights-hello-gate.md, specs/SPEC-010-model-catalog.md.

This revision RE-SCOPES Stage-1 around an ACTIVATION GATE (SPEC-023 §12.2). The oMLX schema (provenance omlx_seeded, bench_gate.gate_seed, verified_provider_matrix) is FORWARD-DECLARED and MUST NOT be emitted into any signed/served candidate catalog until (a) all consumers (coordinator Go validators + CLI Swift decoders) forward-compat-accept it AND (b) Stage-2 enforcement ships: (i) admission identity excludes advisory bench_gate TPS/TTFT+provenance+gate_seed; (ii) coordinator enforces runtime_status==recommendable for network providers (provisional rows local-only); (iii) invalid oMLX rows row-scoped-quarantined, never whole-catalog/join/admission blocking; (iv) verified_provider_matrix promotion evidence-bound. The r3 IMPLEMENTATION-coupled findings (RowIdentity includes advisory TPS/TTFT; runtime_status unenforced; strict validators reject the schema; whole-catalog quarantine) are now DECLARED as normative Stage-2 prerequisites BEHIND that gate — NOT Stage-1 defects. DO NOT re-flag those gated Stage-2 prerequisites as Stage-1 HIGH/CRITICAL unless the GATE ITSELF is insufficient to defer them safely.

YOUR JOB: assess whether, GIVEN the activation gate, the STAGE-1 contract is fail-closed and the #687 trust invariant holds. Concretely test the GATE'S WATERTIGHTNESS: is there ANY Stage-1 path where (1) the oMLX schema could reach a signed/served catalog before the gate is satisfied, (2) oMLX could influence any admission/routing/identity/promotion field before Stage-2, or (3) the bounded fixes (verified_provider_matrix reserved at signing, oMLX field-laundering prohibited, target_cell bound to row, row-scoped quarantine) fail to fail-close? Also check the gate is internally consistent (no contradiction with the r3 body, §5 eligibility, AC-6/static-feed contract) and that the Stage-2 prerequisites are stated as real normative REQUIREMENTS (not hand-waved).

Flag ONLY: gate holes, Stage-1-live invariant violations, or spec defects live in Stage-1. Do not re-flag correctly-gated Stage-2 impl items.

Output: findings first (CRITICAL/HIGH/MEDIUM/LOW/INFO) with location + concrete Stage-1 scenario + fix. Explicitly state whether ANY CRITICAL/HIGH/MEDIUM remain AT STAGE-1 SCOPE, and whether the activation gate is watertight.
