Re-audit ROUND 5 (final convergence check) of the SPEC-023 oMLX provisional-gates amendment + SPEC-032 fix (#687 Stage 1), architect lane. Docs-only, money-path-adjacent.

Worktree: /Users/augstar/macprovider-828-revision. Diff: `git diff origin/main...HEAD -- specs` (captured audits/2026-07-31/pr828-r5.diff). Open specs/SPEC-023-installer-autotune-recommend.md (esp. §3.5/§3.6/§12.2/§12.3, AC-OMLX-11..16), specs/SPEC-032-proof-of-weights-hello-gate.md, specs/CONFORMANCE.json.

STAGE-1 is re-scoped around an ACTIVATION GATE (SPEC-023 §12.2): the oMLX schema AND any oMLX-DERIVED value in any provenance label are forward-declared and MUST NOT reach a signed/served catalog until (a) consumers forward-compat-accept the schema AND (b) Stage-2 enforcement ships (admission identity excludes advisory bench_gate/provenance/gate_seed via the new admission_policy_sha256 subset digest §3.6; runtime_status==recommendable enforced; row-scoped quarantine post-activation; verified_provider_matrix evidence-bound; immutable provenance lineage). The r3 impl-coupled items are DECLARED Stage-2 prerequisites §12.2(b)(i)-(v) — NOT Stage-1 defects; do NOT re-flag them. r5 closed the r4 gate holes: provenance-erasure laundering (broadened no-oMLX-derivation gate + AC-OMLX-16 + lineage), SPEC-032 admission-digest contradiction (now matches on admission_policy_sha256 subset, §3.6 + FR-HG3), and the quarantine phase contradiction (pre-activation global fail-closed / post-activation row-scoped, §3.5/§12.3/AC-OMLX-2/10/15).

YOUR JOB: confirm whether, GIVEN the activation gate as now broadened, the STAGE-1 contract is fail-closed and the #687 trust invariant holds with NO Stage-1-live CRITICAL/HIGH/MEDIUM. Specifically re-test the three r4 holes are CLOSED: (1) can any oMLX-derived value (labeled or laundered into policy/measured_single_host with gate_seed stripped) reach a signed/served catalog or influence any admission/routing/identity field before Stage-2? (2) does SPEC-032 admission now consistently match on the advisory-excluding admission_policy_sha256 subset digest (no residual full-catalog-SHA admission match)? (3) is the pre/post-activation quarantine split internally consistent (no contradiction between §3.5/AC-OMLX-2/10 and §12.3/AC-OMLX-15)? Also confirm the gate is internally consistent and the Stage-2 prerequisites (i)-(v) are real normative requirements.

Flag ONLY Stage-1-live defects, gate holes, or internal contradictions. Do not re-flag correctly-gated Stage-2 impl items.

Output: findings first (CRITICAL/HIGH/MEDIUM/LOW/INFO) with location + concrete Stage-1 scenario + fix. Explicitly state whether ANY CRITICAL/HIGH/MEDIUM remain AT STAGE-1 SCOPE and whether the three r4 holes are CLOSED.
