# Audit — BYOM v0.2 slice 2c: CLI consumption of the SPEC-023 artifact feed with the compiled-in fallback; shared conformance corpus (#1453)

METHOD CONSTRAINT: first-party software-correctness review of a signed-feed consumer on the catalog-identity path (Swift CLI), its generator-side bake (Python), and a three-language conformance corpus. Do NOT author adversarial payloads. Evaluate by reading source and running the EXISTING tests (`cd phase3-binary && swift test --filter 'AutotuneArtifactFeedTests|AutotuneRecommendTests|BYOMDiscovery'` — local `CoordinatorClientTests` failures are a known env-only false failure, ignore them; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_catalog_artifact_feed`; `cd phase4-coordinator && go test ./internal/buyer -run 'Conformance|CatalogArtifacts'`; `python3 scripts/catalog-release.py verify`). Describe gaps abstractly (field + condition). Do NOT commit or modify the worktree; `phase3-binary/Package.resolved` may be rewritten by a local build — ignore that.

Review the COMPLETE diff `git diff origin/main...HEAD` on branch `feat/byom-v02-slice2c-cli-artifact-feed` (main already contains slices 2a, 2b, 2b-ii: generator, coordinator serving + live gate, release assets + deploy + renewal). Every file. Cross-check against SPEC-023 v0.10.0 §3.5 (verification procedure, rules 2–13), §3.7.2 (transport, baked fallback COMPILED into the CLI, signer identity equality), §3.7.3 (schema), §3.7.4 (identity matrix, release binding), §3.7.5 (primary consistency), §3.7.6 (failure classes; rule 5 fail-closed for artifact-derived capabilities; rule 6 NEVER blocks paid recommendation / coordinator join / v0.1 behaviour), §3.7.8 Stage A; SPEC-046-R003 (discovery envelope: `identity_state` `catalog_matched`, closed warning-code enum — no new discovery warning codes may be introduced) and SPEC-046 "must not consume candidate feeds as admission authority".

## What the change does
1. `scripts/catalog-release.py` `generated_swift` bakes `bakedArtifactFeedJSON: String?` (the published feed for an artifact-bound release, `nil` otherwise); `generate` writes it, `verify` checks drift; the committed snapshot is regenerated with `nil`.
2. `phase3-binary/Sources/macprovider-cli/AutotuneArtifactFeed.swift`: `ArtifactFeed` strict decode (duplicate keys rejected, exact key sets at every level, presence-aware optional strings — a present `null` for `rate_class` / `notes` / cross-variant `source_ref` fields is a wrong-typed field; `verified_at` required and nullable — identity matrix, global `(hash_algorithm, hash)` uniqueness, `artifact_id` grammar, primary must be `mlx_safetensors`); `bind(to:candidateBytes:candidateSignerKeyID:artifactSignerKeyID:)` (signer equality, version / generated_at / policy equality, candidate digest, §3.7.5 primary consistency, model keys ⊆ candidate rows); `AutotuneStaticInputs.loadArtifactFeed(candidate:bakedArtifactFeed:)` reusing the shared `loadSignedStatic` (now internal instead of private) with four new `AutotuneRecommendWarning` cases, yielding `value == nil` whenever integrity / update-required / stale is present (rule 5) or the baked feed is `nil` (rule 6, no warnings); `bakedBoundArtifactFeed()`.
3. `BYOMDiscovery.swift` `BYOMCatalogMatcher`: after the candidate rows, a served reference may resolve through the compiled-in, release-bound artifact set (HuggingFace repo ids, GGUF library tags) → `catalog_model_key` / `catalog_matched`. Identity only; the `catalog_match_unverified` warning behaviour is unchanged; no new discovery warning codes.
4. `scripts/tests/fixtures/artifact_feed_conformance.json` (30 cases) consumed by the Python test `ArtifactFeedConformanceCorpusTest`, the Go test `TestCatalogArtifactsSharedConformanceCorpus`, and the Swift test `testSharedConformanceCorpus`.

## Invariants to verify (challenge them)
- Rule 6 in code, not prose: the four artifact warnings are absent from `paidTrustBlockingWarnings` and `networkSubmissionBlockingWarnings`; a nil baked feed yields no warnings and no fetch; v0.1 recommend / consume / update paths are byte-for-byte unchanged (the enum gained cases — check every place that enumerates, orders, serialises, or golden-tests warnings, including CaseIterable consumers and exact-frame goldens).
- Rule 5 in code: any of integrity / update-required / stale → `value == nil`; stale bytes are retained in `selectedBytes` for diagnostics only.
- Signer equality is a checked EQUALITY of the AUTHENTICATED signers of both feeds (fetched-vs-fetched, baked-vs-baked, and the mixed cases), never a fixed key id; a nil signer on either side fails.
- Release binding compares against the candidate catalog SELECTED for this run (fetched or baked) and its exact selected bytes, not against the baked catalog unconditionally; the baked-artifact + fetched-candidate combination is update-required (release mismatch), not silently accepted.
- Schema parity with the generator and the coordinator: every corpus case has the same verdict in all three languages (the corpus asserts it); look for rules present in one validator but absent from another (e.g. `size_bytes` integer-ness vs floats, `min_ram_gb` bool rejection, `verified_at` real-date check, `//` in model keys, trimmed version strings) and for JSON-number edge cases where Swift's `NSNumber` bridging differs from Go/Python.
- The matcher's artifact references are identity only: no admission state, readiness, fit, or earning-path change; discovery stays read-only; served references normalise the same way as candidate rows.
- Baked snapshot: the `String?` multi-line literal is valid Swift for any feed bytes the generator can emit (no `"""` or backslash escapes in the JSON); `verify` drift covers the baked feed; the hermetic Python harness still activates and verifies.
- Tests: each rule above maps to a test that fails if the check is removed; name any Swift rule only the corpus covers and any corpus expectation that a validator satisfies for the wrong reason (e.g. rejecting earlier on a different rule).
- No operator secret or key material; test keys generated at test time.

## Lanes to report (this pass is: {{LANE}})
CRITICAL / HIGH / MEDIUM / LOW / INFO; merge bar 0 C / 0 H / 0 M.
- code-reviewer: Swift decode/bind/loader correctness, warning-enum blast radius, Python bake + drift, corpus adequacy and three-language verdict parity.
- security-reviewer: feed substitution / signer confusion / release-binding bypass in the CLI, fail-closed posture of every artifact-derived capability, the matcher's identity-only boundary, secret hygiene.
- architect: SPEC-023 §3.7 / SPEC-046 fidelity, single source of truth (corpus) across three languages, where the artifact feed will be consumed next (slices 3–4: SPEC-010 multi-artifact identity, coordinator decision path) and whether this slice's shapes serve that cleanly.

End with `VERDICT: <N> CRITICAL / <N> HIGH / <N> MEDIUM / <N> LOW / <N> INFO`. Cite file:line. Do not invent issues to fill a lane.
