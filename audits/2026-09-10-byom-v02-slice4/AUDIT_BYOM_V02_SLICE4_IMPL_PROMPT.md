# Audit — BYOM v0.2 slice 4 IMPL: SPEC-047 v0.1.5 catalog binding + decision path in production (#1453)

METHOD CONSTRAINT: first-party implementation review of a MONEY PATH (catalog binding, operator admission decisions, settlement boundary). Do NOT modify or commit the worktree. Review the FULL working-tree diff `git diff origin/main` on branch `feat/byom-v02-slice4-decision-path` (it includes the uncommitted `phase4-coordinator/cmd/coordinator/main.go` wiring, which the operator commits). The SPEC text on this branch (SPEC-047 v0.1.5, SPEC-010 v1.8) is LOCKED after its own audit loop — review the code against it; report SPEC defects only if the code cannot conform.

Read first: `specs/SPEC-047-network-model-admission.md` R001 (operator decision request, precedence, dual control, offer listing, transition origins, decision critical section, release lock and generation, every append origin under the section, route-time compare-and-insert), the catalog-matched paragraph ("Match", fixed ordering, recorded fields, re-stamp), R002 (value semantics), R003 (preconditions (i)–(iv), session-to-candidate binding, buyer requirements), R006 (a)–(d), R008; `specs/SPEC-010-model-catalog.md` R004 (v1.8 composite proof, per-release identity sets) and R007; `specs/SPEC-023` §3.7.4 (allowed_runtime_sources matrix). Runbook: `docs/runbooks/byom-admission-decisions.md`.

Code under review (coordinator, Go):
- `internal/ws/model_admission.go` — event model (recorded match, tagged members, six decision-bound values, evaluated generation), SQLite columns/INSERT/scan, `CASAppendModelAdmissionDecision`, `AppendModelAdmissionApproval`, `ModelAdmissionEventByRequestID`, provider/state listings; `internal/ws/model_admission_pending.go` — pending dual-control records (memory + SQLite).
- `internal/ws/model_admission_binding.go` — per-provider sections + binding generation, `appendModelAdmissionEventInSection`, session-to-candidate binding derive/refresh/clear, drift predicates (a)/(d), hello/heartbeat/disconnect hooks, offer-time match `matchOfferArtifactHashes`, R003 (i)–(iii) evaluator, release sweeps (b)/(c).
- `internal/ws/model_admission_operator.go` — `/admin/model-admission/decisions`, `/decisions/<id>/approve`, `/offers`, precedence, dual control, R003(iv) `settlementSessionMemberLocked`, operator rate limiter, `CompareAndInsertModelAdmissionRouteSnapshot`.
- `internal/ws/release_snapshot.go` + `server.go` — atomic release snapshot (staging, publish, generation, read lock), `registerProviderSession` split, heartbeat/disconnect hooks, routes, `RefreshTier2HashStatuses`.
- `internal/tier2/catalog.go` — `ReleasePublisher` staging; `internal/buyer/autotune_feeds.go`, `artifact_identity_index.go` — previous-release feeds, per-release identity sets, `AllowedRuntimeSources`; `internal/artifactidentity/index.go`; `internal/autotune/catalog.go`; `internal/pool/provider.go` — `ModelAdmissionBinding`, `SetModelAdmissionBinding`, model-change clear.
- `internal/buyer/model_admission.go`, `route_snapshot.go`, `server.go` — binding-only routing/settlement, content equality, `catalog_row_status`, compare-and-insert via the ws guard (nil guard = local re-read).
- `cmd/coordinator/main.go` — boot sets, staging, observer publish, route guard.
- Tests: `internal/ws/model_admission_operator_test.go`, `model_admission_binding_test.go`, `model_admission_decision_store_test.go`, `artifact_identity_test.go`, `internal/buyer/artifact_settlement_test.go`, `route_snapshot_test.go`, `internal/tier2/release_publisher_test.go`, `internal/buyer/artifact_identity_sets_test.go`.

## Invariants to challenge (cite file:line)
- Settlement boundary: can any path reach a settlement-bearing route snapshot without (offer catalog_matched → operator catalog_priced → dual-control settlement_capable bound to the session's pinned member → binding at head → compare-and-insert)? Served-ref / asserted-key fallbacks anywhere? Legacy records (no `catalog_match_state`)? The buyer with a nil guard?
- Composite proof (SPEC-010-R004 v1.8): is the Tier-2 row material ever read as the member's expected hash? Is an `artifact_feed` member's six values always from the SESSION's own release set?
- Precedence and idempotency: (1)–(5) and (a)–(g) exactly; replay after the head advanced; divergent reuse; concurrent first-seen requests under the section; approval consumption atomic with the append; pending invalidation on every append; stale-head invalidation.
- Concurrency/locks: lock order (section → registry → release read lock) everywhere; any path taking a section while holding the registry lock or the release lock; nested release read locks (deadlock against a waiting writer); sweeps under the write lock; the probe path holding/not holding the section across I/O; `autotuneCatalogMu` vs release lock order; `RefreshTier2HashStatuses` publishing staged parts.
- Binding lifecycle: derive at hello (before observable) and on every append; ambiguity revokes decided candidates and binds nothing; clear on withdrawal/model change/disconnect; `validated_release_generation` stamping; route-time generation checks; a replacement hello with a prior binding.
- Offer-time match: fixed ordering; primary-row ambiguity; span keys before admissibility; asserted key disagreement; integrity-failed set signal; one runtime format per offer; unmatched → null key; R002 status readback value change; provider-facing responses unchanged in field set.
- Drift completeness: hello (a)/(d) with pending receipt keys (SPEC-015 staging); heartbeat model change vs the legacy served-ref path (both run — double revocation? conflicting reasons?); sweeps (b)/(c) reason mapping; a re-stamp (new release id/digest/feed signature, same content) must append nothing; Tier-2 material absent.
- Store/schema: SQLite migrations idempotent on an existing DB; column defaults for legacy rows; JSON member encoding; `UNIQUE(provider_id, request_id)` vs the operator request-id scheme; listing bounds (64 candidates, 16 members).
- Operator surface: per-actor credential only (shared key refused), actor grammar, rate limiter keyed by actor+host independent of provider windows, body bounds, closed error envelope, log lines free of secrets.
- Tests: does R008 coverage claimed in CONFORMANCE.json exist and assert what the SPEC says? Missing cases you would require before merge.

## Lanes to report (this pass is: {{LANE}})
CRITICAL / HIGH / MEDIUM / LOW / INFO; bar 0 C / 0 H / 0 M. code-reviewer: correctness, races, error handling, test adequacy. security-reviewer: settlement-boundary bypass, operator-surface abuse, replay/concurrency, key/release confusion, secrets in logs. architect: single authority across SPEC-010/022/023/047, layering (ws ↔ buyer ↔ pool ↔ tier2), what slices 5–7 will need. End with `VERDICT: <N> CRITICAL / <N> HIGH / <N> MEDIUM / <N> LOW / <N> INFO`. Do not invent issues.

## ROUND 2 EXTRA (all three lanes)
R1 (code 0/2H/5M/2L, security 0/1H/2M/2L/1I, architect 0/2H/2M/1L) fixed; record `AUDIT_BYOM_V02_SLICE4_IMPL_R1.md`. Re-review the FULL `git diff origin/main` (uncommitted `main.go` included). Shapes to re-challenge: the lock graph (section → registry → release; `generationLocked`; registry read before the release hold in decision/approval/guard; the test hook `onReadLocked`); ONE release read hold across precedence (1)–(5) and (a)–(g); the guard's pre-check → insert → post-check under one hold plus the registry re-read after it; exact-release session catalog via `resolveProviderCatalogIn`; `usableIdentitySetLocked` (fresh set) at match/decision/settlement; the mlx-only settlement binding until R007(e); pending replay from the record; single write-lock publication with the integrity flag; nil-guard fail-closed in the buyer and `testRouteGuard` in its tests; the new R008 tests. Do not re-report an R1 item unless its fix is wrong or incomplete.

## ROUND 3 EXTRA (code-reviewer + architect; security at bar since R2)
R2 (code 0/0/4M/2L, security 0/0/0/1L/1I, architect 0/1H/1M) fixed; record `AUDIT_BYOM_V02_SLICE4_IMPL_R2.md`. Re-review the FULL `git diff origin/main` (uncommitted `main.go` included). Shapes to re-challenge: the session identity epoch (`pool.Provider.ModelAdmissionSessionEpoch`, fingerprint, bump sites incl. receipt-key commit; captured by the buyer, required unchanged pre- and post-insert by the guard); hello and heartbeat holding the provider section through registry mutation + (a)/(d) evaluation + binding refresh (`helloSessionBindingLocked`, `heartbeatSessionEvaluationLocked`; section → registry → release order preserved; no section taken under the registry lock); the sweep's (a)/(d) evaluation after refresh; served feed bytes committed inside the release write-lock hold (`SetAutotuneFeeds` → observer `(feeds, commit)` → `PublishArtifactIdentitySetsWith`); R003(ii) ordering; approval (a) before (b) with the SPEC clarification; parse/decode errors surfaced. Do not re-report an R1/R2 item unless its fix is wrong or incomplete.

## ROUND 4 EXTRA (code-reviewer + architect; security at bar since R2)
R3 (code 0/1H/2M, architect 0/1H) fixed; record `AUDIT_BYOM_V02_SLICE4_IMPL_R3.md`. Re-review the FULL `git diff origin/main` (uncommitted `main.go` included). Shapes to re-challenge: `afterReleasePublished` = publish → `refreshSessionIdentities` (registry-wide, registry → release, epoch bump on fingerprint change in `UpdateModelIdentities`) → per-provider sweeps with (a)/(d) → survivor re-stamp; `RefreshTier2HashStatuses` composition; feed commit ordered after the internal release inside the hold; the heartbeat fingerprint compare after the model-id assignment. Do not re-report an R1–R3 item unless its fix is wrong or incomplete.

## CLOSURE PASS (all three lanes) after the independent cold-context review
The anchored loop closed at R4; an independent cold-context round (record `AUDIT_BYOM_V02_SLICE4_IMPL_INDEPENDENT.md` — read it AFTER forming your own view) found two HIGHs (shared-secret dual control; provider squatting of coordinator/operator replay keys) and several MEDIUM/LOW items; all are fixed in the latest commit. Review the FULL `git diff origin/main` (uncommitted `main.go` included) as new code. Shapes to challenge: `operatorDualControlAvailable` (distinct secrets) and the ambiguous-bearer refusal in `authorizedProviderAuthPolicyOperator`; `reservedModelAdmissionToken` and the `:`-separated operator/approval request ids; generation seeded at 1 with 0 = never validated in the guard; the guard's pre-check / insert outside the hold / post-check + registry re-read; legacy decided records revoked by the sweep; hello (a)/(d) over prior + derived candidates; no-op binding refresh; buyer runtime-source re-check and fail-closed default branch; `ConsumePendingModelAdmissionDecision` removed; single `.previous-target` resolver; rate-limiter eviction; 400 envelopes. Do not re-report an item from any record unless its fix is wrong or incomplete.

## CLOSURE PASS 2 (code-reviewer only; security and architect at bar)
Closure pass 1 (code 0/1H/1M/1L, security 0/0/0/1L, architect 0/0/0/1L) fixed; record `AUDIT_BYOM_V02_SLICE4_IMPL_CLOSURE1.md`. Re-review the FULL `git diff origin/main` (uncommitted `main.go` included). Shapes to re-challenge: `setModelAdmissionBindingLocked` re-stamping an unchanged binding with the current section generation (no advance) vs advancing on a real mutation; the route guard's binding-generation comparison against that stamp after unrelated appends and after no-op heartbeats; `operatorDualControlAvailable` (strict normalized actors, ≥2 distinct, pairwise-distinct secrets). Do not re-report an item from any record unless its fix is wrong or incomplete.

## CLOSURE PASS 3 (code-reviewer only; security and architect at bar)
Closure pass 2 (code 0/0/1M) fixed; record `AUDIT_BYOM_V02_SLICE4_IMPL_CLOSURE2.md`. Re-review the FULL `git diff origin/main` (uncommitted `main.go` included). Shape to re-challenge: `operatorDualControlAvailable` — secret multiplicity over all entries, strict normalized actors with a globally unique non-empty secret, normalized-alias disqualification — against `authorizedProviderAuthPolicyOperator`'s ambiguity refusal. Do not re-report an item from any record unless its fix is wrong or incomplete.

## CLOSURE PASS 4 (code-reviewer only; security and architect at bar)
Closure pass 3 (code 0/1H) fixed; record `AUDIT_BYOM_V02_SLICE4_IMPL_CLOSURE3.md`. Re-review the FULL `git diff origin/main` (uncommitted `main.go` included). Shape to re-challenge: `sharedOperatorKeyBearer` refusal ahead of the named match and the shared-secret exclusion in `operatorDualControlAvailable`, against every other credential class the coordinator accepts on `/admin` (gateway service token, provider tokens). Do not re-report an item from any record unless its fix is wrong or incomplete.

## CLOSURE PASS 5 (code-reviewer only; security and architect at bar)
Closure pass 4 (code 0/1H) fixed; record `AUDIT_BYOM_V02_SLICE4_IMPL_CLOSURE4.md`. Re-review the FULL `git diff origin/main` (uncommitted `main.go` included). Shape to re-challenge: `activeProviderTokenSecret` (read-only provider-token classification ahead of the named match and in `operatorDualControlAvailable`), together with the shared-key refusal and the ambiguity refusal — is any credential class the coordinator accepts still able to obtain an operator attribution on these three endpoints? Do not re-report an item from any record unless its fix is wrong or incomplete.

## CLOSURE PASS 6 (code-reviewer only; security and architect at bar)
Closure pass 5 (code 0/1H) fixed; record `AUDIT_BYOM_V02_SLICE4_IMPL_CLOSURE5.md`. Re-review the FULL `git diff origin/main` (uncommitted `main.go` included). Shape to re-challenge: `secretIsNotAProviderToken` tri-state classification (positive negative only; unknown fails closed; no token authority = no provider class) ahead of the named match and in `operatorDualControlAvailable`. Do not re-report an item from any record unless its fix is wrong or incomplete.
