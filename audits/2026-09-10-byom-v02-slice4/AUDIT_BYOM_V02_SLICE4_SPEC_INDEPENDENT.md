# Independent cold-context review — SPEC-047 v0.1.5 / SPEC-010 v1.8 (after the anchored loop closed at R4)

Three Claude reviewer lanes (code-reviewer, security-reviewer, architect; opus; neutral prompts naming only the diff and the sibling specs; forbidden from reading the audit records) reviewed `git diff origin/main -- specs/` at `d1ce1a64`.

| Lane | Verdict |
|---|---|
| architect | 1 C / 3 H / 9 M / 3 L / 2 I |
| code-reviewer | 0 C / 2 H / 9 M / 6 L / 4 I |
| security-reviewer | 0 C / 1 H / 6 M / 5 L / 3 I |

**The finding the anchored loop could not see (CRITICAL, architect):** the production catalog renewal (`renew-autotune-static-feed-signed.yml`, weekly cron) mints a date-embedded release id and re-signs the feeds while leaving rows and artifact sets untouched, and live sessions survive it as compatible-previous releases by design. Every exact-release rule the anchored rounds converged on (exact-release offer binding, provenance-tuple staleness, revocation when a member stops resolving) treated that re-stamp as a rotation: weekly fleet-wide revocation with a provider-fault reason, and `settlement_capable` unreachable after any rotation (architect HIGH-2), plus silent reason-code-free losses of earning (architect HIGH-4). Design-shape lesson recorded in memory: four anchored rounds had oscillated on "release binding" without ever checking what a release IS in production.

**Resolution (one consolidated rewrite, this commit):**
- Catalog identity anchored on CONTENT (row tuple `key, model_id, model_sha256`; per member `source, artifact_id, pair`); release/feed provenance recorded as evidence, never an equality gate; a re-stamp is a no-op for every admission (R001 offer paragraph "What a release re-stamp means"; R006 "drift is a content change").
- SPEC-010 v1.8 R004: the coordinator retains the artifact identity set of each compatible-previous release it retains; a session resolves in its OWN release's set (slice-4 implementation: keyed indexes built from the retained previous release directory, which carries its artifact feed). Closes the weekly GGUF identity drop.
- `catalog_release_mismatch` and the exact-release session check removed; `catalog_release_rotated` (security M3) unnecessary because rotation no longer strands candidates.
- Decision critical section scoped per (provider, candidate); reloads (candidate catalog, feeds, Tier-2 together) publish one monotonic release generation after the whole swap; decisions re-compare it before append; sweeps take each candidate's section; route snapshots use a lock-free compare-and-insert on head+binding+generation; lock order section→registry (architect HIGH-3, code M, security M4).
- Operator surface: per-actor credential class (`auth.operator_keys`, actor id recorded as `actor`, echoed as `decided_by`), dual control on the `settlement_capable` edge mirroring `/admin/hardware-trust/approve` (recorded as an open §5 decision), operator-scoped rate limits independent of the target provider's window (security M5/M6; architect M12), `operator_` reason prefix disjoint from every internal and provider-origin set (security L2, architect M9, code L).
- Closed grammars and codes completed: `expected_coordinator_event_id` `^[0-9a-f]{64}$`; `catalog_match_state` enum and `catalog_match_reason` field (with `primary_row_ambiguous` for a pair equal to two rows); every 409 code assigned to its clause and precedence fixed (head → edge → preconditions → generation → append); `catalog_row_ineligible` kept as a drift reason, the decision error renamed `catalog_row_not_recommendable`; `primary` member flag removed (`source` alone keys the six-value obligation); `runtime_source` persisted; members bounded to 16; idempotency key scoped per (provider, candidate) with the store index change stated; `coordinator_event_id` uniqueness and head-by-append-sequence stated; listing carries live-session facts and the decision response carries `bound_member`.
- `offer_rejected` stated unreachable in v0.1.5 (validation rejects synchronously; no origin appends it); the `offer_submitted` table row names `coordinator admission policy`.
- Session binding: derived only for `catalog_matched` candidates (unmatched never bind → non-BYOM path, stated); ambiguous candidates bind nothing and count as identity drift; revocation REFRESHES the binding to the terminal event (clears routability) rather than colliding with "clear"; hello-time drift evaluated against the newly derived binding when no prior exists (code HIGH-2, security L4); `catalog_row_status` stamped on the session and refreshed by the R006(c) sweep as the route-time carrier (code M, architect M10).
- R002 states the `catalog_model_key`/`earning_path_class` value-semantics change; R008 replaces "byte-for-byte" with a value-semantics test and adds every interleaving and lifecycle case the lanes named; §4 step 9 lists the store/event additions and the runbook's required content (re-stamp = nothing; replayed responses; GGUF cannot reach `settlement_capable` until a serving runtime path exists — code INFO).
- SPEC-010 R004 S1 rewritten so it no longer contradicts the composite-proof sentence (security H1); `catalog_candidate_sha256` labelled as the SPEC-023 candidate-catalog body digest, distinct from the Tier-2 `catalog_body_digest` (architect M11).

**Refuted (recorded, no change):** security M2 claimed a hello reporting the row's primary pair resolves through the feed index; slice 3's `verifyModelIdentity` returns `Verified` with no artifact binding on primary equality BEFORE consulting the index (`internal/ws/server.go`, primary-row short-circuit), so offer-time `candidate_row` and the session path agree, and the R006(b) exemption for all-`candidate_row` candidates is correct.

**Carried:** slice-5 intake counts need a normalized identity for unmatched offers (out of slice-4 scope); slice 6 needs a v2 provider status schema; slice 7 needs a coordinator-side evidence surface for the journey; `depends_on` should add SPEC-026 on the next SPEC-047 revision.

Closure: ONE codex pass over all three lanes on `git diff origin/main -- specs/` (the text changed materially).
