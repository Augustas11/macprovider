# Audit — BYOM v0.2 slice 4 SPEC: SPEC-047 v0.1.5 production decision path (#1453)

METHOD CONSTRAINT: first-party SPEC review (money path: catalog binding, admission decisions, settlement boundary). Do NOT modify or commit the worktree. Review `git diff origin/main -- specs/` on branch `feat/byom-v02-slice4-decision-path`: SPEC-047 v0.1.5 amendments to R001 (operator decision request + offer listing), the catalog-matched paragraph (offer-time match recorded on the offer event), R003 (decision preconditions and session-to-candidate binding), R006 (live drift sources), §4 step 9, §8 change log (v0.1.5 + the missing v0.1.4 entry). Read first: SPEC-047 in full; SPEC-010 §3.7 R001–R007 (v1.7); SPEC-023 §3.2, §3.7.4–§3.7.6, AC-CAT-7; SPEC-022 route-time snapshot minimum; SPEC-046-R003 identity_state; SPEC-026 admission keys. Then read the implementation the text must fit: `phase4-coordinator/internal/ws/model_admission.go` (state table `modelAdmissionAllowedNextStates`, event struct, store interface, offer verification, status readback), `internal/ws/server.go` (`verifyModelIdentity`/`resolveArtifactIdentity`, `revokeSettlementAdmissionForHeartbeatModelDrift`, `authorizedOperator`, admin routes), `internal/buyer/model_admission.go` (`byomRouteSnapshotBinding`, `byomSettlementPrereqsReady`, `byomCatalogModelKey`), `internal/artifactidentity/index.go`, `internal/pool/provider.go` (`IdentityPin`, admission fields).

## Invariants to challenge
- One authority: does every sentence agree with SPEC-010-R007 (identity set, exact pair, resolved key vs asserted key, session pin) and SPEC-047-R003's immutable route-time record as the settlement authority? Any restatement that could drift?
- Fail-closed completeness: can an operator decision reach `catalog_priced`/`settlement_capable` for a `listed` key, an unmatched offer, a stale/changed feed, a key the provider merely asserted, a session not verified for the member, or a candidate with no offer? Concurrency: two operators, replayed idempotency keys, a decision racing a revocation.
- Offer-time match: is "exactly one resolved member across all offered pairs" the right rule (offers may legitimately carry both the primary and a GGUF pair of the same key — should two pairs resolving to the SAME key but different artifact_ids match? the text says "different members" is an integrity failure — challenge that)? Null key for unmatched offers: does that break the v0.1 `catalog_model_key` readback contract (nullable already)? Is recording six values on the OFFER event (mutable admission state) consistent with R003 "never only in mutable admission state" (it is decision evidence, the snapshot stays authority)?
- Session binding: "carried on the session from hello and refreshed on every decision" — what if the provider serves the model before offering, or offers from a different session? What does the buyer do with a session whose binding names a revoked candidate?
- Drift: are (a) and (b) complete (feed rotated to a release with no feed? index nil?) and is `revoked` (vs demotion) the right outcome for feed change? Re-entry path.
- Closed schemas: request grammar, response shape (`model_admission_status.v1` + two fields), listing endpoint fields; SPEC-047-R007 bounds apply to an operator surface?
- Anything the text asserts that the named code cannot do without a further SPEC (SPEC-022, SPEC-023, SPEC-044, SPEC-010)?

## Lanes to report (this pass is: {{LANE}})
CRITICAL / HIGH / MEDIUM / LOW / INFO; bar 0 C / 0 H / 0 M. code-reviewer: implementability + testability of every MUST. security-reviewer: bypasses of the settlement boundary, operator-surface abuse, replay/concurrency, key/release confusion. architect: single authority across SPEC-010/022/023/046/047, contract drift, what slices 5–7 need. End with `VERDICT: <N> CRITICAL / <N> HIGH / <N> MEDIUM / <N> LOW / <N> INFO`. Cite section/paragraph. Do not invent issues.
