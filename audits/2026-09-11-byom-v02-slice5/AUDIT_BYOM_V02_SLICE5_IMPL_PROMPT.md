# Audit — BYOM v0.2 slice 5 IMPL: intake pipeline (SPEC-017 v0.2.1 §5.2b, SPEC-047 v0.1.6 intake aggregate, SPEC-023 v0.10.4 §16.8) (#1453)

METHOD CONSTRAINT: first-party IMPL review (privacy-critical: buyer-request aggregation; money-adjacent: catalog admission evidence and the offer-path sanction predicate). Do NOT modify or commit the worktree. Review the FULL working-tree diff `git diff origin/main -- phase4-coordinator scripts docs/runbooks specs/CONFORMANCE.json` (the working tree carries two uncommitted files, `phase4-coordinator/cmd/coordinator/main.go` and `phase4-coordinator/internal/config/config.go`, that are part of the change; review them as landed) against the governing text: SPEC-017 v0.2.1 §3.2a, §5.2b (all subsections and AC-INTAKE-1..5), §5.3, §7.2, §9.1, §9.2, §9.5; SPEC-047 v0.1.6 R001 intake aggregate paragraph and §4 step 10; SPEC-023 v0.10.4 §16.2(a) items 1–10 and S1–S5, §16.2(b), §16.2(c), §16.3, §16.4, §16.8 rules 1–9, AC-CAT-12, AC-CAT-13, AC-CAT-21. Quote the code you object to with file:line.

## What landed
- `internal/intake`: Space-Saving aggregator (S1–S5), opaque HMAC principal tokens under a per-window random key, per-principal cap and bucket bound as an admission filter, deterministic eviction, closed `other_suppressed` with per-counter saturation, emission only for buckets with ≥ 3 distinct principals AND lower bound ≥ buyer_request_floor, complete (epoch_elapsed) windows only in `Snapshot()`, incomplete windows destroyed at close, random `eligibility_policy_id`, per-window immutable parameters, `DiagnosticDump()` = open window only.
- `internal/buyer/intake_hook.go` + `server.go`: `WithIntakeObserver`; observed only at the `model_not_found` branch, only with an authenticated (gateway-context) account, `demo:` excluded.
- `internal/stats`: `intake` rollup component (`rollup/intake.go`: window merge with fail-closed id conflict, fleet histogram with complementary suppression, `provider_hardware_profiles` activity rule), 15-minute cadence (`rollup/config.go`), `stats_intake_current` + migration 028, store read, `intake_handler.go` (reader allowlist, provider-bound refusal, private cache, 404 when disabled), mux routing/auth, health component and thresholds.
- `internal/ws/model_admission_intake.go`: materialized `GET /admin/model-admission/intake` snapshot (cadence, ceiling, timeout, `intake_unavailable`), `providerIntakeSanctioned` four-category predicate shared with the offer path (`providerModelAdmissionSanctioned` now fails closed on an unreadable source), `ModelAdmissionOfferEventsSince` on both stores + `(state, created_at_utc)` index; `internal/onboarding/store_pg.go` `ProviderHardwareTrustSanctioned`.
- `scripts/catalog-release.py`: §16.8 `validate_intake_decision` (closed schema, one entry per transition, clause re-evaluation, per-signal windows, digests, private audit store re-derivation via `--intake-audit-dir`, fixed k), hooks in `generate`/`verify`; tests `IntakeDecisionManifestTest`.
- `cmd/coordinator/main.go` + `internal/config`: `stats.intake` config, aggregator wiring, `WithIntake` on the stats mux.
- `docs/runbooks/catalog-monthly-intake-release.md`; CONFORMANCE mappings.

## Invariants to challenge
- §16.2(a) fidelity: is S1–S5 order exact (an ineligible request contributes to nothing; out-of-grammar → other_suppressed only; capped/overflow → other_suppressed only and never S5)? Is the raw account id retained anywhere after token derivation (struct fields, closures, logs, dump)? Are `window_key` and principal tables zeroed at every close path? Are the bounds (K entries, K×P counters) actually enforced under fanout? Determinism of the victim rule?
- Emission: can a sub-k or sub-floor key reach the wire or the persisted row through any path (Snapshot, DiagnosticDump, rollup merge, persisted old rows)? Is an open or incomplete window ever persisted? Can a persisted stale window resurrect a key?
- Buyer hook: does `hasAuthenticatedAccount` hold exactly for gateway-context-authenticated requests? Can a bare header or a demo subject reach the aggregator? Is the raw model string retained by the buyer path? Any hot-path cost or lock contention?
- Stats: reader allowlist correctness (empty list, provider-bound key, unlisted key), 404-before-auth when disabled, cache/CORS headers, staleness budget, the rollup's SQL (role grants, `last_reported_at` window), merge fail-closed, health nine components, migration idempotency and grants.
- SPEC-047: counting rule (distinct providers per key; unmatched offers excluded; window inclusive on `created_at_utc`), the predicate's four categories and the availability rule (nil vs error), the offer path failing closed, snapshot staleness, ceiling/timeout, no partial snapshot, no provider field on the wire, rate limiting through `authorizedModelAdmissionOperator`.
- Generator: every AC-CAT-21 negative case; digest/audit-store semantics (missing store, uncited file, mismatched digest, encoding); window selection determinism; thresholds vs window parameters; public-manifest suppression mapping; promotion rules; `generate`/`verify` hooks and CLI flags; hermetic tests.
- Config/wiring: `stats.intake` defaults and validation; aggregator stop at shutdown; `poolsnapshot.WithIntake`; nothing wired when stats is disabled.
- Tests: do they assert the SPEC's AC-INTAKE-1..5 and step-10 lists, or only the happy path?

## Lanes to report (this pass is: {{LANE}})
CRITICAL / HIGH / MEDIUM / LOW / INFO; bar 0 C / 0 H / 0 M. code-reviewer: correctness vs the SPEC text, error handling, tests. security-reviewer: privacy leakage, principal handling, auth gates, fail-closed paths, resource bounds. architect: package boundaries (leaf intake package, stats request-path deny list, rollup ownership), lifecycle/locking, restart semantics, evidence path end to end. For each finding: severity, file:line, the quoted code, why it fails, the minimal fix.
