# Audit — BYOM v0.2 slice 5 SPEC closure pass 3 (#1453)

METHOD CONSTRAINT: SPEC-only review. Do NOT modify or commit the worktree. Review the FULL diff `git diff origin/main -- specs/` (SPEC-017 v0.2.1, SPEC-047 v0.1.6, SPEC-023 v0.10.4, CONFORMANCE.json, README.md) as one revision, together with the governing text it amends. Do not read `audits/` or `.omc/`; form your own view. You may read `phase4-coordinator` and `scripts/catalog-release.py` in this worktree to check implementability.

Since the previous SPEC round the revision changed, in response to an independent review: unmatched = key not a `listed`/`recommendable` row (catalog, not pool); `eligibility_policy_id` = HMAC under an operator salt; intake refusals 401-only with no CORS headers and a disabled endpoint unknown for OPTIONS; `principal_cap_pct ≤ 10` and knob maxima; wire drops `close_reason`/`eligible_request_total`, ≤ 3 windows, exact 30-day epochs, one timestamp form, closed `methodology`; persisted windows validated; fleet histogram frozen per 30-day period with a trust-root join, bounded window, highest-floor tie, sub-floor folding and a reconciliation invariant; §7.2.2 grants; C5 exemption; routability renamed §5.2c; request-log redaction; SPEC-047 counting by offer-time `intake_model_key` over DISTINCT pairs, per-build `nonce`, eligibility = unsanctioned + active trust root, trust category includes never-trusted, predicate counting-only, offer gate v0.1.5 scope, withdrawal never gated, R009; SPEC-023 ppm floored to 50 000 with units, §16.7 gate keyed on CONFORMANCE, rule 9 retention lifecycle (0600, ≥24 / ≤36 months, backup), rule 3 timestamp form; CONFORMANCE depends_on and SPEC-047-R009.

Closure pass 1 found only restatement drift (random vs HMAC policy id, exact-ppm change-log/example, 'fleet could not serve', event vs pair ceiling, SPEC-047 metadata, `created_at` shorthand, 404 `bad_request`), all fixed; since then SPEC-017 §5.2b also states: every intake refusal padded to the §5.4.3 rule-4 latency floor; OPTIONS on an enabled intake endpoint is 405 with no CORS header; a persisted row is served only after a read-side contract check; the excluded-set cardinality is never emitted.

Closure pass 2 found: generic OPTIONS/CORS text still covering intake (now carved out in §4.3, §5.7, §5.9, AC-21); no rate-limit bucket for key-less intake refusals (now: every intake 401 debits the auth-failure tier); served-but-unmatched redaction (now stated: the served model identity is persisted, never an unrecognized string); the canonical fleet example (now 15 = 4 + 5 + 6); R009 wording; salt entropy; a duplicate label — all fixed. Also new: the buyer hook fails closed without an admitted catalog; the pair ceiling is enforced at the store; read-back accepts one timestamp form only.

Judge: contradictions between the three specs and within each (every restatement of a changed rule — examples, ACs, change logs, tables, step lists — must agree); privacy (k-anonymity, differencing across polls and periods, public-manifest leakage, identity in grants and queries); intake-gaming (Sybil, epoch or period selection, sanctioned or never-trusted providers, salt rotation); closed-schema completeness and implementability of every MUST; evidence reconstructibility; ownership boundaries; operational realism.

## Lanes to report (this pass is: {{LANE}})
CRITICAL / HIGH / MEDIUM / LOW / INFO; bar 0 C / 0 H / 0 M. For each finding: severity, the quoted sentence, file:line, why it fails, the minimal edit. End with a table of counts per severity.
