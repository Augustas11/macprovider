# Audit — BYOM v0.2 slice 5 SPEC: intake pipeline sources (SPEC-017 v0.2.1, SPEC-047 v0.1.6, SPEC-023 v0.10.4) (#1453)

METHOD CONSTRAINT: first-party SPEC review (privacy-critical: buyer-request aggregation; money-adjacent: catalog admission evidence). Do NOT modify or commit the worktree. Review the FULL `git diff origin/main -- specs/` on this branch, and read the governing text it amends: SPEC-023 §16 (all of it — §16.2(a) items 1–10 and S1–S5, §16.2(b), §16.2(c), §16.3, §16.4, §16.7, §16.8, AC-CAT-12, AC-CAT-13, AC-CAT-21), SPEC-017 §1.5, §3.2, §4.3, §5.2a, §5.4, §5.6, §5.9, §7.2.1, §8.2, and SPEC-047-R001 (v0.1.5 text) and R007. Report findings against the diff; quote the sentence you object to.

## What the revision claims
1. SPEC-017 v0.2.1 §5.2b adopts the SPEC-023 §16.2(a) `unmatched_model_request_count` field contract by amendment — the §16.7 precondition — and fixes what §16.2(a) left to SPEC-017: eligibility flags (§5.2b.2: authenticated buyer account, reached model resolution, not in an operator-configured excluded-account set), the principal (§5.2b.3: the authenticated account id, transient, HMAC token under a per-window random key), the window (§3.2a: an aggregator epoch closed at 30 days / stop / parameter change, never resumed, never summed), the wire shape (`macprovider.stats-intake.v1`, closed at every level), the endpoint (partner key REQUIRED, provider-bound keys 403, no public projection), and a k-anonymity floor of 3. The same response carries a fleet RAM histogram (§5.2b.6) from which SPEC-023 §16.2(c) `fleet_fit_fraction` is evaluated conservatively by class floor.
2. SPEC-047 v0.1.6 adds `GET /admin/model-admission/intake` (`model_admission_intake_offer_counts.v1`): distinct providers per catalog key over the trailing 30 days from offer-time `catalog_matched` offers, R007-sanction-excluded, suppressed below the SPEC-023-owned floor, no per-provider field.
3. SPEC-023 v0.10.4 adds source and adoption statements only (§16.2(a)/(b)/(c), §16.7, §16.8 rule 2 digest semantics) — it claims NO normative rule of §16 changes.

## Invariants to challenge
- Adoption fidelity: does SPEC-017 §5.2b restate or weaken ANY item of §16.2(a) (items 1–10, S1–S5, the closed `other_suppressed` shape, per-counter saturation, eviction transfer rule, lower-bound-only floor semantics, suppression, transient raw principal, token destruction)? Is "adopts verbatim by reference" sufficient, or does a sentence in §5.2b contradict the referenced text? Is the k-anonymity floor ≥ SPEC-023's minimum and never lower?
- Window semantics: §3.2a makes a window an aggregator epoch closed on restart. Is that conforming to §16.2(a) items 7/10 (window_key never persisted, tokens destroyed at close)? Does the "never summed" rule plus the §16.8 observation-window recording leave a gap an operator could exploit (choosing among windows; a window shorter than the cadence; stale windows)? Does the 8-window / 90-day emission leak anything or create unbounded state? Does the rollup "merge by window_id" rule (mark unheld open window `aggregator_stopped`) have a race or an inconsistency with the aggregator's own close?
- Eligibility: is "authenticated buyer account + reached model resolution + not in excluded set" a faithful reading of item 1 ("auth-valid, non-test buyer requests")? Can a gateway-forwarded request without coordinator-level buyer auth (legacy `X-MacProvider-Account` header) be counted? Can an attacker mint principals cheaply (buyer accounts) to defeat the per-principal cap — and does the spec's answer (bounded principals per bucket, overflow → other_suppressed) hold at the wire?
- Privacy: does any emitted field (bucket keys, eligible_request_total, other_suppressed, fleet_ram classes, window ids, methodology) re-identify a buyer, a provider, or hardware? Is the partner-key gate the right class of consumer for this data, and is refusing provider-bound keys sufficient? Is `Cache-Control: private` + `Vary: Authorization` consistent with §5.4/§5.2a? Is the model key itself (a buyer-chosen string, normalized) a leakage vector (e.g. a buyer encodes data in model names)? Is the grammar + Space-Saving bound enough, and is emission of monitored keys acceptable?
- Fleet histogram: is "active = verified hardware profile reported in window OR 30d leaderboard row" sound? Does class-floor evaluation match SPEC-023 §16.2(c)'s `ram_gb - 4 >= min_ram_gb` conservatively in every case (boundary: min_ram_gb 12 vs floor 16)? Does suppression of small classes interact badly with `INTAKE_FLEET_FIT_MIN_PCT` on a ~10-provider fleet (could suppression make fit unreachable, and is that the intended fail-closed direction)?
- SPEC-047 intake aggregate: is "offer-time catalog_matched match is the ONE authority for 'names this key or matches a verified artifact'" consistent with SPEC-047 v0.1.5 R001/R003 and SPEC-023 §16.2(b)? Should withdrawn/revoked offers count (the text says yes: recorded interest)? Is the sanction predicate reference precise enough to implement and test? Is the 30-day window anchored correctly (created_at of offer events)? Any per-provider leakage via ordering, row presence, or timing? Bounds and rate limits?
- SPEC-023 v0.10.4: does any "sourced/adopted" sentence silently change a normative rule (e.g. `*_source_sha256` equality of the two intake digests, `fleet_fit_absent_reason`, `null` digest rule, the `spec017_amendment_not_landed` enum retention)? Are the restatements consistent with §16.8's example and rule 2? Cross-cutting: grep every restatement of these signal names across all three specs and CONFORMANCE.
- Implementability + testability: can every MUST be implemented in the named coordinator packages (buyer handler hook, stats rollup component, stats store, operator admin endpoint) and tested as AC-INTAKE-1..5 and the SPEC-047 step-10 tests state? Anything asserted that needs a further SPEC (SPEC-005, SPEC-006, SPEC-014, SPEC-022)?

## Lanes to report (this pass is: {{LANE}})
CRITICAL / HIGH / MEDIUM / LOW / INFO; bar 0 C / 0 H / 0 M. code-reviewer: implementability, testability, closed-schema completeness, contradictions between the three specs. security-reviewer: privacy leakage, principal-cap bypass, k-anonymity, intake-gaming, operator-surface abuse, cache/auth semantics. architect: ownership boundaries (SPEC-017 vs SPEC-023 vs SPEC-047), window/epoch model, state bounds, restart semantics, evidence reconstructibility (§16.8). For each finding: severity, the quoted sentence, why it fails, the minimal SPEC edit.

## ROUND 2 EXTRA
R1 (code 0/5H/8M/1L, security 0/0/7M/1L, architect 0/1H/8M/2L/1I) fixed; record `AUDIT_BYOM_V02_SLICE5_SPEC_R1.md` (read it AFTER forming your own view). Re-review the FULL `git diff origin/main -- specs/`. Shapes to re-challenge: distinct-principal k-anonymity with omission (§16.2(a) item 9, SPEC-017 §5.2b.4/5.2b.5) — does omission plus `suppressed_bucket_count` leak anything, and is "≥ k distinct tokens" sufficient given the 64-principal bound; the gateway-context eligibility definition and `demo:` exclusion; `stats.intake.reader_partner_key_ids` as the least-privilege gate (no schema change); per-window immutable `parameters` + `eligibility_policy_sha256` + `complete` and the deterministic latest-complete-window selection in SPEC-023 §16.2(a); §16.8 per-signal windows, rule 2 digest semantics, rule 4 (unmatched never key-attributable; SPEC-047 rows keep key-attributable `suppressed` — decide whether that is a leak given the operator credential's existing per-provider listing access), new rule 8 committed sources + re-derivation; SPEC-047 closed sanction predicate (admission rejection, canary sanction, revoked token) and the exclusions it states; SPEC-017 v0.2.1 LOCKED status; the Sybil residual as stated in §5.2b.3.

## ROUND 3 EXTRA
R2 (code 0/3H/6M/2L, security 0/0/3M/4L, architect 0/3H/4M/3L/1I) fixed; record `AUDIT_BYOM_V02_SLICE5_SPEC_R2.md` (read it AFTER forming your own view). Re-review the FULL `git diff origin/main -- specs/`. Shapes to re-challenge: complete-windows-only emission plus the "≥ k principals AND lower bound ≥ buyer_request_floor" emission rule (SPEC-017 §5.2b.4/5.2b.5) — is anything lost or leaked; the private intake audit store (SPEC-023 §16.8 rule 9) as the retention boundary and the operator-attestation framing; the fixed `INTAKE_K_ANONYMITY_MIN = 3` exception and lockstep rule; the four-category current-state `provider_intake_sanctioned` predicate in SPEC-047 (route/trust/payout/registration mappings, the availability rule, build-time evaluation) and SPEC-023 §16.2(b) citing it; the materialized 15-minute snapshot with ceiling and `intake_unavailable`; rule 3 nullability incl. the null top-level envelope; the 31-day cadence definition; `eligibility_policy_id`; complementary fleet suppression and the activity definition; SPEC-017 DRAFT status until lock and the SPEC-023 v0.10.4 dependency pin.
