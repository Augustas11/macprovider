# Build 1 Lane A artifact staging — SECURITY lane audit prompt

Lane: security-reviewer (LANE = security)

You are a first-party software-correctness / proof reviewer for the MacProvider repository. Method constraint: do NOT author, construct, or emit malformed payloads, exploit strings, or adversarial fixtures. Evaluate by reading source and by running the EXISTING tests only. Describe any gap abstractly in prose (field + condition + consequence).

Context: Product Build 1 Lane A (GitHub issue #1642). This slice makes `macprovider-cli models prepare` stage, verify, and durably adopt the exact Lane A artifact (`meta-llama/llama-3.2-3b-instruct` / `mlx-community/Llama-3.2-3B-Instruct-4bit`, revision `7f0dc925e0d0afb0322d96f9255cfddf2ba5636e`, snapshot-manifest hash `e7e5bff4248768b4db7a53afb3b514ba5867b800f63d1abd0330eaf08e54aa90`) after the signed staging artifact authority verifies. Boundary that MUST hold: no production coordinator, no rewards/payouts/settlement/public-earnings, no general BYOM, the active serving model is never changed, the canonical Hugging Face snapshot is never repaired or deleted, failure/cancellation leaves the durable store as found and reclaims the isolated staging directory, adoption only after the digest matches the signed authority, and public JSON events never leak raw private paths or secrets.

Governing docs (read first): `docs/product-roadmap/build-1/build1-control-recovery-plan-v1.md`, `docs/product-roadmap/build-1/lane-a-executable-provider-path-prd-v1.md`, `docs/product-roadmap/build-1/lane-a-executable-provider-path-test-spec-v1.md`, `docs/product-roadmap/build-1/narrow-mvp-plan-v6.md` (Phase C), `specs/SPEC-044-malibu-model-catalog-economics.md` (R007 preparation safety, R010 privacy boundary), `specs/SPEC-023-*.md` (§3.7 artifact feed, R004).

Worktree: `/Users/augstar/.codex/worktrees/macprovider/1642-lane-a-artifact-staging`. Base: `origin/main` (commit 12de7aea). Review the FULL fix diff as it will land:

    cd /Users/augstar/.codex/worktrees/macprovider/1642-lane-a-artifact-staging && git diff 12de7aea -- phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift phase3-binary/Sources/macprovider-cli/AutotuneRecommend.swift phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift phase3-binary/Tests/macprovider-cliTests/Build1LaneAArtifactStagerTests.swift

Read the touched files in full, plus the primitives they call: `CachedModelArtifactResolver`, `HuggingFaceSnapshotDownloader.downloadSnapshot`, `ModelArtifactVerifier.inspectCanonicalArtifact` (all in `AutotuneRecommend.swift`), `DurableModelArtifactStore.adoptVerifiedStaging`, `ModelPreparationTransactionEvent` (in `ModelPreparationContracts.swift`), and `Build1LaneAArtifactAuthorityResolver`.

Existing tests you may run: `cd /Users/augstar/.codex/worktrees/macprovider/1642-lane-a-artifact-staging/phase3-binary && swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'`.

Do NOT edit any file in the worktree except your report. Write your report to `/Users/augstar/.codex/worktrees/macprovider/1642-lane-a-artifact-staging/audits/2026-09-20/build1-lane-a-artifact-staging-LANE-audit.md` (replace LANE with your lane name). Report format: a findings table with columns Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO), ID, File:line, Finding, Evidence, Consequence, Required correction; then a final line exactly `VERDICT: CRITICAL=<n> HIGH=<n> MEDIUM=<n> LOW=<n> INFO=<n>`. If a lane finds nothing at a severity, say so explicitly.

Security-lane focus: filesystem trust boundaries (symlink/hardlink/path-escape handling for the isolated staging directory, the canonical snapshot, the durable root, and the prepare lock file; O_NOFOLLOW/owner checks; 0700 root); whether any path could let unverified bytes reach the durable store or be served; whether the signed-authority binding can be bypassed (test seams `makeStaticInputs`/`makeStager` are `nonisolated(unsafe) static var` — assess whether they are reachable from a shipped binary in any way); coordinator host allow-list preservation; secret/private-path leakage into stdout JSON or stderr (SPEC-044-R010); resource exposure (disk fill, unbounded staging retention, lock DoS); TOCTOU between verification and adoption; signal-handler safety; and confirmation that nothing in this diff can activate production, rewards, payouts, settlement, admission, or public earnings.
