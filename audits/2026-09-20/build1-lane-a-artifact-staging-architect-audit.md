# Build 1 Lane A artifact staging architecture audit

Lane: architect
Date: 2026-09-20
Base reviewed: `12de7aea`

Scope reviewed:
- `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift`
- `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift`
- `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift`
- `phase3-binary/Sources/macprovider-cli/AutotuneRecommend.swift`
- `phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift`
- `phase3-binary/Tests/macprovider-cliTests/Build1LaneAArtifactStagerTests.swift`
- Called primitives: `CachedModelArtifactResolver`, `HuggingFaceSnapshotDownloader.downloadSnapshot`, `ModelArtifactVerifier.inspectCanonicalArtifact`, `DurableModelArtifactStore.adoptVerifiedStaging`, `ModelPreparationTransactionEvent`, and `Build1LaneAArtifactAuthorityResolver`.

Validation run:

```bash
cd /Users/augstar/.codex/worktrees/macprovider/1642-lane-a-artifact-staging/phase3-binary
swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'
```

Result: passed, 62 selected XCTest tests, 0 failures.

No CRITICAL findings.
No HIGH findings.
No LOW findings.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| MEDIUM | ARCH-001 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:305` | The preparation disk-space gate is below the SPEC-044 storage-projection requirement and is checked only before transfer, not again before publication. | `requireDiskSpace` requires only `2 * expected` when staging and durable roots share a device at `Build1LaneAArtifactStager.swift:311` and only `expected` on each separate device at `Build1LaneAArtifactStager.swift:326`. Publication then proceeds through `adoptVerifiedStaging` at `Build1LaneAArtifactStager.swift:205` without a second free-space probe. SPEC-044 requires preparation free space of `2 * estimated_bytes + 1073741824` before transfer and again before publication, measured from the bound root volume (`specs/SPEC-044-malibu-model-catalog-economics.md:419`). The command-level test currently locks in the lower shared-volume threshold by expecting `required_bytes = size * 2` (`phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift:478`). | The command can start network/staging side effects in a state that SPEC-044 says must fail closed before the side effect. On a tight provider disk, this can convert a deterministic preflight refusal into a late staging/publication failure, and the test suite would not catch the under-reserved case because it asserts the current lower threshold. | Update the stager's disk admission rule to include the 1 GiB reserve from SPEC-044 and re-run the probe immediately before `adoptVerifiedStaging`. Update the command/stager tests to expect the reserve and to cover the second pre-publication check. If Lane A intentionally narrows this requirement, record that deviation in the governing Build 1 docs before keeping the lower threshold. |
| INFO | ARCH-002 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:68` | The stager uses the existing artifact/storage primitives rather than introducing a parallel source of truth. | Production construction goes through `CachedModelArtifactResolver.forConfig` at `Build1LaneAArtifactStager.swift:68`. The stager validates cache paths with the resolver at `Build1LaneAArtifactStager.swift:153` and `Build1LaneAArtifactStager.swift:178`, verifies bytes with `ModelArtifactVerifier.inspectCanonicalArtifact` at `Build1LaneAArtifactStager.swift:179`, and publishes through `DurableModelArtifactStore.adoptVerifiedStaging` at `Build1LaneAArtifactStager.swift:205`. The only visibility changes are the durable-root initializer `ensureRoot` at `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:162` and the cache-path validator at `phase3-binary/Sources/macprovider-cli/AutotuneRecommend.swift:4035`. | This is the right abstraction altitude for a Lane A adapter: the new code supplies tuple authority and orchestration, while the existing resolver/verifier/store remain the artifact identity and publication authorities. The visibility changes are minimal and are justified by the new stager needing root creation and cache-path validation without reimplementing those checks. | No correction required beyond fixing ARCH-001. Keep future preparation slices on the same primitive boundary rather than duplicating resolver, verifier, or durable-store logic. |
| INFO | ARCH-003 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:128` | The command stays inside the Build 1 Lane A boundary and does not claim production/admission/settlement authority. | Authority resolution requires an allowed staging coordinator URL at `ModelsPrepareCommand.swift:128`, checks the baked candidate tuple against the exact Lane A model/revision/hash at `ModelsPrepareCommand.swift:139`, and accepts only the signed artifact-feed primary with `verificationStatus == "verified"` and positive `sizeBytes` at `ModelsPrepareCommand.swift:160`. Production coordinator variants are rejected in tests at `phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift:244` and `phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift:261`. Successful stderr explicitly says preparation grants no admission, settlement, earnings, rewards, payouts, or production activation at `ModelsPrepareCommand.swift:390`. | This slice can prove local signed-authority preparation/adoption once ARCH-001 is corrected, but it does not prove the remaining Lane A blockers: private preparation-state record, serve/status runtime evidence, staging admission, gateway request, receipt/settlement evidence, or physical acceptance. Those remain layerable because the command and stager do not mutate serve runtime, coordinator state, rewards, payouts, or public economics surfaces. | No correction required in this slice. Track the remaining blockers as separate Lane A slices and do not present this local preparation result as Build 1 acceptance. |
| INFO | ARCH-004 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:217` | The event emitter is appropriately thin for the current command seam, but it is not a general transaction runtime. | `Build1LaneAPrepareEventEmitter` serializes monotonic `model_catalog_transaction_event.v1` frames under a lock at `ModelsPrepareCommand.swift:217`, and events use `ModelPreparationTransactionEvent` validation at `phase3-binary/Sources/macprovider-cli/ModelPreparationContracts.swift:1718`. Command tests assert ordered failed/cancelled event streams at `phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift:411` and `phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift:527`. | The seam is sufficient for a standalone CLI JSONL journey and avoids leaking root paths in stdout, but future private-state/cancel/recovery work should not grow this class into a second transaction store. SPEC-044's broader cancellation and reconciliation model still belongs in the existing private preparation-state/store contracts. | No correction required for this slice. When the private preparation-state blocker is implemented, route durable attempt, cancel marker, terminal history, and cleanup state through the existing `ModelPreparationPrivateStore`/contracts instead of extending the emitter. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=1 LOW=0 INFO=3
