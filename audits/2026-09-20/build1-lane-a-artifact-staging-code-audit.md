# Build 1 Lane A artifact staging code audit

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| MEDIUM | CODE-M-001 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:200` | Artifact authority is not revalidated immediately before publication/adoption. | `ModelsPrepareCommand` resolves the signed artifact authority once before staging at `ModelsPrepareCommand.swift:322`, then `stageAndAdopt` can spend the rest of the transaction downloading/verifying and proceeds from the old `authority` directly to `store.adoptVerifiedStaging` at `Build1LaneAArtifactStager.swift:200-210`. SPEC-044's preparation matrix requires the verified signed primary artifact binding to be rechecked at dispatch before network/staging access and immediately before publication. | If the staging artifact feed is revoked, becomes stale, changes signer/release binding, or otherwise fails closed while the transfer is in progress, the command can still durably adopt bytes that only matched the earlier authority snapshot. That weakens the Lane A boundary that adoption happens only after the current signed authority verifies. | Re-resolve the Lane A artifact authority immediately before adoption, after staged bytes verify and before `adoptVerifiedStaging`; compare catalog key, artifact id, model id, revision, hash algorithm, hash, size, release id, feed digest, and signer. Fail closed with `authority_unavailable` or `artifact_identity_mismatch` if the fresh authority is absent or differs. Add a command/stager test that injects authority drift between `artifact_verified` and adoption. |
| MEDIUM | CODE-M-002 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:148` | Disk headroom is checked only before a fresh download, and the calculation omits the SPEC reserve/recheck before publication. | `requireDiskSpace` is called only inside `if source == nil` before download (`Build1LaneAArtifactStager.swift:148-151`), so verified canonical or prefetch sources skip publication headroom checking entirely. The helper itself requires only `2 * expected` on a shared staging/durable volume (`Build1LaneAArtifactStager.swift:311-324`) or `expected` on each separate volume (`Build1LaneAArtifactStager.swift:326-331`), while SPEC-044 requires `2 * estimated_bytes + 1073741824` before transfer and again before publication, with overflow refusal before side effects. | A prepare run can pass preflight with less than the required reserve, or begin durable publication from an existing verified source without proving the durable volume has enough space. The failure then happens later in the copy/adoption path instead of fail-closing before the side effect, and tests currently encode the lower `2 * expected` threshold. | Centralize headroom calculation with checked `+ 1_073_741_824` reserve and overflow handling. Run it before transfer and again immediately before `adoptVerifiedStaging` for every non-reuse path, including canonical/prefetch reuse. Update tests to assert the reserve and the publication-time recheck. |
| MEDIUM | CODE-M-003 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:246` | Durable replacement is not rollback-safe if publication fails after removing an existing destination. | `copyRegularTree` builds a `.tmp-` sibling, then executes `removeItem(at: destination)` followed by `moveItem(at: staging, to: destination)` (`DurableModelArtifactStore.swift:246-247`). The catch path removes only the temporary staging directory (`DurableModelArtifactStore.swift:248-250`) and does not restore the prior destination. Lane A's boundary says failure/cancellation leaves the durable store as found. | If an existing same-hash destination is present but fails validation and the final move fails after removal, the durable store is left without the previous destination. Even when the previous entry was stale or corrupt, this violates the promised "as found" failure semantics and can turn a publication error into local artifact loss. | Replace with a rollback-safe sequence: move the old destination to a private backup, move the verified temp into place, fsync/readback as appropriate, then delete the backup only after success; on any failure, restore the backup. Add an injected final-move failure test proving the original destination remains present. |

No CRITICAL findings.
No HIGH findings.
No LOW findings.
No INFO findings.

Validation evidence: `swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'` passed 62 tests with 0 failures. `git diff --check` on the reviewed file set reported no whitespace errors. No LSP diagnostics tool was available in this Codex session; the Swift build phase from `swift test` is the available type-safety check.

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=3 LOW=0 INFO=0
