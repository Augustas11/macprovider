# Build 1 Lane A artifact staging - round 2 architecture audit

No CRITICAL findings. No HIGH findings. No LOW findings.

Validation run: `cd phase3-binary && swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'` passed 68 tests with 0 failures on 2026-09-20.

| Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO) | ID | File:line | Finding | Evidence | Consequence | Required correction |
|---|---|---|---|---|---|---|
| MEDIUM | ARCH-M-001 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:173` | `models prepare` does not enforce SPEC-044's 1 TiB maximum for the signed artifact `size_bytes` before making the prepare transaction actionable. | The Lane A authority resolver checks the signed primary artifact tuple and only requires `artifact.sizeBytes > 0` before returning authority (`ModelsPrepareCommand.swift:165-185`). The generic artifact-feed decoder likewise only validates `size_bytes` as a positive int64 (`AutotuneArtifactFeed.swift:260-271`). SPEC-044 says locally motivated `prepare_model` may be available only when exact `estimated_bytes` is positive and at most 1 TiB (`specs/SPEC-044-malibu-model-catalog-economics.md:436-438`), and the shared contract already defines `maxEstimatedBytes = 1_099_511_627_776` (`ModelPreparationContracts.swift:23-24`). | A signed staging artifact authority with `size_bytes` above the SPEC-044 action limit can still advance into queued/running prepare state, emit that value as expected bytes (`ModelsPrepareCommand.swift:330-339`), and rely only on disk-headroom refusal or transfer/adoption outcomes. That violates the action-gating projection even though it does not by itself cross into production, rewards, payouts, or active-model changes. | Add an explicit `artifact.sizeBytes <= ModelPreparationContracts.maxEstimatedBytes` guard in `Build1LaneAArtifactAuthorityResolver.resolve` before authority publication, fail closed before staging/download/adoption, and add an existing-test-style negative covering an over-limit signed `size_bytes` with no downloader call and no durable adoption. |
| INFO | ARCH-I-001 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:220` | Round-1 CODE-M-001 is closed: signed authority is re-resolved immediately before durable publication and must exactly equal the authority that gated staging. | The stager stores a `reauthorize` seam for immediate pre-publication re-resolution (`Build1LaneAArtifactStager.swift:82-87`), invokes it before touching the durable store (`Build1LaneAArtifactStager.swift:220-240`), and maps drift or unavailability to fail-closed errors. Tests cover authority drift and authority unavailability with no durable materialization (`Build1LaneAArtifactStagerTests.swift:295-345`). | The slice now preserves the SPEC-044/SPEC-023 requirement that a stale, revoked, or rebound signed feed cannot publish already-staged bytes. | No architecture correction required; preserve this second authority read in future private-state or serve/status layering. |
| INFO | ARCH-I-002 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:356` | Round-1 CODE-M-002 / ARCH-001 is closed: storage headroom now follows the SPEC-044 reserve rule and is rechecked before publication. | The stager documents the 1 GiB reserve (`Build1LaneAArtifactStager.swift:66-69`), checks before transfer (`Build1LaneAArtifactStager.swift:165-169`), rechecks after authority verification and before adoption (`Build1LaneAArtifactStager.swift:234-240`), requires `2 * expected + reserve` on the durable-root volume plus `expected + reserve` on a distinct staging volume, and refuses overflow (`Build1LaneAArtifactStager.swift:356-394`). Tests cover the durable-root reserve, distinct staging volume reserve, two probe passes, publication-time volume fill, and overflow refusal (`Build1LaneAArtifactStagerTests.swift:164-206`, `Build1LaneAArtifactStagerTests.swift:348-375`). | Publication is now fail-closed before durable side effects when storage preconditions are absent or drift during transfer. | No architecture correction required, apart from ARCH-M-001's missing upper bound on the authority's estimated bytes. |
| INFO | ARCH-I-003 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:212` | Round-1 CODE-M-003 / SEC-001 is closed: durable publication verifies the copied temp tree before swap and restores any parked destination if the swap fails. | `adoptVerifiedStaging` leaves an existing destination in place until a verified replacement is ready (`DurableModelArtifactStore.swift:97-111`). `copyRegularTree` rejects non-regular or escaping entries, verifies the temp copy hash before publication, and then calls the replace helper (`DurableModelArtifactStore.swift:212-267`). `replaceDestination` parks an existing destination in `.tmp-replaced-*`, restores it on move failure, and removes the backup only after a successful swap (`DurableModelArtifactStore.swift:274-298`). Tests cover failed-swap restoration and corrupted-copy refusal before publication (`DurableModelArtifactStoreTests.swift:42-89`). | A failed or corrupted copy no longer silently deletes prior durable bytes or publishes unverified bytes under the expected digest path. | No architecture correction required. |
| INFO | ARCH-I-004 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:71` | The stager is at the right abstraction altitude and reuses the existing artifact primitives instead of duplicating trust, download, verification, or durable-store logic. | `makeStager` injects `CachedModelArtifactResolver.forConfig` (`Build1LaneAArtifactStager.swift:71-80`), whose durable-root overlay mirrors serve preflight (`AutotuneRecommend.swift:3803-3810`). The stager calls the existing downloader (`Build1LaneAArtifactStager.swift:171-177`; `AutotuneRecommend.swift:3586-3626`), canonical artifact verifier (`Build1LaneAArtifactStager.swift:194-214`; `AutotuneRecommend.swift:4626-4680`), and durable store (`Build1LaneAArtifactStager.swift:242-255`; `DurableModelArtifactStore.swift:85-128`). The visibility changes are narrow: `ensureRoot` is internal only so the stager can create and lock the bound root (`DurableModelArtifactStore.swift:171-182`), and `validateNoSymlinkCachePath` is internal only so the staging path can reuse the resolver's cache safety check (`AutotuneRecommend.swift:4035-4065`). | Future work can layer private preparation records, serve/status evidence, and physical staging admission on the same durable-store and verifier contracts without forking artifact identity logic. | No correction required; keep Build 1 profile-specific checks in the command/stager layer and keep generic artifact integrity in `CachedModelArtifactResolver`, `ModelArtifactVerifier`, and `DurableModelArtifactStore`. |
| INFO | ARCH-I-005 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:441` | The durable-root prepare lock belongs at the stager transaction layer for this slice, not inside the generic durable store. | The lock is per bound durable root, private, regular-file-only, nonblocking, and held across authority check, staging/adoption decision, and durable publication (`Build1LaneAArtifactStager.swift:120-121`, `Build1LaneAArtifactStager.swift:441-456`). The durable store remains a reusable primitive that validates and swaps one verified tree (`DurableModelArtifactStore.swift:85-128`) without knowing Build 1 profiles, staging authority, cancellation, or event semantics. Command tests verify conflict refusal before transfer (`ModelsSubcommandTests.swift:542-589`). | This avoids coupling generic durable-store operations to a Build 1 transaction vocabulary while still serializing the only workflow that can stage and publish the Lane A tuple. | No correction required for this slice; if later workflows publish multiple artifact identities concurrently, introduce a shared durable-store transaction coordinator rather than embedding Lane A policy in `DurableModelArtifactStore`. |
| INFO | ARCH-I-006 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:6` | The slice stays within the Build 1 Lane A boundary, but it does not prove the remaining physical/economic provider path. | The command hardcodes the approved Lane A tuple (`ModelsPrepareCommand.swift:6-13`), rejects production and arbitrary coordinators via the staging/loopback allowlist (`ModelsPrepareCommand.swift:18-52`; tests at `ModelsSubcommandTests.swift:244-327`), keeps public diagnostics path-free (`ModelsPrepareCommand.swift:438-457`; test at `ModelsSubcommandTests.swift:405-409`), and success copy explicitly states no admission, settlement, earnings, rewards, payouts, or production activation (`ModelsPrepareCommand.swift:393-398`). The control ledger says Lane A still needs physical staging, gateway, receipt, settlement, and validator evidence (`build1-control-recovery-plan-v1.md:88-103`), and the test spec says fixture-only/local-only evidence is non-proof (`lane-a-executable-provider-path-test-spec-v1.md:91-104`). The narrow MVP plan still calls for a root-validated private inventory record and serve/status evidence in later layering (`narrow-mvp-plan-v6.md:95-105`, `narrow-mvp-plan-v6.md:160-171`). | This PR can prove signed authority, local staging, digest verification, durable adoption safety, cancellation cleanup, and non-production boundaries; it does not prove private preparation-state persistence, active serve/status use, staging admission, gateway request/response, receipt/audit, settlement retrieval, rewards/payout disabling in staging infrastructure, or physical Apple Silicon acceptance. | No correction required inside this slice beyond ARCH-M-001. Carry the listed non-proofs as explicit blockers for subsequent Lane A slices. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=1 LOW=0 INFO=6
