# Build 1 Lane A artifact staging code audit round 2

Lane: code-reviewer
Base reviewed: `12de7aea`

Method: read the governing Build 1 Lane A docs and SPEC sections, reviewed the full scoped diff and called primitives, re-checked the round-1 code/security/architecture reports, and ran only the existing permitted tests. No malformed payloads, exploit strings, adversarial fixtures, or source edits were authored.

No CRITICAL findings.
No HIGH findings.
No LOW findings.

Validation evidence:
- `cd phase3-binary && swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'` passed: 68 selected tests, 0 failures.
- `git diff --check 12de7aea -- <reviewed file set>` reported no whitespace errors.
- Pattern scan for common review hazards found no actionable hardcoded secrets or empty catches in the reviewed file set. Matches were existing secret-handling variable names, password stripping, the `HF_TOKEN` environment read, and an ephemeral test signing key.
- No `lsp_diagnostics` tool is available in this Codex session; the Swift build phase from `swift test` is the available type/typecheck evidence.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| MEDIUM | CODE-M-004 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:173` | The Lane A artifact authority accepts an over-limit signed `size_bytes` instead of enforcing SPEC-044's `estimated_bytes <= 1 TiB` preparation precondition. | `Build1LaneAArtifactAuthorityResolver.resolve` verifies the exact model, revision, hash, runtime source, `verification_status == "verified"`, and only `artifact.sizeBytes > 0` before returning authority. `ArtifactFeed.decodeArtifact` similarly enforces positive int64 size, not the SPEC-044 preparation ceiling. SPEC-044's preparation matrix requires `estimated_bytes` to be positive and at most 1 TiB before Prepare is available. | A signed staging feed for the exact Lane A tuple with `size_bytes` above 1 TiB would still emit `artifact_authority_verified` and enter staging/headroom logic. It may fail later as insufficient disk/overflow, or proceed on a very large disk, instead of failing closed as unavailable authority before preparation side effects. This is spec drift and can produce misleading operator progress/headroom diagnostics. | Add the 1 TiB ceiling to the Lane A authority gate, for example `artifact.sizeBytes <= 1_099_511_627_776`, and map violations to the existing authority/tuple failure path before staging. Add a command or resolver test proving an over-limit signed size fails before any disk probe or transfer. |
| INFO | CODE-R2-001 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:223` | Round-1 CODE-M-001 is closed: authority is re-resolved immediately before publication and compared to the staging authority. | The stager calls `reauthorize()` after staged-byte verification and before the publication headroom check/adoption, then refuses `fresh != authority` with `.authorityMismatch`. Tests cover authority drift and authority unavailable between verification and publication. | The original stale-authority adoption gap is closed for this slice. | No correction required. |
| INFO | CODE-R2-002 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:359` | Round-1 CODE-M-002 is closed: headroom now includes the SPEC reserve and is checked before transfer and before publication on non-reuse paths. | `checkedRequirement` computes `size * multiplier + 1_073_741_824` with overflow refusal; `requireDiskSpace` requires `2 * size + reserve` on the durable root and `size + reserve` on a distinct staging volume. The stager calls it before download and again immediately before adoption. Tests cover shared-volume reserve, distinct staging reserve, overflow, canonical-source publication recheck, and volume-fill recheck. | The original under-reserved and missing publication-time headroom gap is closed. | No correction required. |
| INFO | CODE-R2-003 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:217` | Round-1 CODE-M-003 is closed for the reported swap failure mode: publication no longer removes an existing destination before a verified replacement is ready, and failed swaps restore the prior destination. | `copyRegularTree` copies to a private `.tmp-` sibling, verifies the temp tree hash before publication, and only then calls `replaceDestination`; `replaceDestination` parks the old destination in `.tmp-replaced-*`, restores it if the move into place fails, and removes the backup only after success. Tests cover failed final move restoring a prior corrupt destination and corrupted copy being refused before publication. | The original destination-loss-on-final-move failure is closed, and unverified copied bytes are not left under the expected hash path. | No correction required. |
| INFO | CODE-R2-004 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:361` | The command/stager event stream and failure mapping are covered for the relevant Lane A states, with the expected limitation that command-level success cannot use the real signed digest in unit tests. | Command tests cover queued/running/failed for missing authority, digest mismatch, disk failure, conflict, config failure, unsupported guards, and cancellation as `cancel_requested` then `cancelled`. Stager tests cover the successful stage -> verify -> adopt path using a small fixture authority whose digest matches the fixture directory, exercising the same `stageAndAdopt` code path that production uses after authority resolution. | Test adequacy is acceptable for this slice's local correctness boundary; physical success with the real Hugging Face snapshot remains outside unit-test scope and must be proven by the later staging evidence journey. | No correction required for this slice. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=1 LOW=0 INFO=4
