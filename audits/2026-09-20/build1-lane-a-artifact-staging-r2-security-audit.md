# Build 1 Lane A artifact staging security audit (round 2)

Scope: full diff from `12de7aea` to the working tree for `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift`, `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift`, `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift`, `phase3-binary/Sources/macprovider-cli/AutotuneRecommend.swift`, `phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift`, `phase3-binary/Tests/macprovider-cliTests/Build1LaneAArtifactStagerTests.swift`, and `phase3-binary/Tests/macprovider-cliTests/DurableModelArtifactStoreTests.swift`, plus the called primitives named in the prompt.

Method: read-only source review against the governing Build 1 Lane A docs, SPEC-044 R007/R010, and SPEC-023 artifact-feed requirements; existing tests only; no malformed payloads or adversarial fixtures authored. Validation evidence: `git diff --check 12de7aea -- ...` was clean; `swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'` passed 68 tests with 0 failures; scoped secrets scans found no hardcoded secret material, only expected references to token/password field names, test-generated signing keys, and redaction assertions; `swift package show-dependencies` enumerated SwiftPM dependencies, but no SwiftPM CVE audit tool was available in this repo, so no CVE-clean claim is made.

No CRITICAL findings.
No HIGH findings.
No MEDIUM findings.
No LOW findings.
INFO rows below document round-1 closure and non-blocking security evidence only.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| INFO | SEC-R2-INFO-001 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:261` | Round-1 SEC-001 / CODE-M-003 is closed: copied temp trees are verified before publication, failed swaps restore the prior destination, and post-swap readback mismatches remove the destination. | `copyRegularTree` hashes the `.tmp-*` copy before `replaceDestination`; `replaceDestination` parks an existing destination under `.tmp-replaced-*` and restores it on move failure; `adoptVerifiedStaging` removes a destination whose post-swap hash read fails or mismatches. Existing tests `testCorruptedCopyIsRefusedBeforePublication` and `testFailedSwapRestoresPriorDestination` passed. | No new security consequence found; unverified bytes are not durably adopted under the expected hash on the reviewed failure paths. | None. |
| INFO | SEC-R2-INFO-002 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:223` | Round-1 CODE-M-001 is closed: the signed Lane A artifact authority is re-resolved immediately before publication and must match the authority used for staging. | Lines 223-233 call `reauthorize()` after staged bytes verify and before durable publication; missing authority fails as `authorityUnavailable`, mismatch fails as `authorityMismatch`, and command errors map to `authority_unavailable` / `artifact_identity_mismatch`. Existing tests `testAuthorityDriftBetweenVerificationAndPublicationRefusesAdoption` and `testAuthorityUnavailableBeforePublicationRefusesAdoption` passed. | Revoked, rebound, or stale signed authority during transfer does not authorize durable adoption. | None. |
| INFO | SEC-R2-INFO-003 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:356` | Round-1 CODE-M-002 / ARCH-001 is closed: disk headroom now includes the required safety reserve and is checked before transfer and immediately before publication. | `makeDiskRequirement` requires `2 * size_bytes + 1 GiB` on the bound durable-root volume and `size_bytes + 1 GiB` on a distinct staging volume, with overflow refusal; lines 235-240 re-check publication headroom before adoption. Existing tests `testDiskHeadroomRequiresSpecReserveBeforeTransferAndPublication`, `testPublicationHeadroomRecheckRefusesAdoptionOfVerifiedSource`, and `testModelsPrepareRefusesToStageWithoutDiskHeadroom` passed. | Tight-disk and disk-fill cases fail closed before network transfer or before durable publication side effects. | None. |
| INFO | SEC-R2-INFO-004 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:171` | Filesystem trust-boundary checks cover the durable root, destination ancestors, copied tree, canonical snapshot inspection, and prepare lock file. | `ensureRoot` rejects symlink roots and chmods the store root to 0700; `validateNoSymlinkAncestors` rejects symlink/path-escape destination ancestors; `copyRegularTree` rejects symlinks, non-regular entries, and resolving-symlink path escapes before copy; `ModelArtifactVerifier.inspectCanonicalArtifact` rejects symlinks, non-regular files, hardlinks, and manifest path escapes; `ModelPreparationLock.acquire` uses `O_NOFOLLOW`, `O_CLOEXEC`, owner checks, and regular-file checks. Existing symlink, traversal, corrupt-copy, corrupt-destination, and lock-conflict tests passed. | No reviewed path lets unverified bytes reach the durable store or active serving path through symlink, hardlink, or path-escape handling. | None. |
| INFO | SEC-R2-INFO-005 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:28` | Coordinator and signed-authority boundaries remain preserved. | `Build1LaneAModelsPreparePolicy.validateCoordinator` allows loopback and the two staging hosts only, requires HTTPS/WSS plus default port for staging hosts, and rejects production coordinator hosts; `Build1LaneAArtifactAuthorityResolver.resolve` requires the exact baked model/source/revision/hash/runtime tuple, live signed artifact feed, verified status, positive size, and no feed warnings before returning authority. Existing coordinator rejection and authority failure tests passed. | The diff does not create a path to production coordinator activation, general BYOM authority, or unsigned artifact adoption. | None. |
| INFO | SEC-R2-INFO-006 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:217` | Public JSON and stderr boundaries remain consistent with SPEC-044 R010. | `ModelPreparationTransactionEvent` is a closed event schema without filesystem path fields; `ModelsPrepareCommand` emits model id, revision, hash, size, runtime source, release, signer id, and feed hash, but not raw durable, staging, canonical, or token values. Existing tests assert digest mismatch and success JSON do not contain durable, staging, or cache roots. Scoped grep found no new secret constants in the reviewed diff. | No raw private path or secret leakage was found in public stdout JSON or reviewed stderr output. | None. |
| INFO | SEC-R2-INFO-007 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:74` | Test seams do not bypass signed authority in shipped CLI behavior. | `makeStager` and `makeStaticInputs` are internal module static variables used by `@testable` tests; no CLI flag, environment variable, plugin surface, or production call path was found that lets an external caller replace them. Shipped `models prepare` still constructs through `Build1LaneAModelsPreparePolicy.defaultStager`. | The test seams do not create a runtime security bypass for signed-authority verification in the shipped binary. | None. |
| INFO | SEC-R2-INFO-008 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:393` | The reviewed diff does not activate production, rewards, payouts, settlement, admission, or public earnings. | Success output explicitly states the command does not enable admission, settlement, earnings, rewards, payouts, or production activation; source review found no calls into production coordinator, rewards, payout, settlement, or public-earnings code from the Lane A prepare path. | Build 1 Lane A remains an isolated staging/adoption path for the fixed verified artifact only. | None. |
VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=0 INFO=8
