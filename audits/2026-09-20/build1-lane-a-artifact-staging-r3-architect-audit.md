# Build 1 Lane A artifact staging - round 3 architecture audit

Lane: architect
Base reviewed: `12de7aea`

Method: read the governing Build 1 Lane A control/PRD/test-plan docs, SPEC-044 R007/R010 and SPEC-023 artifact-feed authority sections, the full scoped diff, the touched implementation/test files, and the called resolver/downloader/verifier/durable-store/event primitives. Existing tests only were run; no malformed payloads, exploit strings, adversarial fixtures, or source edits were authored.

Validation evidence:
- `cd phase3-binary && swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'` passed: 69 selected XCTest tests, 0 failures.
- `git diff --check 12de7aea -- phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift phase3-binary/Sources/macprovider-cli/AutotuneRecommend.swift phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift phase3-binary/Tests/macprovider-cliTests/Build1LaneAArtifactStagerTests.swift phase3-binary/Tests/macprovider-cliTests/DurableModelArtifactStoreTests.swift` reported no whitespace errors.

No CRITICAL findings.
No HIGH findings.
No MEDIUM findings.
No LOW findings.
INFO rows below document prior-finding closure, architectural fit, and remaining non-proofs.

| Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO) | ID | File:line | Finding | Evidence | Consequence | Required correction |
|---|---|---|---|---|---|---|
| INFO | ARCH-R3-001 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:176` | Round-2 CODE-M-004 / ARCH-M-001 is closed: the Lane A authority gate now enforces SPEC-044's 1 TiB `estimated_bytes` ceiling before preparation can become actionable. | `Build1LaneAPrepareProfile.maxArtifactSizeBytes` is wired to `ModelPreparationContracts.maxEstimatedBytes` (`ModelsPrepareCommand.swift:14-16`; `ModelPreparationContracts.swift:23-24`), and `Build1LaneAArtifactAuthorityResolver.resolve` now requires `artifact.sizeBytes > 0` and `artifact.sizeBytes <= Build1LaneAPrepareProfile.maxArtifactSizeBytes` before returning authority (`ModelsPrepareCommand.swift:168-180`). The new command-level test sets the Lane A artifact one byte over the limit, fails with `[queued, failed]` / `authority_unavailable`, and asserts no downloader or disk probe is reached (`ModelsSubcommandTests.swift:637-682`). | Over-limit signed artifact metadata now fails closed as unavailable authority before disk probing, transfer, staging, or durable adoption, matching SPEC-044's preparation precondition at `specs/SPEC-044-malibu-model-catalog-economics.md:436-438`. | No correction required. Keep the limit in the authority resolver rather than the stager so every dispatch and reauthorization read fails before side effects. |
| INFO | ARCH-R3-002 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:223` | Round-1 authority, disk-headroom, and durable-swap findings remain closed in the current full diff. | The stager re-resolves signed authority immediately before publication and refuses missing or drifted authority (`Build1LaneAArtifactStager.swift:220-233`), rechecks disk headroom before transfer and before adoption (`Build1LaneAArtifactStager.swift:165-169`, `Build1LaneAArtifactStager.swift:235-240`), and computes `2 * size_bytes + 1 GiB` for the durable root plus `size_bytes + 1 GiB` on a distinct staging volume with overflow refusal (`Build1LaneAArtifactStager.swift:356-394`). Durable replacement verifies the temp copy before swap and restores a parked destination on swap failure (`DurableModelArtifactStore.swift:212-298`). Existing regression tests cover authority drift/unavailability, reserve/recheck/overflow, and failed-swap/corrupt-copy paths (`Build1LaneAArtifactStagerTests.swift:164-206`, `Build1LaneAArtifactStagerTests.swift:295-375`; `DurableModelArtifactStoreTests.swift:42-90`). | The full r3 diff preserves the previously required fail-closed behavior: stale authority cannot publish staged bytes, tight storage fails before durable side effects, and failed publication does not delete prior durable bytes or leave mismatched bytes under the expected hash. | No correction required. |
| INFO | ARCH-R3-003 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:74` | The stager remains at the right abstraction altitude and reuses existing artifact primitives instead of creating a parallel artifact authority. | Production construction goes through `CachedModelArtifactResolver.forConfig` (`Build1LaneAArtifactStager.swift:74-79`), the downloader is `HuggingFaceSnapshotDownloader.downloadSnapshot` (`Build1LaneAArtifactStager.swift:171-177`; `AutotuneRecommend.swift:3586-3626`), byte identity comes from `ModelArtifactVerifier.inspectCanonicalArtifact` (`Build1LaneAArtifactStager.swift:196-214`; `AutotuneRecommend.swift:4626-4680`), and durable publication remains `DurableModelArtifactStore.adoptVerifiedStaging` (`Build1LaneAArtifactStager.swift:242-255`; `DurableModelArtifactStore.swift:85-128`). The two visibility changes are narrow: `ensureRoot` is internal for root creation before locking (`DurableModelArtifactStore.swift:171-182`) and `validateNoSymlinkCachePath` is internal for cache-root safety reuse (`AutotuneRecommend.swift:4035-4065`). | Future private-state, serve/status, staging-admission, and evidence-driver layers can build on the same resolver/verifier/store contracts without reworking this slice or forking artifact identity logic. | No correction required. |
| INFO | ARCH-R3-004 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:350` | Config loading and durable-root selection are correctly coupled to the command boundary and remain parallel with serve preflight. | `models prepare` loads config through `ConfigLoader.load(cli: CLIOverrides(configPath: config))` (`ModelsPrepareCommand.swift:345-357`), then builds the stager from that config (`ModelsPrepareCommand.swift:360-364`). `CachedModelArtifactResolver.forConfig` overlays absolute `model_artifact_root` (`AutotuneRecommend.swift:3803-3810`), matching serve preflight's durable-root overlay (`MacProviderCLI.swift:830-832`) and the shared YAML/env config fields (`Config.swift:468`, `Config.swift:556`). | The adopted artifact lands in the same provider-owned durable root that `serve` will later inspect, while this command still avoids mutating YAML, active model selection, control sockets, coordinator admission, settlement, rewards, or payouts. | No correction required. |
| INFO | ARCH-R3-005 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:397` | The slice stays inside Build 1 Lane A, and its non-proofs are explicit. | The command is hard-gated to the exact Lane A tuple (`ModelsPrepareCommand.swift:6-13`, `ModelsPrepareCommand.swift:142-180`), rejects non-staging/production coordinator inputs (`ModelsPrepareCommand.swift:31-55`; tests at `ModelsSubcommandTests.swift:245-323`), and success copy states that preparation grants no admission, settlement, earnings, rewards, payouts, or production activation (`ModelsPrepareCommand.swift:397-401`). Public transaction events are closed `model_catalog_transaction_event.v1` frames (`ModelsPrepareCommand.swift:221-258`; `ModelPreparationContracts.swift:1718-1795`) and tests assert stdout does not leak durable or staging roots on failure (`ModelsSubcommandTests.swift:406-409`). The control ledger and Lane A test spec still require physical staging, runtime/status, gateway, receipt, settlement, and validator evidence (`docs/product-roadmap/build-1/build1-control-recovery-plan-v1.md:88-103`; `docs/product-roadmap/build-1/lane-a-executable-provider-path-test-spec-v1.md:89-104`). | This PR can prove signed authority resolution, local staging, snapshot-manifest verification, durable adoption safety, cancellation/failure cleanup for the transaction-created staging directory, and no-production boundaries. It does not prove private preparation-state persistence, active serving evidence, staging coordinator admission, gateway request/response, receipt/audit correlation, settlement retrieval, rewards/payout disablement in staging infrastructure, or physical Apple Silicon acceptance. | No correction required in this slice. Carry those non-proofs as explicit blockers for later Lane A slices. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=0 INFO=5
