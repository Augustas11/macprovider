# Build 1 Lane A artifact staging code audit round 3

Lane: code-reviewer
Base reviewed: `12de7aea`

Method: read the governing Build 1 Lane A control docs, PRD/test spec, narrow MVP Phase C, SPEC-044 R007/R010, and SPEC-023 artifact-feed requirements; reviewed the full scoped diff and called primitives; re-checked round-1 and round-2 code/security/architecture audit reports; ran only the permitted existing tests. No malformed payloads, exploit strings, adversarial fixtures, or source edits were authored.

No CRITICAL findings.
No HIGH findings.
No MEDIUM findings.
No LOW findings.

Validation evidence:
- `cd phase3-binary && swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'` passed: 69 selected XCTest cases, 0 failures.
- `git diff --check 12de7aea -- <reviewed file set>` reported no whitespace errors.
- Scoped pattern scan for hardcoded secrets / empty catches / JS console logging in the reviewed files returned no matches.
- No `lsp_diagnostics` tool is available in this Codex session; the Swift build phase from `swift test` is the available type/typecheck evidence.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| INFO | CODE-R3-001 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:176` | Round-2 CODE-M-004 and ARCH-M-001 are closed: the Lane A authority gate now enforces the SPEC-044 1 TiB `estimated_bytes` ceiling before returning authority. | `Build1LaneAArtifactAuthorityResolver.resolve` requires `artifact.sizeBytes > 0` and `artifact.sizeBytes <= Build1LaneAPrepareProfile.maxArtifactSizeBytes`, where `maxArtifactSizeBytes` is `ModelPreparationContracts.maxEstimatedBytes`. `testModelsPrepareRejectsSignedFeedWithOverLimitLaneAArtifactSize` covers an over-limit signed feed and asserts no downloader or disk probe is reached. | The prior over-limit signed-authority path now fails closed as authority unavailable before staging, disk probing, transfer, or adoption. | None. |
| INFO | CODE-R3-002 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:223` | Round-1 CODE-M-001 remains closed: signed authority is re-resolved immediately before publication and must match the staging authority. | The stager calls `reauthorize()` after staged bytes verify and before the publication headroom check/adoption, then refuses `fresh != authority` as `.authorityMismatch`; tests cover authority drift and authority unavailability after verification with no durable materialization. | Revoked, stale, rebound, or unavailable signed authority during transfer cannot authorize durable adoption. | None. |
| INFO | CODE-R3-003 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:359` | Round-1 CODE-M-002 / ARCH-001 remains closed: disk headroom includes the SPEC reserve and is checked before transfer and before publication on every non-reuse path. | `checkedRequirement` computes checked `size * multiplier + 1_073_741_824`; `requireDiskSpace` requires `2 * size + reserve` on the durable-root volume and `size + reserve` on a distinct staging volume. The stager calls it before download and again immediately before adoption. Tests cover reserve thresholds, overflow, canonical-source publication recheck, and volume-fill recheck. | Tight-disk and disk-fill cases fail closed before network transfer or before durable publication side effects. | None. |
| INFO | CODE-R3-004 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:217` | Round-1 CODE-M-003 / SEC-001 remains closed: durable publication verifies copied temp bytes before publication and restores any parked destination if the swap fails. | `copyRegularTree` hashes the private `.tmp-*` copy before `replaceDestination`; `replaceDestination` parks an existing destination in `.tmp-replaced-*`, restores it if the final move fails, and removes the backup only after success. Tests cover corrupted-copy refusal and failed-swap restoration. | A failed or corrupted copy no longer deletes prior durable bytes or publishes unverified bytes under the expected digest path. | None. |
| INFO | CODE-R3-005 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:365` | The stage -> verify -> adopt event stream is monotonic and covered for the reachable unit-test paths; command-level success with the real signed digest remains intentionally outside unit scope. | The shared emitter increments `event_sequence` under a lock. Command tests cover queued/running/failed, cancellation as `cancel_requested` -> `cancelled`, timed config/authority/storage/conflict failures, and path-redaction assertions. Stager tests cover successful fresh staging with `.staging`, `.verified`, `.adopted`; command success cannot be unit-tested against the real Lane A digest, but the stager-level success test exercises the same `stageAndAdopt` implementation after authority resolution. | Test adequacy is acceptable for this local slice; physical success with the real Hugging Face snapshot remains a later staging-evidence requirement, not a unit-test claim. | None. |
| INFO | CODE-R3-006 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:397` | The reviewed diff stays inside the Lane A boundary and does not activate production, rewards, payouts, settlement, public earnings, general BYOM, or active-model switching. | The command hardcodes the approved Lane A tuple, rejects production/arbitrary coordinator hosts, resolves only the signed staging artifact authority, emits path-free transaction JSON, and success diagnostics state that preparation grants no admission, settlement, earnings, rewards, payouts, or production activation. Source review found no calls from the prepare path into active serving config mutation, production coordinator activation, rewards, payouts, or settlement code. | This slice proves local signed-authority staging/adoption safety only; remaining physical staging, serve/status, admission, gateway, receipt, and settlement evidence remain out of scope. | None. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=0 INFO=6
