# Build 1 Lane A artifact staging security audit (round 3)

Scope: full security-lane review of the diff from `12de7aea` to the working tree for:

- `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift`
- `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift`
- `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift`
- `phase3-binary/Sources/macprovider-cli/AutotuneRecommend.swift`
- `phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift`
- `phase3-binary/Tests/macprovider-cliTests/Build1LaneAArtifactStagerTests.swift`
- `phase3-binary/Tests/macprovider-cliTests/DurableModelArtifactStoreTests.swift`

Risk level: LOW. No CRITICAL, HIGH, MEDIUM, or LOW security findings were identified in round 3.

Method: source review against the Build 1 Lane A control docs, SPEC-044 R007/R010, and SPEC-023 artifact-feed requirements; existing tests only; no malformed payloads, exploit strings, or adversarial fixtures authored. OWASP Top 10 categories were evaluated where applicable: access control/production gating, cryptographic trust binding, injection/path traversal, insecure design, security configuration, vulnerable components, auth/session boundaries, software/data integrity, logging/privacy, and SSRF/redirect boundaries.

Validation evidence:

- `cd phase3-binary && swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'` passed: 69 selected tests, 0 failures.
- `git diff --check 12de7aea -- <reviewed file set>` passed with no whitespace errors.
- Scoped secrets scan over the reviewed source/tests found no hardcoded production secrets. Matches were URL user/password rejection, comments, existing HF token header handling, token-count identifiers, and ephemeral test signing keys.
- Scoped branch-history secrets scan over `12de7aea..HEAD` found no secret material in the reviewed diff history; the only match was a comment promising no private path/token diagnostics.
- Dependency check: `swift package show-dependencies` enumerated pinned SwiftPM dependencies (`mlx-swift-lm`, `mlx-swift`, `swift-transformers`, `swift-huggingface`, `swift-nio`, `swift-argument-parser`, `yams`, and transitive packages). No SwiftPM CVE audit tool is available in this repo/toolchain, so no CVE-clean claim is made.

No CRITICAL findings.
No HIGH findings.
No MEDIUM findings.
No LOW findings.

| Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO) | ID | File:line | Finding | Evidence | Consequence | Required correction |
|---|---|---|---|---|---|---|
| INFO | SEC-R3-INFO-001 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:176` | Round-2 CODE-M-004 / ARCH-M-001 is closed: the Lane A authority gate now enforces SPEC-044's 1 TiB maximum before any disk probe, transfer, or adoption. | `Build1LaneAPrepareProfile.maxArtifactSizeBytes` binds to `ModelPreparationContracts.maxEstimatedBytes`; `Build1LaneAArtifactAuthorityResolver.resolve` now requires `artifact.sizeBytes > 0` and `artifact.sizeBytes <= Build1LaneAPrepareProfile.maxArtifactSizeBytes` before returning authority. `testModelsPrepareRejectsSignedFeedWithOverLimitLaneAArtifactSize` asserts the command fails as authority unavailable and that the refusing downloader and disk probe are not reached. The targeted Swift test run passed. | Over-limit signed `size_bytes` cannot make `models prepare` actionable or enter staging/headroom logic. | None. |
| INFO | SEC-R3-INFO-002 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:217` | Round-1 SEC-001 / CODE-M-003 remains closed: copied bytes are verified before publication, prior destinations are restored on swap failure, and post-swap digest mismatches are removed. | `copyRegularTree` copies into a private `.tmp-*` sibling, rejects symlinks/non-regular entries/path escapes, hashes the temp tree, and only then calls `replaceDestination`; `replaceDestination` parks an existing destination in `.tmp-replaced-*` and restores it if the final move fails; `adoptVerifiedStaging` removes the destination on post-swap readback failure or digest mismatch. Tests `testCorruptedCopyIsRefusedBeforePublication`, `testFailedSwapRestoresPriorDestination`, and `testAdoptRejectsExistingDestinationReachedThroughSymlinkAncestor` passed. | No reviewed failure path leaves unverified bytes published under the expected durable hash path. | None. |
| INFO | SEC-R3-INFO-003 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:223` | Round-1 CODE-M-001 remains closed: signed artifact authority is re-resolved immediately before durable publication and must equal the staging authority. | The stager calls `reauthorize()` after byte verification and before publication headroom/adoption; unavailability maps to `authority_unavailable`, and changed authority maps to `artifact_identity_mismatch`. Tests `testAuthorityDriftBetweenVerificationAndPublicationRefusesAdoption` and `testAuthorityUnavailableBeforePublicationRefusesAdoption` passed. | Revoked, rebound, or stale signed authority cannot authorize durable adoption of already-staged bytes. | None. |
| INFO | SEC-R3-INFO-004 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:359` | Round-1 CODE-M-002 / ARCH-001 remains closed: storage headroom includes the SPEC reserve and is checked before transfer and before publication. | `requireDiskSpace` requires `2 * expected + 1 GiB` on the durable-root volume and `expected + 1 GiB` on a distinct staging volume, with overflow refusal. It runs before transfer and again after reauthorization before adoption. Tests covering pre-transfer reserve, distinct-volume reserve, publication recheck, and overflow passed. | Disk pressure fails closed before durable side effects, reducing disk-fill and partial-publication risk. | None. |
| INFO | SEC-R3-INFO-005 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:441` | Filesystem trust boundaries remain preserved for the isolated staging directory, canonical snapshot, durable root, and prepare lock. | The durable root is created as a real directory and chmodded `0700`; symlink ancestors are rejected for durable paths; Hugging Face staging paths are required to stay under the cache root without symlink ancestors; canonical verifier rejects symlinks, hardlinks, non-regular files, and path escapes; the prepare lock uses `O_NOFOLLOW`, `O_CLOEXEC`, `flock`, owner check, and regular-file check. Relevant symlink, path traversal, corrupt destination, lock conflict, cancellation, and cleanup tests passed. | The reviewed path does not expose a symlink/path-escape route for unverified bytes to become durable or served. | None. |
| INFO | SEC-R3-INFO-006 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:31` | Coordinator host allow-list and SSRF/redirect boundaries remain preserved. | `coordinatorIsAllowedForStaging` rejects missing credentials, production coordinator hosts including trailing-dot variants, arbitrary hosts, non-default staging ports, and plain HTTP staging hosts; loopback remains allowed for local staging. Live artifact feed fetching uses the no-redirect session. Hugging Face asset redirects strip `Authorization` on allowed asset-host redirects. Coordinator rejection tests passed. | `models prepare` cannot be pointed at production or arbitrary external coordinators through the reviewed CLI path. | None. |
| INFO | SEC-R3-INFO-007 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:442` | SPEC-044 R010 public-output privacy boundary remains preserved. | `ModelPreparationTransactionEvent` contains transaction id/kind, model key, sequence, time, state, progress, error, and warning only; stderr diagnostics include artifact identity, size, release, signer, feed digest, and bounded reason details, not durable/staging/cache roots, tokens, private keys, or raw feed bytes. Tests assert digest-mismatch JSON omits durable and staging roots. Scoped secrets scans found no new secret constants. | Public JSON and reviewed stderr do not leak raw private paths or secrets. | None. |
| INFO | SEC-R3-INFO-008 | `phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:362` | The reviewed diff does not activate production, rewards, payouts, settlement, admission, public earnings, general BYOM, or active serving-model changes. | The command hardcodes the Lane A tuple, verifies signed authority, stages/verifies/adopts bytes, and prints explicit no-admission/no-settlement/no-earnings/no-rewards/no-payouts/no-production-activation copy. Source review found no calls into production coordinator activation, rewards, payouts, settlement, public-earnings, control socket, serve reload, or model switch code. The active model is not changed by this diff. | Lane A remains an isolated artifact preparation/adoption slice only; local preparation still grants no paid routing or economic authority. | None. |
| INFO | SEC-R3-INFO-009 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:74` | The `nonisolated(unsafe)` test seams do not create a shipped-binary authority bypass. | `Build1LaneAArtifactAuthorityResolver.makeStaticInputs` and `Build1LaneAArtifactStager.makeStager` are internal module static variables used by `@testable` tests through `withPrepareStaticInputs` and `withPrepareResolver`. No CLI flag, environment variable, plugin surface, dynamic dispatch hook, or production call path was found that lets an external caller replace them in the shipped executable. | The seams are not reachable as a runtime bypass of signed authority verification from the installed CLI. | None. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=0 INFO=9
