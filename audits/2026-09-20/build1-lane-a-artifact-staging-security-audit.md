# Build 1 Lane A artifact staging security audit

Scope: security lane review of the Build 1 Lane A artifact staging/adoption diff against base `12de7aea`, focused on `Build1LaneAArtifactStager.swift`, `ModelsPrepareCommand.swift`, `DurableModelArtifactStore.swift`, the called resolver/downloader/verifier primitives in `AutotuneRecommend.swift`, `ModelPreparationTransactionEvent`, `Build1LaneAArtifactAuthorityResolver`, and the supplied tests.

Method: source review plus existing tests only. No malformed payloads, exploit strings, or adversarial fixtures were authored. Secrets scan covered the scoped source/test files and the branch history for `12de7aea..HEAD`; matches were only boundary text, redaction comments, or ordinary parser variable names. Dependency check enumerated the SwiftPM dependency tree with `swift package show-dependencies`; the local SwiftPM toolchain has no built-in CVE audit command, so no CVE-clean claim is made.

Validation evidence:
- `cd phase3-binary && swift test --filter 'ModelsSubcommandTests/testModelsPrepare|Build1LaneAArtifactStagerTests|DurableModelArtifactStoreTests|AutotuneArtifactFeedTests'` passed: 62 selected tests, 0 failures.
- Coordinator allow-list preservation was verified in code and tests: production `coordinator.malibu.tech`, trailing-dot production variant, arbitrary host, plain HTTP staging host, and non-default staging port are rejected; loopback remains allowed for local staging.
- Public JSON/private-path boundary was verified in code and tests: transaction events carry model key, state, progress bytes, and error/warning enums; tests assert stdout does not contain durable or staging roots on failure.
- Signed-authority binding was verified in code: prepare requires the baked candidate tuple, live signed artifact feed, primary artifact id, exact repo/revision/hash, `macprovider.snapshot-manifest.v1`, `mlx_cache`, verified status, and positive size before staging.
- Shipped-binary reachability of `nonisolated(unsafe) static var` test seams: `makeStaticInputs` and `makeStager` are internal module variables changed only by `@testable` tests in this repository. I found no CLI flag, environment variable, plugin interface, dynamic loader hook, or production call path that lets an external caller replace them in the shipped executable.
- No diff path activates production coordinator admission, rewards, payouts, settlement, public earnings, or active model switching; the new command only reads config for `model_artifact_root`, downloads/stages/verifies/adopts bytes, and prints "no admission/earnings" operator diagnostics.

Severity levels with no findings: no CRITICAL findings, no HIGH findings, no LOW findings, and no INFO findings.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| MEDIUM | SEC-001 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:246` | A post-copy digest mismatch can leave unverified bytes durably published under the expected hash path. OWASP A08: Software and Data Integrity Failures. | `copyRegularTree` copies into a `.tmp-*` sibling, then removes any existing destination and moves the temp tree into the final destination at lines 246-247. `adoptVerifiedStaging` verifies `canonicalArtifactHash(directory: destination)` only after that move at lines 111-116. If bytes change between the pre-copy inspection and the post-move digest check, or if the copy produces mismatched bytes, the function throws but does not remove or roll back the final destination. `Build1LaneAArtifactStager` catches this as `publicationFailed` at lines 211-213 and only removes the isolated staging source it created, not the now-mutated durable destination. | Local exploitability only: an attacker or process with the provider user's filesystem privileges could race or corrupt source/copy bytes so the command fails while leaving mismatched bytes in the provider-owned durable store. This violates the stated boundary that failure leaves the durable store as found and that unverified bytes never reach the durable store. The active model is still not switched by this diff, and later verified prepare runs should reject or overwrite the bad durable entry, so blast radius is durable-store integrity rather than immediate production serving or payout activation. | Verify the copied temp tree before publication and preserve the existing destination until the temp tree's digest matches the signed authority. Only then atomically replace/adopt; on any temp digest mismatch, delete the temp tree and leave the previous destination unchanged. If replacing an existing corrupt destination is intentional, remove it only after the verified temp is ready to publish. Add a regression test for copy-time/source mutation or injected post-copy mismatch that asserts the durable destination is absent or unchanged after `publicationFailed`. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=1 LOW=0 INFO=0
