| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| MEDIUM | ARCH-001 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:270` | Post-adoption cancellation, deadline expiry, or a late progress/output failure can still leave a private published-inventory record. | The last cancellation/deadline check before commit is at line 270. The durable artifact is adopted at lines 282-287, then `recordPrivateState` writes the receipt and inventory at line 307 before the final `.adopted` progress event at line 308. `ModelsPrepareCommand.emitStagingFailure` reports `.cancelled`, `.timedOut`, or `.failed` terminal states for thrown staging errors at `ModelsPrepareCommand.swift:407-426`. The tests cover cancellation before adoption (`Build1LaneAArtifactStagerTests`, selected run passed), but there is no post-adoption cancellation/deadline/progress-failure case. | This weakens the stated boundary that failure, timeout, and cancellation never write or mutate the private record. A run can cross durable adoption, then observe cancellation/deadline/output failure after or during private-state publication and either write despite an operator cancellation/timeout condition or report a failed transaction after mutating private state. | Add an explicit post-adoption commit boundary. Either refuse record publication when cancellation/deadline is already observed before entering `recordPrivateState`, and test that no receipt/inventory is created, or declare private-record publication part of the non-cancellable commit section and ensure no cancellation/timeout/failure terminal can be emitted after the record is written. Add targeted tests for cancellation/deadline and progress-sink failure between adoption and record/final event. |
| LOW | ARCH-002 | `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:164` | Existing-record reuse does not revalidate the Lane A event/catalog key. | Reuse selects an existing target with `describes`, which checks only display model, revision, artifact id, and release id at lines 164 and 284-289. The receipt revalidation at lines 166-172 checks artifact hash/model/revision/artifact/release, but does not require `match.modelKey`, `match.eventModelKey`, `receipt.record.eventModelKey`, or `receipt.record.tuple.eventModelKey` to equal `authority.catalogKey`. SPEC-044 makes `event_model_key` the immutable historical key for cleanup and event correlation (`specs/SPEC-044-malibu-model-catalog-economics.md:303-311`). | A pre-existing private record with the same artifact identity fields but the wrong historical event key can be accepted as reused rather than failed closed or repaired. That does not create a new malformed entry, but it can carry a bad event key forward into later serve/status evidence binding or cleanup workflows. | When reusing, require the target and receipt event/catalog keys to equal the Lane A authority catalog key, and fail the inventory closed if they do not. Add a focused test that seeds a same-artifact inventory/receipt with a mismatched event key and proves `models prepare` refuses before transfer. |
| INFO | ARCH-003 | `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:44` | No architecture issue found with binding the private authority root and managed-v3 namespace to the durable artifact root for this slice. | The authority root is `<durable_root>/.macprovider-preparation-state-v3` (`Build1LaneAPreparationRecorder.swift:44`, initialized at lines 60-66), while the managed-v3 namespace is `<durable_root>/.macprovider-prepared-v3` (`ModelPreparationSecureFilesystem.swift:48`). Root identity is bootstrapped from the opened durable artifact root and includes canonical path, device, inode, version, and nonce-derived digest (`ModelPreparationPrivateStore.swift:129-149`; SPEC root identity at `specs/SPEC-044-malibu-model-catalog-economics.md:234-263`). The durable store GC skips hidden entries (`DurableModelArtifactStore.swift:138-146`). | The private state follows `model_artifact_root`, avoids public path/nonce projection, and is not swept by inactive-artifact GC. The separate authority and object namespaces remain reconcilable because records bind to root identity and receipt/object digest, not to the current catalog row. | No correction required for this slice. Future managed-v3 work should preserve the saved root locator as authority and avoid treating current configuration alone as root identity. |
| INFO | ARCH-004 | `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:224` | No architecture issue found with the protected cleanup-target shape for the transitional Lane A record. | The target uses a receipt-derived `artifact_identity_digest` at lines 215-224, writes `objects/<artifact_identity_digest>/receipt.json` at lines 323-352, and records a protected cleanup target with `protected_reason = cleanup_transaction_unavailable`, unavailable cleanup action, and measured logical adopted bytes at lines 224-247. SPEC-044 allows protected targets with unavailable cleanup and exact reason (`specs/SPEC-044-malibu-model-catalog-economics.md:271-286`) and defines cleanup `estimated_bytes` as logical prepared-data bytes rather than promised recoverable disk space (`specs/SPEC-044-malibu-model-catalog-economics.md:220-224`, `340-359`). | The record honestly avoids advertising cleanup before the cleanup transaction exists. Because the digest is receipt/root-bound and path-independent, a later full managed-v3 implementation can move or materialize the exact bytes under `objects/<artifact_identity_digest>/` without rewriting the identity fields. | No correction required for this slice. The follow-up full managed-v3 implementation must make the object tree and descriptor-relative byte walk match the already recorded logical bytes before enabling reclaimable cleanup. |
| INFO | ARCH-005 | `phase3-binary/Sources/macprovider-cli/ModelsSubcommand.swift:604` | No public v1 catalog-economics drift found. | `models catalog-economics --json` calls `ModelCatalogEconomicsBuilder.makeProjection` at lines 604-611 and never opens `ModelPreparationPrivateStore`. Private storage is only accepted by `makeProjectionV2` as an optional argument (`ModelCatalogEconomics.swift:896-991`). The selected test run passed `ModelsSubcommandTests.testModelsCatalogEconomicsV1OutputIgnoresPrivateLaneAPreparationRecord`, which seeds a Lane A private record and asserts v1 output has no `storage`, no `cleanup_targets`, and no private digests or paths. | Public `model_catalog_economics.v1` byte shape remains separated from the new private record path, preserving the current public boundary until Lane B/v2 work. | No correction required. |

No CRITICAL findings.
No HIGH findings.
No additional MEDIUM findings beyond ARCH-001.
No additional LOW findings beyond ARCH-002.
INFO observations are ARCH-003 through ARCH-005.

Validation: `cd /Users/augstar/.codex/worktrees/macprovider/build1-lane-a-private-record/phase3-binary && swift test --filter 'Build1LaneAArtifactStagerTests|ModelPreparationRootTests|ModelPreparationPrivateStoreTests|ModelsSubcommandTests/testModelsPrepare|ModelsSubcommandTests/testModelsCatalogEconomics'` passed on 2026-09-21, executing 58 selected XCTest cases with 0 failures.

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=1 LOW=1 INFO=3
