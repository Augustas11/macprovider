| Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO) | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| CRITICAL | CODE-NONE-CRITICAL | N/A | No CRITICAL findings. | Source review plus `swift test --filter 'Build1LaneAArtifactStagerTests|ModelPreparationRootTests|ModelPreparationPrivateStoreTests|ModelsSubcommandTests/testModelsPrepare|ModelsSubcommandTests/testModelsCatalogEconomics'` passed: 58 tests, 0 failures. | N/A | N/A |
| HIGH | CODE-NONE-HIGH | N/A | No HIGH findings. | Source review plus the targeted Swift test run above. | N/A | N/A |
| MEDIUM | CODE-MED-001 | phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:93 | Existing published-inventory validation is incomplete before reuse or append. | `open()` only decodes the existing `model_catalog_published_inventory.v1` payload and checks the root locator at lines 93-111. Receipt verification happens only for the current matching tuple at lines 164-183, and that reuse check does not compare all receipt tuple fields that bind the current authority, including the feed `estimated_bytes`, the event model key, or the target's measured `estimated_bytes` against the adopted byte count. For a distinct release, lines 248-252 copy all existing targets, append the new target, and write generation+1 without loading or validating receipts for older targets. Existing tests cover a dangling receipt only when the same tuple is prepared again, but not a dangling or stale receipt on a nonmatching existing target before appending a distinct release. | A stale, missing, or partially rebound receipt in an older target can survive and be republished in a new inventory generation when another release is prepared, and a matching stale receipt can be reused even when its feed-size/event binding no longer matches the authority. That violates the slice requirement that existing-inventory validation happen before transfer, and that dangling receipt/private inventory failures fail closed without mutation. It can also leave later v2 storage/cleanup projection with a target whose receipt cannot prove the claimed private object identity. | Before any transfer and before any append/reuse, validate every existing target by loading its persisted receipt and checking receipt SHA, root locator, event model key, tuple display model id, revision, artifact id, release id, artifact SHA, feed estimated bytes, and recomputed artifact identity digest against the target. For the current reuse path, also compare the target's measured bytes with the remeasured adopted durable artifact bytes. Add regression tests for a dangling receipt on an older nonmatching target before distinct-release append and for a same-tuple stale receipt with mismatched feed `estimated_bytes` / event key. |
| LOW | CODE-NONE-LOW | N/A | No LOW findings. | Source review plus the targeted Swift test run above. | N/A | N/A |
| INFO | CODE-NONE-INFO | N/A | No INFO findings. | Source review plus the targeted Swift test run above. | N/A | N/A |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=1 LOW=0 INFO=0
