# Build 1 Lane A Private Record Round 2 Architect Audit

Scope: full fix diff from `7c2e4d97` to the working tree for the requested file list, plus the new `Build1LaneAPreparationRecorder.swift` and the called contracts/stores/builders.

Validation: `cd phase3-binary && swift test --filter 'Build1LaneAArtifactStagerTests|ModelPreparationRootTests|ModelPreparationPrivateStoreTests|ModelsSubcommandTests/testModelsPrepare|ModelsSubcommandTests/testModelsCatalogEconomics'` passed: 65 tests, 0 failures.

No CRITICAL findings.
No HIGH findings.
No MEDIUM findings.
No LOW findings.

| Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO) | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| INFO | ARCH-R2-INFO-001 | `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:50`; `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:68`; `phase3-binary/Sources/macprovider-cli/ModelPreparationPrivateStore.swift:116`; `specs/SPEC-044-malibu-model-catalog-economics.md:249` | The private-state authority root and managed-v3 namespace binding is architecturally sound for the current Lane A slice. | The recorder binds private authority to `<durable_root>/.macprovider-preparation-state-v3`, while the store reopens the artifact root, bootstraps the managed namespace under that root, and derives the root locator from descriptor-observed root identity. SPEC-044 requires lifecycle records and managed-object receipts to persist that locator while keeping the nonce only in `root.identity`. | Future implementations can reopen and validate the same root authority without depending on current configuration or catalog projection. I do not see a migration hazard as long as later managed-v3 work preserves the recorded root locator contract. | None. Preserve the root-locator invariant for future v3 migrations. |
| INFO | ARCH-R2-INFO-002 | `phase3-binary/Sources/macprovider-cli/DurableModelArtifactStore.swift:38`; `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:254`; `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:393`; `specs/SPEC-044-malibu-model-catalog-economics.md:271` | The durable artifact layout and receipt object layout are reconcilable without rewriting records, and the published-inventory target shape matches SPEC-044 cleanup target semantics for a protected, non-cleanupable Lane A object. | Artifact bytes remain in the durable store path keyed by model, revision, and artifact hash. The receipt is persisted under `objects/<artifact_identity_digest>/receipt.json`; that digest is recomputed from the tuple, root digest, and receipt hash. The cleanup target is `protected`, uses the exact unavailable protected reason, and stores measured logical bytes from the adopted durable copy. | A future full managed-v3 implementation can attach or migrate physical managed objects by the same artifact identity while keeping current receipts and inventory identities stable. No cleanup action is falsely advertised for this Lane A record. | None. Future reclaimable cleanup must add a real cleanup transaction before changing `keep_set_status` or action availability. |
| INFO | ARCH-R2-INFO-003 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:153`; `phase3-binary/Sources/macprovider-cli/ModelPreparationSecureFilesystem.swift:448`; `phase3-binary/Sources/macprovider-cli/ModelPreparationSecureFilesystem.swift:568`; `specs/SPEC-044-malibu-model-catalog-economics.md:361` | Bootstrap-before-transfer is the right fail-closed boundary, and the ancestor ACL relaxation is narrow enough for real macOS home-directory roots. | The stager opens and validates the private recorder session before disk probe, transfer, or adoption. Sensitive roots and files still require empty ACLs, while traversable ancestors reject writable or allow-style exposure and tolerate deny-only inherited ancestor ACLs. Existing tests cover held locks and deny-only versus allow ancestor behavior before transfer. | An unusable private store now blocks preparation before staging or mutation. The ACL rule remains strict on sensitive nodes while avoiding false rejection of common macOS home-directory ancestry. | None. Keep any future relaxation limited to non-sensitive ancestors and descriptor-bound checks. |
| INFO | ARCH-R2-INFO-004 | `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:313`; `phase3-binary/Sources/macprovider-cli/ModelsSubcommand.swift:604`; `phase3-binary/Sources/macprovider-cli/ModelCatalogEconomics.swift:770`; `docs/product-roadmap/build-1/build1-control-recovery-plan-v1.md:28` | Scope remains within the authorized private record slice; no serve/status binding or production activation was added, and public v1 catalog-economics output does not read the private store. | The post-adoption path writes only the private record after the explicit commit-boundary check. The public `models catalog-economics --json` path calls the v1 projection builder without passing private storage; private store loading is present only on the v2/private-storage helper path. The control ledger's next authorized action remains the later serve/status evidence binding. | Local preparation still does not imply active serving, production coordinator admission, payouts, settlement, public earnings, or automatic paid-provider qualification. Public v1 JSON byte-shape remains isolated from private paths, nonces, receipts, and digests. | None. Keep serve/status and production-admission work in later authorized slices. |
| INFO | ARCH-R2-INFO-005 | `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:100`; `phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:187`; `phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:344`; `specs/SPEC-044-malibu-model-catalog-economics.md:249` | Round-1 architect-relevant findings are closed, and the disputed SEC-002 issue should not be carried. | Existing inventory targets are now validated against persisted receipts before transfer, including receipt hash, recomputed artifact identity, root locator, event key, tuple identity, and reuse-time Lane A authority checks. Durable bytes are re-hashed immediately before record construction. Cancellation/deadline checks now occur after `artifact_adopted` and before the private record. SPEC-044 explicitly requires private lifecycle receipts to persist the root locator while forbidding nonce/path/device/inode exposure in projections; R010 is a projection/UI privacy rule, not a ban on private receipt locators. | CODE-MED-001, SEC-001, ARCH-001, and ARCH-002 are closed by source changes and the passing tests named in the prompt. SEC-002 is not a valid remaining architecture issue under the cited SPEC-044 text. | None. Continue to keep root locators private-only and project only opaque digests when v2 projection lands. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=0 INFO=5
