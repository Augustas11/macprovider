| Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO) | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| CRITICAL | CODE-R2-NONE-CRITICAL | N/A | No CRITICAL findings. | Full code-lane review of the requested diff from `7c2e4d97` found no correctness issue that can create an invalid private preparation-state record, change the active serving model, or cross the no-production-activation boundary. The authorized Swift test filter passed with 65 selected tests and 0 failures. | N/A | N/A |
| HIGH | CODE-R2-NONE-HIGH | N/A | No HIGH findings. | Source review and the authorized Swift test run did not identify a high-impact record-path correctness failure. | N/A | N/A |
| MEDIUM | CODE-R2-NONE-MEDIUM | N/A | No MEDIUM findings. | Round-1 MEDIUM items reviewed for closure and no new MEDIUM issue was found over the full requested diff. | N/A | N/A |
| LOW | CODE-R2-NONE-LOW | N/A | No LOW findings. | Source review and the authorized Swift test run did not identify a LOW correctness issue. | N/A | N/A |
| INFO | CODE-R2-INFO-001 | phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:112 | Round-1 CODE-MED-001 and ARCH-002 are closed: existing published-inventory validation now happens before any transfer and covers every target. | `Build1LaneAArtifactStager.stageAndAdopt` acquires the prepare lock, then opens private state before transfer at `Build1LaneAArtifactStager.swift:148` and `Build1LaneAArtifactStager.swift:158`. `Build1LaneAPreparationRecorder.open()` loops all existing targets and loads/validates their receipts before returning a session. `validatedReceipt` checks receipt hash, recomputed artifact identity digest, root locator, event key, and tuple identity at `Build1LaneAPreparationRecorder.swift:321`. The reuse path additionally requires Lane A model/event key, authority artifact hash, feed estimated bytes, and measured adopted bytes at `Build1LaneAPreparationRecorder.swift:193`. Regression tests for dangling older-target receipt and stale reuse tuple passed. | The prior stale/dangling older target and event/feed/byte reuse gap is not present in this round. | No correction required. |
| INFO | CODE-R2-INFO-002 | phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:187 | Round-1 SEC-001 is closed for the code lane: record creation re-hashes the durable directory immediately before publishing evidence. | `Session.record` now recomputes `ModelArtifactVerifier.canonicalArtifactHash(directory:)` over the durable artifact and refuses unless it equals the signed authority digest. `testRecorderRefusesDurableBytesThatDoNotHashToAuthority` passed. | A path-level or caller-claimed adopted digest is no longer sufficient to mint a private inventory entry. | No correction required. |
| INFO | CODE-R2-INFO-003 | phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:318 | Round-1 ARCH-001 is closed for the code lane: post-adoption cancellation, deadline expiry, and progress-sink failure are observed before the private record write. | The stager emits `artifact_adopted`, checks the commit boundary, then writes private state at `Build1LaneAArtifactStager.swift:318`. The reuse path follows the same adopted-progress, boundary-check, record order at `Build1LaneAArtifactStager.swift:170`. The new tests for cancellation, deadline expiry, and progress-sink failure after adoption all passed and assert no private record is written. | Failure, timeout, or cancellation observed at the commit boundary leaves the durable copy retained but does not write the private inventory/receipt. | No correction required. |
| INFO | CODE-R2-INFO-004 | specs/SPEC-044-malibu-model-catalog-economics.md:249 | Round-1 SEC-002 is not upheld as a code-lane finding: SPEC-044 requires the private lifecycle record and managed-object receipt to persist the root locator; it forbids projecting that locator publicly. | SPEC-044 says private lifecycle records, including managed-object receipts, must persist canonical path, device, inode, record version, and `root_identity_digest`; the nonce remains only in `root.identity`. The projection rule at `specs/SPEC-044-malibu-model-catalog-economics.md:260` forbids path/device/inode/username in the projection, not in owner-only private receipt files. `ModelPreparationPublicationReceipt` and `ModelPreparationInventoryRecord` require `root` fields at `ModelPreparationContracts.swift:2091` and `ModelPreparationContracts.swift:2150`; `ModelPreparationPrivateStore.validatePayload` refuses a published inventory whose root does not match the opened root locator at `ModelPreparationPrivateStore.swift:363`. `testPersistedPrivateRecordsCarryRootLocatorButNeverTheRootNonce` passed. | There is no SPEC-044 clause found that forbids the root locator in the private receipt; the code keeps nonce private and v1 public output clean. | No correction required. |
| INFO | CODE-R2-INFO-005 | phase3-binary/Sources/macprovider-cli/ModelPreparationContracts.swift:53 | Record binding, idempotency, distinct-release generation, and monotonic-generation behavior were reviewed with no new finding. | `artifactIdentityDigest` implements the SPEC-044 domain and ordered fields at `ModelPreparationContracts.swift:53`; `publicationReceiptSHA256(from:)` hashes the persisted receipt bytes after decoding at `ModelPreparationContracts.swift:83`; receipt/root/tuple binding is enforced at `ModelPreparationContracts.swift:2091`; inventory targets are sorted and unique at `ModelPreparationContracts.swift:2150`; store writes reject non-monotonic generations before and after temp write at `ModelPreparationPrivateStore.swift:545` and `ModelPreparationPrivateStore.swift:569`. Tests for reuse-without-rewrite, distinct release appending generation+1 in digest order, and dangling receipt fail-closed behavior passed. | Reuse is idempotent, a distinct release appends a second target in digest order, and stale generations are refused by the store. | No correction required. |
| INFO | CODE-R2-INFO-006 | phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:438 | Error-code mapping and operator diagnostics were reviewed with no new finding. | Private state unavailable maps to `root_unavailable`, invalid inventory maps to `managed_inventory_invalid`, private record failure maps to `publication_failed`, and conflicting locks map to `operation_conflict` at `ModelsPrepareCommand.swift:438`. Post-adoption cancellation/deadline stderr states that the durable artifact is retained and the private record was not written at `ModelsPrepareCommand.swift:421`. The diagnostic helper is documented as omitting private paths and durable root at `ModelsPrepareCommand.swift:456`. Existing command tests for lock conflict, cancellation, digest mismatch, and public-output privacy passed. | Public event codes remain aligned with the requested boundary, and diagnostics expose digests/reason codes rather than private paths. | No correction required. |
| INFO | CODE-R2-INFO-007 | phase3-binary/Sources/macprovider-cli/ModelPreparationSecureFilesystem.swift:448 | The ancestor ACL relaxation was reviewed with no new finding. | `validateTraversableDirectory` still rejects non-directory, unlinked, or owner-writable-by-group/other ancestors, and for owner ancestors calls `rejectExtendedAllowACL`. That helper iterates `acl_get_entry`, treats end-of-list as terminal, tolerates only `ACL_EXTENDED_DENY`, and rejects any other extended ACL tag at `ModelPreparationSecureFilesystem.swift:587`. Private root components still use full extended-ACL rejection. ACL regression tests passed. | Deny-only ancestor ACLs are tolerated, allow entries still fail closed, and private root components remain strict. | No correction required. |
| INFO | CODE-R2-INFO-008 | phase3-binary/Tests/macprovider-cliTests/ModelsSubcommandTests.swift:153 | Test adequacy is acceptable for this code lane. | A successful command-level adoption of the exact production digest remains outside unit-test reach, but the stager/recorder tests cover the private record path, binding, failure boundaries, reuse, distinct release, root locator, durable rehash, and ACL behavior. The public v1 catalog-economics regression seeds a private Lane A record and proves `model_catalog_economics.v1` output omits storage, cleanup targets, private digests, private roots, and private namespace leaves. The authorized command `swift test --filter 'Build1LaneAArtifactStagerTests|ModelPreparationRootTests|ModelPreparationPrivateStoreTests|ModelsSubcommandTests/testModelsPrepare|ModelsSubcommandTests/testModelsCatalogEconomics'` passed with 65 tests and 0 failures. | The remaining un-unit-testable real-digest successful command path is covered indirectly by stager/recorder tests plus command-level failure/privacy tests; no additional code-lane gap is raised. | No correction required. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=0 INFO=8
