| Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO) | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| CRITICAL | SECURITY-NONE-CRITICAL | N/A | No CRITICAL findings. | Full scoped source review and the allowed Swift test filter passed: 65 selected XCTest cases, 0 failures. | N/A | N/A |
| HIGH | SECURITY-NONE-HIGH | N/A | No HIGH findings. | Full scoped source review and the allowed Swift test filter passed: 65 selected XCTest cases, 0 failures. | N/A | N/A |
| MEDIUM | SECURITY-NONE-MEDIUM | N/A | No MEDIUM findings. | Round-1 MEDIUM findings were rechecked against the full diff and are closed or re-adjudicated as non-findings below. | N/A | N/A |
| LOW | SEC-R2-LOW-001 | phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:456 | Best-effort receipt rollback reopens by name instead of proving it is unlinking the receipt descriptor that was just created. | Receipt creation is descriptor-relative with `O_EXCL`/`O_NOFOLLOW`, `0600`, ACL strip/verify, fsync, readback, and parent sync at `Build1LaneAPreparationRecorder.swift:393-421`. If the later inventory envelope write fails, rollback calls `removeReceiptBestEffort`, reopens `objects/<artifact_identity_digest>/receipt.json` by name, and unlinks the currently named file plus possibly the object directory at lines 456-462. It does not retain or compare the original receipt file identity across the inventory write failure path. | A same-owner local replacement in that object directory between receipt write and rollback could make failure cleanup remove a different currently named receipt file, not strictly the just-created receipt. This does not escape the validated private object directory, but it weakens the requested descriptor-bound cleanup proof. | Return or retain a descriptor/identity cleanup token from receipt creation and pass it into rollback so `unlinkFile(... expected:)` proves the named file is still the created receipt before unlinking; only remove the object directory after that identity-bound unlink succeeds and the directory is verified empty. Add an existing-test-safe regression for rollback refusing to unlink after receipt identity replacement. |
| INFO | SEC-R1-CODE-MED-001-ARCH-002-CLOSED | phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:100 | Round-1 existing-inventory validation finding is closed. | `open()` now validates every existing inventory target by loading its receipt before any transfer at lines 100-119; `validatedReceipt` checks receipt SHA, recomputed artifact identity digest, root locator, event key, tuple display/revision/artifact/release at lines 321-355. Reuse additionally checks Lane A model/event key, receipt feed size/hash, and measured adopted bytes at lines 193-205. Tests `testDanglingReceiptOnOlderTargetRefusesBeforeDistinctReleaseTransfer` and `testReuseRefusesRecordedTupleWhoseIdentityFeedSizeOrBytesDiffer` passed. | Existing stale or dangling receipts now fail closed before transfer or disk probe. | No correction required for this round-1 item. |
| INFO | SEC-R1-SEC-001-CLOSED | phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:185 | Round-1 durable-byte rehash finding is closed. | `Session.record` now recomputes `ModelArtifactVerifier.canonicalArtifactHash` over the durable directory immediately before constructing receipt/inventory and refuses unless it equals the signed authority digest at lines 185-190. `testRecorderRefusesDurableBytesThatDoNotHashToAuthority` passed. | A hash-qualified durable path with wrong bytes can no longer mint private publication evidence. | No correction required for this round-1 item. |
| INFO | SEC-R1-ARCH-001-CLOSED | phase3-binary/Sources/macprovider-cli/Build1LaneAArtifactStager.swift:318 | Round-1 post-adoption failure/cancellation boundary finding is closed. | The order is now adopt, reclaim staging, emit `artifact_adopted`, run `checkCommitBoundary()`, then write the private record at lines 313-320; cancellation/deadline at that boundary map to distinct post-adoption cases at lines 342-350 and command text says the durable artifact is retained and the private record was not written at `ModelsPrepareCommand.swift:421-429`. Tests `testCancellationAtCommitBoundaryLeavesDurableCopyWithoutPrivateRecord`, `testExpiredDeadlineAtCommitBoundaryLeavesDurableCopyWithoutPrivateRecord`, and `testProgressSinkFailureAfterAdoptionLeavesNoPrivateRecord` passed. | Failure, timeout, cancellation, and progress-sink failure before the explicit record commit do not write or mutate the private record. | No correction required for this round-1 item. |
| INFO | SEC-R1-SEC-002-READJUDICATED-CLOSED | specs/SPEC-044-malibu-model-catalog-economics.md:250 | Round-1 root-locator privacy finding is not held after re-adjudication. | SPEC-044 requires managed-object receipts and other private lifecycle reopening records to persist canonical path, device, inode, identity version, and `root_identity_digest`, while keeping the nonce only in `root.identity`, at lines 250-263. The implementation's private receipt, tuple, and inventory require `ModelPreparationRootLocator` (`ModelPreparationPublicationReceipt` at `ModelPreparationContracts.swift:2091-2134`, `ModelPreparationTupleRecord` at lines 822-887, and `ModelPreparationInventoryRecord` at lines 2150-2178), and `ModelPreparationPrivateStore.validatePayload` refuses a published inventory whose root differs from the current root locator at `ModelPreparationPrivateStore.swift:363-369`. Test `testPersistedPrivateRecordsCarryRootLocatorButNeverTheRootNonce` passed. | Persisting the root locator in owner-only private records is required reopening authority, not a SPEC-044-R010 violation. The projection/UI boundary remains digest-only. | No correction required for this round-1 item. |
| INFO | SEC-R2-ACL-REVIEWED | phase3-binary/Sources/macprovider-cli/ModelPreparationSecureFilesystem.swift:448 | Ancestor ACL relaxation reviewed with no blocking finding. | `validateTraversableDirectory` still rejects non-owner-writable owner-controlled ancestors and now calls `rejectExtendedAllowACL` for those ancestors at lines 448-462. `rejectExtendedAllowACL` treats only deny entries as traversable and rejects any non-deny tag, while ACL inspection/tag failures throw rather than pass open at lines 587-615. Private root components, files, receipts, state, objects, and locks still use `validateDirectory`/`validateRegularFile`, which reject any extended ACL at lines 429-445 and 465-481. Tests for deny-only ancestor tolerance, allow-ACL refusal before transfer, and root/root.identity ACL rejection passed. | A deny-only ancestor ACL does not widen authority; allow ACLs on owner-controlled ancestors and any extended ACL on private components fail closed. | No correction required for this reviewed area. |
| INFO | SEC-R2-PRODUCTION-PRIVACY-BOUNDARY-REVIEWED | phase3-binary/Sources/macprovider-cli/ModelsPrepareCommand.swift:397 | No production activation or public v1 privacy leak found in the reviewed slice. | Success stderr states the tuple digest evidence and explicitly says preparation grants no admission, settlement, earnings, rewards, payouts, or production activation at lines 397-404. The protected cleanup target uses `keepSetStatus: .protected`, `protectedReason: cleanup_transaction_unavailable`, and an unavailable cleanup action at `Build1LaneAPreparationRecorder.swift:254-276`. Public v1 catalog-economics tests assert no storage, cleanup targets, private digests, durable path, authority leaf, or namespace leaf in stdout at `ModelsSubcommandTests.swift:199-214`; the allowed test filter passed. | The private record is not exposed as admission, settlement, earnings, rewards, payouts, production activation, or an available cleanup transaction. | No correction required for this reviewed area. |
VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=1 INFO=6
