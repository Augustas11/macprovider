| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| MEDIUM | SEC-001 | phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:155 | The recorder can create a published-inventory entry for a hash-qualified durable directory without re-verifying the durable bytes at record-write time. | `Session.record` checks `adoptedSHA256 == authority.hash`, derives the expected durable URL, and accepts `durableStore.contains(durable.path)` plus `isDirectory(durable)` before constructing the tuple, receipt, and inventory. It does not call `ModelArtifactVerifier` or otherwise recompute the durable artifact digest before `writeReceipt`/`writeRecord`. The stager verifies the durable copy on its normal reuse/adoption paths, but the recorder is callable directly and there is a post-adoption/pre-record window. Existing tests cover missing durable directories and mismatched `adoptedSHA256`, not wrong bytes already present under the expected hash-qualified durable path. | A same-user local mutation, stale durable directory, or future caller that invokes the recorder after only path-level checks can mint private published-inventory evidence for bytes that do not match the signed Lane A artifact. That weakens the "mismatched tuple/feed/artifact cannot create a published-inventory entry" boundary and can poison later serve/status evidence binding. | In `Session.record`, immediately before constructing the receipt/inventory, re-open and re-verify the durable artifact with the same canonical artifact hash used by staging/reuse, fail closed unless the actual digest equals `authority.hash`, and add a regression test where the expected durable path exists but its contents do not match the authority digest. |
| MEDIUM | SEC-002 | phase3-binary/Sources/macprovider-cli/Build1LaneAPreparationRecorder.swift:204 | The persisted private inventory/receipt payloads include raw root-locator fields, including the private durable path, despite the Lane A privacy boundary requiring only digests and tuple identity in the record/receipt. | The recorder embeds `rootLocator` in the tuple, receipt, and inventory (`ModelPreparationTupleRecord(root:)`, `ModelPreparationPublicationReceipt(root:)`, and `ModelPreparationInventoryRecord(root:)`). `ModelPreparationRootLocator` serializes `canonical_path`, `st_dev`, and `st_ino`, while only `root_identity_digest` is needed for the public cleanup target. The new public JSON test asserts stdout does not contain the durable root, and the stager test asserts the inventory does not contain the Hugging Face staging path, but no test asserts the persisted inventory/receipt omit the durable root path. | The managed-v3 private record and receipt can disclose local private paths, often including the username and operator filesystem layout, if collected in diagnostics or later surfaced by a projection/debug path. This violates the requested R010 privacy boundary for the record/receipt even though stdout v1 remains clean. | Split internal root validation metadata from the Lane A publication record/receipt, or introduce digest-only publication evidence that stores `root_identity_digest` without `canonical_path`, `st_dev`, or `st_ino`. Add tests that decode or inspect the persisted inventory and receipt bytes and prove they contain no durable root path, username-bearing path, nonce, device, or inode fields. |

No CRITICAL findings. No HIGH findings. No LOW findings. No INFO findings.

Existing-test evidence: `cd phase3-binary && swift test --filter 'Build1LaneAArtifactStagerTests|ModelPreparationRootTests|ModelPreparationPrivateStoreTests|ModelsSubcommandTests/testModelsPrepare|ModelsSubcommandTests/testModelsCatalogEconomics'` passed with 58 tests, 0 failures. Supporting evidence from those tests covers deny-only ancestor ACL tolerance with allow-ACL refusal, private root ACL rejection, lock conflict before transfer, cancellation without private inventory, public v1 catalog JSON staying unchanged, and production coordinator refusal.

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=2 LOW=0 INFO=0
