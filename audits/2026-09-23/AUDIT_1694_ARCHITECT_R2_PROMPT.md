# #1694 loopback settlement bar — ARCHITECT lane audit prompt (R2)

Lane: architect (LANE = architect)

You are a first-party software-correctness / proof reviewer for the MacProvider repository. Method constraint: do NOT author, construct, or emit malformed payloads, exploit strings, or adversarial fixtures. Evaluate by reading source and by running the EXISTING tests only. Describe any gap abstractly in prose (field + condition + consequence).

Context: GitHub issue #1694 (parent #1690). A catalog-matched artifact served through a SPEC-046 loopback runtime (`ollama_loopback`, `lmstudio_loopback`, `llamacpp_loopback`, `openai_compatible_loopback`) relays the external runtime's `usage.prompt_tokens`/`completion_tokens` unchanged (CLI `phase3-binary/Sources/macprovider-cli/OllamaLoopbackRuntime.swift` `decodeUpstreamResponse`). Before this fix, the coordinator's settlement-attempt recorder labelled ANY provider-reported usage `coordinator_observed` (which drives `UsageCrossChecked` and settlement verification), and admission refused loopback `settlement_capable` only via `settlementSessionMemberLocked`'s `mlx_cache` check, justified "until the SPEC-010-R007(e) runtime path reports another source" — nothing downstream re-checked. An adversarial pre-verification found: admission already blocked loopback (headline latent, not live; no `gguf` catalog rows exist); the buyer route/settle check `byomBoundMemberMatchesSession` accepted a loopback `artifact_feed` decision; the recorder had no runtime-source input.

The fix (single commit c6116ce9 on base eed9ca5b = origin/main):
- SPEC-047 v0.1.10 R003(iv) "Loopback settlement bar": loopback never reaches `settlement_capable` until an amendment names a coordinator-recorded trust binding + trusted usage source; R007(e) no longer lifts it; defence in depth (a) buyer routing/settlement refuse loopback decision or session, (b) settlement-attempt recording takes hello-time runtime_source and never labels loopback `coordinator_observed`.
- SPEC-015 v0.4.8 §N.6: loopback-relayed usage is provider-only. SPEC-010 citation-only compatibility note under R007(e).
- ws `settlementSessionMemberLocked` also refuses a session whose hello `RuntimeSource` is loopback; exported `IsBYOMLoopbackRuntimeSource`.
- buyer `byomBoundMemberMatchesSession` refuses loopback event or session source.
- `billing.HotPathInput.ProviderRuntimeSource` added; `billingRecorder.recordRow` takes `providerRuntimeSource` (callers pass the selected `pool.Provider.RuntimeSource`; `logRow` passes ""); `recordSettlementAttemptOutput` records loopback attempts `byte_estimated` (zero billable).
- Tests: the positive six-value artifact settlement test now uses an `mlx_cache` secondary `mlx_safetensors` feed member; new `TestBYOMLoopbackArtifactMemberNeverRoutesOrSettles`, `TestModelAdmissionLoopbackSessionNeverBindsForSettlement`, `TestRecordSettlementAttemptOutputLoopbackUsageIsNeverCoordinatorObserved`, `TestLoopbackLegIsRecordedByteEstimatedThroughRecordRow`. CONFORMANCE.json SPEC-047-R003 mapping updated.

Worktree: `/Users/augstar/macprovider-1694`. Review the FULL fix diff as it will land:

    cd /Users/augstar/macprovider-1694 && git diff eed9ca5b..HEAD -- phase4-coordinator specs docs

Read the touched functions in full plus their callers/consumers: `internal/buyer/billing_recorder.go` (recordRow, both HotPathInput build sites, recordSettlementAttemptOutput), `internal/buyer/server.go` recordRow call sites, `internal/buyer/model_admission.go` (byomBoundMemberMatchesSession, byomDefaultPaidRoutingEligibilityWithContext, byomSettlementPrereqsReady), `internal/ws/model_admission_operator.go` settlementSessionMemberLocked, `internal/ws/server.go` hello handling of RuntimeSource (~3360-3520), `internal/pool/provider.go` RuntimeSource, `internal/billing/settlement_receipts.go`, `settlement_verifier.go`, `settlement_finality.go`, `settlement_output.go` (usage_source consumers), `internal/billing/hotpath.go`. Specs: `specs/SPEC-047-network-model-admission.md` R003, `specs/SPEC-015-receipts.md` §N.6-§N.7, `specs/SPEC-010-model-catalog.md` R007.

Existing tests you may run (all from /Users/augstar/macprovider-1694/phase4-coordinator): `go test ./internal/buyer ./internal/ws ./internal/billing ./internal/routing -count=1`.

Do NOT edit any file in the worktree except your report. Write your report to `/Users/augstar/macprovider-1694/audits/2026-09-23/issue-1694-loopback-settlement-r2-LANE-audit.md` (replace LANE with your lane name). Report format: a findings table with columns Severity (CRITICAL/HIGH/MEDIUM/LOW/INFO), ID, File:line, Finding, Evidence, Consequence, Required correction; then a final line exactly `VERDICT: CRITICAL=<n> HIGH=<n> MEDIUM=<n> LOW=<n> INFO=<n>`. If a lane finds nothing at a severity, say so explicitly. Severity calibration: a finding that is pre-existing and not made worse by this diff is at most LOW (label it PRE-EXISTING).

Architecture-lane focus: SPEC coherence — SPEC-047 v0.1.10 R003(iv) bar vs SPEC-010-R007(d)/(e) (does R007 still claim GGUF members settle anywhere, and is the citation note sufficient without a SPEC-010 version bump?), SPEC-015 §N.6 amendment vs the LOCKED v0.4 tuple and §N.7 chargeability, SPEC-023 §3.7.4 format/source matrix, SPEC-042 / #1690 lift path; whether the SPEC text and code enforce the SAME predicate at admission, routing, settlement, and recording; layering (billing package gaining a runtime-source concept; buyer importing ws predicate) and whether the loopback predicate has a single home; CONFORMANCE.json mapping accuracy; cross-cutting sweep: grep BOTH spec and code for other restatements of 'until the SPEC-010-R007(e) runtime path' or claims that loopback/gguf can settle that now contradict the bar.

R2 context: R1 architect report is `audits/2026-09-23/issue-1694-loopback-settlement-architect-audit.md` (MEDIUM ARCH-1694-01 SPEC-023 §3.7.4/AC-CAT-7 affirmative GGUF settlement language; MEDIUM ARCH-1694-02 SPEC-015-R001 conformant mapping vs new §N.6 rule; LOW ARCH-1694-03 stale R007(e) lift restatements). The R2 commit resolves them by: SPEC-023 v0.14.3 amendment; making the SPEC-015 v0.4.8 §N.6 text informative with SPEC-047-R003(iv) as the single normative home (its CONFORMANCE mapping carries the recorder and the loopback tests; SPEC-047-R003 is `pending`); rewording SPEC-047 §7 step 9, `docs/runbooks/byom-admission-decisions.md`, and the r008 test message. Verify each R1 finding is closed, and review the FULL diff (both commits) for any new issue. Do not re-report closed findings.
