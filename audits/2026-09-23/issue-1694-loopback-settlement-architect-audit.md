# Issue #1694 loopback settlement bar — architect audit

Scope: full `eed9ca5b..c6116ce9` diff, the requested callers/consumers, SPEC-010/015/023/042/047, CONFORMANCE mappings, and cross-cutting source/spec/runbook restatements. The implementation predicate is coherent: WS admission rejects a loopback session, buyer routing/settlement rejects a loopback decision or session, recording downgrades hello-time loopback usage to `byte_estimated`, and receipt verification rejects anything not `coordinator_observed`.

Validation evidence: `go test ./internal/buyer ./internal/ws ./internal/billing ./internal/routing -count=1` passed buyer, WS, and routing, but the prescribed aggregate gate remained red because the untouched billing tests `TestAdminRateLimitBucketConsumesFailures` and `TestDisabledQuarantineRouteChargesAuthenticatedAdminBucketOnly` failed repeatably. The four new #1694 tests selected from buyer and WS passed in targeted runs. `git diff --check eed9ca5b..c6116ce9` passed.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
|---|---|---|---|---|---|---|
| CRITICAL | NONE | — | No CRITICAL findings. | No path was found that permits a loopback decision/session to route and settle with provider-relayed usage. | — | — |
| HIGH | NONE | — | No HIGH findings. | Admission, buyer route/settlement, attempt recording, and receipt verification enforce compatible fail-closed predicates. | — | — |
| MEDIUM | ARCH-1694-01 | `specs/SPEC-023-installer-autotune-recommend.md:910` | LOCKED SPEC-023 still grants non-primary artifacts of either format the ability to reach `settlement_capable` when the SPEC-010-R007 identity conditions hold, without stating the new loopback usage-trust bar. | The closed matrix binds every GGUF artifact to loopback sources (`:901-906`); §3.7.4 then says a non-primary artifact of either format may reach `settlement_capable` when R007 conditions hold (`:910`), and AC-CAT-7(iii) repeats the GGUF settlement path (`:1615`). SPEC-047 v0.1.10 now says implementing R007(e) does not lift the bar and only a later R003 amendment naming a coordinator-recorded trust binding and trusted usage source may do so (`specs/SPEC-047-network-model-admission.md:144`). The SPEC-010 citation-only note correctly limits R007(e) to identity and therefore does not itself need a version bump, but it does not amend SPEC-023's affirmative settlement language. | Two normative specifications permit opposite conclusions for the only currently legal GGUF/source combination; an implementer can claim conformance while enabling a path the coordinator correctly refuses. | Version/amend LOCKED SPEC-023 §3.7.4 and AC-CAT-7 to separate identity/catalog matching from settlement capability: GGUF loopback members remain identity members but cannot reach `settlement_capable` until the SPEC-047 lift condition is normatively defined. Update its changelog and conformance rationale/mapping in the same change. |
| MEDIUM | ARCH-1694-02 | `specs/CONFORMANCE.json:5059` | The SPEC-015 v0.4.8 normative addition is not represented in the SPEC-015-R001 proof mapping, yet R001 remains marked `conformant`. | CONFORMANCE records SPEC-015 version 0.4.8 (`:395-398`), and SPEC-015 defines R001 as the whole §N settlement-capable issuance/ingestion unit (`specs/SPEC-015-receipts.md:830-838`). New §N.6 requires loopback attempts to be recorded `byte_estimated` rather than `coordinator_observed` (`:4129-4138`), but R001 maps neither `billing_recorder.recordSettlementAttemptOutput` nor either new recorder test (`specs/CONFORMANCE.json:5062-5071`). Its retained signed evidence was captured at pre-amendment commit `ea5cd0a8...` on 2026-08-27 (`:5076-5088`). Only SPEC-047-R003's mapping was updated. | The conformance graph asserts complete, current proof for a normative money-path rule that its implementation/test/evidence set does not cover. | Add the recorder implementation and loopback recorder tests to SPEC-015-R001. Mark R001 pending with an explicit stale-evidence gap until fresh signed journey evidence covers v0.4.8, or supply evidence that actually binds the amended behavior before retaining `conformant`. |
| LOW | ARCH-1694-03 | `specs/SPEC-047-network-model-admission.md:172` | The implementation sequence and operator guidance retain the superseded R007(e)-runtime-path lift condition. | R003 now says a serving runtime path does **not** lift the loopback bar (`:144`), but §8 step 9 still says GGUF cannot settle “until a serving runtime path exists, SPEC-010-R007(e)” (`:172`), and `docs/runbooks/byom-admission-decisions.md:122-124` repeats the same implication. One existing assertion message also says “before the runtime path exists” (`phase4-coordinator/internal/ws/model_admission_r008_test.go:422`) although the surrounding comment has the correct trusted-usage condition. | Operators and future implementers may treat R007(e) as the remaining release gate and undo the new trust-source bar despite the normative paragraph. | Replace those restatements with the exact v0.1.10 lift condition: a later SPEC-047-R003 amendment naming both the coordinator-recorded trust binding and its trusted usage source. |
| INFO | NONE | — | No additional INFO findings. | The loopback predicate has one exported implementation in WS; buyer imports it without duplicating the enum, and billing only carries runtime-source context in its input DTO. SPEC-015 §N.6 is compatible with the locked v0.4 tuple and §N.7 because `byte_estimated` zeroes billable usage and fails settlement verification. SPEC-042 does not currently define a lift. | — | — |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=2 LOW=1 INFO=0
