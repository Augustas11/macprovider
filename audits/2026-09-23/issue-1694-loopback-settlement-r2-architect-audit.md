# Issue #1694 loopback settlement bar — architect audit R2

Scope: full `eed9ca5b..83d9ad46` diff, the requested callers and settlement consumers, SPEC-010/015/023/042/047, CONFORMANCE mappings, both R1 follow-up changes, and cross-cutting code/spec/runbook restatements. The implementation predicate is coherent: admission refuses a loopback recorded source or live session, buyer routing refuses a loopback decision or session, settlement-attempt recording derives the serving session source from `pool.Provider.RuntimeSource` and records loopback attempts `byte_estimated` with zero billable usage, and receipt verification accepts only `coordinator_observed` usage as cross-checked.

R1 closure: ARCH-1694-02 is closed because SPEC-015 v0.4.8 now labels §N.6's loopback paragraph informative and leaves SPEC-047-R003 as the normative owner; its pending CONFORMANCE mapping includes the recorder and all four loopback tests. ARCH-1694-03 is closed by the corrected SPEC-047 implementation step, BYOM decision runbook, and R008 assertion. ARCH-1694-01 is not fully closed: the new SPEC-023 v0.14.3 carve-out was added to §3.7.4 and AC-CAT-7, but the earlier §3.7 settlement-authority paragraphs still state the superseded broad rule; this is reported below rather than re-reporting either closed finding.

Validation evidence: `go test ./internal/buyer ./internal/ws ./internal/billing ./internal/routing -count=1` passed buyer, WS, and routing but remained red in the untouched billing tests `TestAdminRateLimitBucketConsumesFailures` (`quarantine_test.go:1088`) and `TestDisabledQuarantineRouteChargesAuthenticatedAdminBucketOnly` (`quarantine_test.go:1510`), the same unrelated failures recorded in R1. A targeted run of the four new #1694 tests passed. `git diff --check eed9ca5b..HEAD -- phase4-coordinator specs docs` and `jq empty specs/CONFORMANCE.json` passed. No malformed payload, exploit string, or new fixture was authored or executed.

Architectural status: BLOCK pending the MEDIUM SPEC-023 coherence correction.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
|---|---|---|---|---|---|---|
| CRITICAL | NONE | — | No CRITICAL findings. | No implementation path was found that permits a loopback decision or session to route and positively settle with provider-relayed usage. | — | — |
| HIGH | NONE | — | No HIGH findings. | Admission, buyer routing, attempt recording, and receipt verification enforce compatible fail-closed predicates. | — | — |
| MEDIUM | ARCH-1694-R2-01 | `specs/SPEC-023-installer-autotune-recommend.md:792` | SPEC-023 v0.14.3 remains internally contradictory, so R1 ARCH-1694-01 is only partially closed. The earlier §3.7 authority text still says every verified non-primary artifact, expressly including GGUF, is a settlement identity under SPEC-010-R007, “may advance further exactly as the primary does,” and that SPEC-010 alone decides what may bind. | The new v0.14.3 rule later says every GGUF source is loopback and reaches at most `catalog_priced` until a SPEC-047-R003 amendment supplies the trust binding and trusted usage source (`:919`); AC-CAT-7(iii) repeats the bar (`:1624`), and SPEC-047-R003(iv) is the normative owner (`specs/SPEC-047-network-model-admission.md:144`). The untouched §3.7 text at `:792-794` still states the opposite settlement predicate and explicitly directs readers to treat SPEC-010 as the sole decision owner. | A reader implementing from the opening authority statement of the LOCKED spec can conclude that R007 identity plus six-value evidence is sufficient for GGUF settlement, while the later field rule and acceptance criterion require refusal. The spec therefore does not present one unambiguous lift predicate. | Amend the v0.14.3 §3.7 opening paragraphs, not only §3.7.4/AC-CAT-7: separate membership/catalog pricing from settlement eligibility, state that GGUF loopback identity stops at `catalog_priced`, and name SPEC-047-R003(iv) as the additional settlement authority and sole lift mechanism. |
| LOW | ARCH-1694-R2-02 | `docs/runbooks/catalog-artifact-feed-release.md:124` | **PRE-EXISTING:** the artifact-feed release runbook still gives obsolete, mutually incompatible GGUF settlement guidance and contains no v0.14.3 loopback bar. | Its current “GGUF settlement identity” section says “A verified member settles” (`:135-141`), while its authoring checklist says GGUF is never a settlement identity, cannot reach `catalog_priced`, and only the primary artifact may settle (`:327-331`). Both passages were present at base `eed9ca5b`; neither reflects the current rule that verified GGUF may reach `catalog_priced` but not `settlement_capable` until the SPEC-047-R003 amendment. | Operators and future modifiers can apply either the superseded primary-only restriction or the over-broad R007 settlement lift, obscuring the actual release gate even after the normative SPEC is corrected. | Rewrite the runbook's GGUF settlement section and checklist to match SPEC-023 v0.14.3 / SPEC-047-R003(iv): identity and `catalog_priced` are allowed when their gates hold; loopback `settlement_capable` remains barred until the named trust-binding and trusted-usage-source amendment. |
| INFO | NONE | — | No additional INFO findings. | The loopback vocabulary has one exported predicate in `internal/ws`; buyer reuses it, while `billing.HotPathInput` carries opaque session context without importing admission policy. SPEC-015 §N.6 remains compatible with the locked v0.4 tuple and §N.7, SPEC-042 does not independently lift the bar, and the SPEC-047-R003 CONFORMANCE row accurately remains `pending`. | — | — |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=1 LOW=1 INFO=0
