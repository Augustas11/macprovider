# Issue #1694 loopback settlement bar — architect audit R3

Scope: full `eed9ca5b..62b516a3` diff under `phase4-coordinator`, `specs`, and `docs`; the requested admission, routing, recording, and settlement consumers; SPEC-010/015/023/042/047; CONFORMANCE mappings; and cross-cutting GGUF/loopback settlement restatements.

R1/R2 closure: the previously reported sites are closed. SPEC-015 §N.6 is informative and leaves the recording rule in SPEC-047-R003(iv); the stale R007(e) lift wording is removed; SPEC-023 §3.7's opening authority paragraph now separates GGUF identity from settlement at line 794; and the artifact-feed release runbook now states the current `catalog_priced` ceiling and SPEC-047 lift condition. The remaining SPEC-023 issue below is a different formal R004 restatement that those corrections did not amend.

Implementation coherence: WS admission refuses a loopback recorded source and a loopback hello-time session source; buyer routing/settlement refuses either source; every provider-aware `recordRow` path propagates the selected `pool.Provider.RuntimeSource`; loopback attempts are recorded `byte_estimated` with zero billable usage; and the receipt verifier accepts only `coordinator_observed` usage as cross-checked. The loopback predicate has one implementation in `internal/ws`; buyer already owns the WS admission dependency, while `internal/billing` remains policy-neutral and carries the source only as input context.

Validation evidence: `go test ./internal/buyer ./internal/ws ./internal/billing ./internal/routing -count=1` passed all four packages. `git diff --check eed9ca5b..HEAD -- phase4-coordinator specs docs` and `jq empty specs/CONFORMANCE.json` passed. No malformed payload, exploit string, adversarial fixture, or new test fixture was authored or executed.

Architectural status: BLOCK pending the MEDIUM SPEC-023 authority correction.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
|---|---|---|---|---|---|---|
| CRITICAL | NONE | — | No CRITICAL findings. | No path was found that permits loopback provider-relayed usage to become positively verified settlement usage. | — | — |
| HIGH | NONE | — | No HIGH findings. | The implementation fails closed at admission, route/settlement eligibility, attempt recording, and receipt verification. | — | — |
| MEDIUM | ARCH-1694-R3-01 | `specs/SPEC-023-installer-autotune-recommend.md:953` | The formal SPEC-023-R004 requirement still says **any** verified artifact may bind `settlement_capable` under SPEC-010-R007, leaving a normative route around the new runtime-source settlement authority. | R004 says any verified artifact may bind `catalog_priced` or `settlement_capable` under R007 (`:953`) and the threat model still says SPEC-010-R007 decides which artifacts may settle (`:2012`). The same spec now says GGUF/loopback reaches at most `catalog_priced` and only `mlx_safetensors` through `mlx_cache` can settle (`:919`, `:1624`). SPEC-047-R003(iv) is explicit that R007(e) is only an identity lift and that a loopback source cannot reach `settlement_capable` until a later R003 amendment names both a coordinator-recorded trust binding and trusted usage source (`specs/SPEC-047-network-model-admission.md:144`). | A conforming implementer or reviewer can cite the formal R004 text to treat verified GGUF loopback identity plus six-value evidence as sufficient for `settlement_capable`, contradicting SPEC-047 and the implemented predicate. | Amend SPEC-023 §3.7.7/R004 and the threat-model restatement to separate identity from settlement: every verified R007 artifact may be an identity and reach `catalog_priced`, but `settlement_capable` additionally requires SPEC-047-R003(iv) runtime-source authority; GGUF/loopback therefore remains capped at `catalog_priced` until that named amendment. |
| LOW | ARCH-1694-R3-02 | `specs/CONFORMANCE.json:2366` | **PRE-EXISTING:** the SPEC-023-R004 proof-map entry is stale and contradicts both the current spec and existing implementation. | Although the registry now records SPEC-023 v0.14.3, R004's rationale still describes v0.10.0's `primary-artifact-only settlement identity` and says no generator, coordinator, or CLI implementation exists. Existing mapped evidence elsewhere names the artifact index, feed loader, WS identity verification, route-snapshot validation, CLI GGUF digest code, and tests (`SPEC-010-R007` at `specs/CONFORMANCE.json:1879-1911`), while SPEC-047-R003 now maps the loopback admission/routing/recording implementation and tests (`:8162-8198`). | Governance consumers cannot tell which R004 surfaces are implemented, which remain pending, or that the pending settlement rule includes the loopback bar; the conformance graph is inaccurate even though runtime enforcement is correct. | Keep R004 `pending` if activation/journey work remains, but replace the stale rationale and populate its implementation/tests with the current partial surfaces, explicitly distinguishing R007 identity support from the SPEC-047 loopback settlement lift. |
| INFO | NONE | — | No additional INFO findings. | SPEC-010's no-version-bump compatibility note is citation-only and does not redefine R007; SPEC-015 §N.6 is compatible with the locked v0.4 tuple and §N.7; SPEC-042 defines no independent lift; and the SPEC-047-R003 mapping correctly remains `pending`. | — | — |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=1 LOW=1 INFO=0
