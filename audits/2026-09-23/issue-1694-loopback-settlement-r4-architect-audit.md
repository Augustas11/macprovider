# Issue #1694 loopback settlement bar — architect audit R4

Scope: full `eed9ca5b..3f691fff` diff under `phase4-coordinator`, `specs`, and `docs`; the requested admission, routing, recording, receipt-verification, and finality consumers; SPEC-010/015/023/042/047; CONFORMANCE mappings; and the cross-cutting GGUF/loopback settlement sweep.

Prior-finding closure: R1 ARCH-1694-01/02/03 and R2 ARCH-1694-R2-01/02 are closed. R3 ARCH-1694-R3-01 is closed: SPEC-023-R004, §3.7.4, AC-CAT-7, Q14, and the artifact-substitution threat row now consistently distinguish R007 identity/catalog pricing from the SPEC-047-R003(iv) runtime-source settlement bar. R3 ARCH-1694-R3-02 is only partially closed: its stale rationale was corrected, but its structured proof lists remain empty, as reported below.

Implementation coherence: the WS decision gate refuses a loopback recorded source or live hello-time source; buyer route/settlement eligibility refuses a loopback decision or session; every provider-aware `recordRow` caller passes the selected `pool.Provider.RuntimeSource`; loopback attempt output is `byte_estimated` with zero billable tokens; and receipt verification accepts only `coordinator_observed` usage as cross-checked. The loopback vocabulary has one implementation in `internal/ws`; buyer reuses it, while `internal/billing` carries opaque runtime-source context only. SPEC-015 §N.6 remains informative and compatible with the locked v0.4 tuple and §N.7. SPEC-010's no-version-bump note is citation-only and does not redefine R007. SPEC-042 does not independently lift the bar.

Validation evidence: `go test ./internal/buyer ./internal/ws ./internal/billing ./internal/routing -count=1` passed all four packages. `git diff --check eed9ca5b..HEAD -- phase4-coordinator specs docs` and `jq empty specs/CONFORMANCE.json` passed. No malformed payload, exploit string, adversarial fixture, or new test fixture was authored or executed.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
|---|---|---|---|---|---|---|
| CRITICAL | NONE | — | No CRITICAL findings. | No path was found that permits loopback provider-relayed usage to become positively verified settlement usage. | — | — |
| HIGH | NONE | — | No HIGH findings. | Admission, route/settlement eligibility, attempt recording, and receipt verification enforce compatible fail-closed predicates. | — | — |
| MEDIUM | NONE | — | No MEDIUM findings. | The R3 normative SPEC-023 contradiction is closed across the formal requirement, authority text, acceptance criterion, open-question status, and threat model. | — | — |
| LOW | ARCH-1694-R4-01 | `specs/CONFORMANCE.json:2356` | **PRE-EXISTING / R3 partially unresolved:** the SPEC-023-R004 structured proof mapping still has empty `implementation` and `tests` arrays even though its refreshed rationale now says generator, coordinator, and CLI implementations exist. | The R004 row leaves both arrays empty at lines 2356–2357 and relies on prose pointing to the separate SPEC-010-R007 and SPEC-047-R003 mappings. Those mappings enumerate the artifact index/feed/CLI identity surfaces and the loopback admission/routing/recording surfaces, respectively (`specs/CONFORMANCE.json:1882-1901`, `8165-8198`). No structured cross-reference field links them back to R004. | Machine consumers of the conformance registry still read SPEC-023-R004 as having no implementation or regression tests, while the human rationale says the opposite; requirement-level coverage and future staleness checks remain unreliable. | Populate SPEC-023-R004's `implementation` and `tests` arrays with the current R004-owned surfaces, or add and use a schema-supported structured cross-requirement reference instead of relying only on rationale prose. Keep the row `pending` until its stated journey evidence exists. |
| INFO | NONE | — | No additional INFO findings. | The full sweep found no remaining stale R007(e) lift phrase or unqualified current claim that a GGUF/loopback member may settle while the bar holds. | — | — |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=1 INFO=0
