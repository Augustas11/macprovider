No CRITICAL, HIGH, or MEDIUM findings were found in the reviewed diff. The declared loopback runtime path appears barred from settlement-capable admission, buyer routing, and settlement verification; the remaining items below are latent or residual defense-in-depth risks.

| Severity | ID | File:line | Finding | Evidence | Consequence | Required correction |
| --- | --- | --- | --- | --- | --- | --- |
| LOW | SEC-1694-LOW-1 | `phase4-coordinator/internal/pool/provider.go:113` | PRE-EXISTING: `runtime_source` remains a provider assertion, so this diff closes declared/known loopback sources but does not prove a provider cannot claim empty or `mlx_cache` while actually serving through a loopback runtime. | `Provider.RuntimeSource` is documented as "the provider-asserted runtime_source"; hello parsing allows absent values to remain empty while rejecting only unrecognized non-empty strings (`phase4-coordinator/internal/ws/messages.go:1033`); the recorder only downgrades when `IsBYOMLoopbackRuntimeSource(in.ProviderRuntimeSource)` is true (`phase4-coordinator/internal/buyer/billing_recorder.go:748`); SPEC-047 explicitly says provider-signed `runtime_source` is only a provider assertion and keeps native `mlx_cache` or empty-hello behavior unchanged (`specs/SPEC-047-network-model-admission.md:144`). | A malicious or compromised provider that can still satisfy the existing catalog/receipt/admission checks while lying about runtime source is outside what this patch cryptographically closes; that residual risk is not newly introduced by this diff. | Keep loopback settlement barred until the future amendment names a coordinator-recorded trust binding and trusted usage source. If stronger protection is required before then, make settlement eligibility depend on coordinator-recorded runtime evidence instead of provider-asserted `runtime_source`. |
| LOW | SEC-1694-LOW-2 | `phase4-coordinator/internal/billing/hotpath.go:207` | The loopback relabel is not an independent ledger no-credit guard: if a loopback-tagged provider-bound attempt reaches `recordRow`, hot-path credit calculation can still run before the settlement-attempt output is relabeled `byte_estimated`. | `recordRow` passes `ProviderRuntimeSource` into `billing.HotPathInput` (`phase4-coordinator/internal/buyer/billing_recorder.go:458`), but `WriteHotPath` computes provider credits without checking that field (`phase4-coordinator/internal/billing/hotpath.go:207`) and inserts the request credit (`phase4-coordinator/internal/billing/hotpath.go:229`). `recordRow` marks the provider credited before persisting settlement evidence (`phase4-coordinator/internal/buyer/billing_recorder.go:511`), while `recordSettlementAttemptOutput` only changes the attempt output to `byte_estimated` and zero billable token evidence (`phase4-coordinator/internal/buyer/billing_recorder.go:748`, `phase4-coordinator/internal/buyer/billing_recorder.go:786`). The payable view and payout query accept non-quarantined `legacy`/`observe` ledger rows without verified settlement evidence (`phase4-coordinator/internal/billing/store.go:1217`, `phase4-coordinator/internal/billing/settlement.go:105`), and the configured default is `observe` with the settlement job enabled (`phase4-coordinator/internal/config/config.go:1630`). The new `recordRow` regression test asserts only `settlement_attempt_outputs.usage_source` for loopback (`phase4-coordinator/internal/buyer/settlement_unsettled_leg_test.go:195`). I found no live declared-loopback route to this state because buyer/ws admission refuses loopback event or session sources (`phase4-coordinator/internal/buyer/model_admission.go:360`, `phase4-coordinator/internal/ws/model_admission_operator.go:619`). | Defense in depth currently depends on the routing/admission barriers. If a declared loopback row nevertheless reaches billing (for example through a future bypass or a deployment with the strict admission gate disabled), provider-reported counts can produce a positive, payout-eligible `legacy`/`observe` ledger credit even though the attempt evidence is zero-billable and non-settleable. `enforce` mode remains protected by verified-receipt gating. This proves provider-payment/accounting divergence; the reviewed coordinator code alone does not prove a completed external buyer debit. | Zero or quarantine ledger credits for known loopback `ProviderRuntimeSource` values before insertion, and add regression assertions covering the raw ledger row, `spec022_payable_request_credits`, and settlement selection in both `observe` and `enforce` modes. If the intended contract is evidence-only, state that explicitly in the SPEC and do not describe the recorder relabel as a complete payment backstop. |
| INFO | SEC-1694-INFO-1 | `phase4-coordinator/internal/buyer/model_admission.go:360` | Declared loopback artifact/session sources are barred from buyer routing and settlement binding. | `byomBoundMemberMatchesSession` rejects known loopback event or provider runtime sources (`phase4-coordinator/internal/buyer/model_admission.go:360`), `settlementSessionMemberLocked` rejects a settlement-capable candidate when the session runtime is loopback (`phase4-coordinator/internal/ws/model_admission_operator.go:619`), and pinned/queued buyer selection paths re-check BYOM eligibility before routing (`phase4-coordinator/internal/buyer/server.go:7815`, `phase4-coordinator/internal/buyer/server.go:8056`). | No declared-loopback payment path was found through the provider-state machine, buyer route snapshot, or operator single-actor admission paths reviewed. | No correction required. |
| INFO | SEC-1694-INFO-2 | `phase4-coordinator/internal/ws/messages.go:1033` | Unknown non-empty `runtime_source` values fail closed; empty source remains a legacy/native-compatible value by design. | `parseOptionalRuntimeSource` rejects unrecognized non-empty strings (`phase4-coordinator/internal/ws/messages.go:1047`), and the admission validator accepts only `mlx_cache` or known loopback constants (`phase4-coordinator/internal/ws/model_admission.go:2718`). The recorder treats empty/non-loopback sources as eligible for `coordinator_observed` when provider token counts exist (`phase4-coordinator/internal/buyer/billing_recorder.go:748`). | The fail-closed behavior covers typos/new unknown strings. Empty-source compatibility preserves the pre-existing provider-assertion residual risk noted in SEC-1694-LOW-1. | No correction required for unknown strings; revisit empty-source settlement only as part of the future trusted-runtime-source amendment. |
| INFO | SEC-1694-INFO-3 | `phase4-coordinator/internal/ws/model_admission_operator.go:986` | I found no path for runtime source to change mid-session without invalidating the route/admission binding. | Heartbeat updates do not carry `RuntimeSource` (`phase4-coordinator/internal/pool/provider.go:2581`), while session admission/replacement bumps `ModelAdmissionSessionEpoch` (`phase4-coordinator/internal/pool/provider.go:1025`). `CompareAndInsertModelAdmissionRouteSnapshot` checks binding generation and session epoch before and after insert (`phase4-coordinator/internal/ws/model_admission_operator.go:986`, `phase4-coordinator/internal/ws/model_admission_operator.go:1025`). | Warm heartbeat drift cannot relabel a session source, and replacement hello drift should fail stale route insertion rather than silently settling under an old source. | No correction required. |
| INFO | SEC-1694-INFO-4 | `phase4-coordinator/internal/billing/settlement_verifier.go:340` | `byte_estimated` loopback attempt outputs are non-settleable in the settlement evidence consumers. | Receipts set `UsageCrossChecked` only for `UsageSourceCoordinatorObserved` (`phase4-coordinator/internal/billing/settlement_receipts.go:165`), and the verifier rejects attempts that are not cross-checked or not `coordinator_observed` (`phase4-coordinator/internal/billing/settlement_verifier.go:340`). `recordSettlementAttemptOutput` sets loopback attempts to `byte_estimated` with zero billable token evidence (`phase4-coordinator/internal/buyer/billing_recorder.go:748`, `phase4-coordinator/internal/buyer/billing_recorder.go:786`). | A correctly identified loopback attempt cannot satisfy settlement verification through provider-reported token usage alone. | No correction required. |
| INFO | SEC-1694-INFO-5 | `phase4-coordinator/internal/billing/quarantine_test.go:1088` | The requested aggregate existing-test command did not pass, although the failures were outside the #1694 diff and passed on focused rerun. | `go test ./internal/buyer ./internal/ws ./internal/billing ./internal/routing -count=1` returned: buyer PASS, ws PASS, routing PASS, and billing FAIL in `TestAdminRateLimitBucketConsumesFailures` plus `TestDisabledQuarantineRouteChargesAuthenticatedAdminBucketOnly`. A focused rerun of those two existing billing tests passed: `go test ./internal/billing -run 'TestAdminRateLimitBucketConsumesFailures|TestDisabledQuarantineRouteChargesAuthenticatedAdminBucketOnly' -count=1`. A separate focused run of the four named #1694 tests passed. | The source review and focused loopback checks are supported, but the requested aggregate command cannot be reported as passing; the observed rate-limit failures are consistent with test interference or flakiness rather than this diff. | Stabilize the unrelated rate-limit tests or their package-level isolation, then rerun the full requested command before landing if a wholly green aggregate gate is required. |

VERDICT: CRITICAL=0 HIGH=0 MEDIUM=0 LOW=2 INFO=5
