## Lane: ARCHITECTURE — contracts, SPEC conformance, operability.
Hunt: SPEC-039 exception text vs code (hybrid attach conditions, first-turn
only); SPEC-038 AC-26 still enforced (positive cached tokens serial);
`/v1/status` contract change consumers; the ordered-probe-pair design vs the
FR-CB6/FR-PKV attach-evidence requirements; the MSB harness vs serve path
(compiled flag); accepted-tuple declaration for a `mixed` cache class.
