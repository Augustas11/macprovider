## Lane: SECURITY / MONEY-PATH — isolation, billing, gating.
Hunt: any cross-request state exposure through the recurrent (Mamba) batch
state or its zero-init for joining rows; whether the hybrid attach gate
(exact model id + architecture + topology + parity + isolation) can be
satisfied by a different model; whether `/v1/status` CB fields expose
anything sensitive; usage/receipt behavior on hybrid rows (first-turn only,
no retained credit).
