Security-review the complete current uncommitted Gate A5 diff in /Users/augstar/macprovider-1646-rest against HEAD as the final round-3 security lane, including the untracked Python counter/tests. Do not edit files.

Round 2 found HIGH issues for same-key signers, a replayable/backdatable manifest, and raw evidence accepted largely by assertion, plus MEDIUM unbounded-object risk. The v3 remediation now requires three distinct signer principals and key fingerprints, a fully bound signed PLAN, an independent signed pre-window plan-hash receipt with reviewer-controlled append-only retention, a separately signed completed-evidence envelope, exact predeclared raw-event identity membership, canonical event/file digests, full companion bindings and identical membership records, observed arm-order enforcement, and strict file/JSON/object bounds. Verify that implementation, preparation modes, tests, SPEC, and runbook actually enforce the claimed boundary.

Threat-model hostile, malformed, misleading, duplicated, reordered, oversized, mixed-provenance, replayed, transplanted, and partially fabricated evidence. Check signature canonicalization and namespaces, signer/trust-file independence, the explicit external trust assumption for reviewer timestamp/retention, raw-to-completed evidence equality, companion/forward dilution, symlink/path/race behavior, subprocess and broken-pipe handling, denial-of-service bounds, and every false-green or uncaught-exception path. Do not demand live enforcement; this must remain offline and disconnected from routing, tiering, sanctions, payout, billing, receipts, settlement, and live configuration.

Rank every concrete finding CRITICAL/HIGH/MEDIUM/LOW/INFO with file:line evidence and an actionable fix. The acceptance gate is zero CRITICAL, HIGH, or MEDIUM findings. If that gate is met, begin the verdict with PASS and list LOW/INFO separately.
