**Verdict:** FIX REQUIRED
**Tally:** C/H/M/m/Q = 1/0/0/0/0

## Closure verified
- r1 C-1: CLOSED. The coordinator HTTP non-200 provider path now extracts the provider envelope code, treats only `malformed_json_response` and `json_schema_validation_failed` as terminal SPEC-019 provider-detail failures, records the row with `billing.FaultBreakerQualifying`, and forwards the provider body without retry/failover (`phase4-coordinator/internal/buyer/server.go:1865`, `phase4-coordinator/internal/buyer/server.go:1866`, `phase4-coordinator/internal/buyer/server.go:1867`, `phase4-coordinator/internal/buyer/server.go:1879`). The shared status allow-list also includes the two SPEC-019 codes (`phase4-coordinator/internal/buyer/server.go:4933`, `phase4-coordinator/internal/buyer/server.go:4935`), while the terminal helper is restricted to those two codes only (`phase4-coordinator/internal/buyer/server.go:4942`, `phase4-coordinator/internal/buyer/server.go:4943`).

## Fresh findings

### C-1: WS non-streaming SPEC-019 errors preserve the code but still log `FaultNone`
- Severity: CRITICAL
- Location: `phase4-coordinator/internal/buyer/server.go:2131`
- Issue: The r1 absorption correctly extended the provider WS end-frame allow-list: `InferenceRelay.errorEndFrame` preserves `malformed_json_response` / `json_schema_validation_failed` as the frame `status` and carries `retryable` from the provider envelope (`phase3-binary/Sources/macprovider-cli/InferenceRelay.swift:529`, `phase3-binary/Sources/macprovider-cli/InferenceRelay.swift:538`, `phase3-binary/Sources/macprovider-cli/InferenceRelay.swift:550`), and the coordinator WS message struct now deserializes `retryable` (`phase4-coordinator/internal/ws/messages.go:213`, `phase4-coordinator/internal/ws/messages.go:220`). The coordinator also renders a SPEC-019-shaped buyer envelope for these WS statuses (`phase4-coordinator/internal/buyer/server.go:5068`, `phase4-coordinator/internal/buyer/server.go:5074`, `phase4-coordinator/internal/buyer/server.go:5083`). However, the non-streaming WS request-log attempt returned for any non-`complete` end status only sets `Status`, `Error`, and `ErrorCode`; it does not set `FaultFlag` for the two SPEC-019 codes (`phase4-coordinator/internal/buyer/server.go:2125`, `phase4-coordinator/internal/buyer/server.go:2131`). That attempt is logged directly by the non-retryable terminal handler (`phase4-coordinator/internal/buyer/server.go:1670`, `phase4-coordinator/internal/buyer/server.go:1674`) through `logAttempt`, which passes the empty `attempt.FaultFlag` to `recordRow` (`phase4-coordinator/internal/buyer/server.go:1413`, `phase4-coordinator/internal/buyer/server.go:1422`). `recordRow` normalizes an empty fault flag to `billing.FaultNone` (`phase4-coordinator/internal/buyer/billing_recorder.go:181`, `phase4-coordinator/internal/buyer/billing_recorder.go:182`), so the billing formula does not take the `FaultBreakerQualifying` zero-provider-credit branch (`phase4-coordinator/internal/billing/formula.go:112`, `phase4-coordinator/internal/billing/formula.go:113`). This violates SPEC-019 AC-16/AC-17/AC-26 for WS-tunneled providers even though the buyer-visible code now survives the hop.
- Recommendation: In `forwardWSNonStreaming`, when `end.Status` is `malformed_json_response` or `json_schema_validation_failed`, return a `requestLogAttempt` with `FaultFlag: billing.FaultBreakerQualifying` alongside the preserved `ErrorCode`. Add a coordinator WS non-streaming regression test that injects each end status with a `retryable` value and asserts: buyer status/body code is preserved, request-log `error_code` is the SPEC-019 code, billing fault is `FaultBreakerQualifying`, and provider-positive credits are zero.

## Verdict justification

The original architect r1 HTTP-path money leak is closed: the terminal no-retry branch is intentionally scoped to the two SPEC-019 post-inference codes, so unrelated 502s and legacy provider errors still follow their existing retry/exhaustion behavior. The gateway streaming-reject regression probe also passes architect review: the gateway has a dedicated AC-20 pass-through recognizer for `streaming_json_schema_unsupported` and `streaming_json_object_unsupported` (`phase5-gateway/internal/router/chat_proxy.go:402`, `phase5-gateway/internal/router/chat_proxy.go:632`, `phase5-gateway/internal/router/chat_proxy.go:637`), with byte-identical coverage in `TestStreamingStructuredOutputRejectEnvelopePassesThrough` (`phase5-gateway/internal/router/structured_output_test.go:34`, `phase5-gateway/internal/router/structured_output_test.go:49`).

The remaining blocker is architectural and money-path critical: the r1 WS-hop fix preserved the SPEC-019 code and `retryable` flag across serialization, but did not extend the coordinator's WS non-streaming billing classification. A WS provider can now return a buyer-visible SPEC-019 502 while the request log/billing hot path records `FaultNone`, failing the same settlement invariant that r1 closed for HTTP providers.
