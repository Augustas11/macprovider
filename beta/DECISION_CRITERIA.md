# Phase 2 — Decision Criteria

> **Operator must fill the bracketed fields and stamp the header below
> BEFORE day 1.** Once stamped, do not change criteria mid-flight — the
> point of pre-committing is to take judgment out of the moment when
> partial data is most persuasive.

---

## Header (sign before kickoff)

- **Operator:** augstar (augstar@gmail.com)
- **Kickoff date (day 0):** **2026-05-26**
- **Timeline:** **3 days compressed** (was 14). Concept validated on day 0 → Phase 3 unblocked sooner.
- **Day 2 mid-review date:** **2026-05-28**
- **Day 3 final decision date:** **2026-05-29**
- **Providers committed:** M4 user (handle: `m4-anon`) + M1 collaborator (handle: `m1-anon`)
- **Contributor expectation:** signed up for 14 days; compressed timeline means they get off the hook at day 3. They may keep tunnels running past day 3 if they want — operator decision only.
- **Total operator time budgeted:** **~2 hours/day** over 3 days
- **Cash budget:** Domain registration $12/yr (streamvc.live, already paid). Cloudflare Tunnel free. Electricity negligible. **Total: $0 marginal.**
- **What I'm using this data for:** Lock or unlock Phase 3 binary build.
  If criteria fail, Phase 3 timing slips or scope changes.
- **Tradeoff accepted:** retention signal (does contributor still tolerate this at day 11?), diurnal patterns across many days, slow-drift bugs — these can't be measured in 3 days. Compression is justified because core throughput/error/leak/sleep data lands fully in 3 days, and 3 Phase 3 spec changes were already captured on day 0.

I commit to **stop or pivot if the day-7 or day-14 criteria below fail**,
regardless of how invested I feel at that moment.

Signed (commit hash on this file is the timestamp): `_______________`

---

## Why this document exists

A premortem on the original Phase 2 plan flagged the cost of *not having*
written criteria. The two failure modes that document is designed to
prevent:

1. **Sunk-cost continuation** — two weeks in, the data is mediocre, but
   you have $X invested and a working harness, so you push to "give it one
   more week." Without pre-committed numbers, this happens by default.
2. **Vague go decisions** — Phase 3 binary build is 4–6 weeks of work. If
   Phase 2 produces "looks fine to me" data, Phase 3 is built on hope.
   Pre-committed numbers force the question: *what specific finding from
   Phase 2 changed my Phase 3 design?*

---

## Pre-launch facts (lock in before day 1)

These are the **measurement baselines** — once kickoff happens, compare
against them. Per-provider table because Phase 2 has two real providers,
not one. Empty cells = TBD when that provider comes online.

| Fact | M4 day-0 | M1 day-0 | Source |
|---|---|---|---|
| Cooperative HTTP 200 rate | **100%** (5/5) | **100%** (6/6) | query A |
| Median tok/s — short_chat | **19.8** | **22.9** | query B |
| Median tok/s — medium_with_system | **17.3** | **22.5** | query B |
| Median tok/s — code_completion | **19.8** | **25.5** | query B |
| Median tok/s — agent_style | **18.3** | **23.8** | query B |
| Streaming TTFT (streaming_check) | **708 ms** | **646 ms** | runs |
| Stop-token leak rate (all workloads) | **0%** | **0%** ← see decision log | query C |
| Adversarial `retry_storm` p95 latency | TBD | TBD | query D |
| `long_context_oom_probe` outcome | TBD | TBD | adversarial_runs |
| `agent_style` valid-JSON parse rate | TBD (manual) | TBD | manual / query E |
| Model serving on day 0 | `Qwen2.5-7B-Instruct-4bit` (16GB tier) | `llama-3.2-3b-instruct-4bit` (8GB tier) | config-mX.yaml |
| Streaming `usage` field present in SSE | **NO** — confirmed Phase 3 spec item | **NO** — same behavior, cross-model | runs |

**M4 baseline captured:** 2026-05-26T09:33Z via `scripts/m4-cooperative.sh`.
Single batch of 5 workloads through https://m4.streamvc.live → Qwen 7B 4-bit
on M4 hardware. Zero failures, zero stop-token leaks, throughput band
17.3-19.8 tps tight across context sizes 37-343 prompt tokens.

**Notable day-0 finding:** `streaming_check` had no `usage` chunk in the SSE
stream from mlx_lm.server — prompt_tokens and completion_tokens both NULL.
Phase 3 binary spec must synthesize usage on streamed responses.

**Reference queries (copy-paste into `sqlite3 runs.sqlite`):**

```sql
-- A. HTTP 200 rate, cooperative cron
SELECT ROUND(100.0 * SUM(http_status=200) / COUNT(*), 1) AS ok_pct
FROM runs;

-- B. Median tok/s per workload
SELECT workload, ROUND(AVG(throughput_tps), 1) AS tps
FROM runs WHERE throughput_tps IS NOT NULL GROUP BY workload;

-- C. Stop-token leak rate per workload
SELECT workload,
       SUM(stop_token_leak) AS leaks,
       COUNT(*) AS n,
       ROUND(100.0 * SUM(stop_token_leak) / COUNT(*), 1) AS leak_pct
FROM runs GROUP BY workload;

-- D. Adversarial retry_storm p95
SELECT model, ROUND(p95_ms) FROM adversarial_runs
WHERE workload='retry_storm' ORDER BY ts_utc DESC LIMIT 5;

-- E. Tool-call JSON parse rate — see "Day 5 hard probe" below
```

---

## Day 2 mid-review — continue / pivot signal (2026-05-28)

**Proceed to day 3 final review only if ALL of the following are true:**

- [ ] **Uptime:** ≥2 days had ≥6 hours each with at least one provider reachable.
      *Query:*
      ```sql
      SELECT substr(ts_utc,1,10) AS day, COUNT(DISTINCT substr(ts_utc,1,13)) AS hours_with_data
      FROM runs WHERE http_status=200 GROUP BY day HAVING hours_with_data >= 6;
      ```
      Count the rows; need ≥ 2.

- [ ] **Cooperative reliability:** HTTP 200 rate per provider ≥ **80%**.
      *Query:* group by `tunnel_url`, expect column `ok_pct >= 80`.

- [ ] **Adversarial survival:** ≥1 successful `adversarial_runs` row per
      provider where `n_ok > 0` AND the provider's `mlx_lm.server` was
      still answering cooperative-cron traffic afterward. (Daily adversarial
      cron means each provider gets ≥2 runs by day 2.)

- [ ] **Stop-token detection diagnosed:** day-0 showed 0% leakage on both
      Qwen and Llama — either upstream mlx-lm now strips correctly, or
      our task-#13 per-model regex regressed. By day 2 we must know which
      (run a deliberate leak probe if needed).

- [ ] **No provider went dark for >24 hours** without explanation.

- [ ] **Operator can answer:** "what % of `agent_style` responses contain
      parseable tool-call JSON?" — at least 80% confidence in the number.

**If any FAIL → enter "iterate" loop:** see "What 'iterate' means" below.
Don't just extend day 2 hoping the next 24h fixes it.

---

## Day 3 final decision — Phase 3 go/no-go (2026-05-29)

**Proceed to Phase 3 (Swift binary build) only if ALL of the following:**

- [ ] All day-2 criteria still hold cumulatively over the 3 days (not just
      day-3 data in isolation).

- [ ] **Provider retention:** both providers reachable on day 3 (lid-close
      cycles OK, full silence not OK). One drop-off mid-experiment =
      downgrade to "iterate;" both gone = pivot.

- [ ] **JSON mode reliability:** ≥**90%** valid-JSON tool-call responses
      on at least one model in the rotation. With ~500 cooperative samples
      per provider over 3 days, this is statistically tight enough.

- [ ] **Cross-model differential exists:** Qwen 7B (M4) vs Llama 3B (M1)
      ran the same corpus-sampled prompts during overlapping hours. SQL
      JOIN should produce ≥**100** prompt-matching pairs to compare
      throughput/quality across models.

- [ ] **Specific Phase 3 design decisions changed:** must name ≥**3**
      Phase 3 binary spec lines that changed *because of* Phase 2 data.
      **Day-0 already captured 3** (502/530 routing, post-wake warm-up,
      capacity/quality tradeoff) — bar is already met. Anything more is
      bonus.

**If any FAIL → "iterate" or "pivot."** Don't ship to Phase 3 on hope.

**Contributor wrap-up:** regardless of go/no-go, send both contributors a
"thanks, you can shut down or keep running" message on day 3. The
cloudflared services keep running if they don't act — no urgency for them.

---

## Hard-stop triggers (any one of these — stop immediately, no review)

- Either provider goes dark for >24 hours **without explanation** (silence,
  not "I'm on vacation, back tomorrow"). Tighter than 14-day version because
  3 days has less buffer for outages.
- A provider's Mac **crashes more than once** from Phase 2 workloads
  (Metal OOM kernel panic, hard reboot needed). One is data; two is harm.
- A provider asks to stop. Their decision; no negotiation.
- Operator cannot answer the JSON parse-rate question by day 5 → the
  measurement apparatus is broken; fix or stop.
- Adversarial `concurrent_burst_8way` consistently puts a provider's Mac
  into thermal throttle that persists >30 min after the burst — that's
  past "fans spin" into "we're hurting their hardware."

---

## Soft-stop self-check (day 7 and day 14)

These don't auto-trigger a stop but force a conversation with yourself:

- Am I still finding this interesting, or am I forcing momentum?
- Is the operator-side workload (config tweaks, report reviews,
  contributor pings) under or over the budget I committed to in the
  header?
- Has anything in the **outside world** changed that makes this less
  important? (Anthropic ships local MLX inference, Darkbloom open-sources,
  Antseed changes seller protocol, etc.)
- If I had to **start Phase 2 over today** knowing what I know, would I?

If two or more of these tip negative, treat the day-7/14 review as a
"pivot" by default unless the data is exceptional.

---

## What "iterate" means

Not all failures kill the project — some need a second pass. The iterate
menu, ordered by severity:

| Failure | Iteration |
|---|---|
| Cooperative HTTP 200 rate <80%, root-caused to tunnel instability | Switch tunnel mechanism (named cloudflared ↔ ngrok), extend 1 week |
| Cooperative reliability fine, but one workload type fails reliably | Drop that workload from `batch:`, re-run week with reduced set |
| JSON mode <90% on all models | Add a non-MLX cloud fallback as comparison data point, do NOT pivot |
| Adversarial probes crash one provider's Mac once | Disable that specific adversarial workload, continue beta |
| Day-7 numbers fine but no Phase 3 spec change emerged | Add 1 more week of targeted data collection on the highest-uncertainty Phase 3 question |

**Cap: one iteration cycle.** If iterate-1 also fails its criteria,
pivot.

---

## What "pivot" means (the menu)

Phase 2 isn't load-bearing for anything outside this project — pivot is
real, not theoretical. Options, ordered by reversibility:

1. **Narrow scope:** Drop the second provider, run Phase 2 as N=1.
   Acceptable if the M4 user's data alone is rich enough to inform Phase 3.
2. **Change beachhead:** If Type B (AI power user) data shows poor
   retention, shift the Phase 3 spec target to Type A (passive earner).
   Same code, different pitch.
3. **Defer Phase 3 binary entirely:** Fork Darkbloom under their license
   constraints if the legal/dependency risk is now smaller than the
   re-implementation cost. (Requires re-reading their license.)
4. **Stop and write up findings:** If Phase 2 produces a clear "Mac
   inference at this scale doesn't work" answer, that result is itself
   publishable and informs other people's decisions.

The cost of NOT pivoting when you should: 4–6 weeks of Phase 3 binary
build burned on a foundation you don't trust.

---

## Decision log (fill in as data lands)

Append a row whenever you make a Phase 2 design or operational decision
based on harness data. The point: by day 14, this table is either rich
(go) or thin (no-go).

| Date | Observation (with query/row id) | Decision | Phase 3 implication |
|---|---|---|---|
| 2026-05-26 | M4 sleep transition observed: 502 (cloudflared up, mlx down, persisted ~14 min) → 530 (full disconnect). Tunnel API `conns_active_at` lags actual buyer-visible failure. | Distinguish failure modes in coordinator routing. | **Phase 3 coordinator must route around 502 with short backoff (~30s), remove 530 from pool until cloudflared reconnects. Poll `cfd_tunnel` connection count to predict imminent drops.** |
| 2026-05-26 | M4 post-wake throughput dip: 17.4 tps vs 19.8 pre-sleep (-12%) on identical `short_chat`. mlx weights survived sleep but first request was slower. n=1, needs more cycles to confirm pattern. | Track post-wake performance across 14 days. | **Phase 3 coordinator should fire synthetic warm-up request to a provider after detecting wake (cf. `conns_active_at` resuming after a gap) before routing buyer traffic.** |
| 2026-05-26 | Llama 3.2 3B 4-bit on M1 8GB: **0% stop-token leakage** across 6 cooperative requests, contradicting Phase 1 which observed `<|eot_id|>` on every short response. Response previews fully clean. Same model, same hardware tier, ~17 days apart. Likely mlx-lm update fixed upstream stripping. | Investigate if our per-model regex (task #13) regressed OR upstream fixed it. Either way: lower urgency for Phase 3 stop-token logic. | **Phase 3 binary may NOT need defensive stop-token stripping if upstream mlx-lm is clean. Still implement defensively, but it's not the critical-path bug we feared.** |
| 2026-05-26 | Cross-provider throughput inversion: smaller-model-on-slower-hardware (Llama 3B/M1 8GB → 22-25 tps) beats bigger-model-on-faster-hardware (Qwen 7B/M4 16GB → 17-20 tps). Even TTFT favors M1 (646 vs 708 ms). | Confirms capacity-vs-quality is a real routing tradeoff. | **Phase 3 buyer-facing API must expose model-size choice or auto-route by latency/quality preference. Coordinator cannot assume "newer/bigger Mac = faster" — depends on model.** |
| 2026-05-26 | Day 0 already captured 3 Phase 3 spec changes + clean baseline data across both providers. 14-day plan was paced for retention/diurnal/drift signal we don't need. | Compress to 3 days. Cron: cooperative every 15 min (4×), adversarial daily (vs 2×/week). Contributors stay on if they want (1A) — operator decision only. | **Phase 3 build starts 11 days sooner. Tradeoff: retention data is lost — re-add to Phase 5 pilot when real buyers come on.** |
| 2026-05-27 | Across **372 cooperative rows** spanning days 0-1 (M1 Llama 3B 8GB + M4 Qwen 7B 16GB), stop-token leak rate = **0%**. Two distinct tokenizer families (Llama `<\|eot_id\|>`, Qwen `<\|im_end\|>`) and ~24 hours of varied prompt content. Earlier worry that our task-#13 detection might be broken is dispelled by the sample size — if detection were broken we'd see 0 with any model; we now have positive evidence that mlx-lm's recent releases strip stop tokens cleanly upstream. | Downgrade FR-6 (defensive stop-token stripping) from "critical correctness" to "defense in depth." Still implement, but it is no longer the highest-risk Phase 3 surface. | **Phase 3 binary FR-6 stays as written but stops being a build priority. Run a deliberate leak-injection test during Step 9 acceptance instead of relying on production traffic to surface failures (which now appear vanishingly rare).** |
| 2026-05-27 | M1 first adversarial cron run (Wed 14:00 WITA, 4 workloads): retry_storm 50/50, concurrent_burst_8way 8/8, midstream_disconnect 10/10 ALL clean. **malformed_tool_call only 1/5 ok — 4× HTTP 404 instead of expected HTTP 400.** mlx_lm.server validates model existence BEFORE validating request shape; malformed JSON with bad `model` field falls through to model-not-found rather than invalid-request. | Confirms SPEC-001 § 6.2 validation order (1-5 before 6) is a real Phase 3 quality improvement over `mlx_lm.server`. | **Phase 3 binary must implement validation order strictly per SPEC-001 § 6.2 step 1→6. Add an AC asserting malformed_tool_call returns 400 (not 404). This is a buyer-facing semantic difference that affects error-handling downstream.** |
| 2026-05-27 | M4 reliability data across day-0/day-1: day-0 only 36% HTTP 200 (31/86) because of multiple lid-close cycles; day-1 recovered to 89% (85/95). M1 by contrast: 95% day-0, 100% day-1. Variance between providers is large and driven by hardware usage pattern, not hardware capability. | The Phase 2 D1 routing decision (502 vs 530) is empirically validated in real traffic. Coordinator must handle this variance gracefully — not just architecturally as we'd predicted, but operationally. | **SPEC-002 FR-P11 (502/530 distinction + recovery preflight) and FR-P8 (warm-up dispatch on wake) are not over-engineered — they're load-bearing for real provider hardware behavior. Validate during SPEC-002 build with mock providers that simulate the M4 lid-cycle pattern observed here.** |
| 2026-05-27 | Phase 3 binary Step 9 acceptance: **MLX inference under concurrent access crashes the Metal command buffer.** Codex deliberately serialized all `/v1/chat/completions` calls behind a single inference worker to pass adversarial AC-2 (`concurrent_burst_8way`, 8 simultaneous requests). The burst tests passed cleanly because requests get **queued**, not parallelized. This is a hard limitation of `mlx-swift` 2.29.1 in v1, not a Codex implementation choice. | SPEC-001 FR-9's per-RAM-tier concurrency numbers (`16GB → 2-way`, `32GB → 4-way`) are aspirational, not achievable in v1. Effective `max_concurrency` is **1 across all RAM tiers** with the current MLX runtime. Provider advertises `slots_total: 1` and a request queue absorbs bursts. | **Amend SPEC-001 FR-9 in v1.2: declare `max_concurrency = 1` for all RAM tiers in v1, with explicit note that this reflects mlx-swift's serialization limit not the hardware ceiling. SPEC-002 FR-R1 routing already filters on `slots_free > 0`, so single-slot providers route correctly — no SPEC-002 amendment needed. Phase 4+ may revisit if mlx-swift releases real concurrent inference (track upstream). RAM-tier capacity differences in v1 are about **context length** (8GB→20K, 16GB→50K, 32GB→120K tokens), not concurrency.** |
| 2026-05-27 | **M4 parity validated: phase3-binary swap landed cleanly on M4 partner (admin user, Qwen 7B 4-bit, port 8080). Across 2 cooperative cron cycles (10 workloads total), throughput holds within ±10% of mlx_lm.server baseline (short_chat -9%, medium +3%, code_completion +4%, agent_style mixed in band), HTTP 200 rate 100% (vs M4's 89% baseline under mlx_lm.server), zero stop-token leaks. `streaming_check` now reports usage tokens (18.2 tps, 80 completions) where mlx_lm.server returned None/None — confirms FR-7 (synthesize usage chunk) is working as a buyer-visible quality improvement. | First successful third-party deployment of phase3-binary via tarball + nohup install (tmux-less path). Validates: relocatable build, owned_by:macprovider identity signal, FR-7 streaming usage synthesis, AC-3 24h soak now running on real M4 traffic. | **AC-3 24h soak validation now satisfied by real M4 deployment, not synthetic; SPEC-001 v1.2 patch for FR-9 max_concurrency=1 is locked in. Deployment tooling for future partners: tmux is NOT a hard prerequisite (notmux install path proven). Next: monitor for 24h, then plan M1 swap or accept M4-only validation as sufficient.** |
| 2026-05-27 | **Phase 4 coordinator built in <6 hours (Steps 1-11 committed; ~3,764 lines of Go + tests).** ~10× faster than Phase 3 binary (7 days, bottlenecked on Xcode/Metal/MLX toolchain quirks). Go's batteries-included toolchain + the spec rigor from 4 audit rounds combined to produce a clean build with mock-backed acceptance: hello/heartbeat/state-machine/wake-detect/routing/SSE-relay/preflight/auth/operator-endpoints/SIGTERM-drain all green via unit + integration tests. Three integration gaps remain (AC-2/AC-3 need external Phase 2 harness orchestration; AC-6 needs full process-level streaming SIGTERM harness) — these are orchestration tasks, not coordinator-code gaps. | Confirms the spec investment paid off: an over-spec'd module ships fast because every design question is pre-resolved. Validates the **build velocity asymmetry** between Apple-toolchain (Swift+MLX+Metal) and standard-tooling (Go) ecosystems for similar-scope work. | **For SPEC-003+ (Antseed integration), SPEC-004 (smart router), SPEC-005 (rewards), SPEC-006 (public API): prefer Go/Rust/Python over Swift unless there's a hard reason to use Apple tooling (i.e., model inference on Apple Silicon — the only place Swift earns its complexity in this project). Use Phase 4 coordinator's clean build as the v1 quality bar for downstream specs.** |
| 2026-05-28 | **Phase 4 AC-2/AC-3/AC-6 closed locally with a Go mock-provider harness** (`phase4-coordinator/tools/mockprovider/main.go` 605 LOC + 4 acceptance scripts). AC-2: 10/10 HTTP 200 across 2 cooperative rounds, blacklist of mock-A deterministically routes next request to mock-B. AC-3: 69/73 OK (94%) across retry_storm + concurrent_burst_8way + midstream_disconnect + malformed_tool_call; malformed_tool_call failures are spec-correct rejections, not coordinator faults; pool stays up. AC-6: 3/3 in-flight SSE streams complete with `[DONE]` across coordinator SIGTERM; drain dispatched to both providers; both reply `drain_status=complete`; new request post-SIGTERM correctly rejected; coordinator exits in 5s. Three orchestration findings surfaced: (a) **default routing is order-sticky under equal metrics** — `sort.SliceStable` on `SlotsFree ASC, Throughput DESC` means identical providers always route to first-registered until one differs on metrics; (b) **dynamic provider registration is not supported** — `config.providers[]` must enumerate every provider or hello close-codes 4002 `unknown_provider_id`; (c) **`/admin/blacklist` is on the provider WS port (8444), not buyer port (8443)**. | All 11 SPEC-002 acceptance criteria now closed locally with deterministic evidence; safe to cross-compile and deploy to VPS. Order-sticky routing is a documented behavior, not a bug, but worth a SPEC-002 v1.0.4 note before two-or-more providers are wired in production. | **VPS deployment unblocked. Pre-deploy: (1) add `.gitignore` for built binaries (`coordinator`, `coordinator-cli`, `mockprovider`, `default.profraw`); (2) SPEC-002 v1.0.4 patch — document order-sticky routing behavior and decide whether to randomize tiebreak when N≥2 providers share metrics within tolerance ε; (3) operator runbook entry that admin endpoints live on provider WS port. Mock-provider toolkit is reusable for any future spec needing pool simulation without real Macs.** |
| 2026-05-28 | **Phase 4 coordinator deployed to production VPS + end-to-end cloud path validated.** Three sub-findings, all surfaced in <3 hours of deploy work: (1) **VPS selection** — Antseed VPS at 165.22.182.207 is 1vCPU/458 MiB/8.7 GB already 28% in swap and hosting the revenue-bearing Antseed seller; co-locating the coordinator there would have put real money at OOM risk. Pearl VPS at 159.223.165.194 is 2vCPU/3.8 GiB/120 GB idle with separate workload; deployed there. (2) **provider_id was UUID-per-connect** — Codex implemented SPEC-001 § 6.5's example text (`"provider_id": "uuid-of-this-instance"`) literally with `UUID().uuidString` on every coordinator hello. But SPEC-002 v1.0.4 F-2 requires stable operator-issued IDs (close code 4002 unknown_provider_id on mismatch). Two specs disagreed; SPEC-002's static-config-map is the load-bearing trust-pool admission mechanism. Fixed: phase3-binary v1.1.2 gains `provider_id` field across YAML/env/CLI layers, with UUID fallback only for dev/test; SPEC-001 v1.1.2 replaces misleading example and adds normative paragraph cross-referencing SPEC-002 F-2. (3) **Cloud path validated** — Go mockprovider on operator's Mac connected via wss://coordinator.streamvc.live/ws/provider (DNS → nginx → LE TLS → coordinator), got hello_ack with provider_id=m4-anon, registered in pool as `state: ready` with full metrics, clean disconnect detection on kill. nginx reverse-proxy site separated `/v1/*` (buyer_port 8443, SSE-aware buffering off) from `/ws/provider`, `/admin/*`, `/poolz`, `/healthz` (provider_port 8444). | Spec rigor failed once at the audit phase — the SPEC-001/SPEC-002 disagreement on provider_id semantics wasn't caught across 4+3 audit rounds. The example placeholder text (`"uuid-of-this-instance"`) was suggestive enough that a literal-minded implementer read it as normative. The audit rounds checked for missing requirements but didn't flag inconsistency between SPEC-001 example text and SPEC-002 normative behavior. | **For future cross-spec audits: add a specific check for "fields shared across specs" that compares example-value semantics, not just field presence. Practical lesson: when an example uses a placeholder that could be interpreted as a contract (UUID, port number, fixed string), either make it obviously fake (`"<operator-issued-id>"`) or accompany it with a normative paragraph. SPEC-003 (Antseed seller integration) is the next critical-path spec; apply this lesson before audit rounds begin. Build velocity asymmetry continues: spec patches + Swift fix + Release rebuild + tarball + install script + cloud validation + 3 commits in ~75 minutes — faster than Phase 2 cron cycle.** |
| 2026-05-28 | **Three-stream parallel build + cross-model audit cycle + production deploy + hotfix — the full Day-2 implementation arc.** SPEC-003 v0.4 (locked after 3 audit rounds) was implemented across three coordinated streams in parallel: Stream A (Swift, SPEC-001 v1.2.1 + § 6.6 WS-tunneled inference message types + status/update/uninstall subcommands), Stream B (Go, SPEC-002 v1.1.2 + WS relay + dynamic admission + tier-weighted routing + /v1/pool/check), Stream C (shell + YAML + markdown: install.sh + launchd plist + GitHub Action + README). Each stream owned an isolated codebase (no Swift/Go overlap; SPEC-003 Swift subcommands intentionally grouped with SPEC-001 in Stream A to avoid MacProviderCLI.swift conflict). Per-stream Codex audits surfaced ~10 findings each, fixed; then **cross-model integration audit** (Codex round 1 + Claude round 2) found 3 CRITICAL + 8 MAJOR + 3 QUESTIONS — Claude confirmed all 3 Codex CRITICALs AND added 5 new MAJORs Codex missed (notably: Go's mockprovider sent Go-struct NAK shape, masking the Swift↔Go wire-format mismatch in C1; Phase 3 had zero preflight + drain unit tests). Integration fixes applied in one pass, no version bumps. **Local integration test passed end-to-end**: stranger-shaped install via provisional admission, WS-tunneled inference (Llama 3.2 3B served Llama→coord→provider→Swift binary→MLX → response in 4.0s; "WS-TUNNELED-WORKS" returned exactly as instructed). Streaming SSE token-by-token through nginx + WS relay verified. SIGTERM drain composes correctly with v1.1.4 state-reset. C3 unknown-request_id cleanup hit warn+discard path (correct). **Production deploy to Pearl VPS hit two new bugs in <30 min:** (a) v1.1.3/v1.1.4 phase3-binary's reconnect loop didn't fire after coord-side drain — both M4 + M1 binaries stayed alive serving local /v1/models but never rejoined coord pool; resolved by AirDropping relaunch-m{4,1}.sh which kill+relaunch the existing install. (b) **silent production-breaking regression**: Go stream's per-stream audit fix "pinned providers require validated bearer when token validator is configured" was implemented in `cmd/coordinator/main.go` as `WithTokenValidator(tokenStore)` UNCONDITIONALLY, so server.go line 168 rejected ANY pinned provider (M4/M1 don't send tokens — they predate the token system) with WS close 4005. **And `s.close()` did not log**, so 15 min of production downtime showed only nginx access logs (101 Switching Protocols) with zero coord-side rejection traces. Hotfix shipped: `auth.require_provider_tokens` config flag (default false) + s.close now logs close_code + reason at warn level. After hotfix redeploy, M4 + M1 reconnect loops fired automatically; pool returned to N=2 ready within 30s. Multi-model routable, Llama 3B 1.77s, Qwen 7B 2.52s. | **Three lessons compound:** (1) **Three-stream parallel build pattern works** — saved ~40% wall time vs sequential per honest estimate, no merge conflicts because partitioning was by codebase ownership not by spec. Reusable pattern for SPEC-004+. (2) **Cross-model audit value confirmed at scale.** Per-stream Codex audits caught what each per-stream agent missed; cross-model integration audit (Codex + Claude) caught what BOTH per-stream agents missed (mock-implementation parity failure masking C1; the integration bug class needs both eyes). Specifically: Claude confirmed all 3 of Codex's integration CRITICALs (so we knew they were real) AND added 5 NEW MAJORs Codex missed. The repeated cross-model pattern from SPEC-001/002 generalized to implementation. (3) **The two production bugs we hit AFTER three audit rounds + integration test + local end-to-end were the same class:** "code path that depends on production config being a specific way, not exercised in any test, fails silently." First: v1.1.3/v1.1.4 reconnect-after-drain (the Swift Task lifecycle path that didn't get hit in unit tests). Second: pinned-provider token check (the auth path that needed RequireProviderTokens=false in tests but hadn't been). And the *concealment* layer was identical in both: missing logs. **The next audit pattern needs explicit "verify both code paths" category — every new gate, every conditional, must have a test for the un-set / un-enabled / un-configured case.** Add to the integration audit prompt for SPEC-004+. | **Production state at Entry-19 commit time: v1.1.2 coordinator live on Pearl VPS, M4 + M1 in pool, multi-model routable, both buyer-direct paths working (m4/m1.streamvc.live). Spec corpus: SPEC-001 v1.2.1, SPEC-002 v1.1.2, SPEC-003 v0.4 (all audit-clean). Three audit cycles complete (per-stream Codex × 3, integration Codex + Claude). Total commits today: ~32. Filed follow-ups for SPEC-002 v1.1.3: (a) `auth.require_provider_tokens` flag documented normatively in § 7.1; (b) "log every WS close" added as MUST in § 7.1; (c) anti-pattern "code paths gated by a non-nil pointer where the pointer is always non-nil" called out in audit category I. Filed for SPEC-001 v1.1.5: reconnect-task-lifecycle reliability after CoordinatorDrainComplete (M4 v1.1.4 + M1 v1.1.3 both stuck reconnecting until manual relaunch). The product side: distribution stack is fully built and audit-clean; M1 release-signing key placeholder remains the only operator-side step before strangers can `curl get.streamvc.live/install.sh | bash`. Tomorrow's Day-3 formal close becomes ceremonial — the network IS the product now, modulo the signing key.** |
| 2026-05-28 | **Day 2 wrap: the network works, the product doesn't yet exist.** Late-Day-2 state: M4 (phase3-binary v1.1.4) and M1 (v1.1.3) both `state=ready` in the production pool serving two distinct models. `POST /v1/chat/completions` with `model=Qwen 7B` routed to M4 in **2.48s**; same call with `model=Llama 3B` routed to M1 in **2.30s**; both via `https://coordinator.streamvc.live`. Lid-close on M1's MacBook at 04:00 UTC triggered the production-validated FR-P11 recovery cycle: HTTP 530 detected on first routed request, M1 marked `unavailable`, lid-open at ~04:05 brought the tunnel back, M1 reconnected with fresh `hello`, coord marked `ready` again, multi-model routing resumed. v1.1.4 drainFromCoordinator state-reset fix shipped + verified on M4 reconnect post-FORCE_RESTART (no stuck-draining). Cron simplified to one lane (`coord-cooperative.sh` every 15 min + `coord-adversarial.sh` daily) — direct-tunnel lanes retired since they're redundant with `/poolz` heartbeat liveness and become impossible under the planned architecture. **But: the same Day-2 session surfaced that the current product is operator-locked.** A stranger reading a GitHub README cannot become a provider without (a) a subdomain on `streamvc.live` you own, (b) a Cloudflare tunnel token you issue, (c) `provider_id` enumerated in `coordinator.yaml` (requires SSH + restart). Three hard blocks, all baked into the SPEC-002 v1.0.4 "Tier 1 cooperative trust pool" choice. Fine for 2 vetted partners; breaks at 5; impossible at 50. | The Phase 4 deliverable as built is **the technical core of an inference network, not a downloadable product.** Calling that out explicitly because every individual piece (binary, coordinator, routing, SSE, drain, recovery, multi-model) works under real partner hardware behavior — but the supply-side onboarding is operator-rate-limited. The architecture pivot to fix this is: **route inference through the existing provider WebSocket** instead of HTTPS-to-public-URL. Provider needs only outbound WSS, works behind any NAT, eliminates operator-managed DNS/tunnel infrastructure entirely. The pattern is industry-standard for outbound-only worker pools (Tor, Tailscale, GitHub Actions runners, Cursor agents) — convergent design driven by the "no public URL" constraint, not derivative. Cross-spec audit pattern (Claude+Codex, 3-4 rounds) ran clean on SPEC-001/002; we'll repeat it for SPEC-001 v1.2 + SPEC-002 v1.1 + new SPEC-003. Honest scope: 3-4 days of solid work for spec + audit + build + verify, plus a distribution layer (GitHub Releases + `install.sh` + launchd plist + `macprovider-cli update`) and a dynamic-admission layer (relax F-2 with a "provisional providers" tier where unknown `provider_id`s get probationary status). | **Next spec pivot:** SPEC-003 Antseed seller (was: first-revenue path) deferred to SPEC-007. SPEC-003 reframed as **"Open onboarding + WS-tunneled inference + dynamic admission + distribution lifecycle"** — the work to make this downloadable. SPEC-001 v1.2 adds normative `inference_request` / `inference_response_chunk` / `cancel_request` message types over the existing WebSocket. SPEC-002 v1.1 replaces the HTTP-forwarding path with WS-multiplexed relay + cancellation propagation + backpressure semantics; relaxes F-2 with a provisional-admission tier. Buyer-side surface stays stable (`POST /v1/chat/completions` unchanged). M4/M1 keep their existing direct-tunnel setups as "pinned providers" via the legacy `endpoint_url` path; new strangers default to WS-tunneled mode. After it ships, `curl -fsSL get.streamvc.live/install.sh \| bash` puts a stranger in the pool in <2 min with zero operator action. Day 3 (Friday) becomes design pass + cross-spec audit start, not a final go/no-go on the original criteria (those gated Phase 3 build, which shipped on Day 1). Also captured: v1.1.5/v1.2 should make phase3-binary's model_id comparison case-insensitive (mlx_lm.server was; current isn't — caused today's M1 cron 404 storm before Title Case fix). |
| 2026-05-28 | **Pool reaches N=2 + first multi-model end-to-end + v1.1.3 stuck-draining bug surfaced.** M1 partner (the M1 partner's Mac, Llama 3.2 3B 4-bit on 8 GB) ran install-m1-coordinator.sh with the v1.1.3 tarball. Initial issue: the coordinator's `config.providers[]` had drifted out of git when I'd added `m1-anon` directly on the VPS earlier, and a later `bash deploy-pearl-vps.sh` re-uploaded the stale local copy that only had `m4-anon`. M1 connection attempts close-coded 4002 `unknown_provider_id` until I fixed local `dist/coordinator.yaml`, asked M4 to upgrade to v1.1.3 (so they'd survive the redeploy drain), then ran `FORCE_RESTART=1 bash deploy-pearl-vps.sh`. **Result: pool N=2, both providers connected, /v1/models aggregates Llama 3.2 3B AND Qwen 7B with owned_by:macprovider.** First multi-model real inference through coord: `POST /v1/chat/completions {"model":"...Llama-3.2-3B..."}` routed to M1, returned `"HELLO"` in 2 tokens. **But: M4 reconnected after the restart in `state=draining`** and never returned to `ready` — v1.1.3's drainFromCoordinator() sets `providerStatus.status = .draining` for the drain handshake, closes the WS, the reconnect loop dials a fresh WS and sends hello, but the internal status never resets, so heartbeats keep reporting draining indefinitely. Coordinator router filters on `state=ready`, so M4 stays excluded from coord-routed traffic until binary restart (lid-close cycle, manual relaunch). M4 still serves direct buyer traffic via `m4.streamvc.live` correctly. | Two intertwined findings: (1) **Operational: out-of-band config edits on the VPS get reverted by `deploy-pearl-vps.sh`.** The script SCPs the local `dist/coordinator.yaml` as the source of truth. Fix: keep all provider list changes in local file + commit. Added a warning comment to `coordinator.yaml.example` (the gitignored real file is hand-synced). (2) **Implementation: v1.1.3's drain fix was structurally correct (process survives) but incomplete (state not reset).** The class of bug is "state-machine transition without reverse transition." After acknowledging drain we transitioned forward to `draining`, but reconnect needed an explicit forward transition back to `ready`. Spec-side fix opportunity: SPEC-001 § 6.5 should make the post-drain state explicit (recommended: hello/reconnect implicitly resets state per protocol invariant). | **Pool effectively N=1 ready, N=2 connected for the rest of today. v1.1.4 patch queued for next session: (a) drainFromCoordinator resets providerStatus.status to .ready (or pre-drain value) after WS close; (b) SPEC-001 § 6.5 adds normative paragraph: "the post-drain reconnect MUST start from a clean state machine; hello is the equivalent of a fresh hello, not a continuation of the prior session." (c) deploy-pearl-vps.sh could optionally diff /opt/macprovider/coordinator.yaml against the local file pre-upload and warn on drift — defense in depth against the operational class of bug. (d) Phase 4 acceptance hit: pool was N=2 with two distinct models serving real inference — a milestone the original 3-day plan didn't expect to hit until well after Day 3.** |
| 2026-05-28 | **Day 2 formal mid-review.** Status against the six Day-2 gates: G1 (≥2 days w/ ≥6h data) PASS — 26th 6h, 27th 14h. G2 (HTTP 200 ≥80% per provider) **FAIL** — coordinator 100% (n=5), M1 78.1% (n=411), **M4 67.6% (n=401, 0% today due to drain incident)**. G3 (adversarial survival) PASS — both providers all 4 adv workloads with n_ok > 0; malformed_tool_call failures are spec-correct rejections. G4 (stop-token detection) PASS — already resolved Entry 7 (upstream mlx-lm clean). G5 (no provider >24h dark) PASS — M4 last 200 was 27th 14:45, returned <12h later. **G6 (agent_style ≥80% valid-JSON) is mis-specified**: the agent_style workload was built to measure prefill cost on ~3K-token agent prompts, not to elicit JSON tool calls. Both Qwen 7B (M4) and Llama 3B (M1) produce coherent agent-style output — markdown reasoning + Python-style function-call syntax like `run_shell("git checkout main")`. Zero `{` characters across 127 samples isn't a quality fail; it's the criterion measuring a property the workload was never designed to produce. | Two real findings on top of the mechanical gate check. Finding A: **the original 3-day plan is technically blocked at Day 2 by G2, but every gate was designed to inform a Phase 3 go/no-go that is already moot** — Phase 3 shipped, Phase 4 shipped, M4 is live in pool, first production inference returned in 2.5s. The framework served its purpose by surfacing G6's mis-specification and confirming G3+G4 are fine; G2's M4-today failure is operator-caused (the drain incident) with a fix already shipped. Finding B: G6's mis-specification is the second cross-spec audit class that should be name-checked in SPEC-003 audits — "criteria written before the workload was finalized may measure a property that the workload doesn't produce." | **Day 2 review verdict: PROCEED (informally). The original plan's "iterate if any FAIL" applies to gating Phase 3 — Phase 3 is done. Real-status: ahead of schedule, single live provider, end-to-end working. Day 3 formal close tomorrow becomes lightweight. Spec/criteria action items: (1) DECISION_CRITERIA G6 should be rewritten to point at a JSON-strict workload OR retired; (2) consider adding a new `agent_json_strict` workload for buyers who actually need parseable tool calls (deferred until a buyer asks); (3) SPEC-003 audit rounds should include "criteria vs. workload coverage" as a class to check (criterion drift vs. workload semantics).** |
| 2026-05-28 | **Operator-caused incident: re-running deploy-pearl-vps.sh exposed a critical phase3-binary drain bug.** Sequence: (1) re-ran the deploy script to verify idempotency after fixing its broken step-6 sed; (2) the script ran cleanly end-to-end including `systemctl restart macprovider-coordinator` in step 7; (3) coordinator's SIGTERM handler correctly sent `drain` to M4 per SPEC-002 §6; (4) **M4's phase3-binary v1.1.2 handled `drain` by calling `Darwin.exit(0)` — process died**; (5) m4.streamvc.live started returning 502 (cloudflared tunnel up, origin gone); (6) coordinator pool empty. Tunnel-direct buyer traffic to M4 was broken until operator messaged M4 partner to re-run install-m4-coordinator.sh (~10 min outage). | The bug is SPEC-001/phase3-binary conflating two distinct paths: SIGTERM ("shut the whole provider down") vs. coordinator drain ("stop registering, keep serving direct buyers"). SPEC-001 v1.1.2 § 6.5 actually says `"same as SIGTERM behavior in FR-12"` — Codex implemented it literally, calling the same function. The "same" was meant to be procedural symmetry (drain_status sequence shape) not lifecycle conflation. Audit rounds didn't catch it because both sides looked correct in isolation. The class of bug — **shared internal helper used for two semantically different external events** — is worth named-checking in future audits. | **Fixes shipped in one patch cycle: (1) phase3-binary v1.1.3 splits `drainAndExit()` (SIGTERM, exits) from `drainFromCoordinator()` (drops WS, keeps HTTP, reconnects after 15s grace). (2) SPEC-001 v1.1.3 § 6.5 drain message now has explicit normative paragraph spelling out the distinction and calling out that conflating the paths is a critical bug. (3) deploy-pearl-vps.sh gains a step-6c pre-flight check against /healthz pool_size; refuses to restart with connected providers unless `FORCE_RESTART=1` is set. (4) tarball repackaged as phase3-binary-m4-v1.1.3-drain-fix.tar.gz, ready to ship. Field upgrade is non-urgent — v1.1.2 works fine until the next coordinator restart, and the deploy script safeguard now blocks that path by default. Next: SPEC-003 (Antseed seller) audit rounds should include a specific check for "two external events backed by the same internal handler."** |
| 2026-05-29 | **External security audit response: H-001 through H-006 triaged; Tier A spec-text patches close 3 findings, lock 2 forward-looking guards, defer 1 to BUILD_PHASE5 verification.** Late-Day-4: an independent security auditor delivered MACPROVIDER_HIGH_SEVERITY_RISK_AUDIT.md covering the spec corpus + selected code surfaces. **Six high-severity findings.** Auditor's executive opinion is the sharpest external framing the project has received: *"MacProvider does not appear to contain a critical architectural flaw if its current Tier 1 system is positioned as a cooperative, provisional provider network with no claim of provider-private prompts, hardware attestation, or malicious-provider resistance. The high-severity risk is expectation drift."* The findings: **H-001** privacy and trust claims exceed current enforcement (Tier 1 sends plaintext to provider runtime; positioning language like "decentralized, no big cloud" invites the inference that providers can't read prompts — which is false). **H-002** provider WS authentication too weak for an open pool (`auth.require_provider_tokens` defaults false; no pre-WS-upgrade rate-limits at proxy layer). **H-003** advertised RAM-tier max_concurrency >1 but Swift runtime enforces semaphore-of-1 (documentation drift between spec and code). **H-004** model identity is provider-reported, not cryptographically verified (untrusted-provider risk for future open pool). **H-005** billing/cancellation settlement not yet proven end-to-end (auditor likely hadn't seen latest SPEC-006 v0.5 D-CROSS-1 refund matrix or SPEC-001 v1.2.3 cancel-usage normative). **H-006** sticky caching, if shipped before Tier 2 trust controls, creates privacy surface larger than raw request forwarding (forward-looking). **Verification of H-003 against code:** reading `phase3-binary/Sources/macprovider-cli/MacProviderCLI.swift:64` produced the smoking-gun comment: *"MLX generation is currently guarded by a process-local semaphore of 1. Advertise the real runtime concurrency until the runtime is proven safe for parallel generation."* The code path: `let capacityDefaults = ProviderCapacity(maxConcurrencyOverride: 1)`. Live pool data confirms: both providers (augustass-macbook-air 8GB + air5 16GB) report `max_concurrency: 1` regardless of RAM tier. The code is conservative; the spec was aspirational. **Tier A coordinated FIX cycle.** Three FIX prompts drafted, run, and committed as a coordinated set (commit e0649b8): SPEC-001 v1.2.3→v1.2.4 (concurrency reality alignment: RAM-tier table values lock at 1, normative paragraph specifies process-local semaphore-of-1 as floor, operator override preserved, parallel generation deferred to a future v1.3 candidate when runtime safety is validated, audit-category entry added for "advertised vs enforced capability drift"); SPEC-002 v1.1.4→v1.1.5 (5 production gates PG-1 through PG-5 documented as normative invariants for public-launch transition: provider tokens mandatory, pre-WS-upgrade rate limits + per-IP connection caps at nginx layer, conservative provisional admission rate, aggressive unknown-provider-id rejection, provisional-spike alerting; nginx routing block expanded with `limit_req_zone` and `limit_conn_zone` directives; audit category I.Y "default-permissive flag in production deployment" added); SPEC-006 v0.5→v0.6 (§ 1.6 normative Tier 1 disclosure with 4 properties — plaintext to provider, no hardware attestation, model identity provider-reported, no privacy/attestation/integrity claims; § 5 extension adds `tier1_disclosure` block to /v1/models response, non-operator-overrideable; § 1.3 forward-looking guard forbids sticky caching unless 5 explicit privacy preconditions met; § 19 audit category Y "expectation drift between roadmap and current enforcement"; § 22 production launch gate checklist with 8 items adapted verbatim from auditor's recommendations). 363 line insertions across the 3 patched specs; no code changes (verified empty diff for `phase3-binary/`, `phase4-coordinator/`, `phase5-gateway/`). **Dependency-line drift caught + fixed.** During verification: SPEC-002 v1.1.5's "Depends on:" line still said SPEC-001 v1.2.2 (two minor versions stale — the FIX prompt explicitly told the executing session to leave deps unchanged); SPEC-003 v0.7 said SPEC-001 v1.2.3 + SPEC-002 v1.1.4 (one minor stale each); SPEC-006 v0.6 said SPEC-003 v0.6 (one stale). Three sed-fix patches across the four specs synchronized everything to the audit-response final state: SPEC-001 v1.2.4 + SPEC-002 v1.1.5 + SPEC-003 v0.7 + SPEC-006 v0.6, with every "Depends on" line accurate. **H-005 disposition.** The auditor's H-005 finding (billing/cancellation settlement not proven end-to-end) is largely closed by D-CROSS-1's refund matrix (SPEC-006 v0.5 § 17.5: provider-reached-vs-not framing across 200/503/502/504/disconnect) + SPEC-001 v1.2.3 § 6.6 cancel-usage normative (provider MUST include actual usage tokens in cancel-response inference_response_end). The auditor likely audited an earlier spec snapshot before these landed. **Verification of the spec→code→end-to-end accounting invariant is deferred to BUILD_PHASE5 Phase C** — the integration test phase MUST exercise the full disconnect + provider-restart + coord-restart matrix and produce evidence that every request produces exactly one terminal accounting outcome (auditor's framing, adopted verbatim). **H-004 disposition.** Cryptographic model-weight verification + coordinator-managed model catalog is genuine Tier 2 / SPEC-008 work; out of scope for SPEC-006 v0.6. The patch closes the disclosure surface (model identity labeled provider-reported in § 5.5 normative + docs caveat in § 13) so buyer expectations don't drift. Filed: SPEC-008 candidate for catalog + hash policy. | **Six lessons compound from this audit response + earlier cycles:** (1) **External audits catch what internal audits cannot by construction.** Our cross-spec audit pattern (Entries 19, 22) was the closest internal approximation but operates within the project's own conceptual frame. The independent auditor's "expectation drift" framing — that future Tier 2 promises bleed into current Tier 1 positioning — is a *language-layer* finding our own audits weren't structured to catch. The Codex+Claude audit prompts asked for spec consistency and contract precision; they didn't ask "does the spec promise something the code can't deliver." That's a class only an external reader can surface, because they don't share our internal model of "what we mean by it." **Action item:** add a category to all future audit prompts that explicitly asks "what does this spec promise that the code does NOT deliver today?" — pointed at the language gap, not the consistency gap. (2) **Verifying audit findings against code matters even for high-confidence external auditors.** H-003's verification against `MacProviderCLI.swift:64` took 30 seconds and produced the precise smoking gun (literal comment matching the auditor's claim + the override line). Treating audit findings as confirmed-until-verified instead of confirmed-because-stated avoids both false positives (rare) and false confidence (more common). **Action item:** every audit-response cycle MUST include a code-verification step for at least the highest-impact mechanical findings; the spec patch should cite the verified code reference. (3) **Expectation drift lives at the language layer; spec-text discipline closes it.** SPEC-006 v0.6 § 1.6's 4 normative Tier 1 properties + the `tier1_disclosure` block in /v1/models are the load-bearing fix for H-001, H-004, and H-006 simultaneously. None required architecture change. All three closed by making positioning language explicit and non-overridable. The pattern: when an audit finds "your system promises X but doesn't deliver X," the fix is usually "stop promising X," not "deliver X." This is the same discipline as Entry 21's "no premium positioning" rule — extended now to "no privacy positioning, no attestation positioning, no untrusted-provider positioning." **Action item:** future SPEC-006 + BUILD_PHASE5 implementation work MUST treat § 1.6 disclosure paragraph + § 22 production launch gate checklist as non-negotiable launch criteria, not "nice to have." (4) **Production launch gates encode the audit response operationally.** The auditor's Production Gate Recommendations section was framed as advice; SPEC-006 v0.6 § 22 adopts all 8 items verbatim as a normative checklist with cross-references to each item's source spec section. This converts external advice into operational accountability: BUILD_PHASE5 Phase E's deployment runbook MUST include § 22 checklist execution evidence. The 8 items: provider tokens mandatory (PG-1), proxy rate limits (PG-2), gateway exposes only buyer endpoints (already in v1.1.4 nginx routing), advertised = enforced concurrency (SPEC-001 v1.2.4), model identity labeled as provider-reported (§ 5.5), one terminal accounting outcome per request (D-CROSS-1 + cancel-usage), Tier 1 plaintext-to-provider disclosure (§ 1.6), Tier 2 claims blocked until Tier 2 enforcement is live (§ 1.6 + § 19 audit category). (5) **The five-tier audit cycle now: per-spec → cross-spec → regression → external → cross-spec regression.** The project's audit discipline has organically grown through Day-1 through Day-4 cycles: per-spec audits (Entry 18+ pattern), cross-spec coherence audits (Entry 22), regression checks (Entries 22, 23), and now external review (this entry). Each catches a different class. Per-spec catches single-spec quality; cross-spec catches inter-spec drift; regression catches narrow fix-side mistakes; external catches expectation drift. **Action item:** make external audit a normative pre-launch gate for any major surface (SPEC-006 v1.0 lock pre-BUILD_PHASE5, then re-audit before SPEC-008 Tier 2 lock). Budget ~$2-5K + a few weeks for the external audit cycle each time. (6) **Dependency-line drift is a tiny class of bug but compounds.** Three of four specs had stale "Depends on" lines after the audit-response patches landed; one had been stale for two minor versions. The FIX prompt explicitly told sessions to leave deps unchanged (because the audit findings didn't touch dependencies), so the drift was inherited from the FIX prompt itself, not from the executing session. **Action item:** every FIX prompt MUST include an explicit "sync all dependency lines to the corpus state after this patch lands" instruction, regardless of whether the findings touched dependencies. The mechanical sweep takes ~30 seconds; the drift class is annoying but trivial to prevent. | **Spec corpus at Entry-24 commit:** SPEC-001 v1.2.4 (locked, concurrency aligned to code reality), SPEC-002 v1.1.5 (locked, 5 public-launch production invariants + nginx pre-WS-upgrade controls + audit category I.Y), SPEC-003 v0.7 (locked, deps refreshed), SPEC-006 v0.6 (locked, Tier 1 disclosure language + 8-item production launch gate + 3 forward-looking guards). Five of six audit findings closed in spec text (H-001, H-002, H-003 fully closed; H-004 and H-006 closed via forward-looking disclosure/guards). One deferred to BUILD_PHASE5 Phase C verification (H-005 settlement invariant). All dependency lines synchronized to current corpus state. **Filed for future cycles (not blocking BUILD_PHASE5):** (a) **SPEC-001 v1.3 candidate** — parallel generation when runtime safety is validated; non-urgent, requires runtime engineering work. (b) **SPEC-008 candidate (Tier 2)** — model identity catalog + cryptographic hash verification + hardware attestation + provider-leg encryption + untrusted-provider safety; multi-week project, gated by external audit on each major surface. (c) **BUILD_PHASE5 Phase C** — verify H-005 settlement invariant via disconnect + provider-restart + coord-restart end-to-end test matrix; produce evidence that every request produces exactly one terminal accounting outcome; auditor's framing adopted verbatim as the test contract. (d) **BUILD_PHASE5 Phase D** — implement § 5 `tier1_disclosure` block on /v1/models; implement § 1.6 disclosure paragraph in front-door signup flow + docs page model-identity caveat + client SDK READMEs; ensure the disclosure surface is non-operator-overrideable. (e) **BUILD_PHASE5 Phase E** — execute § 22 production launch gate checklist (8 items) before api.streamvc.live opens to public buyers; produce checklist execution evidence in deployment runbook. (f) **Operational at Phase E launch time** — issue bearer tokens to M4 (currently `air5`), M1 (currently offline), and augustass-macbook-air; update `/opt/macprovider/coordinator.yaml` providers entries to include `bearer_token`; set `auth.require_provider_tokens: true`; restart coordinator; verify all providers reconnect with valid tokens. Estimated 1-2 hours operator + 5 min per partner. (g) **External re-audit candidate** before SPEC-006 v1.0 public launch (after BUILD_PHASE5 implementation) — closes the loop on whether the spec-text disclosures actually flowed into implementation, with budget ~$2-5K + a few weeks. **Next major work:** BUILD_PHASE5 against the audit-hardened spec corpus. The implementation contract now includes the disclosure surface, the production launch gate, the settlement invariant verification, and the operational migration runbook. Day-4 closes for real this time: the spec-design phase has been external-audit-validated. The customer-facing product is implementation work against a corpus where every promise has a corresponding code path or an explicit "not yet" disclaimer. The expectation-drift class is closed at the spec-text layer; BUILD_PHASE5's job is to flow it into the product surface. |
| 2026-05-29 | **Day-4 close: two release cycles + one partner upgrade + one install.sh hardening pass — spec-design phase formally ends, BUILD_PHASE5 unlocks.** Day-4's work resolved every open follow-up from Entry 22 and ran into one entirely unplanned arc (M4 partner upgrade) that surfaced six new install.sh bugs the project couldn't have predicted from spec text alone. Specifically: (1) **SPEC-001 v1.2.3 + phase3-binary v1.2.4 release cycle.** Two normative additions in one provider-side patch: § 6.6 cancel_request response MUST include actual `usage` tokens (enables SPEC-006 v0.5 to swap byte-estimation for exact); § 6.2 producer-side `withoutEscapingSlashes` MUST (catches spec up to v1.2.3 binary's already-emitted unescaped form, locks producer discipline for v1.2.4+). Swift implementation: cancel path now lets generation return partial usage before sending `status:"cancelled"`; unknown cancel returns zeroed usage; encoder configured for unescaped slashes everywhere. Hardware drain + cancel test captured exact numbers (`chunks_sent=26, completion_tokens=26`, prompt_tokens=56, total_tokens=82) — the cancel-usage path was verified end-to-end before v1.2.4 tagged. Commit c94da11, tag v1.2.4 released cleanly. (2) **SPEC-006 v0.5 estimation-to-actuals swap.** Narrow follow-up patch: § 7.2 prefer-actuals normative with `ceil(bytes_emitted_so_far / 4)` fallback for pre-v1.2.4 binaries (M4 + M1 if they don't upgrade); § 17.5 refund matrix splits client-disconnect into v1.2.4+ row (exact, from usage field) and pre-v1.2.4 row (estimated, ±5 token tolerance); AC-37 expanded with Branch A (actuals) + Branch B (fallback). 38 insertions across SPEC-006; dependency line updated to SPEC-001 v1.2.3. SPEC-002/003 untouched. (3) **Self-canary v1.2.4 on augustass-macbook-air** via install.sh upgrade-in-place. Surfaced the FIRST install.sh upgrade bugs: install.sh defaulted to port 8080 even though existing install was on 18080 (no config-port detection); `ensure_port_free` refused to proceed because the current macprovider-cli service held the port (own-service-as-foreign-holder). Manual launchctl bootout + tarball swap got the v1.2.4 binary running locally; pool registration verified within 10s; production round-trip via coord returned `V124-PROD-OK` in 1.73s. (4) **M4 partner upgrade: 6 distinct install.sh failure modes in one session.** What was supposed to be a 30-second `curl get.streamvc.live/install.sh | bash` turned into ~90 minutes of partner-debugging via Signal. The bugs (in order they surfaced): (a) port-detection failure (same as self-canary); (b) own-service-port collision (same); (c) M4's old install wasn't under standard LaunchAgent domain so `launchctl bootout gui/$UID ... 2>&1` returned "input/output error"; (d) **install.sh proceeded to model download + service start, but the binary crashed silently with launchd exit code 255 + empty err log**; (e) manual launch directly via `~/macprovider/macprovider-cli` worked cleanly, exposing that the issue was the launchd plist invoking the binary via `~/.local/bin/macprovider-cli` (the symlink path); (f) Swift's Bundle resolution on M4's macOS didn't dereference the symlink, so it searched for `.bundle` directories in `~/.local/bin/` (empty) instead of `~/macprovider/` (where they actually are) → "MLX error: Failed to load the default metallib." This is **Entry 20 Bug A pattern resurfaced for the upgrade-in-place case** — fresh installs worked, upgrades broke. M4's filesystem also revealed Python venv leftovers in `~/macprovider/` (May 26 from some unrelated project) that install.sh silently overlaid the binary + bundles onto. After diagnosis: sed-patch M4's plist to invoke the real binary path; service came up clean; M4 registered as `air5` (M4 self-chose at install.sh's interactive prompt) in WS-tunneled mode (transitioned from legacy HTTP-forwarding via dead cloudflared tunnel). Cloudflared killed on M4's side; old `m4-anon` config entry orphaned but harmless. (5) **Coord config + restart in parallel with M4 debugging:** SSH'd to Pearl, edited `/opt/macprovider/coordinator.yaml` to drop M4's `endpoint_url` (force WS-tunneled mode), mirrored change in local `dist/coordinator.yaml`, restarted coord. My Mac (v1.2.4) reconnected in <5s per SPEC-001 v1.2.2's reconnect lifecycle fix. M4 came up as v1.2.4 / ws_tunneled / ready shortly after. (6) **BUILD_PHASE5_PROMPT.md drafted** while M4 was debugging in parallel: 501-line single prompt with 5-phase checkpoint structure (Phase A scaffolding + storage interfaces, B auth + identity, C quota + streaming + buyer surface, D status + feedback + capacity + kill switches, E integration + deployment + docs). Each phase prints a handback summary and stops for operator review. Hard constraints encoded: no upstream spec edits, no design relitigation, stateless handlers, sub-ms p95 auth check, backward-compat for pre-v1.2.4 providers via estimation fallback, no premium positioning, no Tier-3 deprecation. (7) **SPEC-003 v0.7 install.sh hardening cycle.** Six findings filed from M4's debugging + self-canary, all closed in one coordinated FIX session: F-603-V7-1 read existing config to detect port; F-603-V7-2 `ensure_port_free` stops own service via launchctl bootout before erroring (foreign holders still exit 6); F-603-V7-4 plist invokes real binary path (`~/macprovider/macprovider-cli`) so Swift Bundle resolution finds `.bundle` directories adjacent — symlink at `~/.local/bin/` remains for shell PATH; F-603-V7-5 cold-cache deadline 20 min vs 5 min warm (detects HF cache directory presence); F-603-V7-6 self-test timeout message diagnostic-rich (tells user how to verify binary alive vs actually crashed, where to find logs, when to give up); F-603-V7-7 warns on non-empty INSTALL_DIR with non-macprovider entries (doesn't block). install.sh +135 lines, SPEC-003 +97 lines. **Hardware-verified scenario 2 on augustass-macbook-air:** ran the LOCAL install.sh (with v0.7 patches) against existing v1.2.4 install. F-603-V7-1 logged "Detected existing config port: 18080"; F-603-V7-2 logged "Existing macprovider-cli holding port 18080; stopping it for upgrade-in-place. Port 18080 freed; proceeding with upgrade"; service rebootstrapped on PID 60656; v1.2.4 reinstalled cleanly in 23s. Pool reconnect to ready. **Plist now explicitly verified to use the real binary path** (`/Users/augstar/macprovider/macprovider-cli`), not the symlink. Commit fa5f14b. install.sh changes land immediately via `get.streamvc.live` → main-branch redirect per SPEC-003 v0.5 § 4 distribution-channel-decoupling property; no release tag bump required. | **Three lessons compound from Day-4 + earlier audit-pattern lessons:** (1) **Field evidence beats theoretical reasoning** — M4's actual upgrade exposed 6 install.sh bugs in 90 minutes that no audit round (per-spec, cross-spec, or regression) had caught. The bugs were in the *interaction* between install.sh's assumed clean-state and a real partner's accumulated filesystem state (old install not under standard LaunchAgent domain; non-symlink-respecting Swift on their specific macOS; Python venv leftovers in the install directory from unrelated project). The cross-spec audit Category β ("implicit surfaces — code accepts what no spec defines") was the closest match in the audit framework but missed the upgrade-in-place case entirely. **Action item:** SPEC-003 v0.8+ audit prompts should include a new category for "upgrade-in-place paths exercise different OS-resource interactions than fresh installs" — and partner-replication testing should be a normative pre-release gate, not optional. (2) **"Small clean cycles before big work" discipline paid off again.** v0.7 took ~1 day (FIX prompt draft + execute + scenario 2 verification + commit). Cost vs delaying BUILD_PHASE5 by 1 day: the alternative is every future provider install requiring partner-support time. Compounding over expected provider growth: dramatic positive return. The discipline pattern continues to outperform shortcutting. (3) **Hardware-verification gates work even when the executing session feels confident.** Stream 1 deferred v1.2.3 tagging at the gate (Day 3); v1.2.4 cycle's executing session deferred tagging until the hardware cancel test produced `chunks_sent=26, completion_tokens=26` exact-match numbers; v0.7's executing session deferred scenario 1+2 since they'd require destructive live state changes. In all three cases, the operator-side hardware test surfaced what self-attested unit tests couldn't. **The gate is the artifact, not the verification step** — it's what makes self-attestation untrustworthy on production-bite paths by design. **One M4-specific finding worth filing for future audits:** the `launchctl list | grep macprovider` returning `255 live.streamvc.macprovider` (exit code 255) with empty stderr log was the smoking gun. macOS launchd exits binaries silently when their crash happens before stderr flushes (typical of pre-main-loop Metal load failures). **install.sh's failure path should pre-emptively check launchctl exit codes** and print them as part of the timeout diagnostic — v0.8 candidate. | **Production state at Entry-23 commit:** SPEC-001 v1.2.3 (locked), SPEC-002 v1.1.4 (locked), SPEC-003 v0.7 (locked), SPEC-006 v0.5 (locked) — full spec corpus production-ready. phase3-binary v1.2.4 (released, tagged d55a576 conceptual ancestor + actual commit c94da11). install.sh v0.7 hardened (6 fixes from real partner-upgrade evidence). Pool: 2 providers connected (augustass-macbook-air v1.2.4 ws_tunneled provisional; air5 v1.2.4 ws_tunneled provisional, formerly M4 in pinned HTTP-forwarding mode). Coord config updated to remove M4's endpoint_url; local dist/coordinator.yaml mirrored. M1 still offline (no progress this cycle). **Filed for future cycles:** (a) SPEC-006 v0.6 candidate — remove byte-estimation fallback once all providers are v1.2.4+; (b) SPEC-003 v0.8 candidate — install.sh pre-emptive launchctl exit-code check in timeout diagnostic; (c) M1 partner upgrade to v1.2.4 (whenever they reconnect); (d) M4's orphaned `m4-anon` entry in `config.providers[]` cleanup (cosmetic). **Next major work: BUILD_PHASE5_PROMPT.md** drives ~7-10 days of Go gateway implementation across 5 checkpoint phases. The executing session has zero design space to argue with — 9 operator pre-commitments (D1/D2/D3 + D-CROSS-1 through D-CROSS-6) are in spec text. Day-4 wrap: the customer-facing product is no longer a spec problem; the spec corpus has been audit-clean, hardware-verified, and battle-hardened against the one real partner upgrade we've done. The distribution stack is genuinely stranger-installable and tested at curl-pipe-bash + partner-upgrade scale. The next blocker to a working buyer surface is implementation time, not design or distribution. |
| 2026-05-29 | **SPEC-006 design+audit arc closes; four-spec corpus locks at SPEC-001 v1.2.2 + SPEC-002 v1.1.4 + SPEC-003 v0.6 + SPEC-006 v0.4.** Late-Day-3 into Day-4 spec work: the buyer-API surface went from "operator pre-commitments" to "locked, audit-clean, implementation-ready spec corpus" across **53 findings closed in 4 distinct audit cycles**. The arc: (1) **Two-stage prompt pattern.** `SCOPE_SPEC_006_PROMPT.md` produced a 1,139-line design exploration (10 design questions, market-price reality check, falsification framework, 90-day metric instrumentation) BEFORE drafting the spec; the design analysis went to a fresh session to lock a coherent product shape against operator's pre-commitments. `BUILD_SPEC_006_PROMPT.md` then drafted SPEC-006 v0.1 (2,373 lines, 25 ACs) against the locked design choices — fresh session reading the prompt cold, no relitigation of locked decisions. (2) **Cross-model audit cycle on v0.1.** Codex round 1 (2 CRITICAL + 15 MAJOR + 5 MINOR) + Claude round 2 (1 CRITICAL + 21 MAJOR + 9 MINOR, with one Codex CRITICAL reclassified to MAJOR via § 1.4 SPEC-001 preservation backstop). Both rounds verdict READY WITH FIX PASS; cross-model agreement on the single OAuth callback CRITICAL + 13 of 15 MAJORs. (3) **FIX_SPEC_006_V0_2 + 3 operator decisions D1/D2/D3** (v1 single-instance SQLite; demo token HMAC-SHA256 with operator-secret + IP + max-24h; streaming + error quota refund matrix where quota debits only for work the provider actually performed). SPEC-006 v0.2 added 633 lines, 12 new ACs (AC-26..AC-37), all 23 findings closed in a single FIX pass. **The D3 contradiction was caught at FIX-prompt review time** before the executing session hit it — operator review of the FIX prompt's matrix-vs-paragraph inconsistency surfaced the issue, edited the prompt (D3 lock + matrix aligned to provider-reached-vs-not framing), then ran. (4) **SPEC-006 v0.2 regression check** (single Codex round): 0 CRITICAL + 3 MAJOR (all narrow AC text quality). (5) **Cross-spec coherence audit** — NEW for the project, first 4-spec composition audit ever: SPEC-001 + SPEC-002 + SPEC-003 + SPEC-006 read together. Codex round 1 found 0 CRITICAL + 5 MAJOR + 3 MINOR + 1 QUESTION; Claude round 2 confirmed all R1 + added M2.1 response header strip gap (R2-only, the highest-impact finding), M2.4 degraded definition, M2.5 gateway /poolz config missing, M2.7 502 error terminology mismatch. Both rounds verdict READY WITH NARROW PATCHES, 0 CRITICAL across both. (6) **Cross-spec FIX (3 specs in lockstep)** with **6 operator decisions D-CROSS-1 through D-CROSS-6**: gateway-side `ceil(bytes/4)` estimation for disconnect (with SPEC-001 v1.2.3 candidate filed for provider-side actual usage); `/v1/pool/check` stays on coordinator (operator surface); X-Request-ID UUID v4 propagation; degraded boolean normatively defined in SPEC-002 v1.1.4; SPEC-006 capacity tiers explicitly independent from SPEC-002 admission tiers (no cascade); logprobs forwarded via SPEC-001 unknown-field tolerance. SPEC-006 v0.2→v0.3 + SPEC-002 v1.1.3→v1.1.4 + SPEC-003 v0.5→v0.6 in 289 added lines across three specs; SPEC-001 v1.2.2 verified untouched via empty diff. (7) **Cross-spec regression check** (single Codex round): 0 CRITICAL + 2 MAJOR (degraded cross-reference + duplicate, four ACs missing per-branch status/body). (8) **Final narrow FIX** locked SPEC-006 at v0.4 in 47 added lines (under 80 budget). All four specs now locked, audit-clean, dependency-line synchronized. | **Five audit-pattern lessons compound from Day-3 + the SPEC-006 arc:** (1) **The two-stage prompt pattern (SCOPE → BUILD) produces sharper output than direct drafting.** The SCOPE session forced the design space to be enumerated as 10 explicit questions; the BUILD session then locked answers as normative pre-commitments. A naive single-prompt would have collapsed design and draft into one pass, hiding tradeoffs in normative MUSTs. Use SCOPE→BUILD for all future product specs (SPEC-005/007). (2) **Cross-spec coherence audit catches the Entry 19 bug class that per-spec audits cannot find by construction.** Per-spec audits forbade upstream mutation; the cross-spec audit explicitly allowed findings proposing patches to any of 4 specs. That permission was load-bearing: M2.1 (coordinator response headers leak provider IDs because SPEC-006 had implicit catch-all but no explicit list), M2.5 (gateway.yaml can't actually call /poolz from its own config), and M2.6 (/v1/pool/check exists in coordinator code but isn't normatively defined anywhere) all required the cross-spec lens. Same bug class as Entry 19's `WithTokenValidator` always-non-nil and Entry 18's SIGTERM=drain conflation. Add cross-spec audit as standard discipline for any spec touching existing specs. (3) **The audit-finding count curve (21→3→9→2→0) shows audit value compounds and converges.** Each cycle catches a different class: v0.1 cross-model caught most of the contract-precision issues; v0.2 regression caught AC text quality; cross-spec audit caught implicit surfaces; cross-spec regression caught residual section-number drift. The diminishing-returns curve held at every step — and zero CRITICAL across all 4 cycles is the proof that the architecture was correct from the BUILD prompt. (4) **Operator-decision pre-commitment is the spec corpus's strongest property.** D1/D2/D3 + D-CROSS-1 through D-CROSS-6 = 9 operator decisions encoded in spec text. The SPEC-006 § 2 locked-decisions section is read-only across all FIX cycles; the audit prompts explicitly REJECT findings that recommend changing locked choices. This forces audits to engage with what was decided rather than relitigating. The discipline is what kept 4 audit cycles narrow instead of spiraling into "what about premium pricing?" or "should we use OpenRouter shape?" again. (5) **Operator-self-review of FIX prompts catches drafter errors before they propagate.** The D3 streaming-disconnect contradiction in the FIX_SPEC_006_V0_2 prompt was caught by operator reading the prompt itself, not by the executing session or a downstream audit. Same with the 600→800 length budget that would have forced the executing session to skip findings, and the F-M11 playground-feedback auth ambiguity. The discipline of "review the FIX prompt before running it" should be normative going forward — it's where the drafter's biases get caught at lowest cost. | **Spec corpus locked at Entry-22 commit:** SPEC-001 v1.2.2 (untouched throughout the arc — verified empty diff at every cycle), SPEC-002 v1.1.4 (gained /v1/pool/check normative § 4 FR-B1, nginx routing block, X-Request-ID correlation, /poolz summary fields, buyer-port rebind doc), SPEC-003 v0.6 (gained installer cross-reference to SPEC-002 v1.1.4 + dependency line refresh), SPEC-006 v0.4 (full spec from scratch: 2,968 lines, 37 ACs across 20 sections, 9 operator pre-commitments encoded). Filed for SPEC-001 v1.2.3 candidate (future cycle, not blocking): provider MUST include actual completion-token usage in cancel_request response, replacing gateway byte-estimation; address when next phase3-binary release lands. Filed for SPEC-002 v1.1.5 candidate: if real coordinator code surfaces accept inbound headers not yet in SPEC-002 v1.1.4 normative tables, those need adding (none surfaced in this audit cycle). M4 v1.1.4 + M1 v1.1.3 partner binaries still on pre-reconnect-fix versions — schedule v1.2.3 upgrade with partners when they have a moment (their current binaries are working but carry the reconnect bug). Next major work: BUILD_PHASE5_PROMPT.md drives 7-10 days of gateway implementation against the locked spec corpus in a separate session — first product spec to enter implementation phase, all operator pre-commitments are in the spec text so the executing session has no design-space to argue with. The full spec-design phase for the buyer surface is closed; the customer-facing product is now an implementation problem, not a spec problem. |
| 2026-05-28 | **Day-3 close: three-stream patch cycle + hardware drain verification + self-canary — full follow-up trail from Entry 19/20 closed.** Entries 19 (production hotfix arc) and 20 (four stranger install bugs) had filed three queued patch revisions: SPEC-001 v1.2.2 (reconnect lifecycle + case-insensitive model match + JSON-escape tolerance), SPEC-002 v1.1.3 (auth.require_provider_tokens normative + log-every-WS-close + audit category I anti-pattern), SPEC-003 v0.5 (self-test wire-byte print + distribution-channel decoupling + shell-script audit category). All three drafted as per-stream FIX prompts (specs/FIX_SPEC_*_PROMPT.md, commit 502c53e), executed in parallel by three Claude Code sessions, and committed cleanly: 6a91257 (Stream 1, Swift behavior + spec), 813f502 (Stream 2, spec-only catch-up to commit 47d6433), 7fca999 (Stream 3, install.sh diagnostic polish + spec text). **Stream 1 agent correctly respected the hardware-verification gate** in its prompt — declined to tag v1.2.3 despite swift test passing 11/11, because the local /poolz wasn't running and the production-bite bug couldn't be self-attested. Operator ran the hardware drain test against a local phase4-coordinator on isolated ports (18443/18444): patched .build/release/macprovider-cli joined as drain-test provider; SIGTERM to coord fired DrainAll (coord log: `"provider drain sent"`); coord restarted 36s later; **patched binary autonomously sent fresh hello at T+4s after coord listened, pool_ready:1 confirmed at T+12s.** The exact Entry-19 failure mode (M4 v1.1.4 + M1 v1.1.3 stuck-forever requiring manual relaunch-m{4,1}.sh) was reproduced in v1.2.2 and resolved end-to-end in v1.2.3 in a single session. After hardware test, self-canary: stopped launchd v1.2.2 on augustass-macbook-air, swapped in v1.2.3 binary + missing swift-transformers_Hub.bundle, re-bootstrapped launchd against production coordinator. v1.2.3 joined pool in ~10s; `/v1/models` returned unescaped `"id":"mlx-community/Llama-..."` (Bug D fix on the wire); buyer request with lowercase model name `mlx-community/llama-3.2-3b-instruct-4bit` returned `"WORKS"` (Bug C fix); full production round-trip via coordinator.streamvc.live returned `V123-CANARY-OK` in 3.4s. 25-min stability monitor at /tmp/canary-monitor.sh ran every 60s checking local /v1/models + coord pool/check; clean throughout. | **Three audit-pattern lessons compound from today + Entry 19/20:** (1) **The hardware-verification gate works as a discipline.** Stream 1's agent had every "this code is correct" signal (unit tests green, build clean, --version reports 1.2.3) but the prompt explicitly said "do not tag a release on a self-attested fix for a production-bite bug." Agent stopped at "patch ready, untested on real hardware." This is the right behavior, and worth name-checking in every future BUILD prompt that touches production-bite paths. (2) **Self-canary is now a discipline between hardware-test and tag.** Local hardware test exercises lifecycle (Swift Task semantics, WS state machine) but uses plain `ws://` to localhost — no TLS, no nginx, no cloudflared. Self-canary exercises the production network stack on a Mac the operator can introspect, before strangers see the artifact. ~30 min cost; catches WSS/nginx/cloudflared issues that the local test can't. Add as a normative step in SPEC-003 distribution lifecycle for any future release that changes binary behavior. (3) **Ship-then-spec is acceptable under hotfix pressure, NOT default for design work.** Commit 47d6433 shipped the auth.require_provider_tokens flag yesterday; SPEC-002 v1.1.3 today catches the spec up to the implementation. That sequence is fine BECAUSE the production fire required immediate action and the operator owns both the code and the spec. Should not become the pattern for SPEC-004+ design work, where spec-first → audit → build is the right discipline. The three-stream FIX prompts each carried an explicit clause "if shipped code disagrees with the spec being drafted, shipped code wins" — that's the safety-net version of ship-then-spec; works for narrow patches, fails at design scale. | **Spec corpus at Entry-21 commit: SPEC-001 v1.2.2 (locked), SPEC-002 v1.1.3 (locked), SPEC-003 v0.5 (locked). All three Stream-1/2/3 fixes hardware-verified (Stream 1 explicitly, Streams 2+3 verified incidentally during Stream 1's drain test — local coord log carried the v1.1.3 token-mode message and close-logging fields; Stream 3 install.sh diagnostic preserved in the canary swap). v1.2.3 tagged immediately before this entry (annotated tag d55a576), GitHub Action 26577043280 in progress on macos-15, publishes new "latest" release for future `curl get.streamvc.live/install.sh \| bash` invocations within ~10 min. M4 v1.1.4 + M1 v1.1.3 remain on their current binaries — partners cooperated once today for the relaunch scripts; ask them again for v1.2.3 upgrade tomorrow with a clean message (their current binaries have the reconnect bug but no active suffering — drain only triggers on coord restart, which we now defer past partner working hours). Open: (a) SPEC-001 v1.2.3 candidate — make withoutEscapingSlashes a normative requirement on the producer side (today's spec text marks both `/` and `\/` legal but doesn't mandate which); revisit after buyers report; (b) SPEC-004/005/006/007 priority sequence is now in scope. Operator direction is **own buyer surface (SPEC-006) before AntFeed (SPEC-007)** — owned customer relationship beats marketplace-mediated transactions for the long-term position. Product *shape* still being scoped: my initial "Venice-flavored premium" recommendation was wrong (no defensible premium for running Llama 3B / Qwen 7B when Groq serves Llama 70B at $0.59/M at 500 tok/s); real axis is commodity (race-to-zero) vs. vertical (privacy / sovereign / specific buyer) vs. consumer (B2C product, not API). SPEC-006 BUILD prompt waiting on operator picking buyer + use case + willingness-to-pay before drafting. Day-3 close: distribution stack is genuinely stranger-installable and tested; the customer-facing product is the next binding constraint.** |
| 2026-05-28 | **First stranger-shaped `curl-pipe-bash` install surfaces 4 bugs in install.sh (A–D); all shipped same-day.** With the GitHub repo flipped public + `get.streamvc.live` → raw.githubusercontent.com redirect live + v1.2.1 signed release published, I ran the literal command a stranger would run: `curl -fsSL https://get.streamvc.live/install.sh \| MACPROVIDER_PORT=18080 bash`. Four distinct bugs surfaced across two retest cycles. **Bug A (the obvious one):** the install put the binary at `~/.local/bin/macprovider-cli` but copied the `mlx-swift_Cmlx.bundle` + `swift-nio_NIOPosix.bundle` Metal/transport bundles into `~/macprovider/` — mlx-swift loads `.bundle` resources adjacent to the executing binary, so on first run Metal kernel JIT failed with "Library not found". Fix: install bundles + binary together in `~/macprovider/` and make `~/.local/bin/macprovider-cli` a symlink (PATH discoverability preserved, bundles found). **Bug B (the user-experience one):** `MACPROVIDER_PORT=18080 curl ... \| bash` sets the env on `curl`, not on `bash` — port stayed default 8080, collided with the previous v1.2.1 install still listening, second copy crashed silently. Fix: added `ensure_port_free()` that runs `lsof -nP -iTCP:$PORT -sTCP:LISTEN -t` after quarantine-clear, prints the holding process + correct invocation form (`curl ... \| MACPROVIDER_PORT=18080 bash`), exits non-zero. **Bug C (the cosmetic one):** when the install script redirects /dev/tty for confirm prompts inside a pipe, bash leaked `read: 1: Bad file descriptor` noise every iteration. Fix: `read_line` now opens /dev/tty as fd 4 with `exec 4</dev/tty` only after `[ -c /dev/tty ]`, falls back to plain `read` from stdin if no controlling tty exists. **Bug D (the deep one, surfaced only after A+B+C shipped as v1.2.2):** the self-test `wait_for_local_model` did `grep -Fq "$model"` against the response — but Swift's JSONEncoder emits forward-slashes as the legal-but-cosmetic `\/` escape, so `"id":"mlx-community\/Llama-..."` never matched the unescaped `mlx-community/Llama-...` we searched for. The binary was actually up and serving correctly within 12s of port-bind; the install script just never recognized it and printed "Local self-test failed" while everything was green. The previous "extend deadline to 300s + progress logging" fix made the failure mode louder (waiting 5 minutes instead of 1) but not correct. Real fix: normalize the response with `sed 's\|\\\/\|/\|g'` before the grep, tolerating both encodings. | **Pattern: the audit rounds caught what's wrong with the design; only the stranger-shaped test catches what's wrong with the surface.** All four bugs are in glue code that no audit reviewer would flag because each line is locally correct: install.sh copies a tarball, ensures a port is free, reads a prompt, parses JSON. The failure mode in every case was an interaction with the user's actual shell environment (curl-pipe-bash semantics, /dev/tty in a pipe, JSON-escape conventions, mlx-swift bundle-loading rules). Worth name-checking in future SPEC-003+ audit categories: **"every shell-script path that touches a real OS resource (tty, fd, port, FS layout, JSON over loopback) needs an integration test that actually exercises it, not a code review."** Decision-side lesson: the cost of shipping v1.2.1 → v1.2.2 → install.sh-only patch (no release bump) was small precisely because the install.sh distribution channel is decoupled from the binary release tarball — the get.streamvc.live → main-branch redirect means a 1-line install.sh fix lands in seconds without re-running the macos-15 build action. That's worth preserving as an explicit architecture property in SPEC-003. | **State at Entry-20 commit: install.sh patched and pushed to main (commits d1093de + 7aae075). `curl -fsSL https://get.streamvc.live/install.sh \| MACPROVIDER_PORT=18080 MACPROVIDER_NO_PROMPT=1 bash` now goes green end-to-end in <30s on a warm cache: port bound 12s, model registered immediately, coordinator pool/check reports `tier: provisional, state: ready`. v1.2.2 binary tarball stays current (binary unchanged); install.sh from main carries the four fixes. The full distribution stack is now actually-strangers-can-install — repo public, redirect live, signed release published, install script tolerates the wire format we actually emit. **Open follow-ups:** (a) consider patching the Swift JSON encoder to stop emitting needless `\/` escapes (cosmetic, would unbreak other downstream tooling that may have the same assumption); (b) Bug A's symptom was reported as "Local self-test failed" — the same generic message as Bug D — which delayed root-cause attribution by one whole retest cycle; the next install.sh revision should print the failing grep's actual input bytes on failure so the operator sees the wire format directly; (c) Bug D should be a SPEC-003 spec-side normative requirement that "owned_by:macprovider" presence in /v1/models response is the readiness signal, with an explicit example showing both `\/` and `/` are acceptable in the id field (decouples client tolerance from server encoder).** |
| 2026-05-28 | **First real production end-to-end through coordinator.** M4 partner ran `install-m4-coordinator.sh` on the v1.1.2 tarball. Result: phase3-binary on their MacBook Air swapped in cleanly, hello'd the coordinator with provider_id=m4-anon, got hello_ack, started heartbeating. Pool snapshot 90s post-install: `m4-anon` state=ready, ram_gb=16, max_context=50000, max_concurrency=2, slots 2/2, throughput=9.12 tps, hostname="MacBook Air". A buyer-style request to `https://coordinator.streamvc.live/v1/chat/completions` (Qwen 7B, max_tokens=10, prompt "say OK") returned `"OK"` in **2.5 seconds** via the full cloud stack: nginx TLS → coordinator routing → HTTPS to m4.streamvc.live tunnel → phase3-binary → Qwen 7B 4-bit → response back. SSE streaming validated separately; tokens flow chunk-by-chunk through nginx with proxy_buffering off as configured. Install script bug surfaced: step 7/7 grepped the local binary log for "hello_ack" to confirm WS link, but phase3-binary acts on hello_ack silently (no log emit); M4's install printed a scary "WARNING: coordinator link not confirmed" while the link was provably up. Fix: step 7 now queries the coordinator's own `/v1/models` from the partner's Mac and checks whether the local model appears in the aggregated list (coordinator only includes models from connected providers, so presence is positive proof of WS link). Applied to both install-m4 and install-m1 scripts. | Phase 4 architecture is no longer hypothetical — there is a working buyer→coordinator→provider path serving real Apple-silicon MLX inference from a contributor's Mac. The 2.5s round-trip is dominated by Qwen 7B inference on M4 (the coordinator adds <100ms of routing+proxy overhead). Spec rigor pays off again: nginx config drafted from SPEC-002 § 7 with explicit Upgrade-header + SSE buffering directives worked first-attempt at port 443. Verification-pattern lesson: validate from the side that owns the truth (the coordinator's pool view) rather than the side that consumed the event (the provider's binary log). | **Phase 4 is production-ready for a single provider. Next: (1) point Phase 2 harness at https://coordinator.streamvc.live (replace tunnel_url in beta/config.yaml) to put continuous cron load on the production coordinator path; (2) send M1 partner the same upgrade flow now that the install script no longer cries wolf; (3) SPEC-003 (Antseed seller integration) is the next critical-path spec for first revenue — the coordinator now has a stable upstream that an Antseed seller can register against. Operator-side runbook addition: monitor /healthz uptime as the Phase 4 SLI and /poolz total_providers as the second-order capacity SLI.** |
| `____` | `_______________________________` | `___________` | `___________________` |

If this table has fewer than 5 entries by day 14, that itself is a
finding: Phase 2 isn't producing decisions, only data. That should heavily
weight the day-14 review toward "pivot" or "iterate."

---

## One-line summary to remember

**Phase 2 exists to surface specific Phase 3 design changes.** If it
doesn't produce a single "I changed Phase 3 spec line X because of Phase
2 row Y" sentence, it didn't earn its 14 days.
