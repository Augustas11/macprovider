# Transaction owner testability addendum r1

Status: proposed. No production policy or result substitution.

Expose defaulted internal dependencies on ModelCatalogTransactionRunner: timeout seconds (production 1800), available filesystem bytes (existing Foundation query), preparation boundary observer (default no-op, only existing lifecycle stage points), owned cleanup operation (existing store cleanup), conflict detection (existing ProviderConflictDetector), lifecycle adapter (existing ProviderDrainer start crash restore guard/drain/restore/dismiss), hardware sampling (existing MachineFingerprinter after scoped HMAC derivation), and benchmarker factory (existing AutotuneRecommendationBenchmarker with scoped logs/resolver and CandidateProviderRunner). Production command initializer supplies no overrides. Keep isolation conflict predicates before lifecycle invocation. No injectable recommendation output, recommendation engine, trusted metadata validator, artifact verifier, atomic publisher or persisted terminal/result writer.

Fixture recommendation tests inject a Stage1 prober through the existing real benchmarker, record candidate runner creation and termination, and deliberately return measured values different from catalog benchGate thresholds. The complete prefetched map, verifiedExisting branch, real engine, real full JSON document serializer, persisted result readback, signed row validator and adoption consumer all run. Downloader spies fail on every attempted use after prepare. HMAC, logs, config, artifact store, admission namespace and adoption locks use test-owned temporary roots; no operator state or external service calls.

Preparation tests use existing injected HTTP transport for metadata/transfer cancellation, partial/corrupt bytes and authoritative feed replacement on second fetch. A no-op production boundary observer permits deterministic cancellation immediately before copy, within copy checks, before publish intent, after atomic rename and before terminal write. It may signal a test subprocess to exit to verify owner-loss reconciliation at actual persisted boundaries; it does not change production persistence order. Existing canonical hash loop observes Task cancellation; tests coordinate large-file hash cancellation or a defaulted cancellation observer if needed without skipping byte verification. Cleanup/space failure injection tests production error handling without chmod against operator data or filling real disk.

Lifecycle adapter fixture records drain/restore/dismiss order and uses a real child fixture process. Cancel/timeout/pressure tests at probe start/middle/result require stopped child, restored lifecycle, unchanged incumbent config and no actionable result before commit; after publication/result commit terminal truth is preserved. Existing actual child parent-death test continues to test the production no-join guard, not just a mock.

Test labels must distinguish deterministic fixture composition from actual MLX/physical paid qualification. No prewritten eligible recommendation substitutes for the owner producer. Run existing default-adapter tests and command grammar tests plus full affected Swift suites after changes. Any discovered production behavior change beyond these default-preserving seams reopens this gate.
