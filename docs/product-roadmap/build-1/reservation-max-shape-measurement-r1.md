# Reservation maximum-shape measurement r1

Measurement-only draft for root review before execution. No reservation-v4 runtime implementation, schema change, scheduling change, latency injection into production, or raised operation budget is included. Authorized fallback plan: `reservation-search-progress-addendum-r4.md`, SHA-256 `3bb911a0793f569226c9ca103e3607f9563e864fb688dbb5a57e0594d6071b21`. The smaller index-receipt correction remains the measured baseline.

## Existing evidence and resources

Swift32's unchanged healthy full-capacity fixture passed in 15.368 seconds; the mixed >2,052 history fixture passed in 84.279 seconds. These are results from the root's shared log, not an independent invocation. They do not measure 1,024 maximum-size primary records. The earlier attribution of healthy starvation to primary scans was source inference and is not retained as a measured conclusion.

`df -h .` before fixture creation reported 72 GiB available on the worktree volume. The planned fixture writes 1,024 files of exactly 4,194,304 bytes (4 GiB total), plus bounded origins/index and one saved alternative primary. Require at least 12 GiB available at execution, and fail explicitly if unavailable; never shrink the fixture. Use one test-owned temporary private root, bounded per-record buffers, and teardown that removes only that exact created root. No repo artifacts, secrets, downloads, subprocess servers, process limit changes or sparse zero-filled invalid JSON. JSON whitespace padding uses real space bytes accepted by the production parser.

## Fixture and validity proof

Add only `Tests/macprovider-cliTests/ModelCatalogReservationCapacityMeasurementTests.swift`; do not modify production code or existing tests for this measurement. Reuse the existing public conformance fixture to resolve the same qualified candidate/artifact authority. Initialize an empty store through `initializeRetention()`, retaining its real v3 index and completed migration lineage.

Construct 1,024 distinct sorted valid UUIDs and distinct valid operation-generation UUIDs. Build actual `ModelCatalogTransactionRecord` values with the closed production schema and exact authority tuple. The first 1,023 are `prepare_model` records; the last is `evaluate_model`. Each allocated origin is produced by `ModelTransactionOrigin.allocated` from its own exact initial queued bytes. Every initial queued document is padded with JSON whitespace to exactly 4 MiB, and its origin commits that exact hash.

For the first scenario, current primaries have exactly 2,048 valid, sequential, identity-bound events: queued followed by running events, with `startedAt` set. The last record instead ends in cancelled and remains uncommitted, without sidecars/staging/cleanup. It is a legitimate reclaimable terminal slot; the other 1,023 remain unresolved. Pad every current primary to exactly 4 MiB. Do not add unknown fields, unbounded strings, duplicate keys, invalid enum values or impossible event sequences. Validate each exact padded current document through `decodeRetentionRecord`, not merely JSONDecoder or in-memory construction. Publish fixture files through the private descriptor-relative writer, create exact origins, and populate the already-initialized index with their real hashes and allocated provenance. This direct fixture construction avoids 1,024 increasingly expensive reservation scans; it supplies no runtime mutation bypass to the measured calls.

The fixture setup has a 600-second monotonic ceiling checked between records; exceeding it fails and cleans up rather than silently reducing shape. Record setup time, exact record/event counts, primary bytes, index bytes, and representative decode timing. A single near-limit record must capture and validate within eight seconds to qualify the structural search experiment; otherwise report that distinct per-record feasibility failure. All 1,024 exact documents still receive production closed validation during setup.

After publication, capture the final record using `captureActiveReceipt`; verify actual origin/index provenance and validate its receipt under the real global lock. Capture and validate the existing retirement proof for its exact cancelled state under UUID custody. No certificate or primary mutation is performed by this proof. Pin baseline index/cursor metadata and hashes of the terminal primary/origin; baseline `maintenance.json` must be absent because setup used no reservation loop.

## Calls, observations and exact assertions

1. Run three fresh `reserveOperation(authority:kind: "evaluate_model")` calls against the same 1,024-entry state, using the unchanged default eight-second budget. Each call gets fresh operation-local receipts. No artificial delays, clock substitution, byte-limit override, or extra helper budget is supplied in the primary experiment.
2. Observe only existing `retentionBoundary` labels. Count `reservation_scan_started`, `reservation_scan_complete`, `retirement_captured`, `retention_cursor_published`, `index_decode`, and `bulk_read` callbacks. The first four establish phase progress directly. A `bulk_read` callback precedes each at-most-65,536-byte read, including EOF; label its count honestly as read attempts, not exact bytes successfully returned or decoder calls. For these uniformly 4-MiB scan-only records, 65 successful callbacks correspond to a full capture including EOF; report that arithmetic only as an estimate and report exact fixture bytes separately. Do not invent an observed per-primary decode counter absent a production seam.
3. Time each call with monotonic uptime. Expected structural-starvation evidence is typed `busy`, one scan start, zero scan completion, zero retirement/cursor publications, one initial index decode, and unchanged active index/cursor metadata after each bounded call. This demonstrates repeated prefix work and no durable maintenance progress within these calls. Do not accept `unsafe`, malformed-record errors, unexpected publication failures or a missing fixture record as starvation evidence. A completed allocation or completed scan is a measurement that falsifies the expected natural-storage starvation case; report it honestly rather than add delays to rescue that conclusion.
4. Preserve the 1,024-entry index and exact reclaimable proof after the three calls. Compare pinned directory/file metadata for every primary and origin to prove none changed; recheck full digest/receipt evidence for the last terminal slot. No primary, origin, certificate or pointer is deleted or modified by measurement.
5. In the same fixture, replace only the last terminal primary with its saved, exact original queued bytes using the explicit fixture writer. Its existing origin already commits those bytes; do not rewrite the origin or index. Capture/validate the queued receipt and all fresh-reuse predicates using production helpers. This creates a plausible matching queued candidate last, at the same full 4-GiB primary size, with the only necessary event-count difference (a reusable queued record has one event). Run three fresh calls with identical observations. Expected result is busy before reaching it, never a duplicate allocation. Again, successful reuse is contrary evidence, not a test excuse.
6. At the end, validate all primary/origin metadata against their scenario baselines, index bytes and membership, and absence of new `.retired`/result/pointer/staging authority. The last queued receipt remains valid. Persist the actual command, exact outcomes and counters, elapsed times, setup bytes/validation, resource check, and full result in the measurement document.

A controlled cooperative latency profile from approved R4 is a separate follow-up only if the natural-storage experiment does not reproduce prefix starvation. It must be explicitly reported as controlled, retain per-record feasibility and the same full fixture, and cannot replace or rewrite the natural-storage outcome. This draft does not execute that profile.

## Stop and handoff

Root reviews this method before the new test is executed. Run only the new measurement class under a coordinated source freeze; no independent SwiftPM invocation by this lane. The healthy passing fixture remains unchanged. An observed maximum-shape starvation result supports considering the already-reviewed structural fallback; it does not authorize silently implementing v4 or establish acceptance of that fallback. A non-reproducing result is retained and the leader decides the next measurement. Test/source completion and runtime completion are separate claims.
