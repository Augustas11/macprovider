# Build 1 preparation v22 cleanup and publication reconciliation

Status: proposed; **implementation remains blocked pending independent zero-Critical/High/Medium plan and test review**. This is an addendum to v20 and v21, not implementation or network authority. The reviewed baselines are [v20 plan](reservation-rebaseline-plan-v20.md) (`8045eccb1660e98a8213bbcf41b5a0b26f72970324a7f92bcf3afca152addf57`), [v20 tests](reservation-rebaseline-test-spec-v20.md) (`f218249c00586c829a8364971ea3fa2382c789e2f7c78b019c60741a96794c0f`), [v21 plan](reservation-rebaseline-plan-v21-staging.md) (`1818dc488d10db91d26c6579405e0151f5f6a6a83f27ed5b2fd14ca37bbeeec9`), and [v21 tests](reservation-rebaseline-test-spec-v21-staging.md) (`c31af116b5896914d5d4ed204336dba1b3ebf933128fc3e8c7a9ddadb07a6622`). All their other safety, lock, byte-budget, cancellation, UI, migration, and qualification gates remain in force.

## Authority and explicit overrides

Landed SPEC-044 v0.2.8 defines `artifact_identity_digest` as a domain-separated, length-prefixed SHA-256 over display model ID, model revision, artifact ID, release ID, root identity digest, and **publication receipt SHA-256**, and mandates that this digest is the managed published-object leaf (R002). It also mandates an exact attempt-owned staging cleanup target, bounded object-count admission before network/staging, and reversible same-parent cleanup with durable readback-validated `tombstoned` as sole cleanup commit evidence. These already govern this correction. The private record/index spelling proposed here is an implementation contract; if operator review finds any normative conflict or requires private spelling to be normative, amend and govern SPEC-044 **before** changing the codec, rerun this plan gate, and do not invent authority in code. No SPEC or production-network change is authorized by this document.

This v22 text supersedes v20's `objects/<tuple-sha256>` layout, its publication/count/idempotence statements to the extent they imply an artifact-identity digest exists before a receipt, its five-kind private-state inventory and corresponding temp cap, and any v20 deletion example that derives a published leaf from `tuple_sha256`. It supersedes v21's published `objects/<tuple_sha256>` path and its assertion that **all** receipt/artifact-digest checks apply to staging. Both v20 and v21's attempt-owned staging parent, cancellation, root/ACL, atomic envelope, byte budget, and fail-closed principles remain. `tuple_sha256` is a separate recomputed private tuple/reservation binding; it is never the published leaf or a substitute for the SPEC digest. The current unshipped storage draft at `phase3-binary/Sources/macprovider-cli/ModelPreparationContracts.swift` has `model_catalog_cleanup_record.v1`, a required receipt on both target kinds, and staging leaves derived from cleanup `attempt_id`; these are implementation defects, not precedent. No existing production writer is presumed from this draft.

## Distinct closed cleanup records and exact paths

The proposed private `model_catalog_cleanup_record.v2` has one shared closed portion: `schema`, `target_kind`, `phase`, cleanup `transaction_id`, cleanup `attempt_id`, `event_model_key`, complete saved `root` locator/identity, `tuple`, recomputed `tuple_sha256`, root-relative `final_leaf` and `tombstone_leaf`, and checked exact logical `expected_bytes` and `expected_files`. Its two closed variants have **different required keys**; missing, extra, explicit null for an absent-branch key, or cross-kind reinterpretation is invalid. Both retain the existing bounded private envelope, generation/readback, descriptor, ACL, single-root, record-cap, and phase rules. `final_leaf` and `tombstone_leaf` retain their historical field names but hold canonical root-relative paths, not arbitrary filesystem paths.

| `target_kind` | Additional required fields | Exact relative final and tombstone paths |
|---|---|---|
| `staging` | `source_transaction_id`, `source_attempt_id`, `source_record_sha256`; **no** `receipt`, `receipt_sha256`, or `artifact_identity_digest` keys | `work/staging/<source_transaction_id>/<source_attempt_id>` and `work/staging/<source_transaction_id>/.tombstone-<cleanup-transaction_id>` |
| `published` | complete verified `receipt`, recomputed `receipt_sha256`, recomputed `artifact_identity_digest`; **no** source pair or source-record digest keys | `objects/<artifact_identity_digest>` and `objects/.tombstone-<cleanup-transaction_id>` |

All IDs are canonical lowercase UUIDv4. The cleanup pair identifies the cleanup operation; the source pair identifies the earlier interrupted preparation. Neither pair is derived from the other, even when bytes coincide. Resolve both leaves as immediate children of the **same opened, identity-validated parent descriptor**, using exact component bytes and no symlinks, hard links, ACLs, mount crossings, aliases, or traversal. The tombstone name is reserved exclusively and cannot collide with any live intent; retries use the same record/leaf and cannot create a second tombstone. A published receipt must bind the saved tuple, immutable historical event key and root; independently recompute receipt SHA and SPEC's artifact identity digest from exact validated bytes, compare to recorded digest and opened published leaf, and validate immutable inventory/action/keep-set bindings before a destructive operation. An incomplete staging attempt has no publication receipt and cannot be made to pass published validation by fabricated/null receipt fields. Its action's public v2 `artifact_identity_digest` stays null.

## Durable staging source provenance and discovery

Before the first **transfer or staged artifact payload byte**, the preparation worker under `operation.lock` atomically creates the owner-only attempt parent and a closed `model_catalog_staging_source.v1` file at exactly `work/staging/<source_transaction_id>/<source_attempt_id>/source.json`. The descriptor-relative file is created unpublished with empty ACL before its first sensitive byte; it is fully synced, parent-full-synced, reopened and readback-validated before work. Its closed fields are `schema`, `source_transaction_id`, `source_attempt_id`, saved complete `root`, complete immutable `tuple`, recomputed `tuple_sha256`, immutable `event_model_key`, `projection_binding_sha256`, and exact root-relative `staging_leaf`. It carries no receipt, publication digest, URL, credential, feed body, or claim of current download/admission authority. The source pair/root/tuple/event/projection values must match the already durable source preparation reservation and active attempt; the source record's exact serialized bytes have a SHA-256 recorded below. A crash before this complete barrier cannot have produced staged artifact bytes. The source record itself counts as logical staging metadata when measuring exact cleanup totals.

Add one private, bounded `staging-sources.json` registry at the authority-state root, with a closed `model_catalog_staging_sources.v1` payload (`schema`, saved `root`, sorted unique `entries`). Each entry has exactly the source pair, `root_identity_digest`, and `source_record_sha256`, and points only to that pair's exact relative file. Maximum 256 entries and a 131072-byte inner payload; read at most 257 entries to detect overflow and fail closed without truncation or a cleanup action. Publish its generation-checked, full-synced, readback-validated envelope after the source file and before the first transfer/staged payload byte. Never invent an entry by scanning `work/staging`, a current config, current active attempt, or a compacted terminal history. Crash between the source-file barrier and registry barrier is pre-payload: recover only through the still-durable exact active attempt, finish the indexed publication if valid, or preserve the empty source attempt for out-of-band diagnosis. Missing, inconsistent, full, overfull, or malformed registry blocks new staging before network and all affected cleanup projections; unrelated serving remains intact.

This is an explicit **sixth** private envelope kind `staging_sources` alongside v20's `reservations`, `active`, `cancel`, `published_inventory`, and `deletion`; its target leaf is exactly `staging-sources.json`. Enforce target/kind/generation/payload-cap agreement, the existing 360000-byte envelope cap, and the identical temp-write/readback discipline. Revise the `state-tmp` recovery cap from 16 to 24 recognized entries total, still at most four per kind; unknown kinds, sixth-kind temp collisions, surplus entries, or checksum-valid semantic mismatches fail closed under the same deterministic recovery rules. Do not stuff registry entries into `reservations.json`, `cancel.json`, or published inventory. **Separate blocker outside this cleanup correction:** the independently reported `reservations.json`/`cancel.json` payload-shape ambiguity still needs its own exact multi-entry reservation and cancellation-ack/marker contract review; v22 does not resolve it or assume either target contains one arbitrary record. The source file is not an extra replaceable authority-state envelope target: it is an immutable, per-attempt, descriptor-validated provenance record.

Only the validated registry's at-most-256 exact pairs may be inspected at projection, with bounded per-pair record and tree walks. A staging cleanup action is available only for one incomplete, non-live, unprotected, exact pair whose source record hash, saved root, tuple, event key, and path all validate. The private projected-transaction reservation must move to a closed branch-aware version (proposed `model_catalog_reservation.v4`): for `cleanup_staging`, add the exact source pair and `source_record_sha256`, all included in `projection_binding_sha256`; for every other kind these keys are absent. The public action/event schemas remain unchanged. Direct dispatch validates that immutable reservation plus the source file and registry under the existing lock protocol; worker and restart recovery reopen **only the saved root**, compare descriptor identity, registry generation/entry, exact source file bytes/hash, source tuple/event/projection binding, and the cleanup record before intent, rename, restoration, or tombstone removal. Terminal/history compaction may never remove the registry entry or source record; cancellation before the cleanup commit restores/preserves both. After durable `removed` and full-synced exact target absence, **while the deletion record still exists**, retire only its exact index entry with a full-synced/readback-validated registry generation, then durably clear the deletion record and commit terminal state. A crash between those barriers leaves `removed` as authority: recovery verifies saved root, exact paths and absence, the recorded source hash/tuple/event key, and that this exact index entry is either still present for retirement or already absent, then resumes clear without scanning or touching another tree. It does not require a source file already removed with the tombstone. A missing entry during `intent` or `tombstoned` fails closed; a failed retirement keeps `removed` for retry and never emits success. Successful preparation may retire an entry only after verified publication and proof the corresponding staging tree is absent; interrupted staging retains it indefinitely until explicit cleanup or operator diagnosis.

## Count admission and publication without a speculative receipt

At pre-network dispatch under the common `operation.lock`/cleanup serialization, bound the verified inventory to 256 with one extra entry only for overflow detection and apply the already required logical budget/free-space/verified signed artifact gates. At count 256, use an existing object **only if** its separately opened, verified prior receipt and signed tuple/root/artifact bindings establish the exact previously published artifact identity and valid leaf; return that object idempotently without network/staging. Same displayed name, tuple SHA alone, or predicted receipt digest is insufficient. If no such exact proof exists, refuse before any network or staging side effect. At count <=255, a prospective **distinct** publication reserves one count slot in durable active state while holding the common operation lock; this reserves capacity, not an unknown artifact leaf/digest. An existing verified identity is idempotent and takes no new slot. At publication, recheck feed/guidance/root/cancel/physical space/logical budget and inventory under the retained locks, construct and durably verify the final receipt from completed verified bytes, compute `receipt_sha256`, then compute SPEC's `artifact_identity_digest`, choose only `objects/<artifact_identity_digest>`, and exclusively rename the exact unpublished tree. If the final digest resolves to a previously verified identical object, prove byte-for-byte receipt and tuple equivalence and use the existing object without overwrite; any discrepancy fails closed. A new object may become only the 256th; an externally consumed slot, malformed inventory, conflicting final leaf, or 257th observation fails before rename. Preserve the v20 bottom-up file/parent full-sync, publish-ready, exclusive rename, destination-parent full-sync, terminal and crash-recovery barriers. A physical post-crash published object is accepted only by verifying its receipt and SPEC digest against the opened leaf; no tuple-named fallback or automatic repair.

## Delivery and stop condition

First independently review this plan and the [v22 test addendum](reservation-rebaseline-test-spec-v22-cleanup-reconciliation.md) against the landed SPEC and code draft to zero Critical/High/Medium. If a SPEC clarification is needed, govern it and re-review before any codec edit. Next amend the private codec and focused regression fixtures, then write registry/source persistence, projection/dispatch, worker/recovery, and count/publication code in that dependency order. A draft v1 cleanup record is **never** decoded as v2, guessed from current active IDs, or auto-migrated/deleted. On encountering one, preserve source/published/incumbent/legacy bytes and report a bounded failure; a supervised offline migration may proceed only with independently validated original source attempt provenance or a verified published receipt and exact root, otherwise leave it quarantined for diagnosis. Rollback to a v1-only executable must reject v2/registry state and preserve trees without altering them; re-upgrade validates the same bytes. No migration of legacy or arbitrary orphan trees is allowed. All v20/v21 broader gates, physical qualification, and complete-diff independent code/security/architecture review at zero Critical/High/Medium remain outstanding. This plan is ready for implementation only when review, exact codec governance, and companion tests pass; it does not claim shipped CLI, network admission, payout, or release acceptance.
