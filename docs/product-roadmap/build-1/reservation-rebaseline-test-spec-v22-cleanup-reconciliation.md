# Build 1 preparation v22 cleanup and publication test addendum

Status: proposed in PR #1487; no implementation/physical acceptance credit until the exact in-branch plan, test specification, SPEC authority, and complete PR diff are independently reviewed to zero Critical/High/Medium. Execute all v20 and v21 tests except the following expectations, which this correction explicitly supersedes: v20 T01.2's five-kind private envelope inventory; v20 T01.3's complete-newer-temp promotion; v20 T01.3's 16-total-temp cap, replaced by the seven-kind 28-total/four-per-kind cap below; v20 T01.3/T10.3 expectations that `tombstoned` with no final and no tombstone may advance to `removed`; v20 T10.1's tuple-named published object leaf, replaced by the receipt-derived `artifact_identity_digest` leaf below; the narrow configured-root reading of v20 T10.4/T10.5 for indexed staging cleanup; v21's tuple-named published leaf, branch-independent receipt checks, and any staging receipt requirement; and v21's broad statement that it supplements every v20 test where it conflicts with the closed seven-kind, branch-specific v22 contracts below. Under the governed crash rule, a surviving complete validated `tombstoned` final is postcommit and overrides v20 T10.3/T10.5 marker-first restoration; when no such final survives, durable `intent` plus an exact marker retains restoration, including temp-only `tombstoned`; `tombstoned` with both final and tombstone absent fails closed for diagnosis unless the durable phase is already `removed`. The surviving-final crash roll-forward tests exercise PR #1487 in-branch SPEC-044 v0.2.10 authority; the addendum confers no implementation or acceptance credit before its own independent review and merge. Review the exact hashes and authority recorded in the [v22 plan](reservation-rebaseline-plan-v22-cleanup-reconciliation.md); retain all other acceptance and complete-diff audit gates.

## Contract and authority matrix

For v20 R003/T16 admission compatibility, retain the exact twelve closed wire values and reject an unknown thirteenth. In SPEC-047 v0.1.9's v0.2 admission-state rule, inject **synthetic only** `coordinator:offer_rejected` input frames with both a null event ID and a claimed syntactically valid event ID/readback; no actual coordinator append is invented. Test every economics class, exact source SHA, current candidate/event/guidance correlation, all five verbatim guidance fields, signed artifact verification, fitting hardware, `needs_preparation`, positive bounded bytes, and otherwise valid local Prepare prerequisites. Every such frame must project only the SPEC-044 v0.2.10 inconsistent, non-actionable row: `economics_state: unavailable`, `rate_source: none`, both admission authorization booleans false, all nullable rate-card identity/rate/share/payout/demand fields null, and **all actions unavailable**. Mutate each forbidden field or action one at a time and require Malibu to fail closed with generic unsupported/action-unavailable copy. At projection, dispatch, and immediate prepublication revalidation, require no `prepare_model`, transfer, staging byte, authoritative rejection, paid-admission/trust/pricing/earning copy, or synthesized coordinator event. Do not coerce the value to `local_default`, `not_offered`, or a different coordinator state. Separately retain positive `local_default:not_offered`, exact no-event coordinator `not_offered`, and event-backed coordinator `not_offered` under their distinct guidance/copy and local preparation gates. All other v20 T16 matrix, economics, localization, accessibility, and negative fixtures remain required; only its positive `coordinator:offer_rejected` Prepare expectation is superseded.

For v20 T18 and `B1-AUTH-V6-M1`, replace only the version/commit-specific selector. The PR #1487 branch acceptance target is SPEC-044 v0.2.10 SHA-256 `5333dbd2a4c86de0bda2832703cf4162b47a6583c3b391dd652a57c441e2dbd7`, plus the matching AUTHORITY, CONFORMANCE, generated index, SPEC-001 consumer mapping, and exact plan/test hashes recorded for this addendum after each rebase. Every v20 clause that says forward-current SPEC-044 v0.2.8, exact landed merge `c8c97f6625a88fa7c83ae2b5cf4d68078409cc6f`, or v0.2.8 source/index/digest agreement is satisfied only by the corresponding in-branch v0.2.10 authority while PR #1487 is under review. The `c8c97f66` merge remains historical review evidence and an ancestor check input, not the current authority target or the first-6B base after this patch. All non-version v20 T18 requirements remain mandatory: exact grammar, fallback behavior, admission/copy matrix, failed-dispatch and cancellation contracts, lock graph, cleanup/accounting/root/ACL/ranking/transport guarantees, compatibility, review lineage, governance output, physical qualification, and zero-Critical/High/Medium independent review. A later 6B implementation must prove the merged v0.2.10 authority commit is a strict ancestor of the first implementation commit and must fail if it is only present on an unmerged branch.

1. Assert the PR #1487 in-branch SPEC-044 length-prefixed artifact-identity digest formula against independent known vectors, including field boundaries, UTF-8 byte lengths, no normalization, raw root/receipt lowercase-hex inputs, and changed receipt bytes. Published leaf must equal this digest, never `tuple_sha256`; require an explicit governed SPEC patch before codec implementation if any chosen private shape contradicts SPEC. Verify private v1 implementation remains unshipped draft and give no production-migration credit on that assumption alone.
2. Round-trip the two exact closed `model_catalog_cleanup_record.v2` branch shapes and the branch-aware reservation. Staging has source pair and source-record SHA and lacks all three publication receipt/digest keys; published has the complete receipt/SHA/artifact digest and lacks all source keys. Vary one field/key independently: omitted/null/extra/duplicate, wrong schema/target kind/phase, upper/malformed/version-wrong UUID or SHA, changed tuple/event/root/projection binding, swapped branch fields, published object with staging fields, staging object with fabricated receipt, valid checksum around a semantically bad payload. Require the literal `model_catalog_reservation.v4` schema tag for every v4 reservation: a v3 tag with v4 keys, a v4 tag with v3 keys, and either decoder applied to the other version reject without migration or fallback. No invalid record produces an action, rename, restoration, deletion, or success.
3. Check exact canonical paths and opened same-parent descriptors: `work/staging/<source_tx>/<source_attempt>` to `work/staging/<source_tx>/.tombstone-<cleanup_tx>`, and `objects/<artifact_identity_digest>` to `objects/.tombstone-<cleanup_tx>`. Source and cleanup pairs differ; accidental equality cannot become derivation authority. Mutate one source UUID, cleanup UUID, path component, root digest, parent, tombstone prefix, casing, percent/Unicode alias, dot/traversal component, symlink, hard link, mount, ACL, owner, device or inode. Test old `<cleanup_attempt>.staging`, `.staging.tombstone`, tuple-named published leaf, and old `artifact.tombstone` spelling as rejects. Both leaves present always fails; both absent fails in `intent`/`tombstoned` and is required in `removed`; a preexisting reserved tombstone outside the exact recorded phase fails closed.

## Durable staging provenance and bounded projection

4. Create a preparation with durable source reservation and active identity, but intercept every boundary: source parent create/ACL strip, source file write, file sync/readback, parent full-sync, registry temp write/full-sync, rename, parent full-sync/readback, first network byte and first staged artifact payload byte. Neither payload nor transfer may precede complete source **and** index barriers. Before index completion, recovery may finish only the active attempt's exact empty source registration or preserve it for diagnosis; it may never infer a cleanup source or claim cleanup success. The source file contains exact closed source pair/root/tuple/tuple SHA/event key/projection binding/path, no receipt/digest or sensitive feed/credential/URL.
5. Exercise the exact `staging_sources`/`staging-sources.json` closed schema: only `schema` and sorted unique `entries`; each entry has source pair, complete saved root locator, and source-record SHA, with no global root. Prepare incomplete attempts on independently bound A and B roots, restart with C configured, and require each action to reopen only its entry's saved root. Treat v20 T10.4/T10.5 “different-root” rejection as a mismatch with that action's authenticated saved-source/root chain, not mere inequality to post-crash config C; verify SPEC-044 saved-root recovery authority. Published cleanup under C retains its different-root prohibition and cannot borrow staging's A/B exception. Mutate/swaps of path, device, inode, identity version/digest, pair, or source SHA across A/B fail before action; equal pair on two roots is a conflict. Enforce 256-entry ceiling, 262144-byte inner and 360000-byte envelope caps independently. With long but otherwise valid escaped root paths, make the byte cap fail at fewer than 256 entries and prove refusal before the first network/staged payload byte, with no truncation or current-root fallback. Inject malformed entry, wrong key/null/duplicate, 257th entry, source mismatch, and missing index; fail closed without affecting either incumbent.
6. Create 256 small-root, size-valid indexed incomplete attempts plus a neighboring unindexed directory and compacted/evicted terminal history; project only exact validated indexed, non-live, unprotected source candidates. For each projected `cleanup_staging` action, verify public artifact digest is null and private v4 reservation's source pair/hash binds the immutable projection digest. Change source bytes, entry/root, tuple, event key, source ID, action ID, reservation digest, or protection/liveness after projection and before dispatch, before intent, before rename, and at restart: no target mutation. The unindexed neighbor remains unreachable, not auto-deleted. Compact terminal/history/pending repeatedly and prove source record and registry retention; after precommit cancel prove both remain. Under `intent`, final-only loads final/source; a post-rename crash with tombstone-only loads tombstone/source and can restore on exact marker. Both present or both absent reject. Under `tombstoned`, final is absent and tombstone/source is required during contents removal; source is unlinked last. Inject a crash just before/after source unlink: source absence is accepted only with exact durable `tombstoned`, empty tombstone, saved deletion proof, and no remaining work. Any missing source with content rejects. Under `removed`, both leaves/source are absent; recovery uses saved deletion record only, retires at most the matching entry, then clears the record. Crash before/during/after retirement and clear; missing entry in `intent`/`tombstoned` rejects, while an already-retired exact entry under `removed` resumes. Change current config/active attempt to C at every phase; neither may select the root. Repeat after historical source reservation/active/terminal compaction: source and index remain sufficient only because their equality to the original pair/root was durably proved before payload. Change the saved-root path, device, inode, version/digest, source/index/reservation/deletion binding, or opened descriptor independently; reject every mismatch, including a published different-root target, before rename/unlink while preserving A/B/C sentinels.

## Private envelope, pending history, and marker

7. Assert exactly seven `(kind, leaf, cap)` pairs: `reservations`/`reservations.json`/262144, `active`/`active.json`/65536, `cancel`/`cancel.json`/4096, `published_inventory`/`published-inventory.json`/262144, `deletion`/`deletion.json`/32768, `staging_sources`/`staging-sources.json`/262144, and `failed_dispatch_pending`/`failed-dispatch-pending.json`/16384. Every outer envelope is at most 360000 bytes. Inject target/kind mismatch, raw payload at target, empty/truncated temp, stale/equal/newer generation, wrong UUID, checksum-valid semantic mismatch, unknown kind, wrong filename/type, 29 recognized temps, and five temps of one kind; only 28 total/four per kind are decoded for hostile semantic checks, never promoted from temp. Recovery streams at most 29 names plus one EOF/non-EOF probe per invocation with bounded memory/descriptors, without `listDirectory().sorted()` or a global lexical-minimum promise. No temp promotes even after EOF: only the durable final or an independently authorized fresh writer can establish state. EOF and safe-sibling checks gate bounded retirement; a non-EOF probe suppresses reconstruction from the prefix. For exact recognized excess temp names, including a fifth complete interrupted temp, derive only kind/UUID from the name, never decode or promote payload, revalidate the same-parent descriptor, owner, ACL, device, inode, type and name, and unlink the excess entry only; durable targets remain byte-identical. With 29 recognized names and a hostile unknown 30th, assert the 29th may be safely removed but no state is reconstructed from the prefix; retry must eventually encounter and refuse the hostile sibling. Repeat recovery to drain provably safe excess; after a fresh EOF-proven scan, independently rebuild only what final state and durable witnesses authorize. Unknown names, unsafe nodes and descriptor drift fail closed. Flood with more than 10000 unexpected siblings and assert bounded memory, descriptors and per-invocation examination/time; prove a hostile excess cannot be unlinked or promoted. No accepted pending record is hidden inside reservations, no source entry inside cancel, and no temp can cross target kinds.

For **each of the seven kinds**, crash after empty/partial write, complete write, file sync, full-sync and readback but **before rename**, then after rename and before each parent barrier/readback. A complete newer-generation temp with valid checksum is never itself promoted. An absent final remains absent until a fresh authorized writer reconstructs from independent durable evidence; a present valid final wins over even a newer temp. After process crash and separately after power loss, a surviving complete identity-valid final at its canonical leaf is reopened, exact envelope/payload/generation/caps and independent bindings are checked, and missing parent `fsync`/`F_FULLFSYNC` plus final readback are completed under the applicable locks before use; absent, torn or mismatched final cannot be inferred from a prior rename. Freeze each crash point before/after rename, parent fsync, parent full-sync and readback. Use the following witness and negative cases:

| Kind | Pre-rename complete-temp crash assertion |
|---|---|
| `reservations` | No action or terminal from temp; preserve final snapshot and independently durable pending/history, then only a freshly qualified projection may write. |
| `active` | No live attachment, worker event, network or staging from temp; a new dispatch alone may attach. |
| `cancel` | No marker and no `recorded` acknowledgement from temp; direct cancel must re-evaluate its predicate under failure→cancel. |
| `published_inventory` | No object count/identity from temp; only an independently verified receipt, digest leaf and saved root can support fresh inventory, never deletion or guessed publication. |
| `deletion` | No intent/phase from temp alone. With durable `intent`, a complete `tombstoned` temp plus renamed tombstone remains precommit: exact durable marker restores the final; without marker worker independently repeats barriers and commits phase. |
| `staging_sources` | No indexed source or cleanup action from temp; only durable matching active/source evidence may finish registration before payload, otherwise preserve empty source for diagnosis. |
| `failed_dispatch_pending` | No failed-dispatch terminal/event from temp; only durable pending/final history may satisfy cancel `terminal` or be copied before unlink. |

For mixed kinds, interleave a failure-only pending writer and direct cancel around startup. Record deterministic lock traces: operation→failure→cancel for all cancel-visible final-state reconciliation, reverse release of failure/cancel, operation→one exact cleanup→cancel for deletion with repeated phase/marker/root/target validation, then reverse release and operation→failure→cancel for terminal compaction. Reject operation-only recovery of cancel-visible state, cleanup+failure overlap, a stale first-scope marker view, and any temp-derived terminal/marker/commit. Crash at each transition and repeat with a valid final plus a conflicting newer complete temp, checksum-valid semantic mismatch, or hostile sibling.
8. Round-trip `model_catalog_reservations_history.v1` with exactly schema, at most 64 sorted unique branch-aware v4 `projected_reservations`, and oldest-to-newest at most 256 `terminal_history` entries. Exercise exact closed ordinary `model_catalog_terminal_history.v1` fields, state/error nullability, positive final event sequence and root/event/tuple bindings; separately exercise the unchanged exact SPEC-044 `model_catalog_failed_dispatch.v1` pending record at its own target, at most 16384 bytes, one pending only, no wrapper. Unknown/missing/extra/duplicate fields, raw pending array, wrong root, diverging same-ID history/pending, and over-cap entries reject. A checksum-valid permutation of nested history has no independent order witness in the exact SPEC-044 failed-dispatch shape: test writer append and oldest-first eligible eviction by an observed operation→failure→cancel trace, but do not assert that a decoder detects an arbitrary permutation. Count distinct pending/history terminal identities once across the global 256-record/262144-canonical-byte limit; test 255/256/257, exactly-at/one-byte-over, byte-identical crash duplicate, projection entries inflating snapshot past 262144, and projection-time ordered eviction of oldest eligible history while preserving independently durable pending, the active/current correlation, and eight newest terminal entries. At 64 projected actions and near-count/byte-cap histories, an operation→failure→cancel projection evicts the minimum oldest eligible entries to fit both the 262144-byte snapshot and global 256-record/262144-canonical-byte caps, then publishes the action. If those protected records alone exceed a cap, projection fails closed without changing serving state, pending, current or recent terminal records; a later smaller projection may succeed. Inject crash and direct-cancel reads at replacement-history temp/sync/rename/parent/readback, new-pending temp/sync/rename/parent/readback, later compaction snapshot and pending unlink. A prior pending is copied before unlink; a new pending is not claimed terminal before its live writer readback or a separately governed, fully validated surviving-final crash roll-forward; duplicate recovery is idempotent; no durable terminal vanishes from both places and no interrupted process fabricates stdout. For a new complete final `failed-dispatch-pending.json` surviving crash after rename but before either parent barrier/readback, race direct cancel first against startup first. In both process-crash and power-loss fixtures, direct cancel under failure→cancel and startup under operation→failure→cancel validate the exact final identity, kind/leaf, generation, checksum, attempt/root/tuple/event/projection fields, 16384 inner/360000 outer and shared 256-record/262144-byte caps, finish pending-parent fsync and F_FULLFSYNC, reopen/readback the final, and only then allow `terminal` or compaction. Inject failure at each validation/barrier/readback: direct cancel exits 5 with no acknowledgement or marker, startup leaves recovery unavailable without compaction, and neither emits an event or mutates history. If only a temp or no final survives, there is no new pending terminal; any other cancellation outcome comes only from independently durable state. A valid old final surviving loss remains only its own identity; a new intended temp cannot overwrite its outcome. Replace or evict the original projected reservation before direct-cancel/startup recovery: a matching retained reservation must corroborate the pending, but its legitimate absence after churn cannot erase the pending terminal; mismatching retained reservation fails closed. No recovery emits sequence-1 failed stdout.
9. `cancel.json` contains only the closed `model_catalog_cancel_marker.v1` transaction/attempt/kind/event-key/root/tuple/projection-binding/RFC3339-recorded-at record, or is absent; it never contains `model_catalog_transaction_cancel_ack.v1`. Independently round-trip the exact public stdout acknowledgement with its outcome and observed-at, including six outcomes, nullability, echo, timeout/busy, and terminal precedence from SPEC-044. Attempt marker-as-ack and ack-as-marker substitution; both reject. Freeze marker `recorded_at` to the canonical 20-byte UTC second-precision `YYYY-MM-DDTHH:MM:SSZ` format. With the longest admitted complete root locator and event key containing worst-case JSON escapes, measure canonical prospective marker bytes using actual fields and a fixed-width UUIDv4 placeholder; exercise exactly 4096 and 4097 inner bytes, outer-envelope cap, and equal encoded size with an actual UUID/time. An oversized marker makes the action unavailable at projection before first staging/network byte; recheck actual attempt/time before any active/live state or marker and return SPEC stale precedence or typed action-unavailable through non-live failed dispatch. Do not truncate root/event; an admitted action remains cancellable. Public ack retains its separate exact 4096-byte cap. Under failure→cancel, repeated exact cancel yields `already_recorded`, durable failed dispatch pending/history yields `terminal` with no marker, stale prior marker is removed only by the exact permitted process, and the cancel process emits no event or cleanup/tree/history mutation. Crash before marker temp/sync/rename/parent/readback cannot claim `recorded`; after durable marker readback the acknowledgement may say `recorded` but never worker-observed success. The mandatory new pending-final direct-cancel barrier holds failure→cancel and does not repair history, deletion, root, inventory, or source state. Under the governed read-side rule, direct cancel barrier-completes and readback-validates every surviving final active, ordinary history, pending, projected reservation and exact marker used for its predicate while retaining failure→cancel; barrier failure or contradictory identity exits 5 without acknowledgement. It never barrier-completes a cleanup object or phase, repairs contents, or compacts history. Never infer `already_recorded`, `recorded`, or `terminal` from an unverified namespace entry.

## Count, receipt, and publication

10. At counts 0, 254, 255, 256, and an observed 257th entry, test valid new identity, a verified exact existing receipt/leaf, same tuple with altered receipt, same display name with different revision/artifact/release/root, stale/missing/corrupt receipt, conflicting leaf, and externally consumed slot. At 256, only independently verified exact prior identity succeeds idempotently without transfer/staging; every ambiguous or distinct case refuses before either side effect. At <=255, assert one durable count reservation under the operation/cleanup serialization without any guessed receipt SHA or object leaf. At publication derive receipt SHA **after** verified bytes/receipt durability, recompute SPEC digest, recheck count/budget/free space/feed/root/cancel, exclusively rename only to that leaf and never create object 257. A matching preexisting leaf needs byte-for-byte receipt and tuple proof; mismatch fails closed, no overwrite. A crash after rename validates receipt against actual digest leaf and exact root, never falls back to tuple leaf.
11. Inject cancellation, SIGKILL and readback/sync failures at source-index publication, intent, final marker check, exclusive staging/published rename, shared-parent `fsync`/`F_FULLFSYNC`, `tombstoned` phase write/sync/readback, restoration, tombstone removal, `removed` sync/readback, index retirement, and normal terminal/history compaction. With durable `intent`, an exact marker, and no surviving complete identity-valid `tombstoned` final, the owning recovery worker restores or retains exact target and returns cancelled; with durable `tombstoned`, it finishes or keeps recoverable `cleanup_failed`, never cancelled. Direct cancel only records/reads its exact marker under failure→cancel and never changes source index, cleanup phase, tree, or history. Assert startup first holds operation→failure→cancel for cancel-visible target/marker/pending/history reconciliation, then releases cancel→failure while retaining operation, acquires the exact cleanup lock→cancel, and revalidates durable deletion phase, marker, root, source/receipt and target before any cleanup mutation. Hold operation→cleanup→cancel continuously through final check/rename/barriers/readback, release cancel→cleanup before reacquiring failure→cancel for terminal compaction, and never overlap cleanup/failure. Race direct cancel and failure-only pending writers at every lock release/reacquisition; a cancelled **durable intent without a surviving tombstoned final** restores under the second scope, and no stale first-scope view may authorize deletion. For both `published` and `staging`, crash after `tombstoned` final rename but before parent fsync, F_FULLFSYNC, and readback, then run cancel-first and startup-first under both process-crash and power-loss survival. When the complete identity-valid final and exact tombstone survive, direct cancel may record a marker but cannot restore; recovery under operation→exact cleanup→cancel validates saved root, source/receipt, target absence/tombstone identity, completes `fsync`/`F_FULLFSYNC` for both the exact tombstone parent and deletion-record parent, readback-validates the same phase and target identity, then resumes postcommit cleanup. Keep live-writer uninterrupted lock/readback requirements unchanged. If only `intent` final plus uncommitted `tombstoned` temp survives, marker-first restoration still wins. If final `tombstoned` survives but target/tombstone is missing, moved, altered, or root/receipt/source mismatches, fail closed without restoration, unlink, `succeeded`, or fabricated stdout. For power loss also vary whether rename/final/tombstone survived independently; accept only a self-consistent witnessed phase, never infer a phase from intent or temp. Crash after legitimate exact tombstone removal but before durable `removed` with both leaves absent and `tombstoned` final still present: fail closed for diagnosis, without inventing `removed` or success, in both cleanup branches. Prove no loss or retargeting across cancel-first/recovery-first races, root replacement, moved root, remount, receipt replacement, index compaction, and repeated restart.

## Migration, release, and gate

12. Feed v1 draft staging/published cleanup records, raw single-reservation and acknowledgement-as-marker drafts, absent v2/source registry, valid seven-kind envelopes, malformed new envelopes, mixed pending/history, and old executable/new durable state through upgrade→rollback→re-upgrade. Draft state is never silently decoded as a closed new payload or migrated; preserve every file and produce bounded diagnosis. A supervised migration fixture succeeds only with independently proven original staging source or exact verified published receipt/root **and** complete pending/history preservation; absent proof leaves bytes quarantined. Rollback rejects unsupported cleanup/reservation/registry/pending/marker formats and never repairs/deletes them. Legacy current/draft, unconfigured legacy, serving incumbent, and unrelated v3 identities retain byte/hash/stat sentinels throughout.
13. Run focused codec, envelope/store, inventory, source/worker/recovery, and projection/dispatch tests; then the relevant Swift package, app adapter, governance, and repo checks. Independent code, security, and architecture lanes review the **complete combined diff** against the current governed SPEC authority to zero Critical/High/Medium. Preserve v20/v21 admission, UI/copy, lock/deadline, logical-byte, APFS, real-hardware and release gates except where this addendum explicitly supersedes versioned storage and cleanup expectations; skipped, zero-selected, timed-out, or simulation-only runs do not count as their acceptance. Record test command/results, exact source hashes, review commit, and any remaining LOW/INFO findings without inventing network or physical evidence.
