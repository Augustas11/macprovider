# Build 1 preparation v22 cleanup and publication test addendum

Status: proposed; no implementation/physical acceptance credit until independently reviewed to zero Critical/High/Medium. Execute all v20 and v21 tests unless this v22 correction explicitly replaces a conflicting expectation. Review the exact hashes and authority recorded in the [v22 plan](reservation-rebaseline-plan-v22-cleanup-reconciliation.md); retain all other acceptance and complete-diff audit gates.

## Contract and authority matrix

1. Assert the landed SPEC-044 length-prefixed artifact-identity digest formula against independent known vectors, including field boundaries, UTF-8 byte lengths, no normalization, raw root/receipt lowercase-hex inputs, and changed receipt bytes. Published leaf must equal this digest, never `tuple_sha256`; require an explicit governed SPEC patch before codec implementation if any chosen private shape contradicts SPEC. Verify private v1 implementation remains unshipped draft and give no production-migration credit on that assumption alone.
2. Round-trip the two exact closed `model_catalog_cleanup_record.v2` branch shapes and the branch-aware reservation. Staging has source pair and source-record SHA and lacks all three publication receipt/digest keys; published has the complete receipt/SHA/artifact digest and lacks all source keys. Vary one field/key independently: omitted/null/extra/duplicate, wrong schema/target kind/phase, upper/malformed/version-wrong UUID or SHA, changed tuple/event/root/projection binding, swapped branch fields, published object with staging fields, staging object with fabricated receipt, valid checksum around a semantically bad payload. No invalid record produces an action, rename, restoration, deletion, or success.
3. Check exact canonical paths and opened same-parent descriptors: `work/staging/<source_tx>/<source_attempt>` to `work/staging/<source_tx>/.tombstone-<cleanup_tx>`, and `objects/<artifact_identity_digest>` to `objects/.tombstone-<cleanup_tx>`. Source and cleanup pairs differ; accidental equality cannot become derivation authority. Mutate one source UUID, cleanup UUID, path component, root digest, parent, tombstone prefix, casing, percent/Unicode alias, dot/traversal component, symlink, hard link, mount, ACL, owner, device or inode. Test old `<cleanup_attempt>.staging`, `.staging.tombstone`, tuple-named published leaf, and old `artifact.tombstone` spelling as rejects. Both present/both absent and preexisting reserved tombstone fail closed.

## Durable staging provenance and bounded projection

4. Create a preparation with durable source reservation and active identity, but intercept every boundary: source parent create/ACL strip, source file write, file sync/readback, parent full-sync, registry temp write/full-sync, rename, parent full-sync/readback, first network byte and first staged artifact payload byte. Neither payload nor transfer may precede complete source **and** index barriers. Before index completion, recovery may finish only the active attempt's exact empty source registration or preserve it for diagnosis; it may never infer a cleanup source or claim cleanup success. The source file contains exact closed source pair/root/tuple/tuple SHA/event key/projection binding/path, no receipt/digest or sensitive feed/credential/URL.
5. Exercise the sixth `staging_sources` envelope kind and exact `staging-sources.json` target, 131072-byte inner cap, 360000-byte outer cap, at most 256 sorted unique entries, one bounded extra entry for overflow detection, at most four temps per kind and 24 total. Inject every target/kind mismatch, duplicate pair, missing/changed source SHA, changed saved root, broken generation/checksum, 257 entries, 25 recognized temps, five of one kind, empty/truncated recognized temp, stale equal/newer generation, unknown kind, unexpected filename/type, and checksum-valid semantic mismatch. A full/malformed/overfull index blocks new staging **before network** and all unsafe staging cleanup projection, without damaging incumbent or legacy files. Keep `reservations.json`/`cancel.json` payload-shape ambiguity as a separately tracked blocker requiring its own multi-entry/acknowledgement proof; no v22 test may pass by treating either as a single arbitrary record or by borrowing cancellation bytes for the registry.
6. Create 256 valid indexed incomplete attempts plus a neighboring unindexed directory and a compacted/evicted terminal history; project only exact validated indexed, non-live, unprotected source candidates. For each projected `cleanup_staging` action, verify public artifact digest is null and private v4 reservation's source pair/hash binds the immutable projection digest. Change source bytes, registry entry, root, tuple, event key, source ID, action ID, reservation digest, or protection/liveness after projection and before dispatch, before intent, before rename, and at restart: no target mutation. The unindexed neighbor remains unreachable, not auto-deleted. Compact terminal/history/pending repeatedly and prove source record and registry retention; after precommit cancel prove the record and entry remain; after successful `removed`, synced target absence and durable record clear prove only that exact entry is removed. Recovery with different current config/active attempt must use saved locator and exact source, not a scan.

## Count, receipt, and publication

7. At counts 0, 254, 255, 256, and an observed 257th entry, test valid new identity, a verified exact existing receipt/leaf, same tuple with altered receipt, same display name with different revision/artifact/release/root, stale/missing/corrupt receipt, conflicting leaf, and externally consumed slot. At 256, only independently verified exact prior identity succeeds idempotently without transfer/staging; every ambiguous or distinct case refuses before either side effect. At <=255, assert one durable count reservation under the operation/cleanup serialization without any guessed receipt SHA or object leaf. At publication derive receipt SHA **after** verified bytes/receipt durability, recompute SPEC digest, recheck count/budget/free space/feed/root/cancel, exclusively rename only to that leaf and never create object 257. A matching preexisting leaf needs byte-for-byte receipt and tuple proof; mismatch fails closed, no overwrite. A crash after rename validates receipt against actual digest leaf and exact root, never falls back to tuple leaf.
8. Inject cancellation, SIGKILL and readback/sync failures at source-index publication, intent, final marker check, exclusive staging/published rename, shared-parent `fsync`/`F_FULLFSYNC`, `tombstoned` phase write/sync/readback, restoration, tombstone removal, `removed` sync/readback, index retirement, and normal terminal/history compaction. With intent and marker before durable `tombstoned`, the owning recovery worker restores or retains exact target and returns cancelled; with durable `tombstoned`, it finishes or keeps recoverable `cleanup_failed`, never cancelled. Direct cancel only records/reads its exact marker under failure→cancel and never changes source index, cleanup phase, tree, or history. Assert continuous operation→cleanup→cancel lock custody through final check/rename/barriers/readback and no cleanup/failure overlap. Prove no loss or retargeting across cancel-first/recovery-first races, root replacement, moved root, remount, receipt replacement, index compaction, and repeated restart.

## Migration, release, and gate

9. Feed v1 draft staging/published cleanup records, absent v2/source registry, valid v2, malformed v2, mixed history, and old executable/new durable state through upgrade→rollback→re-upgrade. Draft v1 is never interpreted as v2 or silently migrated; preserve every file and produce bounded diagnosis. A supervised migration fixture succeeds only with independently proven original staging source or exact verified published receipt/root; absent proof leaves bytes quarantined. Rollback rejects unknown v2/registry and never repairs/deletes it. Legacy current/draft, unconfigured legacy, serving incumbent, and unrelated v3 identities retain byte/hash/stat sentinels throughout.
10. Run focused codec, envelope/store, inventory, source/worker/recovery, and projection/dispatch tests; then the relevant Swift package, app adapter, governance, and repo checks. Independent code, security, and architecture lanes review the **complete combined diff** against the landed SPEC to zero Critical/High/Medium. Preserve v20/v21 admission, UI/copy, lock/deadline, logical-byte, APFS, real-hardware and release gates; skipped, zero-selected, timed-out, or simulation-only runs do not count as their acceptance. Record test command/results, exact source hashes, review commit, and any remaining LOW/INFO findings without inventing network or physical evidence.
