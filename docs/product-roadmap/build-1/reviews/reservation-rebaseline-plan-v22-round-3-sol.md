# Build 1 v22 cleanup plan: independent adversarial round 3

Status: **FAIL — 0 Critical, 2 High, 1 Medium**. Native GPT-5.6 Sol read-only review of commit `2b38d3ea83464f0dc298557ebb0a07ec7cf94282`, plan SHA-256 `df4db6bc2938d2c25f04c3ac776f3dbf2dcaf3ddf54312b3c08f47be01234454`, test SHA-256 `5f0490dbbf4e7245754fdcf4c833f1c2ad3dc20b58109d8d959dd8787e3343dd`, against landed SPEC-044, inherited v20 and the draft Swift storage contracts. No implementation tests were run. The five round-2 corrections are closed at plan level; the revised recovery contract exposed the following blockers.

| Severity | Evidence and consequence | Required correction |
| --- | --- | --- |
| High | The mixed seven-kind temp scan holds only `operation.lock` (v22 plan:30), but SPEC-044:528–539 permits failure/pending and direct cancel writers under `failure.lock` → `cancel.lock` without operation custody. An EOF-proven scan can race a temp write, unlink live work, or promote stale state over terminal/marker evidence. T7 has no cross-writer interleaving. | Reconcile under the SPEC lock graph: `operation` → `failure` → `cancel` for cancel-visible state; release failure/cancel while retaining operation, then `operation` → exact `cleanup` → `cancel` for deletion. Revalidate target/temp after each lock-set change and test races at scan, unlink, publication, and readback. |
| High | v22 plan:28–30 and T7:19 inherit v20 plan:208/T01.3 generic complete-newer temp promotion. Atomic rename consumes the temp name, so a remaining complete temp is ordinarily *prepublication*, not proof of commit. Promotion can fabricate any of seven kinds: reservation/history, active attempt, cancel marker, inventory, deletion phase, source registry, or failed-dispatch terminal. SPEC-044:681–682 expressly denies terminal status before pending publication; SPEC-044:803–831 makes durable tombstoned readback the cleanup commit boundary and lets an intent-phase cancellation restore. | Explicitly supersede generic promotion in v20 plan/test. Use validated durable target and independently verified physical/phase witnesses per kind; discard tentative temps, reconstructing fresh state only under authorized locks and evidence. Cover complete-temp-before-rename crashes for all seven kinds, especially pending with no terminal, cancel with no marker, and tombstoned temp with cancel-first recovery. |
| Medium | v22 plan:26,42 and T5:14 allow staging cleanup for saved roots A/B while current configuration is C. Inherited v20 T10.4–T10.5:266–272 forbids different-root staging rename/unlink; SPEC-044 R006:960–966 separately prohibits different-root published cleanup. Opposite implementations appear valid. | State and govern the narrow staging comparison (saved source identity versus current configuration), explicitly supersede the conflicting inherited test if authority permits, and prove authenticated historical-root continuity while retaining the published cleanup restriction. |

The gate remains closed. No Swift implementation or physical acceptance follows from this plan review; the next revision requires a new exact-byte independent review.
