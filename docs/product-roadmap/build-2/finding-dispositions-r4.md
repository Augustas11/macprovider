# Product Build 2 R4 finding dispositions

**Disposition revision:** R4
**Status:** authored for a fresh independent GPT-5.6 Sol gate; no runtime implementation is authorized
**Failed review commit:** `a2bbfbc12632cf0c89e449c4db66cdeb82ee8f9a`
**Failed review:** `reviews/plan-r3-sol.md` at SHA-256 `4a2148bce95b74f8f5ad856edf66637d4fa95811fd4c25439a638fe528a924e7`
**Reviewed MacProvider plan commit:** `014788013f92e72abb7f9be1b4f2f8120a871140`
**MacProvider base:** `1d2c930bad81704dd0acc0322226725d8b64aceb`
**Malibu read-only base:** `dc7f425ba7d50c86467f31a82f419df6a0904b13`

No finding was downgraded, waived, or closed by reducing scope or acceptance. R4 retains R3's accepted provider selection, pool/SQLite double collect, signed-bundle plus invitation bootstrap, C6A refund evidence, fixed wallet-status authority, provider-plaintext disclosure, no ciphertext failover, rejection-only refund rule, ordinary settlement boundary, and actual-MLX evidence requirement.

| R3 finding | R4 correction | Required proof |
|---|---|---|
| H1 pending profile target cannot recover exact replacement/revoke authority | C3A v2 stores target state, request disposition, bundle and invitation identity, signer KID, exact signed-bundle bytes, selected fingerprints/pinframes, expiry, authority digest and revocation watermark. It binds state outside the immutable profile digest. Only locally persisted evidence constructs the successor. Server timestamps are excluded because they are unknowable pre-call. Client revoke upgrades the zero pre-tombstone sentinel with signed nonzero C3B evidence before stable commit. | T-P07 crashes before/after disposition, server success, C3B upgrade and stable append; deletes response/cache; mutates each persisted field; distinguishes active/revoked equal profile digests; and verifies pending retention/compaction/capacity. |
| H2 append-chain compaction has no rooted result | C8B defines exact pointer, manifest, immutable compacted base, append tail and checkpoint schemas/frames/MACs. Per-key retained/omitted heads preserve logical generations and old predecessors. One pointer CAS publishes a fully fsynced generation; crash recovery has deterministic old/new/quarantine outcomes and states the coherent same-user rollback limit. IndexedDB uses an atomic equivalent. | T-G03 and T-W02 share binary vectors and fault base/tail/manifest/pointer writes, fsyncs, reopen, rename, root switch and retirement. They prove removed predecessors have checkpoint witnesses and rollback never restores send ownership. |
| H3 one error action cannot be emitted coherently by server and client | Section 6 splits `relay-blind-wire-error-v2` from `relay-blind-effective-error-v2`. Servers never emit action/fence. The client reducer derives action only from a MACed local fence plus an origin-specific wire tuple. Mixed origins have local status 0 and explicit server status. All pinned SPEC-041 v1 codes have closed mappings; generated source inventory rejects gaps. | T-C06 separately generates server and reducer fixtures, rejects injected action, exercises mixed origins and every legacy/auth/wallet/quota/cancel/transport mapping, and runs both fence classes plus precedence. |
| H4 recovery bound omits aggregate persistence and retry order | C6 freezes scan epochs/cutoffs, 100 distinct untouched rows before retries, rows after cutoff in the next epoch, one claim transaction, five network waves and one aggregate result transaction. It budgets both transactions and proves the last first-call bound as 255 seconds. Persistent dependency outage is explicitly outside availability convergence. | T-Q05 runs the real scheduler with slow/failing/cancelled rows, clearing DB contention, aggregate-commit fault/restart, retry/new-arrival pressure and first-call timestamps for all 1,000 rows. It rejects per-row persistence. |
| H5 shared emergency audit can block mandatory revoke | C9 removes the shared revoke/recovery partition. Profile creation atomically preallocates one fixed-size revoke operation and audit slot per profile. Recovery quarantine owns 24 disjoint fixed slots. Logical capacity never requires allocation/blob growth during revoke; physical I/O failure is a storage outage and remains fail closed. | T-P04 exhausts normal operations/audits, every quarantine slot, wallet/recovery/client capacity, then revokes all 32 active profiles within four-record pre-reserved local revoke bytes and coordinator page/WAL reserves; repeated revoke reuses slots and I/O faults never masquerade as capacity. |
| H6 lock and post-rename descriptor rules contradict | C8A establishes exactly one `private.lock`, distinct HMAC keys, total order, prohibited callbacks/network/SQLite while held, and a feasible rename state machine: old equals path pre-rename, mismatch is expected after rename, new equals temporary inode, old close checks captured identity only. | T-G02 races each ancestor/path boundary and checks the exact pre/post-rename identities, stable lock, fault stages and five-second acquisition ceiling on macOS. |
| M1 journal narrows request IDs without compatibility boundary | C4 requires canonical lowercase UUIDv4 only when Build 2 profile headers are present; headerless legacy v1 keeps printable ASCII and never enters the Build 2 journal. C8 frames UUID bytes. | T-C02A supplies UUID/non-UUID/mixed-header vectors across Go, Swift, JavaScript and both services, proving legacy preservation and Build 2 fail-closed enforcement. |
| M2 revocation freshness is unauthenticated and rollback-prone | C3B defines a challenge/account/profile/bundle/pin-bound signed preflight with generation, append-only root, issue/expiry, key lifecycle, no-store HTTPS, monotonic local persistence and exact stale/replay/reorder/outage behavior. A 15-second client-use window is backed by coordinator lifecycle revalidation. | T-P07 mutates every proof/binding, clock and generation relation; tests key rotation, TLS/redirect/outage and concurrent tombstones; proves no cached/offline send and catches post-preflight revoke in C5. |
| M3 capacity boundaries lack reachable construction | C9 sets normal mutation/audit limits to the reachable 2,048 successful create/replace heads, specifies zero-retention for rejected operations, gives a complete operation-to-charge table, and includes B-tree/index/WAL/page reserves rather than payload-only accounting. | T-P04 constructs 32 profiles times 64 revisions through supported APIs, exercises every retained event, compares accounting with database growth, and independently reaches 128/profile and 512/account reservations. |
| M4 real-browser acceptance lacks an executable dependency-compliant harness | C10 selects built-in Node W3C WebDriver for Safari and CDP for Chrome, loopback secure-context checks, exact commands, installed tool versions, isolated profiles/sessions, owned crash controls, redacted artifacts and explicit CI/authorization blocker behavior. No npm dependency is added. | T-W04 runs `npm run test:private-browser`, records Safari and Chrome counts/versions separately, executes every tab/process/storage/lock cut, refuses unrelated Safari termination and forbids Node mocks as browser evidence. |

## Current Malibu evidence

The R4 author fetched and inspected Malibu `origin/main` read-only at `dc7f425ba7d50c86467f31a82f419df6a0904b13`. The canonical checkout remains two commits behind with unrelated untracked `.omc/` and `social/`; it was not edited. `package.json` has Node tests, documentation checks, Vite build/preview and no browser automation dependency. `console/api.js::fetchChatCompletions` retains ordinary 502/503 retry behavior and local/session storage. The reviewed host has Node `v22.23.2`, Safari `26.5` with `/usr/bin/safaridriver`, and Chrome `152.0.7977.85` without `chromedriver`. C10 is a proposed no-new-dependency harness contract, not fresh browser evidence.

## Gate instruction

A fresh independent native GPT-5.6 Sol reviewer must inspect the exact R4 plan, paired test specification, this disposition, baseline, failed R3 review, MacProvider base and Malibu base. It must challenge feasibility, trust boundaries, economics/refund safety, storage rollback and crash recovery, denial resistance, revocation freshness, browser execution, UX truthfulness, and whether each acceptance test proves its claim. Any Critical, High or Medium finding requires R5. Implementation remains prohibited until a fresh review reports zero in all three severities.
