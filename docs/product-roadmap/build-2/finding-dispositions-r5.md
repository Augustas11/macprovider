# Product Build 2 R5 finding dispositions

**Disposition revision:** R5
**Status:** authored for a fresh independent GPT-5.6 Sol gate; no runtime implementation is authorized
**Failed review commit:** `e2ab2ce221efb2731408f1d852f1e16e1a86b867`
**Failed review:** `reviews/plan-r4-sol.md` at SHA-256 `3a2ce35dce0fe1fc4f16168fe19fd06790ed4d07297cc3b0662d29032c3d4f9c`
**MacProvider base:** `1d2c930bad81704dd0acc0322226725d8b64aceb`
**Malibu read-only base:** `dc7f425ba7d50c86467f31a82f419df6a0904b13`

No finding was downgraded, waived, or closed by reducing the requested buyer journey. R5 preserves server-side approved-provider selection, double collect, signer/bundle/invitation bootstrap, provider plaintext disclosure, response-relay visibility, no ciphertext failover/retry, signed rejection-only refunds, ordinary settlement, real browser evidence, and actual MLX acceptance.

| R4 finding | R5 correction | Required proof |
|---|---|---|
| H1 first create has no representable durable pending state | C3A v3 is a closed record union. A create pending record has tagged absent/genesis expected authority and a complete locally verified active target. It is anchored before the request; `sent_or_unknown` is anchored immediately before send. Not-sent cancellation appends an absent marker; later states reconcile only. | T-P07 covers every create record/head/request/server/invitation/response/stable/restart cut plus concurrent create and cancellation. Recovery deletes responses/caches and uses only the persisted target. |
| H2 mutable tails and old pointers are unrooted | File stores write immutable tail/pointer objects and authenticate every append/publication with a non-synchronizable Keychain external head. `CURRENT` is a cache. Browser transitions use an account-authenticated opaque server head CAS before mutation/send. | T-G02/G03/T-W02 restore tail prefixes, old pointers, retained generations, and mixed roots; fault every external-head boundary; prove old/new outcomes and no send-owner restoration. |
| H3 no empty revocation-log state | C3B defines signed generation 0 as a domain-separated empty-root digest, makes it the first-entry predecessor, and freezes domain-separated signer/bundle/profile/pin target frames plus checkpoint continuity. | T-C02/T-P07 share empty/first/all-kind/subsequent/pruned vectors across Go/Swift/JavaScript and restart from an empty deployment. |
| H4 wallet cannot perform mandatory preflight | Wallet profile mode explicitly requires a separate account API key for identity/profile/C3B and wallet credentials for reservation/status. C3B v2 binds auth mode and exact wallet session after same-account verification; credentials never share a request. Malibu remains account-key private mode. | T-C05 tests the valid split flow, wallet-only/missing/mixed credentials, cross-account subject, stale/revoked session, response mismatch, and post-preflight lifecycle changes. |
| H5 browser command can proxy to production and violates HTTPS authority | C10 forbids Vite preview. A test-only HTTPS static server preserves `/api/mp` rewrite and accepts only explicit TLS loopback fixtures with peer/CA/redirect checks and a production-host zero-socket guard. Safari process crashes require an owned disposable user/VM. | T-W04 proves canonical HTTPS origin, proxy routing, target refusal, zero production sockets, isolated certificates/credentials/processes, and labels unavailable Safari process-crash evidence BLOCKED. |
| M1 255-second formula omits final waves | C9 freezes the general zero-based wave formula and `9*28+3+4*2=263s`, updates the 300-second/retention calculation, and separates restart faults. | T-Q05 measures the final row at 263 seconds or less, non-divisible shapes, invalid configs, and separately reports crash/restart recovery. |
| M2 auth/wallet/quota/transport mappings deferred | Section 6 freezes the complete primary tuple/action inventory and a pinned emission-site table for generic gateway, wallet, auth, cancellation, and transport cases. Generated lookup uses callsite constants; malformed is the sole default. | T-C06 source-scans every reachable constructor/helper/adapter, requires one exact reviewed row, executes each mapping before/after fence, and rejects stale/missing/duplicate/regex mappings. |
| M3 physical SQLite decision deferred | C9 freezes SQLite settings, coordinator/gateway live-page, transaction, WAL, max-page, and free-disk thresholds. Slice 2 is restricted to exact DDL plus measurement and requires a new independent gate before runtime storage. | T-P04 reaches every logical bound, records dbstat/page/freelist/WAL/transaction observations, rejects threshold overruns, and digest-binds the required follow-up gate. |
| M4 global revocation authority unbounded | C9 freezes 65,536 rows, 64 MiB canonical bytes, 256 MiB pages, 64/hour normal rate, 65,280 normal rows, four 64-row kind reserves, 64 checkpoints/8 MiB, 38-day/reference retention, pruning continuity, and fail-closed saturation. | T-P04 constructs normal maximum, then commits one tombstone of every kind, exhausts one kind reserve, checks isolation/restart/checkpoint/pruning/reference holds, and verifies exact physical boundaries. |

## Malibu evidence refreshed

The R5 author re-inspected pinned Malibu `origin/main` at `dc7f425ba7d50c86467f31a82f419df6a0904b13` from git objects without editing its stale canonical checkout. `console/api.js` uses `BASE = '/api/mp'` and ordinary `fetchChatCompletions` retries selected failures. `vite.config.js` maps `/api/mp` to `https://api.streamvc.live`; installed pinned Vite 8.0.16 resolves preview proxy as `preview.proxy ?? server.proxy`. `package.json` has no browser automation dependency. These facts require C10's separate HTTPS loopback harness and production-target startup refusal.

## Next gate

A fresh independent native GPT-5.6 Sol reviewer must receive the exact R5 plan, test specification, this disposition, both pinned bases, R4 failed review, and R4 predecessor artifacts. It must independently inspect both repositories and return severity, evidence, consequence, and required correction. Any Critical, High, or Medium finding requires R6. Even after an R5 zero gate, runtime storage remains prohibited until the Slice 2 DDL/physical measurement artifact passes the explicitly required second gate.
