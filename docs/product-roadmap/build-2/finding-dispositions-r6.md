# Product Build 2 R6 finding dispositions

**Disposition revision:** R6
**Status:** authored for a fresh independent GPT-5.6 Sol gate; no runtime implementation is authorized
**Failed review commit:** `0661923226645abca5e8f0d11491c84fc6531664`
**Failed review:** `reviews/plan-r5-sol.md` at SHA-256 `00b9d3a808e734fbaf60d4c813091ca1b17fd7a5011734bdbb8f71509a02d93f`
**MacProvider implementation base:** `1d2c930bad81704dd0acc0322226725d8b64aceb`
**Malibu read-only base:** `dc7f425ba7d50c86467f31a82f419df6a0904b13`

No finding was downgraded, waived, or closed by weakening buyer-approved selection, signed bootstrap, provider-plaintext disclosure, relay-visible responses, no ciphertext retry/failover, signed rejection-only refunds, ordinary settlement, browser evidence, or actual-MLX acceptance. R6 also retains the mandatory DDL/physical-measurement gate and makes its exact error-emission inventory an additional prerequisite.

| R5 finding | R6 correction | Required proof |
|---|---|---|
| H1 sent-or-unknown mutation lacks executable recovery | C3A makes the persisted exact method/route/content type/body/digest the sole recovery operation. Coordinator terminal rows seal byte-identical committed or rejected-no-commit responses in the mutation transaction. Before-receipt and transient operation-capacity outcomes remain pending and replayable; GET/list/invitation state is never absence authority. | T-P03 and T-P07 cut create, replace, and revoke before receipt, after auth/row reservation/validation/writes/commit, during response, and after restart/concurrency. They require exactly one mutation/effect and byte-identical replay. |
| H2 no fresh Go external-head bootstrap | C8B adds a third creation-only Keychain bootstrap selector. A successful `SecItemAdd` while all selectors and directory content are absent is the only fresh discriminator. It freezes initializing/committed schemas, deterministic genesis checkpoint/base/manifest/tail/pointer/head bytes, order, readbacks, and recovery/quarantine for every partial combination. | T-G02 faults every Keychain/file/bootstrap boundary, resumes only the same bootstrap ID, rejects empty-directory inference and orphan item/file combinations, and validates both authorities. |
| H3 append pointer/head contradiction and unbounded full-copy tails | C8B publishes a new immutable v3 pointer for every append and binds it in an external-head v2 update. Pointer/head live fields are equal and both carry prior digests. Old/new readback is authoritative; retirement occurs only after readback plus a full new-set load, blocks another append on failure, and limits normal peak to current+candidate tails and two pointers. | T-G03 freezes accepted genesis/append/retired-append/compaction vectors, faults every publication/readback/cache/retirement boundary, tests mismatch negatives, and measures physical peaks. |
| H4 browser two-kind ledger protocol incomplete | C8B freezes paired allocation, per-kind GET/CAS, paginated list, and atomic DELETE methods/paths/closed schemas. Allocation always creates both generation-zero kinds and preallocates DELETE capacity. List/DELETE use a tagged presence union solely to clean legacy/fault-injected one-kind pairs; lost responses use exact operation replay or read reconciliation. | T-W02 executes literal request/response bytes, idempotency, generation zero, cross-account fixed 404, response loss, capacity, one-kind cleanup, both-absent rejection, revocation retention, and pair pruning. |
| H5 emergency reserves cannot revoke every live target | C9 caps and preallocates one typed slot for every reachable signer (8), bundle revision (4,096), live profile ID (32,768), and unique stored-bundle pin (8,192): 45,064 emergency slots plus 65,536 normal slots. Publication/activation and slot allocation are atomic; startup requires a live-target/slot bijection. Global profile/revision/operation/audit/invitation/browser/wallet caps keep the set physically co-reachable. | T-P04 constructs all 45,064 live targets before normal saturation, fills all 65,536 normal slots, revokes every target from its own slot with restarts, verifies 110,600 total rows and checkpoint/root continuity, and tests precise over-limit rejection and delayed reuse. |
| M1 exact error callsite keys absent | Section 6 freezes the exact inventory row schema and domain-separated key over repository/commit/path/function/stable label/raw code/exact method-path, requires route and dynamic-branch expansion, and records the pinned gateway scan counts. Because planned labels/routes do not exist at the historical base, Slice 2 must deliver every JSON row and digest to the mandatory second zero-C/H/M gate; no error emitter/adapter/reducer/UI implementation may begin first. | T-C06 recomputes keys, checks the pinned 40+22+25 direct gateway count plus coordinator/new-client emitters, expands three dynamic branches, executes every tuple/action, and fails missing/stale/duplicate/ambiguous/newly reachable/default mappings. |
| M2 revocation checkpoint auth/framing unspecified | C3B defines the exact v1 checkpoint fields/order, framing, digest and signature domains, Ed25519 evidence key/KID authority, retention/rollover, 1,024-entry ranges, final pruning checkpoint, suffix predecessor, empty suffix, and fail-closed corruption/rollback handling. | T-C02/T-P04/T-P07 compute shared Go/Swift/JavaScript vectors and test wrong key/domain, rollover, gaps/overlap/rollback, first suffix, empty suffix, singleton mismatch, pruning, and restart. |
| M3 five-second Keychain ceiling permits late mutation | C8A limits five seconds to nonblocking lock acquisition. After acquisition, a started noncancellable Keychain operation retains the lock until synchronous completion and exact readback; five seconds becomes an alert/admission deadline and never permits another writer to enter. | T-G02 delays replacement both below and beyond five seconds, proves lock exclusion and admission blocking throughout, then reconciles old/new before release. |

## Additional consistency corrections

- Profile mutation operation capacity now accounts for sealed rejection rows; transient inability to reserve a row cannot restore local authority.
- Browser allocation/CAS operations have exact per-account/global bounds, while every allocated pair owns a DELETE slot that remains usable at normal saturation.
- Global profile revision, operation, audit, invitation, reservation, browser, and wallet-status caps prevent incompatible per-account maxima from being counted inside fixed SQLite ceilings.
- The checkpoint count is 128, which covers all 110,600 possible retained tombstones at 1,024 entries per full checkpoint plus a final eligible pruning boundary.

## Read-only source reconciliation

MacProvider remained at the stated implementation base for code-grounding. Pinned Malibu was re-read with `git show` at `dc7f425ba7d50c86467f31a82f419df6a0904b13`: `console/api.js` still uses `BASE = '/api/mp'` and retries selected 502/503 ordinary-chat failures; `vite.config.js` still proxies `/api/mp` to `https://api.streamvc.live` with prefix removal; `package.json` still declares Vite 8.0.16 and no browser-automation package. These facts continue to require the isolated trusted-HTTPS harness and separate no-retry private transport.

## Next gate

A fresh independent native GPT-5.6 Sol reviewer must receive the exact R6 plan, test specification, this disposition, checkpoint, failed R5 review, and both pinned repository revisions. It must independently inspect both repositories and return structured severity/evidence/consequence/required-correction findings. Any Critical, High, or Medium result requires R7. Even after a zero R6 gate, only the governance/vector slice may proceed; runtime storage and all error-facing implementation remain prohibited until the exact DDL/measurement and complete error-inventory artifacts pass the separate second gate.
