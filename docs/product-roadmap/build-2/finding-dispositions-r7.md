# Product Build 2 R7 finding dispositions

**Disposition revision:** R7
**Status:** authored for a fresh independent GPT-5.6 Sol gate; no runtime implementation is authorized
**Failed review commit:** `e8c44ade8dac56e70cb091ff2bcc0f6e33a51e46`
**Failed review:** `reviews/plan-r6-sol.md` at SHA-256 `710b34aa775c4262b5c217c274b60cc93b2dc0b42ebfed5766a552e88b00bc01`
**MacProvider implementation base:** `1d2c930bad81704dd0acc0322226725d8b64aceb`
**Malibu read-only base:** `dc7f425ba7d50c86467f31a82f419df6a0904b13`

R6 had zero Critical findings, three High findings, and one Medium finding. R7 does not downgrade, waive, or relabel any finding. Buyer-approved selection, exact mutation recovery, no ciphertext failover, provider-plaintext disclosure, relay-visible responses, signed rejection-only refunds, ordinary settlement, browser evidence, and actual-MLX acceptance remain unchanged. The exact DDL/physical/error-inventory second gate remains mandatory.

| R6 finding | R7 correction | Required proof |
|---|---|---|
| H1 maximum exact response cannot fit operation storage | C1 caps both the exact active profile response and its conservative maximum-timestamp revoked projection at 16,384 encoded bytes. C3 stores no regenerated success body in a 4 KiB operation row: migration preallocates 16,384 fixed 16 KiB response extents, operation rows reference immutable sealed bytes, errors remain inline within 2,048 bytes, and every live profile owns an in-place revoke response extent. C9 bounds 8,192 success plus 8,192 revoke extents at 256 MiB and includes them in the 1 GiB database ceiling. | T-C01 consumes the exact 16,384/16,385 byte oracle and every maximum error body. T-P03 faults slot/write/commit/replay and rejects digest/padding/kind drift. T-P04 runs exact maximum bodies through proposed DDL, measures every table/index/overflow/WAL page, and proves a maximum revoke after normal capacity saturation allocates no page, row, index key, or longer BLOB. |
| H2 two valid roots can fork one Keychain head | C8A adds one non-overridable `getpwuid_r` coordination path and retained-descriptor whole-file `fcntl` lock acquired before every root-local lock and Keychain read. Root markers are bootstrap-bound. A writer immediately re-reads the predecessor under both locks; after clone A publishes H-A, clone B at H lacks H-A's named objects and quarantines before update. Exact offline copy/move is permitted only at the current head; merge/adoption/repair is absent. | T-G02 proves global/local lock order, descriptor/path integrity, bootstrap collisions across roots, and late Keychain completion while both locks remain held. T-G03 enumerates two-root bootstrap/append/readback/retirement/compaction and profile/request fence interleavings, requiring one accepted successor per head and at most one send. |
| H3 normal-tombstone saturation history is unreachable | C9 uses exact jointly reachable bounds: 1,024 normal slots, 7,176 live-target emergency slots, and 16,384 physically preallocated response extents. Supported APIs create 1,024 temporary profiles, revoke them into all normal slots over 16 hours while atomically releasing only their still-unsealed emergency slots, then create 4,096 final profiles before the 24-hour activation boundary. The resulting state has full normal history, the exact 7,176-target live set, 5,120 retained revisions, 10,240 bound response extents, and 6,144 free extents. Leaf-to-root emergency revocation covers every target within pin/evidence horizons. Invalid/duplicate/historical revocations create no tombstone. | T-P04 enumerates all activations, invitations, owners, the 16-hour normal fill, slot releases, timely final activations, cap boundaries, 113 further emergency-revocation hours, restarts, 8,200 generations, at most 9 checkpoints, expiry/retention boundaries, and forbidden slot reuse. Independent retained-revision/response maxima are tested separately and never falsely asserted as co-reachable. No direct insertion or disabled competing limit is allowed. |
| M1 Keychain anchor lacks exact digest/selectors/policy | C8B defines `account_scope_digest`, exact service/account selectors, root/policy/access-group digests, and complete Security.framework add/read/update dictionaries. It freezes Data Protection, AfterFirstUnlockThisDeviceOnly, nonsynchronizable, noninteractive, process-default-group behavior; forbids named groups/custom ACL/authentication contexts; normalizes exact readback attributes; and binds returned group/policy/root bytes into bootstrap/head MACs. Future policy migration uses distinct selectors and authenticated `migrating` state; unversioned items quarantine. | T-C02 and `keychain-policy-v1.json` freeze cross-runtime bytes, symbolic dictionaries, normalized attributes, and digest vectors. T-G02 runs a dictionary spy plus native Security.framework, every wrong-policy/interaction case, group consistency, add/read/update, initialization crash boundary, and future-policy migration negative. |

## Cross-finding consistency

- The response oracle validates the future revoked form at the maximum safe timestamp during create/replace, so an accepted active profile can always fit its reserved revoke extent.
- Migration physically instantiates all 16,384 fixed response extents. The joint capacity history binds 10,240 of them: 1,024 temporary successes, 1,024 maximum historical revoke bodies, 4,096 final successes, and 4,096 zero-filled final live revoke reserves; the other 6,144 remain exact zero-filled free extents. Independent response maxima are exercised in separate supported-API scenarios.
- Normal profile operations cap at 16,384 fixed 4 KiB rows; normal committed audits cap at the reachable 8,192 fixed 2 KiB rows. Profile revocations use their fixed operation/audit/response sets.
- Normal-slot revocation releases only an unsealed emergency slot in the same transaction that makes its target nonlive and commits authoritative retained history. A sealed emergency slot never releases early.
- The root-independent lock covers all origins/accounts/authorities for the login user. Root identity permits an exact offline move while stale-source publication fails against the current Keychain-named set.
- Exact policy dictionaries match the repository's standalone CLI constraint that release binaries carry no restricted keychain-access-group/application-identifier entitlement.

## Read-only source reconciliation

MacProvider was re-read from `1d2c930bad81704dd0acc0322226725d8b64aceb`. `phase4-coordinator/internal/buyer/relay_blind.go::selectRelayBlindProvider` still sorts the global `pool.Snapshot()` and selects the first matching serving provider without a buyer-profile intersection. Pinned gateway code still routes relay-blind reservation through its existing surface; the planned profile/response-slot/Keychain contracts are not landed.

Malibu was re-read with `git show` at `dc7f425ba7d50c86467f31a82f419df6a0904b13`. `console/api.js` still uses `BASE = '/api/mp'`, and `fetchChatCompletions` retries selected 502/503/500 ordinary-chat failures. `vite.config.js` still proxies `/api/mp` to `https://api.streamvc.live` with prefix removal. `package.json` still declares Vite 8.0.16 and no browser-automation package. The separate no-retry private transport and isolated HTTPS harness remain required.

## Next gate

A fresh independent native GPT-5.6 Sol reviewer must receive the exact R7 plan, test specification, this disposition, checkpoint, failed R6 review, and both pinned repository revisions. It must independently inspect both repositories and report severity, evidence, consequence, and required correction. Any Critical, High, or Medium finding requires R8. Even after a zero R7 gate, only governance/vector work may begin; runtime storage and error-facing implementation remain prohibited until exact DDL, response-oracle measurements, and the full error inventory pass the separate second gate.
