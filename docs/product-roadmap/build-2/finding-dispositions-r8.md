# Product Build 2 R8 finding dispositions

**Disposition revision:** R8
**Status:** authored for a fresh independent GPT-5.6 Sol gate; no runtime implementation is authorized
**Failed review commit:** `c2960f14ca966ec4aac9b2c74fb2ab42af8f31e6`
**Failed review:** `reviews/plan-r7-sol.md` at SHA-256 `a48ea052c9846976ef42bc0ac508a332aa45bb318a14c92247ee7d27729dd32a`
**MacProvider implementation base:** `1d2c930bad81704dd0acc0322226725d8b64aceb`
**Malibu read-only base:** `dc7f425ba7d50c86467f31a82f419df6a0904b13`

R7 had zero Critical findings, six High findings, and zero Medium findings. R8 does not downgrade, waive, or relabel any finding. Buyer-approved selection, exact mutation recovery, no ciphertext failover, provider-plaintext disclosure, relay-visible responses, signed rejection-only refunds, ordinary settlement, browser evidence, and actual-MLX acceptance remain unchanged. The exact DDL/physical/error-inventory second gate remains mandatory.

| R7 finding | R8 correction | Required proof |
|---|---|---|
| H1 traditional `F_SETLK` is not process-wide authority serialization | C8A gives every exported Go authority/store constructor one `sync.Once` process-lifetime `AuthorityProcessGate`. After that gate, one canonical retained `authority.lock` descriptor takes macOS BSD `flock(LOCK_EX|LOCK_NB)`; each root lease similarly owns one retained private-lock descriptor. Traditional process-associated `F_SETLK` and per-operation descriptors are forbidden. The contract fixes same-process/cross-process contention, descriptor nonduplication, unrelated close, reverse unwind, cancellation, panic/`Goexit`, fork/exec, idempotent `Store.Close`, and noncancellable late-Keychain completion. | T-G02 races two Stores, accounts, authorities, and roots in one process and across processes; opens/closes unrelated descriptors; inspects the singleton registry; faults panic/`Goexit`, fork/exec, Close, and late Security.framework completion. T-G03 retains the complete two-root/head/fence schedule and requires one successor and at most one send. |
| H2 retained revoked bundle/pin rows contradict the emergency-slot bijection | C9 defines `active_revocable` from each target's own active state plus absence of its own tombstone, and `retained_revoked` from its own revoked state plus matching witnessed tombstone. Ancestor invalidation and retained references affect admission/retention but do not create an active target. Only active-revocable records own `bound_unsealed` slots; normal-revoked records release theirs atomically, emergency-revoked records own `sealed_revoked` slots, and all others are `free`. Exact post-kind counts are frozen. | T-P04 enumerates every supported activation/revocation, complete owner/free/sealed ledger, normal-release transition, post-profile/pin/bundle/signer tuples `(3080,3080,4096,0)`, `(1032,1032,6144,0)`, `(8,8,7168,0)`, `(0,0,7176,0)`, restarts at every boundary, retained references, replay, and pruning. |
| H3 in-database signatures cannot detect a complete SQLite rollback | C3C makes production private mode depend on an independently operated, linearizable, immutable, monitored revocation-witness namespace outside the coordinator host/volume/backup credentials. Exact challenge-bound head/append/suffix APIs and proofs bind every generation/root/capsule. Revocation uses SQLite prepare -> witness CAS/readback -> SQLite finalize -> fresh equality read. Startup compares the external head; an older DB catches up only from exact capsules/state or remains `rollback_recovery_required`. Witness rollback has no automatic override. Internal checkpoints are explicitly only consistency evidence. | T-C02 freezes witness/preflight bytes. T-P00/T-P03 fault every prepare/CAS/finalize/read boundary. T-P05 restores complete SQLite snapshots before each target kind for fresh and anchored clients, proves exact suffix catch-up or fail-closed recovery, and separately rolls back the witness. T-P07 proves preflight cannot sign below the external head. Production qualification measures independence, immutable retention, monitoring, restore, and outage behavior. |
| H4 signer/bundle/pin lifecycle has no executable operator contract | C2A freezes exact signer activation, bundle upload, and signer/bundle/pin revoke methods, paths, versions, closed bodies/results, target authority digests, named-actor authentication, request digest, `(actor,operation_id)` replay key, CAS/parent checks, normal/emergency/rejection charges, response/witness headers, and prepared/witnessed/finalized recovery. The route cannot add signing key bytes; an explicitly revoked signer KID cannot reactivate. | T-P00 runs every exact route through first delivery, replay, changed/cross-actor replay, stale target, duplicate, parent/child race, auth/listener failure, capacity/rate boundary, every witness/SQLite crash cut, restart, and response loss. Shared schema/error/vector gates must enumerate every route before runtime work. |
| H5 buyer-retained operation caps permit cheap global denial | C9 replaces shared buyer partitions with 256 operator-assigned, nonborrowable account shards. Each profile shard owns 64 normal operation and 32 committed-audit rows; each matching gateway shard owns 256 browser operations, eight authority pairs, and fixed DELETE rows. A buyer cannot self-assign/borrow a shard. Exhaustion affects only that account; exact replay, reads, status/recovery, and preallocated revocation/DELETE remain available. Physical global totals and thresholds remain frozen. | T-P04 fills one and then 255 hostile account shards with semantic/CAS failures and proves the final assigned account retains its exact create/replace/revoke/recovery service. T-W02 does the same for browser operations and both authority kinds. Limit-minus-one/exact/over, retention/drain, zero-charge auth/parse/reservation failures, fixed revokes, and physical page/WAL maxima are measured. |
| H6 unapproved provider churn can exhaust global selection retries | C5 replaces the global generation with an epoch plus a monotonic generation and decision digest per provider, bounded to 4,096 authenticated configured IDs. The reservation token contains the exact operator mapping projection and every mapped approved provider state, including absent/ineligible. Only changes affecting that projection consume one of three rounds. After selection, consume/final arm compare only the selected-provider token. Removal retains absence/generation for the epoch; reinsertion advances it; overflow disables only that provider. Pool locks never overlap SQLite/network. | T-S01 freezes table bounds, provider generation, removal/reinsertion, mapping projection, and decision-digest vectors. T-S04 checks exact T1/T2/T3 invalidation only for approved facts. T-S05 sustains unapproved provider churn across every barrier while A-only/A+B reservations finish without consuming a round, then proves approved churn exhausts exactly. T-S06 proves unrelated churn cannot burn a selected row or redirect ciphertext. |

## Cross-finding consistency

- Witness entries carry the terminal body but exclude their append-produced witness proof, avoiding circular bytes. Final stored/replayed revocation responses add the exact three witness headers; fresh private work still requires a new challenge-bound preflight.
- The external witness retains at most 8,200 capsules at 32 KiB each within at least 512 MiB independently provisioned live capacity. Coordinator SQLite retains its frozen 1 GiB ceiling; the witness is not counted as a second coordinator database.
- Per-account profile limits now state the actually provisioned guarantees: 64 terminal operations and 32 committed audits, so at most 32 committed create/replace outcomes/account. The normative 20-create history/account remains reachable. Browser capacity is 256 operations/account and 65,536 globally, with 2,048 pair IDs globally.
- The exact 129-hour history retains normal-revoked rows without unsealed slots, then seals 7,176 emergency units leaf-to-root. Every live target remains independently revocable and every restart checks the same state predicate.
- The process gate serializes all in-process roots before BSD locks; the retained kernel locks serialize other processes and cannot be released by an unrelated descriptor close. All locks remain held through late Keychain completion/readback.
- Provider-scoped tokens preserve the reviewed selection-before-encryption and exact-session/no-failover contracts while making unrelated provider churn invisible.

## Read-only source reconciliation

MacProvider remains deliberately pinned to `1d2c930bad81704dd0acc0322226725d8b64aceb`; current `origin/main` `c123ae2d2d08053612d940b3077994f7c4d709d7` was not incorporated into these historical-base planning bytes. At the pinned base, `phase4-coordinator/internal/buyer/relay_blind.go::selectRelayBlindProvider` still sorts the global `pool.Snapshot()` and selects the first matching serving provider without a buyer-profile intersection. The current relay-blind store has no Build 2 profile, operator lifecycle, external witness, per-account shard, or approved-set token authority.

Malibu was re-read from exact git object `dc7f425ba7d50c86467f31a82f419df6a0904b13`. `console/api.js` still uses `BASE = '/api/mp'` and retries selected ordinary-chat failures. `vite.config.js` still proxies `/api/mp` to a public upstream with prefix removal; Vite preview inherits that proxy when not overridden. `package.json` still has no browser-automation dependency. The separate no-retry private transport and explicit TLS-loopback production-shaped harness remain required.

## Next gate

A fresh independent native GPT-5.6 Sol reviewer must receive the exact R8 plan, test specification, this disposition, checkpoint, failed R7 review, and both pinned repository revisions. It must independently inspect both repositories and report severity, evidence, consequence, and required correction. Any Critical, High, or Medium finding requires R9. Even after a zero R8 gate, only governance/vector work may begin; runtime storage and error-facing implementation remain prohibited until exact DDL, response-oracle measurements, external-witness backend qualification inputs, and the full error inventory pass the separate second gate.
