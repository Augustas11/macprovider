# Product Build 3 — Plan v2 Finding Dispositions in Revision 3

Status: corrections authored; independent review pending
Source review: `reviews/plan-v2-sol.md`
Source review SHA-256: `9d68006ae464ce0859af0cd6e5a7455e840cd4a0ffed19304d82e1d41fcf50da`
Corrected artifacts: `prd-implementation-plan-v3.md`, `test-spec-v3.md`

No finding was downgraded, waived, or converted into a weaker acceptance claim.

| R2 finding | Required correction | Revision 3 disposition | Verification added |
| --- | --- | --- | --- |
| H1 cyclic pilot/reference/calibration/release digests | Define an acyclic hierarchy and defer release binding until after implementation. | Defines six ordered artifact phases: antecedent profile; antecedent protocol; reference/golden/calibration results; evidence bundle; post-build release candidate and candidate-bound nonpositive qualification runs; final release qualification. No record binds itself or a descendant. Gate A0 precedes evidence, Gate B precedes implementation, and Gate C precedes positive availability. Every runtime/capture payload's required digest set is explicit. | B3-F007–F012, B3-C001–C003. |
| H2 post-hoc calibration rules | Freeze numeric protocol before measurements and separate observe-only claims from SPEC-036 warn/enforce. | Freezes exact cohort size/diversity, prompt/position/K/warm/cold matrix, seed/encoding, 99%/98% tail gates, threshold formula/ceiling, at least 600 decisions, one-sided 95% Clopper-Pearson <=1%, exclusions, stopping and expiry before data. Failed evidence blocks rather than moves thresholds. The result is prohibited for enforcement, verified settlement, or production economics. | B3-CAL001–C005 and B3-F008–F010. |
| H3 WS session/model generation not linearized | Bind a durable request to the live WS tuple and reject nil generation. | Adds unique nonreused session-instance UUID, mandatory full tuple, single-use route token, atomic SQLite capture, post-commit WS claim, session-actor send fence, orphan/nonrewardable handling, and deterministic lock order with no WS lock held across the money writer. Nil `ProviderGenerationID` is uncovered/ineligible. | B3-O010–O014 plus disconnect/reuse, swap, release/catalog/rate, BYOM epoch, revocation and late-result races. |
| H4 restore can erase a committed suffix | Record intent outside SQLite before commit and fail closed on ambiguity. | Replaces post-commit-only anchor with fsynced append-only PREPARE/COMMIT/ABORT lineage journal. PREPARE contains exact proposed event digest. Crash with PREPARE and absent restored DB event remains unavailable and is never auto-aborted, so the commit-crash-restore suffix cannot disappear silently. | B3-O008; B3-M010, M012, M019–M020; fault injection at every file/directory/DB/target boundary. |
| H5 no journal feasibility/capacity gate | Add measurable supported-storage budgets and fail Gate B if unmet. | Requires exact-protocol benchmarks at three DB sizes and concurrency levels with throughput, latency, lock, queue, event-size, growth, bootstrap, startup, RSS, disk reserve, fsync error, and one-year retention limits. Build 3 permits no deletion; failed retention/capacity requires a new architecture review. | B3-H001–H006. |
| M1 running MLX probe has no safe preemption | Define lease states, synchronization fence, deadline, and fallback. | Defines stage-specific running/cancelling/quiescing/quiesced states. Buyer Metal work cannot overlap. A 2-second miss drains the provider from routing until device synchronization proves quiescence. Lease generation fences late callbacks. | B3-P008, P014–P015 with actual-MLX arrival at all stage boundaries. |
| M2 reward reasons and inputs are not total | Close input enums, full reason vocabulary, precedence, actions, secondary ordering, and cross-client vectors. | Adds `provider_reward_status_inputs.v2` with common/request/earning/withdrawal/payment inputs, explicit reason/state/action mapping, conservative unknown behavior, deterministic secondary ordering, and byte-identical generated vectors for Go/Swift/portal. | B3-R001–R018 and B3-U001–U012. |
| M3 SPEC-022 observe is confused with SPEC-036 observe | Name both dimensions and isolate any test accrual. | Adds separate `settlement_verification_mode` and `compute_observation_mode`. SPEC-022 observe is always economically excluded. Only a qualification target with SPEC-022 enforce and all other valid facts can write an isolated shadow sink; production binaries contain neither sink nor enabling flag. | B3-D001–D006, B3-R003, R012, R014. |

Approval is not claimed. The exact R3 commit and artifact digests must be supplied to a fresh independent GPT-5.6 Sol high-reasoning reviewer. Any Critical, High, or Medium finding requires another revision before Gate A0 work or implementation.
