# Product Build 3 — Plan v3 Finding Dispositions in Revision 4

Status: corrections authored; independent review pending
Source review: `reviews/plan-v3-sol.md`
Source review SHA-256: `4c3d5a70345413bc90ae09907fe78806370cd44440d9c147d7e4a8637088db41`
Corrected artifacts: `prd-implementation-plan-v4.md`, `test-spec-v4.md`

No finding was downgraded, waived, or converted into a weaker acceptance claim. New hardware, custody, witness, and operator prerequisites are named blockers until freshly proven.

| R3 finding | Required correction | Revision 4 disposition | Verification added |
| --- | --- | --- | --- |
| H1 quantitative feasibility visible before protocol freeze | Prevent protocol authors from seeing any governed numeric acquisition before Gate A0. | Pre-Gate-A0 work is limited to stage/API reachability, tensor shape/dtype, cancellation/synchronization/allocation metadata and output-token parity. Governed values are discarded and zeroized in process; vectors, scalars, extrema, aggregates, hashes, repeated numeric acquisitions and value-correlated timing are prohibited. A numeric leak invalidates the gate and requires a new protocol with unexposed authors or independent blind custody. | B3-F001–F009 audit the sealed structural hook, logs/artifacts, custody sequences and acquisition times. |
| H2 no independent decision unit or useful-power gate | Define clusters, disjoint development/held-out data, confidence rules and blinded meaningful positive controls. | One physical host/operator/power-network/runtime campaign is one decision unit; positions and repetitions are correlated within it. The immutable 20-development/60-held-out split uses disjoint hosts/fault domains and excludes references/provider. Threshold freezes before held-out access. Held-out good requires 0/60 and <=5% one-sided 95% upper bound. Paired minimum-effect controls require >=59/60 and >=90% one-sided 95% lower bound across both runtime and hook-boundary strata. | B3-CAL001–CAL006 prove unit counting, partition/custody, false quarantine, power and rejection of correlated/post-hoc evidence. |
| H3 route-token clock/restart/orphan model incomplete | Serialize a checkable epoch/generation contract and durably reconcile every boundary. | Tokens bind random boot epoch, MAC-key epoch, monotonic mint sequence, signed UTC audit bounds, nonserialized live monotonic deadline, full session/model tuple and token ID. They are same-boot-only. Durable capture, WS claim reservation and durable dispatch-attempt authorization precede send. Startup/key rotation terminalizes unclaimed captures and uncertain attempts as nonrewardable, releases/refunds reservations, and rejects old-epoch late results. | B3-O005–O010 kill/restart at mint/PREPARE/commit/claim/attempt/send/result boundaries, rotate keys, move clocks and race every tuple field. |
| H4 ambiguous PREPARE/journal loss lacks safe restoration | Define a complete state machine, independent durable authority, dual control, obligation handling and nonduplicating incarnation rules. | PREPARE and COMMIT must be durable locally and on an independent zero-RPO witness. Recovery has closed cases for exact commit repair, dual-control supersede-abort only when DB/mirror/ack/dispatch are absent, contradictory ambiguity, one-copy loss, both-copy loss and new incarnation. Outstanding requests/reservations/credits are restored exactly or terminally voided/refunded. Genesis binds prior heads, recovery evidence and a global idempotency namespace; compensating history is append-only. | B3-M001–M015 run destructive local/witness/DB/mirror failures through safe service restoration or an explicit permanent qualification blocker. |
| H5 exact benchmark precedes exact workload contract | Commit and independently gate the exact schemas, mutation inventory, durability/storage topology, workload and benchmark before measurement. | A digest-bound `compute_preimplementation_contract.v1` and Gate B0 precede all hot-path/recovery measurements. It includes closed event/lineage/token/reward/recovery schemas, field sizes, exhaustive symbol/SQL inventory, exact SQLite/APFS/witness topology and capacity, deterministic mutation mix/fault schedule, baseline and benchmark binary. Any implementation/profile drift invalidates evidence and reopens the gate. | B3-F010–F011 and B3-H001–H008 prove package completeness, bound execution, capacity/fault limits and invalidation. |
| H6 reward input loses seven-scope dispositions/cap replay | Use an actual machine-readable authoritative effective projection equivalent to canonical SQL and preserve simultaneous scoped facts. | Gate B requires committed input/output schemas. Every ledger row carries all dispositions across ledger-row, useful-work source, provider, wallet, cohort, duplicate-class and epoch scopes, lifecycle, amounts and canonical order. Provider/wallet cap facts, withheld amounts and `cap_replay_pending` are explicit. Unknown/disagreement/pending replay fails unavailable; one blocked row cannot enter withdrawable aggregation. | B3-R001–R009 and R014–R015 cover SQL byte equivalence, seven scopes, simultaneous/released/terminal facts, cap replay, amounts and cross-client totality. |
| H7 shadow activity can appear as earning | Make shadow evidence structurally unreachable from provider APIs/clients and require production accrual plus enabled capability for earning. | Qualification shadow uses a separate schema/database/dependency graph and is operator-only. Provider API/client schemas contain no shadow ID/state. `earning` requires production environment, authoritative production-ledger accrual and separately authorized reward capability. Build 3 fixes capability disabled, so earning and eligible-idle remain unavailable even after positive observation/shadow decisions. | B3-R010–R016, B3-D001–D005, B3-U002–U007 and B3-A004–A005 prove no shadow leakage, balance/activity mutation or provider earning copy. |
| M1 scheduler omits independent MLX owners | Inventory every current MLX owner and enforce one resource arbiter or exclusive process lifecycle lease. | Arbiter covers buyer generation, compute/losslessness probes, `IdlePrewarmer`, `ProviderPreWarmer`, control-socket load/swap/adoption/finalize, unload/shutdown and enabled maintenance. Standalone decode/SPEC-028/autotune paths require a mutually exclusive process lease. Model transitions use exclusive drain/synchronize/generation-change semantics. New MLX call sites fail an owner-registry test. | B3-P003–P012 include idle/startup prewarm, adoption/swap/model transitions, three-way races, standalone paths, cancellation and old-runtime fencing. |

Approval is not claimed. A fresh independent GPT-5.6 Sol high-reasoning reviewer must recompute the committed plan, test-spec and disposition digests and inspect the repository/base independently. Any Critical, High, or Medium finding requires revision before Gate A0 work, evidence collection or implementation.
