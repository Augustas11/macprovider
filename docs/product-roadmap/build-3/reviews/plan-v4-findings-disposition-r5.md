# Product Build 3 — Plan v4 Finding Dispositions in Revision 5

Status: corrections authored; independent review pending
Source review: `reviews/plan-v4-sol.md`
Source review commit: `615d6fa676a702cad775c80675f1aa842f38239f`
Source review SHA-256: `cdb57d26b3a2325e1c3cfca4c5553c606a3951cf4cb66f0385976987573e35e4`
Corrected artifacts: `prd-implementation-plan-v5.md`, `test-spec-v5.md`

No finding was downgraded, waived, or converted into a weaker acceptance claim. Revision 5 adds two bounded executable pre-product slices, but neither is authorized by this document alone: each has a preceding design gate, a following complete-diff gate, exact path ownership, and production exclusion. Governed numeric evidence, product implementation, enforcement, economics, deployment, and positive qualification remain blocked by their named gates.

| R4 finding | Required correction | Revision 5 disposition | Verification added |
| --- | --- | --- | --- |
| H1 calibration precedes missing collector hook | Implement and bind the real sampler hook before numeric calibration; prove equivalence to production. | The base `1.8.123` CLI is explicitly ineligible. Gate H0 approves `compute_hook_design.v1`, then Collector Slice H0 may change only `ComputeObservationMeasurement`, `compute-observation-collector`, tests and SwiftPM manifests. Gate H1 freezes source, dependency/compiler/SDK, signed executable and separately emitted measurement-module object bytes. Gate A0 binds that concrete collector before acquisition. Production and Gate C must embed byte-identical module object bytes; any semantic/build/object mismatch returns to H0/H1/A0 and recollects the full campaign. The collector has no production command, credential, coordinator, settlement or reward dependency. | B3-F001–F010 and F015–F016 cover gate order, structural real-MLX parity, leakage/cancellation, signing, module byte identity, nonshipment and invalidation. |
| H2 reward projection lacks atomic source revision | Define one coherent source snapshot across ledger, dispositions, caps, wallet, activity and completeness, including pagination. | A Postgres-owned monotonic `reward_source_revision` is allocated by one database mutation entry point inside every inventoried writer transaction. Runtime roles lack direct DML/sequence privileges; SECURITY DEFINER entry points bind revision to transaction plus a server-only token, and governed table triggers reject bypass. The reward owner reads all domains and the first activity page in one `REPEATABLE READ READ ONLY` snapshot, binding source revision and database commit time. Activity and mutable facts are append-only/versioned for revision-as-of pagination; cursors MAC-bind provider/revision/boundary/filter/page-size/expiry. Explicit refresh opens a new snapshot; unavailable as-of history fails closed. | B3-R017–R022 race every writer class, mirror advancement and pagination against reads, and test direct-write rejection, aborted/reused revisions, cursor tampering and snapshot expiry. |
| H3 pre-journal binary can bypass activated lineage | Fence every old/legacy writer before mutation and define safe forward recovery. | One-way activation runs offline: a signed supervisor allowlist first withholds the dedicated money-directory identity/handle from old binaries, then activation PREPARE becomes durable on both authorities, one SQLite transaction sets minimum writer protocol 2 and durable BEFORE triggers calling a function absent from old binaries, and only dual COMMIT permits startup. External witness/supervisor state rejects a restored pre-activation DB; post-activation restores retain triggers. Binary rollback is prohibited. A forward recovery build preserves all protocol-2 writers/recovery while disabling readers/probes/UI and cannot lower activation state. | B3-M016–M018 start every supported old release, exercise direct legacy SQL/mutation entry points, restore both pre/post-activation databases and prove forward recovery without a first mutation. |
| H4 daily reference successors are unbound to calibration/qualification | Define series roots, successor admission, calibrated numeric envelope, atomic transitions and expiry behavior. | Gate A0 now freezes per-source predecessor movement, A/B mutual divergence and skew bounds. Held-out campaigns prove known-good refresh false-suspension and meaningful single/two-source drift detection at the same host-level confidence gates. Each source has a series root and predecessor chain; an atomic epoch advances only when both fresh successors bind the same prior epoch and satisfy independence/golden/numeric rules. Gate C binds both roots and the successor-policy digest. Admitted successors preserve qualification only inside calibration/qualification/protocol expiry; fork, gap, skew, disagreement, revocation or envelope failure yields a deterministic nonpositive transition while in-flight captures remain immutable. | B3-REF005–REF008 and B3-CAL007–CAL008 cover valid advance, one-source pending, skew/disagreement, fork/gap/replay, changed provenance, expiry/revocation, immutable in-flight capture and held-out refresh power. |
| M1 Gate B0 is both docs-only and executable | Establish an explicit executable feasibility slice with its own pre-implementation and post-implementation gates. | Gate B0D approves closed schemas, inventory, topology, workload, protocol interface, exact paths and nondeployability controls. Only then may Slice B0I implement the core lineage module, synthetic adapters and `lineage-feasibility` command in named paths. The command needs a qualification build tag and compile marker. Gate B0E performs code/security/architecture review and freezes source/dependency/compiler/executable digests before a benchmark counts. Product Slices 1–7 remain blocked until Gate B. | B3-F011–F014 and B3-H001–H010 prove gate order, exact diff, synthetic-only input, production graph/binary/package/deploy exclusion, executable freezing, drift invalidation and benchmark results. |
| M2 `ProviderPreWarmer` is an out-of-process owner | Put candidate launch and lifecycle under cross-process exclusion, including orphan recovery and PID reuse. | The in-process arbiter is limited to work submitted by the serving `ModelRuntime`. `ProviderPreWarmer`/`CandidateProviderRunner` joins standalone tools under a supervisor-owned lifecycle `flock`. The supervisor holds the lease from pre-spawn through promotion/termination, records boot/nonce/PID-start/process-group identity, uses close-on-exec plus a child liveness channel, and performs verified TERM/KILL/reap recovery. Serving-to-candidate transition requires drain, synchronization, exit and release before spawn. Ambiguous PID reuse or an unkillable orphan fails closed. | Rewritten B3-P004/P008 and new P013–P017 cover in-process races separately from pre-spawn exclusion, two-parent concurrency, parent death, orphan/PID reuse, process-group cleanup and no-overlap transition. |
| M3 lineage has no sustainable lifetime | Define bounded hot segmentation, digest-preserving checkpoints, two-copy archival relocation, rolling capacity qualification and multi-year recovery. | Identical local/witness segments seal at 1 GiB or seven days. Checkpoints every 30 days/four segments bind the entire prior seal chain, applied heads, bounded nonterminal recovery records, idempotency namespace, archive catalog and prior checkpoint. Hot replicas move only after two independent archive copies pass read-back and catalog CAS, retention and obligation gates; logical events are never rewritten or lost. Replacement preserves two copies before retirement. Qualification lasts at most 30 days and renewal proves restore/growth/health plus 180-day reserve; 210-day exhaustion blocks renewal and 180-day blocks PREPARE. | B3-M019–M023 and B3-H006–H010 cover seals/checkpoints, five-year continuation, two archive migrations, full/genesis-equivalent restore, every crash boundary, fault-domain/copy/retention/obligation rejection, exhaustion thresholds, and deletion/head-reset rejection. |

Approval is not claimed. A fresh independent GPT-5.6 Sol high-reasoning reviewer must independently inspect the repository/base, recompute the committed artifact digests, and challenge feasibility, trust boundaries, economics, UX truthfulness, failure recovery, and test sufficiency. Any Critical, High, or Medium finding requires another revision before Gate H0, Collector Slice H0, Gate B0D, Feasibility Tooling Slice B0I, governed numeric acquisition, product implementation, or qualification.
