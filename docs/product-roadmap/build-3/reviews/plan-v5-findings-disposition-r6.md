# Product Build 3 — Plan v5 Finding Dispositions in Revision 6

Status: corrections authored; independent review pending
Source review: `reviews/plan-v5-sol.md`
Source review commit: `0dfbb8cf`
Source review SHA-256: `31ba0be5c46302e44bf814ffa1db6fa18441f7244b4532458e2f9a46745f4341`
Corrected artifacts: `prd-implementation-plan-v6.md`, `test-spec-v6.md`

No finding was downgraded, waived or converted into a weaker acceptance claim. Revision 6 adds authority boundaries and executable tests while preserving the prohibition on governed numeric acquisition before Gate A0, product implementation before Gate B, positive qualification before Gate C, and production observation before separate rollout authorization. It does not implement a collector, lineage tooling, product runtime, payout changes, reward changes or serving lease; collect numeric evidence; enable enforcement/economics/payment; deploy; change secrets; or inspect `d-inference`.

| R5 finding | Required correction | Revision 6 disposition | Verification added |
| --- | --- | --- | --- |
| H1 acquisition can bypass Gate A0 | Put a separately governed Gate-A0 authorization immediately before every collector/runtime value read, with one-time sequence, anti-rollback, revocation and recovery. | Gate H0 now jointly approves `compute_acquisition_authority.v1`, its independent approval key/quorum, public trust root, online replicated sequence authority, schemas and one shared value-access operation. Gate A0 is the earliest point at which the approval quorum can sign a capsule binding exact collector/profile/protocol/reference policy and authority root. A job reserves one monotonic sequence and binds mode, nonce, unit, partition, destination and required release/qualification digests. Immediately before the first value operation, the independent authority atomically consumes it and issues a one-use execution-bound grant. Offline/cached/grace access is forbidden; crashes burn sequences; stale recovery or old incarnation blocks. Compile/link inventory rejects alternate CLI or runtime numeric paths. Release and production modes require progressively stronger candidate, Gate-C and rollout antecedents. | B3-F001, F008–F010 and F017–F021 instrument the first value access and cover missing/pre-approval/wrong/replayed/expired/revoked/rolled-back/offline grants, every crash boundary, authority restore/rotation, alternate call paths and mode ordering. |
| H2 process-wide SQLite protocol number permits direct same-process mutation | Require a transaction-scoped, single-use authorization created only after dual PREPARE and consumed atomically with the exact mutation/event. | PREPARE now binds an ordered exact table/operation/key/before/after/event manifest. Only the unexported lineage owner may set connection-local authorization on one dedicated native connection after both PREPARE acknowledgements. Durable BEFORE triggers validate and consume exact manifest occurrences. `macprovider_lineage_finalize` requires the complete projection event and exhausted manifest, while a driver commit hook rolls back missing/extra/reordered/unfinalized work. Authorization binds native connection and SQLite transaction, commits once, and clears on rollback/cancel/connection return/crash. Protocol-2 direct helpers and alternate connections have no authority. Gate B0D must prove exact driver support before implementation. | B3-M001, M016 and M024–M028 cover new-process direct SQL, body/key/multiplicity changes, split/incomplete transactions, token reuse/cross-connection races and every crash boundary. B3-H001/H013 make driver enforcement a feasibility gate rather than an assumption. |
| H3 Postgres reward revision omits separately polled payout-wallet authority | Include wallet mutations in durable source lineage or define a truthful cross-database joined-version protocol with completeness/freshness. | SPEC-016 SQLite remains authoritative and receives a separate local+witness wallet-lineage stream. Every wallet lifecycle mutation dual-PREPAREs, appends an immutable ordered event and advances the SQLite source head in the same authorized transaction, then dual-COMMITs before acknowledgement. The mirror consumes only contiguous ranges whose SQLite/local/witness heads agree and applies versioned wallet facts/head in one Postgres reward revision. The endpoint opens a Postgres snapshot first, then compares its applied head to the wallet-source owner's SQLite+local+witness snapshot and returns composite `(reward_revision,wallet_head)` only on exact equality, matching incarnation, no gap/fork/restore/recovery and fresh source time. Mismatch/unavailable/stale affects wallet-bound and withdrawal facts only, with last-known values; other domains retain independent truth. Cursors bind both revisions. Poll time never substitutes for source time, and handlers no longer read wallet rows directly. | B3-R017–R028 cover every writer, mutation/mirror crashes, changes around both snapshots, replay/gap/fork/restore/authority disagreement, source unavailability, independent USDC/payment freshness, direct-read exclusion, pagination and linearization after the head read. |
| H4 foreground `macprovider-cli serve` lacks lifecycle owner | Assign the same exclusive lease to every serving entrypoint before model/Metal initialization and define crash, signal, launchd/app and upgrade behavior. | Direct foreground serve and launchd-direct are self-supervised owners: they lock before `ModelRuntime`, write a boot/nonce/PID-start/process-group record, stop admission, drain/cancel, synchronize and destroy the runtime before unlocking. Malibu.app and supervisor mode retain parent ownership across the child lifecycle. The launcher registry makes modes exclusive. Live re-exec/handoff is unsupported; upgrades drain/exit/release/restart. Crash recovery requires the newly acquired lock, identity/registry checks, verified orphan cleanup and a bounded device-quiescence barrier; ambiguity fails closed. | B3-P018–P022 add direct-versus-every-launcher, double-direct, app/launchd mode, signal timing, crash/PID reuse/quiescence and upgrade/re-exec cases while preserving P013–P017 supervisor coverage. |
| M1 physical calibration unnecessarily blocks independent feasibility work | Split numeric and nonnumeric lanes and join only at the evidence bundle/Gate B. | The dependency graph now has an independently runnable Gate-B0 lane with schemas, mutation inventory, nonnumeric protocol, synthetic workload and explicitly excluded numeric fields/imports. Gate B0D/B0I/B0E and synthetic benchmarks can proceed without Gate H0/H1/A0 or physical hosts. The numeric lane remains unchanged in qualification strength. Both are required to construct the evidence bundle and pass Gate B; either lane alone has no product, qualification, settlement or economic authority. | B3-F020 and B3-H011–H013 prove lane independence, numeric-import absence, nonproduction labeling, driver feasibility and the Gate-B join invariant. |

Approval is not claimed. A fresh independent GPT-5.6 Sol high-reasoning reviewer must inspect the repository/base, recompute the committed artifact digests, challenge feasibility/trust/economics/UX/failure recovery/test sufficiency, and report structured severity/evidence/consequence/correction. Any Critical, High or Medium finding requires another plan revision before Gate H0, Gate B0D, Collector Slice H0, Feasibility Tooling Slice B0I, governed numeric acquisition, product implementation or qualification.
