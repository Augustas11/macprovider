# Product Build 3 — Plan v6 Finding Disposition in Revision 7

Status: correction authored; independent review pending
Source review: `reviews/plan-v6-sol.md`
Source review commit: `5223982d`
Source review SHA-256: `7ee110e92ae8b83baaa1e292b400b1f71d32c9d4a1ac21140eedde3cf062cf17`
Corrected plan: `prd-implementation-plan-v7.md`, SHA-256 `514d024ad2b6d44ae09d9fce1f5d211627512689e77c4f82924aeb0baf103752`
Corrected test specification: `test-spec-v7.md`, SHA-256 `6a6c877ffcc05764a7b6e4c601be539e99cc6e8549e93eedd21a91d2288bcd5a`

No finding was downgraded, waived or converted into a weaker acceptance claim. Revision 7 changes only the payout-wallet observation-currency contract and paired tests required by the revision-6 Medium finding. It preserves every prior acquisition, calibration, lineage, lifecycle, product, qualification and economic boundary. It does not implement source or tests, collect evidence, enable observation enforcement or economics, execute payments, deploy, change secrets, or inspect `d-inference`.

| R6 finding | Required correction | Revision 7 disposition | Verification added |
| --- | --- | --- | --- |
| M1 Quiet payout-wallet sources inevitably age into false unavailability | Preserve wallet mutation provenance, add a replay-resistant live observation or unchanged-head checkpoint, bind current SQLite/local/witness heads and incarnation, and cover quiet sources, replay, expiry, clock faults, restarts, witness loss and mutation races. | `source_committed_at` remains immutable wallet-mutation provenance. A separate durable `payout_wallet_head_checkpoint.v1` chain advances only checkpoint sequence `C`, digest and `wallet_checkpointed_at`; it cannot mutate payout rows, wallet event sequence/head or mutation time. Checkpoint creation is dual-PREPARE/conditional-SQLite/dual-COMMIT, binds exact `H`, uses a three-attempt race bound, and renews whenever the current head differs or after 150 seconds. Each summary refresh creates a five-second CSPRNG challenge bound to coordinator boot, provider, audience and requested `H`. SQLite owner, local authority and witness independently sign their current fully committed `(incarnation,H,C)`, checkpoint record digest, times, serving incarnation, key epoch and challenge digest over mutually authenticated channels. The coordinator accepts them once only when `C`'s record binds the asserted `H`, heads match, checkpoint age is at most five minutes, monotonic duration and two-second wall-clock bounds pass, and every lineage/recovery rule passes. Restart invalidates old responses. The joined version becomes `(R,H,C)`. Source-global singleflight, closed mutation/refresh rates and measured retention bound checkpoint work. Mutation, checkpoint, source observation, coordinator verification and response-generation clocks stay separate. Closed wallet-only failure reasons preserve balances, compute, payment and USDC truth. | B3-R019–R021 and R024–R028 are strengthened for `(R,H,C)` and independent clocks. New B3-R029–R039 cover ten quiet freshness windows; coordinator and three source-authority restarts; nonce/assertion/substitution replay; expiry, future time, rollback and skew; local/witness loss; every checkpoint crash boundary; mutations around checkpoint/assertion/serialization; stale pagination; Go/Swift/portal truthfulness for every failure reason; and bounded concurrent checkpoint load. |

Approval is not claimed. A fresh independent GPT-5.6 Sol high-reasoning reviewer must inspect the repository/base, recompute the committed plan, test and disposition digests, verify that all earlier contracts remain intact, and challenge feasibility, trust boundaries, replay recovery, clock semantics, economics, UX truthfulness and whether B3-R019–R038 prove the claim. Any Critical, High or Medium finding requires another plan revision before Gate H0, Gate B0D, Collector Slice H0, Feasibility Tooling Slice B0I, governed numeric acquisition, product implementation or qualification.
