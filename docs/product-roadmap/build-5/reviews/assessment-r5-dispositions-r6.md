# Product Build 5 assessment R5 finding dispositions

Date: 2026-09-11

Source review: `assessment-r5-sol.md`

Source review SHA-256:
`f4e78200c06a6ef54773d2e0b9e05d861dbcaa07eda9118f5d02e08a178ff2b0`

Correction revision: assessment/evidence/benchmark/checkpoint R6

This record does not approve R6. A fresh independent GPT-5.6 Sol reviewer must
recompute the committed R6 digests, inspect the governing code and specs, and
report zero Critical, High, and Medium findings before the assessment gate
passes.

| Finding | R6 correction | Disposition |
|---|---|---|
| H1 unproved `RLIMIT_AS` CPU/Metal hard stop | Removed `RLIMIT_AS` from hard-cap and admission authority. R6 records it only as diagnostic state. The a-priori preload/campaign ledgers govern admission; 10 ms footprint/pressure observation, admission closure, worker termination, detection/exit latency, and overshoot are retained damage-containment evidence and never described as synchronous prevention. Optional causal probes now require unlimited and below-limit controls, cumulative small allocations, identical CPU and forced-synchronous Metal operations, reason capture, and clean starts, but can prove only the tested allocator path. Until an independently reviewed exact-OS mechanism synchronously covers every committed CPU/Metal path, target-shaped calibration and capacity promotion remain blocked. | Corrected without claiming that sampling or worker kill is an OS memory cap. Ledgers, ramps, pressure checks, and termination remain mandatory independent defenses. |
| H2 impossible supervisor-loss `waitpid` | Froze one launchd job/process-group topology with `AbandonProcessGroup=false`, a direct worker child forbidden to fork/exec/setsid, and boot/session/job-instance/process-group/PID/start-time/audit/generation identity. Normal and controller-loss paths retain parent `waitpid`. On supervisor EOF, the controller detects within 250 ms, closes admission, durably marks unresolved work `generation_orphaned`, and requests exact launchd job stop. Launchd termination and group-empty observations are retained but grant no recovery; an unconditional boot-scoped orphan fence survives all restarts until boot UUID changes. | Corrected by bounding the claim to detected-and-orphaned fail-closed behavior. A new parent is never required or allowed to claim `waitpid` over an orphan. |
| H3 incomplete preload overlap arithmetic | `preload-budget-v2` has an event-indexed maximum-live ledger for unloaded baseline, stored/file-backed source residency, compression, expanded tensors, conversion/decompression scratch, tokenizer/config, loader/runtime, staging, Metal commands, caches, duplicate/tied weights, logits, and output/IPC. Source and expanded bytes coexist until forced synchronization and source-release evidence. One campaign ledger spans adoption through unload and includes KV and serving temporaries. Measured loaded idle replaces explicitly covered analytical resident rows rather than adding or omitting them. Every edge/formula/replacement is manifest-bound and overflow checked; unknowns forbid load. | Corrected. The gate uses the maximum simultaneous live sum across all phases, not `max(stored, expanded)` or fixed reserves alone. |
| M1 disturbance counts not implied by arrival rate | Split an exact deterministic `SATURATION-ORACLE-R1` from real-runtime boundary cells. Its committed reference state machine fixes sent/accepted/rejected/terminal IDs and reason codes at Entry 110 depths one through four. Real boundary cells are lease/queue-state driven. Each cancellation boundary subcell supplies exactly 1,000 boundary-reached observations across ten clean starts; active subcells require 1,000 accepted requests while the queued subcell requires 1,000 pre-admission entries and zero accepted requests; slow-consumer and warm-swap cells freeze their own accepted/marker/terminal counts. Saturation, precondition, or marker loss is a retained failure. | Corrected. Request indices and open-loop send rates no longer stand in for accepted or boundary-reached observations. |
| M2 intervals ignored by decisions | Froze ten matched clean-start blocks and 50,000 paired hierarchical resamples. Every distributional uplift uses its lower 95% bound; every latency, CV, peak/envelope, and recovery ceiling uses its upper bound. Point estimates and request-level intervals are report-only. Exact invariants and deadline maxima must pass every observation and cannot be rescued statistically. A bound that does not clear its endpoint rule is inconclusive or failed and leaves the tuple disabled. | Corrected. Cluster-aware uncertainty now changes every statistical go/no-go outcome. |
| M3 nonexistent pre-runtime loader callback | R6 leaves loader-history qualification blocked until a protected runtime audit entrypoint exists. The proposed minimal native launcher may link only Apple sealed platform images, installs the evidence channel and dyld capture before loading any manifest-bound non-platform runtime, and loads Swift/MLX/inference libraries afterward. Each add/remove event requires synchronous durable sequence acknowledgement; loss, backpressure, gap, or reentrancy failure terminates the cell. The launcher bytes, entitlements, environment, sandbox, IPC, and API join the closure. An early-constructor transient load/unload fixture under final signing is mandatory. | Corrected as an explicit executable prerequisite and blocker. Static closure or a callback installed after ordinary Swift startup cannot qualify runtime-loader history. |

No runtime, scheduler, configuration, conformance, release, hardware,
procurement, remote-transfer, or production-enablement change is part of R6.
