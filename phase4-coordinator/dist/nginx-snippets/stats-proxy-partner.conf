# Partner-capable /v1/stats/* proxy locations (leaderboard, provider).
#
# CORS is projection-aware in the coordinator (echoed Origin for
# partner keys; ACAO:* for public). Do not add ACAO here.
include /etc/nginx/conf.d/stats-security-headers.conf;
proxy_hide_header X-Stats-Rate-Limit;
