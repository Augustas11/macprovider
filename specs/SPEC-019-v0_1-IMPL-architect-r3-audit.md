**Verdict:** READY TO MERGE
**Tally:** C/H/M/m/Q = 0/0/0/0/0

## Closure verified
- r2 C-1: CLOSED. `forwardWSNonStreaming` now builds the failed WS end-frame attempt with the preserved SPEC-019-visible code via `spec001EndStatus(end.Status)`, then sets `attempt.FaultFlag = billing.FaultBreakerQualifying` only when `isSpec019ProviderDetailCode(attempt.ErrorCode)` matches (`phase4-coordinator/internal/buyer/server.go:2131`, `phase4-coordinator/internal/buyer/server.go:2132`, `phase4-coordinator/internal/buyer/server.go:2133`). That attempt is returned to the WS non-streaming terminal handler and logged through `logAttempt` (`phase4-coordinator/internal/buyer/server.go:1670`, `phase4-coordinator/internal/buyer/server.go:1674`), which passes `attempt.FaultFlag` into `billingRecorder.recordRow` (`phase4-coordinator/internal/buyer/server.go:1422`). `recordRow` carries the flag into `billing.HotPathInput` (`phase4-coordinator/internal/buyer/billing_recorder.go:184`, `phase4-coordinator/internal/buyer/billing_recorder.go:197`), and the billing formula exits before provider-positive credit calculation when the row is `FaultBreakerQualifying` (`phase4-coordinator/internal/billing/formula.go:112`, `phase4-coordinator/internal/billing/formula.go:113`).

## Fresh findings
None.

## Verdict justification

The r2 fix is narrowly scoped to the two SPEC-019 post-inference provider detail codes. `spec001EndStatus` still preserves the older WS/status names for existing logging behavior (`phase4-coordinator/internal/buyer/server.go:4937`, `phase4-coordinator/internal/buyer/server.go:4939`), but the billing fault branch does not key off that broader allow-list directly. It keys off `isSpec019ProviderDetailCode`, whose predicate is exactly `malformed_json_response` or `json_schema_validation_failed` and nothing else (`phase4-coordinator/internal/buyer/server.go:4946`, `phase4-coordinator/internal/buyer/server.go:4947`). Legacy codes such as `error_internal`, `error_model_not_loaded`, `error_context_exceeded`, and `error_queue_full` therefore do not newly become `FaultBreakerQualifying` through the r2 branch.

The regression test added for the r2 surface exercises both SPEC-019 codes on the WS-tunneled non-streaming path, asserts no fallback retry, verifies the buyer error envelope preserves the code and retryable flag, verifies request-log `error_code`, and verifies billing records zero provider credits with `FaultBreakerQualifying` (`phase4-coordinator/internal/buyer/structured_output_provider_error_test.go:106`, `phase4-coordinator/internal/buyer/structured_output_provider_error_test.go:146`, `phase4-coordinator/internal/buyer/structured_output_provider_error_test.go:183`, `phase4-coordinator/internal/buyer/structured_output_provider_error_test.go:198`, `phase4-coordinator/internal/buyer/structured_output_provider_error_test.go:204`). Targeted verification passed: `go test ./internal/buyer -run 'TestStructuredOutputProviderDetailErrorsPassThroughWithoutRetry' -count=1`.
