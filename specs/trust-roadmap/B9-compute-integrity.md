# B9 — SPEC-036 compute-integrity (observe mode)

**Type**: deferred design brief — a FUTURE separate SPEC with its own three-lane audit loop. Analysis, not a commitment.

**Gated on**: post-beta.

## Problem / shape
The only mechanism that would make a **cross-model-class** ceiling raise
observation-backed rather than capacity+operator-backed — the named closure for
B4's acknowledged weak spot (observed latency cannot distinguish honest
large-model service from cheaper substituted work). Implement per the merged
SPEC-036 in observe mode; enforce is unreachable at current supply (SPEC-036
§6.1) and stays maintainer-gated.
