# SPEC-015 AC-9 pinned OpenAI Python SDK fixture.
openai==1.30.1
# openai 1.30.1 passes the legacy `proxies=` kwarg to httpx; httpx>=0.28
# removed it, so constrain transitively to keep the SDK runnable.
httpx<0.28
